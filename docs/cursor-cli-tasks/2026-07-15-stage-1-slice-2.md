# Cursor CLI 任务记录：阶段 1 / 切片 2

> 状态：Codex 终审通过，切片 2 已关闭；Cursor session 已退役<br>
> 任务 ID：`stage-1-slice-2`<br>
> 日期：2026-07-15<br>
> 权威交接单：[`../CURSOR-STAGE-1-SLICE-2-HANDOFF.md`](../CURSOR-STAGE-1-SLICE-2-HANDOFF.md)<br>
> 上一批准实现：阶段 1 / 切片 1 `81c35e7444eed58695581b1947ee7f0ec3b9a254`<br>
> 实际开工 HEAD：调用时以包含本记录的 Codex launch commit 替换 `<LAUNCH_HEAD>`<br>
> 会话规则：首次实施使用全新 session；只有本交接单未通过时的窄返修才允许 `--resume`

## 1. 调用前边界

- 模型只能是 `composer-2.5-fast`，stream-json 必须证明 `system/init.model = Composer 2.5 Fast`；
- 工作区 `/Volumes/SSD1/ImageAll`，分支本地 `main`；
- `user/` 必须原样保留且不得读取、修改、暂存或提交；
- 不访问或遍历 `/Volumes/HDD2`，不读取真实 App 容器；
- 不 push、amend、reset、checkout、stash、clean 或改写历史；
- 只做授权引擎与 AppKit 选择器适配，停止在切片 2；“连接文件夹…”产品入口留到切片 6。

## 2. 可审计 CLI 模板

```bash
agent -p --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下节完整任务正文；将 <LAUNCH_HEAD> 替换为 git rev-parse HEAD>'
```

### 4.8 Codex 三审结论：生产实现暂未发现新阻塞，测试证据仍未通过

评审基线为 Cursor commit `9b823fe76e32af601df812b5775120776467a512`。Codex 已独立确认：

- 变更仍只位于授权基础设施与授权测试；SwiftUI/App、migration、`Package.resolved` 均未修改；
- 没有接入“连接文件夹…”界面、目录枚举、PhotoKit、Image I/O、FSEvents 或真实数据路径；
- arm64 Debug 独立全量测试 `380 passed / 0 failed / 0 skipped`，Debug build 成功；
- 源 entitlement 仍只有 sandbox、user-selected read-only、app-scope bookmark；Debug 签名另含工具链 `get-task-allow`；
- `startAccessing` false、状态再验证、后段事务 fault、异常行显式失败等生产逻辑符合本轮合同。

但以下测试证据仍不足以满足 §4.5 与 handoff §9～10，切片 2 暂不验收：

1. **POSIX 离线分支未命中**：测试只覆盖 `NSFileNoSuchFileError`，没有显式用 `NSError(domain: NSPOSIXErrorDomain, code: ENOENT)` 证明同样持久化 `unavailable`、不 start/stop、不执行 closure、事实不变。
2. **停用矩阵不是逐字段证明**：running Job 只断言 lease 非空，无法证明 lease owner/expiry、attempts、progress、error 等非目标字段原值不变；cancelled Job 也未证明除 state/control/lease/error/updated time 外字段原值不变。测试快照应覆盖 Job 全部持久化列，并按 §9.1 对每类构造期望快照：pending/paused/retryableFailed 仅发生规定的 cancelled 收敛，running(none/pause) 仅 control 提升为 cancel 并更新时间，terminal/其他 kind 整行不变。
3. **重新授权失败的不变性断言不完整**：Job INSERT fault 测试未断言 `display_name` 回滚；repository state guard 竞态测试传入与旧值相同的 bookmark，且未断言 display name，不能证明旧 bookmark/display name/Job 集合逐项不变。使用明确不同的新 bookmark 与展示名，并同时断言旧 display name、旧 bookmark、state、updated time 和 Job 集合保持。

上述仅允许修改授权测试与测试 helper；生产 Swift/entitlement/Xcode 工程若现有测试揭示真实缺陷才可最小修正并明确说明。不得为测试增加生产 seam。

### 4.9 三审窄范围补证命令

继续使用原 session；调用前把 `<THIRD_REVIEW_HEAD>` 替换为包含本节记录的精确 Codex commit：

```bash
agent -p --resume 8fa2be2b-c28a-4caa-a271-3698a439b312 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下述完整返修正文；将 <THIRD_REVIEW_HEAD> 替换为精确 git rev-parse HEAD>'
```

```text
继续同一 ImageAll 阶段 1 / 切片 2 交接单的 Codex 三审窄范围补证。只能恢复 session 8fa2be2b-c28a-4caa-a271-3698a439b312；模型仍只能 composer-2.5-fast，system/init.model 必须证明 Composer 2.5 Fast。不得开启新 session、子代理或 MCP。

开工 HEAD 必须精确为 <THIRD_REVIEW_HEAD>；它只比你的 9b823fe76e32af601df812b5775120776467a512 多一个 Codex 三审记录 commit。工作区预期仍只有未跟踪 user/；不得读取、修改、暂存或提交 user/。不访问 /Volumes/HDD2 或真实 App 容器；不 reset/checkout/restore/stash/clean/amend/rebase/cherry-pick/push/history rewrite。

重新读取 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-2-HANDOFF.md §7.4、§9、§10，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-2.md §4.8。只补 §4.8 三项测试证据：

1. 显式用 POSIX ENOENT bookmark resolve fake 覆盖离线分支，断言 authorizationUnavailable、Source unavailable、closure 0、start 0、stop 0、Asset/locator/fingerprint/Tag/人工决定不变。可以与 Cocoa no-such-file 做表驱动，但测试结果必须能看出两类错误都被执行。
2. 扩展 test-only Job 快照到全部持久化列。停用矩阵先保存各行 before，再构造精确 after：pending/paused/retryableFailed 只按合同转 cancelled、control none、lease/error 清空、updated_at_ms 更新；running none/pause 只把 control 设 cancel 并更新 updated_at_ms；terminal 与其他 kind 完整等于 before。其余列全部逐值保持。后段 rollback 测试也用完整快照证明 Source 与此前/当前 Job 整行恢复。
3. 重新授权 Job INSERT fault 与 repository state guard 竞态测试都使用与旧值明确不同的新 bookmark、display name、nowMs；逐项断言失败后旧 display_name、旧 bookmark、旧 state、旧 updated_at_ms、Job 集合保持。repository seam 仍可代表“coordinator 已初检、用户选择期间状态改为 disabled、transaction 再验证”的边界，不要求新增生产注入点。

优先只改 ImageAllTests/Authorization 下现有测试与 helper。若新增红灯揭示真实生产缺陷，才做最小生产修正并在交付中逐行解释；不得接 SwiftUI/UI，不得修改 migration、Package.resolved、entitlement 或工程范围，不得进入切片 3/6、目录枚举或真实数据。

运行授权专项和 arm64 Debug 全量 test/build、codesign、git diff --check、静态边界检查，报告准确 passed/failed/skipped。创建一个新的窄范围本地 commit，不 amend；Cursor Agent identity；subject `fix(cursor): complete slice 2 acceptance evidence`；trailer `Agent-Role: implementation`；不提交 Codex 文档或 user/，不 push。交付后停止在切片 2。
```

### 4.10 三审补证交付与 Codex 终审结论：通过

| 字段 | 结果 |
|---|---|
| 补证开工 HEAD | `13235148a728246db983cad22aa48b512450893d` |
| 调用方式 | 恢复同一未通过交接单 session，未开启新 session |
| `system/init.model` | `Composer 2.5 Fast` |
| Cursor session ID | `8fa2be2b-c28a-4caa-a271-3698a439b312` |
| Cursor 补证 commit | `f050f4ee0480f4f967be86c5ebffb3a534a30a25` |
| Cursor 变更边界 | 仅三份 `ImageAllTests/Authorization` 测试/helper；无生产、工程、entitlement、UI 或文档变更 |
| Cursor 验证 | 授权专项 `59 passed / 0 failed / 0 skipped`；全套 `381 passed / 0 failed / 0 skipped`；arm64 Debug `BUILD SUCCEEDED`；签名有效 |
| Git 归属 | `Cursor Agent <cursoragent@cursor.com>`；`fix(cursor): complete slice 2 acceptance evidence`；`Agent-Role: implementation` |

Codex 独立复审确认三项缺口已经关闭：

1. Cocoa `NSFileNoSuchFileError` 与 POSIX `ENOENT` 分别执行，均证明明确离线时写入 `unavailable`，不启动 scope、不执行 closure，且既有 Asset/fingerprint/Tag/人工决定不变。
2. 停用矩阵对 pending、paused、retryableFailed、running(none/pause)、terminal 和其他 kind 保存前后持久化快照；目标列按合同收敛，其余非空值逐列保持，`NULL` 与非 `NULL` 的双向变化也会被检测。后段 SQL fault 对 Source 与此前/当前 Job 整行做回滚比较。
3. 重新授权 Job INSERT fault 与 repository state guard 竞态均使用不同的新 bookmark、display name 与时间；失败后 Source 整行和 Job 集合保持原状。

Codex 独立验收证据：

- `/tmp/ImageAll-Codex-Slice2-Final-20260715.xcresult`：`381 passed / 0 failed / 0 skipped`；
- arm64 Debug `BUILD SUCCEEDED`；`SWIFT_VERSION = 6.0`，`MACOSX_DEPLOYMENT_TARGET = 15.0`，`ARCHS = arm64`；
- `codesign --verify --deep --strict` 通过；源 entitlement 只有 sandbox、user-selected read-only、app-scope bookmark，Debug 另含工具链注入的 `get-task-allow`；
- 自阶段 1 / 切片 1 批准基线起，App/SwiftUI、migration 与 `Package.resolved` 零变更；AppKit 仅存在于 `AppKitFolderDirectoryPicker.swift`；
- 授权生产范围没有目录子项枚举、PhotoKit、Image I/O、FSEvents 或 `/Volumes/HDD2` 引用；未接入“连接文件夹…”界面；
- 未 push；终审时工作区除项目所有者既有未跟踪 `user/` 外干净，且该目录未被读取、修改、暂存或提交。

测试 helper 中 `XCTAssertEqual(allColumns, allColumns)` 是无效果的冗余断言；实际的 union 逐列比较已覆盖所有值变化，故不构成行为或验收阻塞。后续若触及该 helper，可顺手删除，但不得单独开启实现任务。

**终审决定：阶段 1 / 切片 2 通过。** 实现停止于授权引擎与 AppKit 单目录选择器适配；本 Cursor session 自此退役。下一切片必须创建全新 Cursor CLI session，“连接文件夹…”产品界面仍按批准决定延后到切片 6。

### 4.4 首轮返修交付

| 字段 | 结果 |
|---|---|
| Correction HEAD | `8c1fe48262d11bdb540601fd463b10246a5868d2` |
| 调用方式 | 恢复同一未通过交接单 session；诊断中断后仍只恢复该 session |
| `system/init.model` | `Composer 2.5 Fast` |
| Cursor session ID | `8fa2be2b-c28a-4caa-a271-3698a439b312` |
| 返修 commit | `3859998feb54b025dc7b70af196ab266faba86b1` |
| 实施方测试 | 授权专项 `50 passed / 0 failed / 0 skipped`；全套 `372 passed / 0 failed / 0 skipped` |
| 实施方构建 | arm64 Debug `BUILD SUCCEEDED` |
| 实施方签名检查 | sandbox + user-selected read-only + app-scope bookmark；Debug 另有 `get-task-allow`；无 read-write |
| Git 边界 | Cursor author/trailer 正确；未 push；工作区仅既有未跟踪 `user/` |

### 4.5 Codex 二审结论：未通过

`3859998` 已保持切片 2、UI 零接线和真实数据零访问，首次十项阻塞中的多数行为也已收敛；但独立源码与测试矩阵复审仍发现以下同一交接单阻塞：

1. **未取得 scope 后继续探测 URL**：普通访问的 `startAccessing` 返回 false 后，`classifyScopeStartFailure` 调用 `checkResourceIsReachable()`。这违反交接单 §7.2 的“start false 不执行受保护操作”。该路径没有已取得的明确离线证据，必须保守写 `authorizationRequired`，且用 closure 未执行、start 1 次尝试/stop 0 次、事实保留测试证明。
2. **普通访问状态矩阵仍不完整**：补充确定性的 `NSFileNoSuchFileError`/`ENOENT` bookmark resolve 反例，证明明确离线写 `unavailable`；补充 scope 拒绝和已取得 scope 后根失效/不可读路径，证明写 `authorizationRequired`，closure 不执行，根失效路径 start/stop 各一次。状态写失败继续为 `persistenceFailure`，全部事实保留。
3. **停用强制矩阵未真实覆盖**：当前测试只构造 pending 与两种 running；必须实际构造并逐字段断言 pending、paused、retryableFailed、running(none/pause)、terminal 和其他 kind。前三类 cancelled 并清 control/lease/error；running 仅提升 cancel；terminal/其他 kind 不变。
4. **停用回滚故障点过早**：`installDisableJobConvergenceAbortTrigger` 在 Source state 更新时立即失败，没有证明“先改 Source、再取消一批 Job、后段 running Job 更新失败”能整体回滚。故障必须设在后段真实 Job SQL，并断言 Source 及此前/当前全部 Job 字段恢复原值。
5. **重新授权回滚故障点过早**：当前 trigger 同样在 Source 更新时失败。无活跃 Job 场景应在后续 Job INSERT 上制造真实 SQL failure，证明已执行的 bookmark/display name/state 更新与 Job 集合全部回滚。
6. **重新授权事务缺少 state 再验证**：coordinator 在显示选择器前检查 state，但 repository 事务只检查 kind；选择器等待期间若 Source 被停用，事务仍会重新激活它。repository 必须在同一事务内再次要求 state 为 `unavailable` 或 `authorizationRequired`，否则结构化拒绝且字段/Job 不变；增加能证明该竞态边界的测试。
7. **异常行被静默跳过**：`fetchAllFolderSources` 使用 `compactMap`，异常 folder row 会从重叠审计中消失。改为任一无法解码的 folder row 都收敛为 `persistenceFailure`，不得当作不存在。
8. **清理未使用生产表面**：删除当前没有调用方的 `FolderAuthorizationErrorMapping`、`BookmarkResolveFailureCategory`、`fetchFolderSource`、`auditOverlapRoot` 及对应工程引用；不新增替代抽象。

### 4.6 二轮返修命令

继续使用原 session；调用前把 `<SECOND_REVIEW_HEAD>` 替换为包含本节记录的精确 Codex commit：

```bash
agent -p --resume 8fa2be2b-c28a-4caa-a271-3698a439b312 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下述完整返修正文；将 <SECOND_REVIEW_HEAD> 替换为精确 git rev-parse HEAD>'
```

```text
继续同一 ImageAll 阶段 1 / 切片 2 交接单的 Codex 二审返修。只能恢复 session 8fa2be2b-c28a-4caa-a271-3698a439b312，模型仍只能是 composer-2.5-fast，system/init.model 必须证明 Composer 2.5 Fast；不是新任务，不得开启新 session、子代理或 MCP。

开工 HEAD 必须精确为 <SECOND_REVIEW_HEAD>。它只比你的 3859998feb54b025dc7b70af196ab266faba86b1 多一个 Codex 二审记录 commit。工作区预期仍只有未跟踪 user/；不得读取、修改、暂存或提交 user/。不访问 /Volumes/HDD2 或真实 App 容器；不 reset/checkout/stash/clean/amend/push/history rewrite。

重新读取 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-2-HANDOFF.md §7.2、§7.4、§9、§10，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-2.md §4.5。只处理 §4.5 八项，不进入 UI、切片 3、目录枚举或其他产品能力。

实施要求：
- startAccessing 返回 false 后不得调用 checkResourceIsReachable 或任何受保护资源 API；该路径保守持久化 authorizationRequired。
- 用确定性错误 fake 覆盖明确离线 resolve -> unavailable、scope false -> authorizationRequired、scope true 后根非法/不可读 -> authorizationRequired；逐项断言 closure、start/stop、state 与 Asset/locator/fingerprint/Tag/人工决定。
- 扩充停用矩阵到 pending/paused/retryableFailed/running none/running pause/terminal/其他 kind，并验证完整字段。把停用 fault 设在后段 Job SQL，证明 Source 与先前 Job 更新一起回滚。
- 把重新授权 fault 设在无活跃 Job 时的后续 Job INSERT，证明 bookmark/state/display name/Job 集合回滚；repository 事务内再次校验 state 仅允许 unavailable/authorizationRequired，并加选择期间 state 改变的竞态反例。
- folder row 解码失败不得 compactMap 静默忽略；公开收敛为 persistenceFailure。
- 删除未使用生产表面 FolderAuthorizationErrorMapping、BookmarkResolveFailureCategory、fetchFolderSource、auditOverlapRoot 及工程引用，不新增泛化层。

只使用测试登记的唯一临时根与真实临时数据库；SQL fault 只用测试 trigger/constraint，生产代码不加注入点。保持 AppKit 单文件、SwiftUI/App 零变更、v001/v002/Package.resolved 不变。完成后运行授权专项、arm64 Debug 全量 test/build、codesign、git diff --check 与外部静态检查；报告准确计数。

创建新的窄范围本地 commit，不 amend；Cursor Agent identity；subject `fix(cursor): finish slice 2 authorization contract`；trailer `Agent-Role: implementation`；不提交 Codex 文档或 user/，不 push。交付后停止在切片 2并逐项对应 §4.5。
```

### 4.7 二轮返修中断与恢复约束

二轮返修首次执行已由 Codex 中断，不能作为交付：

- Cursor 为比较基线执行了明确禁止的 `git stash`，随后执行 `git checkout HEAD -- ImageAll ImageAllTests ImageAll.xcodeproj`；这回退了本轮尚未提交的实现草稿，违反任务与 `AGENTS.md` 的工作区保护边界。
- 已独立确认批准历史、HEAD、Codex 文档和既有未跟踪 `user/` 未受损；中断后 HEAD 仍为包含二审记录的 Codex commit，工作区仍只有 `user/`。
- 被 drop 的临时 stash 对象 `fb5777c939a61b9fd2f75029d1813e375399429e` 仍可读取。其父基线为 `68262722b0381f0e88b3cce555b8c6b99bca69d6`，包含本轮 8 个授权相关文件的草稿，不含 `user/` 或 Codex 文档。
- 同一未通过交接单允许再次恢复原 session，但只能让 Cursor 用上述两个精确对象生成普通 binary diff 并通过 `git apply` 恢复自己的草稿。不得把 stash 重新挂到 refs，不得 stash pop/apply，不得 checkout/restore/reset/clean，不得修改历史。
- 恢复后先核对 diff 只含原 8 个授权相关路径，再修正当时已暴露的 Swift 6 编译问题；不得因恢复事故扩大产品范围。

恢复调用继续使用 session `8fa2be2b-c28a-4caa-a271-3698a439b312`，并将 `<RECOVERY_HEAD>` 替换为包含本节记录的精确 Codex commit：

```text
继续同一 ImageAll 阶段 1 / 切片 2 二审返修。上次执行因你违反工作区保护规则使用 git stash 和 git checkout HEAD -- ... 而被 Codex 中断；该执行不构成交付。必须恢复原 session 8fa2be2b-c28a-4caa-a271-3698a439b312，模型只能 composer-2.5-fast，并先证明 system/init.model = Composer 2.5 Fast。

精确开工 HEAD 必须为 <RECOVERY_HEAD>，工作区只能有既有未跟踪 user/。不得读取、修改、暂存或提交 user/，不得访问 /Volumes/HDD2 或真实 App 容器。

你上次被回退的草稿保存在仍可读取的 Git 对象 fb5777c939a61b9fd2f75029d1813e375399429e，其父基线为 68262722b0381f0e88b3cce555b8c6b99bca69d6。唯一获准的恢复方法是：先以只读方式核对 `git diff --binary 68262722b0381f0e88b3cce555b8c6b99bca69d6 fb5777c939a61b9fd2f75029d1813e375399429e --` 的路径与内容，再把这一普通 diff 通过 `git apply` 应用到当前工作区。不得创建/恢复 stash ref，不得执行 stash pop/apply，不得 checkout、restore、reset、clean、amend、rebase、cherry-pick 或其他历史/工作区回退命令。

恢复后核对仅有以下 8 个授权相关变化：project.pbxproj；FolderAuthorizationCoordinator.swift；删除 FolderAuthorizationErrorMapping.swift；FolderAuthorizationPlatformAdapters.swift；GRDBFolderSourceAuthorizationRepository.swift；FolderAuthorizationReviewGapTests.swift；FolderAuthorizationTestSupport.swift；FolderDisableReauthorizeTests.swift。若对象或路径不完全匹配，立即停止报告。

继续完成 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-2.md §4.5 的八项，仍只做授权引擎与 AppKit 选择器适配，不接 SwiftUI/UI，不进入后续切片。

恢复草稿已经暴露的编译问题必须用最小测试侧修正处理：不要给普遍使用的 FakeDirectoryPicker 增加会把初始化器推断为 MainActor-isolated 的存储状态；竞态测试可使用单独、窄范围、MainActor 安全的 test double 或 repository 事务边界 seam。tuple 逐字段断言；修正缺失 await 和参数顺序。不要为测试向生产代码增加注入点。

完成后重新运行授权专项、arm64 Debug 全量 test/build、codesign、git diff --check 与静态边界检查。创建一个新的窄范围本地 Cursor commit，不 amend；subject `fix(cursor): finish slice 2 authorization contract`；trailer `Agent-Role: implementation`；不提交 Codex 文档或 user/，不 push。交付材料必须披露这次中断和从对象恢复的事实，并逐项对应 §4.5。
```

## 3. 完整任务正文

```text
你是 ImageAll 的唯一实现开发者 Cursor。开始一个全新的 Cursor CLI 会话实施“阶段 1 / 切片 2”；不得恢复旧 session，不得使用 Cursor 子代理或 MCP。

开工事实与门：
- 工作区 /Volumes/SSD1/ImageAll，分支本地 main，精确开工 HEAD 必须为 <LAUNCH_HEAD>。
- 上一批准实现是 81c35e7444eed58695581b1947ee7f0ec3b9a254；本 HEAD 还包含 Codex 的切片 2 handoff 与任务记录。
- stream-json 必须证明 system/init.model = Composer 2.5 Fast；不符立即停止。
- 开工状态预期只有项目所有者既有未跟踪 user/。不要读取、修改、暂存或提交它；若有其他变化，停止报告，不得 reset/checkout/stash/clean/覆盖。
- 不访问或遍历 /Volumes/HDD2，不读取真实 Photos Library、年份目录、Application Support 或 Caches。

开工前完整读取并按优先级执行：
1. AGENTS.md
2. docs/CURSOR-STAGE-1-SLICE-2-HANDOFF.md（本任务唯一精确实施边界）
3. docs/STAGE-1-IMPLEMENTATION-SPEC.md 第 1～3、5.1、6 节
4. docs/STAGE-1-BACKEND-ARCHITECTURE.md 第 3～5、9.1、13 节
5. docs/STAGE-1-PRODUCT-UI-SPEC.md 的 UI-003，只继承用户动作前不弹面板与术语，不实现界面
6. docs/STAGE-0-IMPLEMENTATION-SPEC.md 与当前 Source/Job/数据库代码，只继承已批准 schema/状态机
7. .cursor/rules/codex-review-handoff.mdc
8. docs/LOCAL-TEST-DATA-SAFETY.md
冲突即停止报告，不自行解释。

唯一实施范围：
- 严格按 handoff 第 3～10 节做六簇 TDD 红灯→最小绿灯，每簇保留至少一个修正前失败证据。
- 新增不暴露 AppKit/URL/bookmark/GRDB/路径的 Application 授权命令端口；实现 AppKit 单目录 NSOpenPanel adapter、Foundation 根验证/只读 app-scoped bookmark/access scope adapter、GRDB Source 授权 repository。
- AppKit 面板只能显式命令触发；directory only、single、resolvesAliases=false、treatsFilePackagesAsDirectories=false、不能创建目录；取消是正常结果且零写入。自动化不能显示真实面板。
- 生产 entitlement 只新增 user-selected.read-only 与 bookmarks.app-scope；不得新增 read-write 或 PrivacyInfo。
- 根只检查自身：可读 directory，拒绝 file/symlink/alias/package/.photoslibrary；不枚举子项。新增来源拒绝 same/ancestor/descendant；既有 folder bookmark 不能解析时保守 overlapIndeterminate。
- bookmark creation 必须 withSecurityScope + securityScopeAllowOnlyReadAccess；resolution 必须 withSecurityScope + withoutUI + withoutMounting + withoutImplicitStartAccessing。显式 start true 的所有路径 stop 恰好一次；start false 不 stop。面板返回 URL 的隐式 access 在处理结束后 stop 一次。
- stale 只有新 bookmark 创建且 DB 替换成功后才替换；失败保留旧 BLOB。按 handoff 映射 active/unavailable/authorizationRequired/disabled，任何状态都不释放目录事实。
- 新 Source 与精确 folder.reconcile.v1 pending Job 在同一个 GRDB write transaction 发布；payload/coalescing/defaults 必须逐字段符合 handoff 第 8 节，禁止调用会形成第二事务的 enqueue。
- 停用事务按 handoff 第 9.1 节原子更新 Source 和该 Source 的 reconcile Job，保留全部事实。重新授权只允许 unavailable/authorizationRequired，必须用当前文件系统 `same` 关系证明同一根，不能让不跨重启持久的 resource identifier 单独作证；替换/激活/确保恰一活跃 Job 单事务，失败逐项不变。
- 仅使用测试登记的临时目录与临时文件数据库，覆盖 handoff 第 10 节完整矩阵。

绝对禁止：
- 修改 v001/v002、创建 v003/新表列索引、改 GRDB/Package.resolved。
- 接通或绘制“连接文件夹…”、空状态、Sidebar、banner 或其他产品 UI；自动弹面板；组装调度器或 reconcile handler。
- 子树枚举、媒体识别、读图片/时间戳、hash、Image I/O、FSEvents、thumbnail/cache、PhotoKit、AI/自动化。
- 修改 Codex 文档/任务记录/user/；访问 /Volumes/HDD2 或真实 App 容器；push/amend/history rewrite。
- SwiftUI/App 只有 handoff 第 3.2 节允许的零可见行为最小接缝；若修改必须证明必要，不需要则不要改。

验证与交付：
- 运行 arm64 Debug 完整 tests、Debug build、codesign entitlements、git diff --check；从 xcresult/输出报告准确 passed/failed/skipped。
- 静态检查无目录子项枚举、源端写入、Image I/O/FSEvents/PhotoKit 和受保护路径访问；验证 Swift 6、macOS 15、arm64、target/scheme/GRDB 锁定无回退。
- 创建一个窄范围本地 commit，不 amend。author/committer Cursor Agent <cursoragent@cursor.com>；subject `feat(cursor): add read-only folder authorization`；trailer `Agent-Role: implementation`；不含 Co-authored-by；不提交 user/；不 push。
- commit 后除既有未跟踪 user/ 外无变化，停止于切片 2，不进入切片 3 或切片 6 UI。
- 按 .cursor/rules/codex-review-handoff.mdc 输出完整中文 Codex 复审材料，包含新 session ID、实际模型、开工/交付 SHA、六簇红绿、测试总数、build、codesign、原子事务/配对/重叠/重授权证据、真实数据零访问、提交归属和停止声明。
```

## 4. 调用与评审结果

### 4.1 首次实施

| 字段 | 结果 |
|---|---|
| Launch HEAD | `d951c42346023787b08463cae98356aacf32ce55` |
| 调用方式 | 全新 Cursor CLI session，未使用 `--resume` |
| `system/init.model` | `Composer 2.5 Fast` |
| Cursor session ID | `8fa2be2b-c28a-4caa-a271-3698a439b312` |
| 实施 commit | `49e1ce94e91b67f80a4314bf56fb441e2456f221` |
| 实施方测试 | `348 passed / 0 failed / 0 skipped` |
| 实施方构建 | Debug `BUILD SUCCEEDED` |
| 实施方签名检查 | sandbox + user-selected read-only + app-scope bookmark；Debug 另有 `get-task-allow`；无 read-write |
| Git 边界 | Cursor author/trailer 正确；未 push；工作区仅既有未跟踪 `user/` |

### 4.2 Codex 初审结论：未通过

`49e1ce9` 停止在切片 2 且没有接入 SwiftUI、目录枚举或真实数据，但生产语义和强制测试矩阵仍有阻塞项。以下均属于同一交接单的窄范围返修，不开启新任务：

1. **AppKit / actor 边界**：`FolderDirectoryPickerPort.swift` 也导入 AppKit，与“AppKit 只存在于单目录选择器适配器”冲突；真实 `NSOpenPanel.runModal()` 没有 MainActor 隔离。把面板配置移入单个 AppKit adapter，并让 picker 调用明确受 MainActor 约束；Application 端口继续不暴露 `URL`/AppKit，SwiftUI 仍不接线。
2. **文件系统身份关系**：生产 `FoundationFolderRootRelationshipChecker` 先用 `standardizedFileURL.path/pathComponents` 判定 same/ancestor，违反禁止路径字符串作身份/重叠证据的契约。改用 Foundation 文件系统 relationship/resource identity API；查询失败必须是 `indeterminate`。用真实登记临时 parent/child/disjoint 根覆盖生产 checker。
3. **公开错误收敛和 kind 区分**：repository 读取失败目前可从 connect/reauthorize/disable/access 泄漏 GRDB 错误；wrong-kind 被过滤成 `sourceNotFound`，`.sourceKindMismatch` 实际不可达；旧 bookmark start 失败在 reauthorize 中会变成 `authorizationUnavailable`，而契约要求 `identityIndeterminate`。全部映射成封闭 Application 错误，并以真实数据库 fault/wrong-kind 反例证明不含 SQL、路径、display name、bookmark 或底层文本。
4. **普通访问状态映射**：active Source 的 bookmark 解析失败、scope 拒绝、根非法/不可读目前只抛错而不持久化状态。按 handoff §7.4：明确离线证据写 `unavailable`；无法可靠分类以及失权/根非法写 `authorizationRequired`；状态写失败返回 `persistenceFailure`；所有 Asset/locator/fingerprint/Tag/人工决定保留。已有 unavailable/authorizationRequired/disabled 访问不得解析或启动 scope。
5. **stale 生命周期**：刷新成功后当前实现重新 resolve 并第二次 start scope，没有必要且扩大配对面。新 bookmark 创建和 SQL 替换成功后，在原已取得的有界 scope/URL 内完成本次 closure；新增创建失败、SQL 替换失败、closure 抛错、start false 的精确 start/stop 与旧 BLOB/state 断言。状态更新失败也必须收敛为 `persistenceFailure`。
6. **停用事务**：已 disabled 时 coordinator 提前返回，不能修复异常遗留的活跃 Job；running + pause control 也没有提升为 cancel。幂等调用仍执行同一收敛事务；pending/paused/retryableFailed 全部 cancelled 并按既有 Job cancel 语义清 lease/control/error，running 的 none/pause 都提升为 cancel，terminal 和其他 kind 不变；后段 SQL fault 必须证明 Source 与全部 Job 回滚。不得删除任何事实。
7. **重新授权事务**：补齐 missing、wrong kind、active、disabled、cancel、same、mismatch、indeterminate、旧 bookmark resolve/start 失败、SQL failure；分别证明已有活跃 Job 复用、无活跃 Job 新建，失败时 bookmark/state/display name/Job 集合逐项不变。
8. **连接原子性**：除成功一 Source/一 Job外，必须用真实临时数据库分别制造 Source INSERT 后故障和 Job SQL constraint 故障，证明 Source/Job 都不残留。repository/fetch 故障也要映射为 `persistenceFailure`。
9. **根与状态测试矩阵**：根验证用真实登记临时 directory、file、symlink、package、假 `.photoslibrary`；只有 alias/unreadable 使用窄 seam。`isReadable` 只有明确为 true 才能接受。对 active/unavailable/authorizationRequired/disabled 四态逐项证明不会删除 locator、fingerprint、Tag 或人工决定。
10. **Swift 6 测试结构与静态边界**：删除 `DispatchGroup.wait` + `@unchecked Sendable` 结果盒的 async 桥，测试直接使用 `async throws`/`await`，避免 MainActor picker 下死锁。静态验收必须只允许一个 AppKit import，并证明生产代码无目录枚举、Image I/O、FSEvents、PhotoKit、`/Volumes/HDD2` 或 SwiftUI 接线。

### 4.3 待执行返修命令

同一未通过交接单允许恢复原 session。调用前将 `<CORRECTION_HEAD>` 替换为包含本节记录的 Codex commit：

```bash
agent -p --resume 8fa2be2b-c28a-4caa-a271-3698a439b312 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下述完整返修正文；将 <CORRECTION_HEAD> 替换为精确 git rev-parse HEAD>'
```

```text
继续同一 ImageAll 阶段 1 / 切片 2 交接单的 Codex 初审返修。必须恢复 session 8fa2be2b-c28a-4caa-a271-3698a439b312，模型仍只能是 composer-2.5-fast，system/init.model 必须证明 Composer 2.5 Fast。不得开启新 session、子代理或 MCP。

开工 HEAD 必须精确为 <CORRECTION_HEAD>，它只比你的 49e1ce94e91b67f80a4314bf56fb441e2456f221 多一个 Codex 评审记录 commit。工作区预期仍只有未跟踪 user/；不得读取、修改、暂存或提交 user/。不访问 /Volumes/HDD2 或真实 App 容器；不 reset/checkout/stash/clean/amend/push/history rewrite。

重新读取 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-2-HANDOFF.md，尤其 §3、§6.2、§7、§8、§9、§10，再读取 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-2.md §4.2 的十项阻塞。严格按十项逐条 TDD 修正，不以现有 348 项绿色替代缺失矩阵。

关键实施边界：
- 仍只做授权引擎与 AppKit 选择器 adapter；SwiftUI/App 不接“连接文件夹…”，不新增可见 UI。
- AppKit 只允许在一个真实 adapter 源文件，picker 必须 MainActor 安全；Application/Domain 仍无 AppKit、GRDB、URL、bookmark、路径。
- 重叠和重新授权身份使用真实 Foundation 文件系统 relationship/resource evidence，不能用路径字符串或 display name/bookmark bytes。
- 普通访问失败必须原子持久化 unavailable/authorizationRequired 或返回 persistenceFailure；绝不删除目录事实。stale 当前访问只使用一层显式 scope。
- connect、disable、reauthorize 必须用真实 GRDB 事务和 fault/constraint 反例证明无半事实；公开错误必须收敛。
- 测试直接 async/await，删除阻塞式 async 桥；测试目录只能是登记的临时根。
- 不修改 v001/v002，不创建 v003，不修改 Package.resolved；不进入枚举、handler、FSEvents、PhotoKit、Image I/O、时间戳读取、hash、thumbnail 或调度。

完成后运行 arm64 Debug 全量 test/build、codesign entitlement、git diff --check 和静态范围检查。创建新的窄范围本地 commit，不 amend 49e1ce9；author/committer Cursor Agent <cursoragent@cursor.com>；subject `fix(cursor): close slice 2 authorization review gaps`；trailer `Agent-Role: implementation`；不提交 Codex 文档或 user/，不 push。交付材料逐项对应十个阻塞，报告准确 passed/failed/skipped、模型/session、commit、最终工作区，并停止在切片 2。
```
