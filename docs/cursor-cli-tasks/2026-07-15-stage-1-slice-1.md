# Cursor CLI 任务记录：阶段 1 / 切片 1

> 状态：Ready to launch<br>
> 任务 ID：`stage-1-slice-1`<br>
> 日期：2026-07-15<br>
> 权威交接单：[`../CURSOR-STAGE-1-SLICE-1-HANDOFF.md`](../CURSOR-STAGE-1-SLICE-1-HANDOFF.md)<br>
> 上一批准功能基线：阶段 0 `892f4e29e1ebf492c1540c5a29d9c54abc05a78f`<br>
> 实际开工 HEAD：调用时用包含本记录的 Codex launch commit 替换 `<LAUNCH_HEAD>`<br>
> 会话规则：全新 session，禁止 `--resume`

## 1. 调用前边界

- 模型只能是 `composer-2.5-fast`，stream-json 必须证明 `system/init.model = Composer 2.5 Fast`；
- 工作区 `/Volumes/SSD1/ImageAll`，分支本地 `main`；
- 已知项目所有者未跟踪文件 `user/用户-前端想法.md` 必须原样保留且不得读取、暂存或提交；
- 不访问 `/Volumes/HDD2`；
- 不 push、不 amend、不 reset/checkout/stash/clean；
- 本轮只做 v002、查询和人工标签事务，停止在切片 1。

## 2. 可审计 CLI 模板

```bash
agent -p --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下节完整任务正文；将 <LAUNCH_HEAD> 替换为 git rev-parse HEAD>'
```

## 3. 完整任务正文

```text
你是 ImageAll 的唯一实现开发者 Cursor。开始一个全新的 Cursor CLI 会话实施“阶段 1 / 切片 1”；不得恢复任何旧 session，不得使用 Cursor 子代理或 MCP。

开工事实与门：
- 工作区 /Volumes/SSD1/ImageAll，分支本地 main，精确开工 HEAD 必须为 <LAUNCH_HEAD>。
- 该 HEAD 包含 Codex 批准的阶段 1 文档与 handoff；历史必须包含阶段 0 批准实现 892f4e29e1ebf492c1540c5a29d9c54abc05a78f。
- stream-json 必须证明 system/init.model = Composer 2.5 Fast；不符立即停止。
- 开工状态预期只有项目所有者已有未跟踪文件 user/用户-前端想法.md。不要读取、修改、暂存或提交它。若还有其他变化，停止并报告；不得 reset、checkout、stash、clean 或覆盖。
- 不访问或遍历 /Volumes/HDD2，不读取真实 Photos Library、年份目录或真实 App 容器。

开工前完整读取且按优先级执行：
1. AGENTS.md
2. docs/CURSOR-STAGE-1-SLICE-1-HANDOFF.md
3. docs/STAGE-1-IMPLEMENTATION-SPEC.md 第 1～4、6 节
4. docs/STAGE-1-PRODUCT-UI-SPEC.md 的 UI-005、UI-007、UI-008
5. docs/STAGE-1-BACKEND-ARCHITECTURE.md 第 3、9、12、13 节
6. docs/STAGE-0-IMPLEMENTATION-SPEC.md 与当前数据库/Job 测试，只继承已批准行为
7. .cursor/rules/codex-review-handoff.mdc
8. docs/LOCAL-TEST-DATA-SAFETY.md
冲突即停止报告，不自行解释。

唯一实施范围：
- 先按 handoff 第 3 节六簇 TDD 红灯→最小绿灯；每簇保留至少一个失败测试证据摘要。
- 新增且只新增 migration v002_add_stage_1_catalog_query_support：asset nullable file_name 及精确约束，六个命名 partial/expression index；更新 knownOrdered 为 v001、v002。V001CreateCatalogCoreMigration.swift 任何字节都不得改变，不做表重建，不建新表/FTS/cache/prediction/undo history。
- 新增不暴露 GRDB 的 Application 查询/命令端口和 GRDB adapter：current Asset 的 source/tag/availability/UTI/tag-presence/search 组合筛选；tagged/untagged 只按 accepted 判断；newest/oldest/fileNameAscending 严格 keyset pagination；grid/Inspector projection；Tag catalog、三态选择聚合、create、batch accept/reject/clear、create-and-apply 与无状态 prior-state restore。
- Tag 批量 1...10,000，允许同一 write transaction 内分块，禁止分事务。复用现有 Domain normalization/decision rules。Undo 只返回会话级前态，不加持久化历史或 UI。
- 使用真实临时文件数据库完成 handoff 第 7 节全部正反测试，并更新因 v002 必须变化的 snapshot/startup migration-prefix 期望，不弱化 future-schema 前置拒绝。

绝对禁止：
- entitlement、PrivacyInfo、NSOpenPanel、bookmark、目录/图片 I/O、Image I/O、FSEvents、thumbnail/cache 文件、SwiftUI、Composition Root、Job scheduler、v003、切片 2+ 占位、Photos/AI/自动化。
- 修改 Codex 文档、任务记录、user/、GRDB 版本、Package.resolved、foundationReady/CatalogReady。
- /Volumes/HDD2 或真实 Application Support/Caches I/O。
- push、amend、history rewrite。

验证与交付：
- 运行完整 Debug tests、Debug build、git diff --check；从输出或 xcresult 报告准确测试总数。
- 检查 Swift 6、依赖方向、target/scheme/entitlement/package/启动语义未变。
- 创建一个窄范围本地 commit，不 amend。author/committer Cursor Agent <cursoragent@cursor.com>；subject `feat(cursor): add stage 1 catalog query foundation`；trailer `Agent-Role: implementation`；不含 Co-authored-by；不提交 user/；不 push。
- commit 后除既有未跟踪 user/ 外无变化，停止于切片 1，不进入切片 2。
- 按 .cursor/rules/codex-review-handoff.mdc 输出完整中文 Codex 复审材料，包含新 session ID、实际模型、开工/交付 SHA、测试总数、构建、逐簇红绿、v001 零修改证据、六索引/查询/事务证据、提交归属和停止声明。
```

## 4. 调用后待补字段

| 字段 | 结果 |
|---|---|
| Codex launch commit / 实际开工 HEAD | 待调用 |
| `system/init.model` | 待调用 |
| Cursor session ID | 待调用 |
| Cursor 交付 commit | 待调用 |
| 测试总数 | 待调用 |
| Debug build | 待调用 |
| 最终工作区 | 待调用 |
| push | 否（预期） |
| Codex 评审 | 待复审 |

调用和复审后由 Codex 在独立文档 commit 中追加事实；不得让 Cursor 修改本记录。
