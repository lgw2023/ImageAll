# Cursor CLI 任务记录：阶段 1 / 切片 4

> 任务 ID：`stage-1-slice-4`<br>
> 状态：Revision required — 初版 `02f0003` 未通过 Codex 复审，等待同 session 窄范围返修<br>
> 日期：2026-07-15<br>
> 权威交接单：[`CURSOR-STAGE-1-SLICE-4-HANDOFF.md`](../CURSOR-STAGE-1-SLICE-4-HANDOFF.md)<br>
> 上一批准实现：`main@bfa0cf0bcedd58ffbdc19dc163a065589f7aeb0d`<br>
> 上一 Codex 验收：`main@bd87fc5e91b110d62dda8c1706b8ce573ad48d72`<br>
> 实际开工 HEAD：`<LAUNCH_HEAD>`，由调用时替换为包含本记录的精确 Codex 文档 commit

## 1. 任务边界

本任务只实现阶段 1 / 切片 4：

- `v003_add_derived_image_cache`；
- 一个 STRICT `derived_image_cache_entry` 表和两个命名索引；
- Application 异步派生图/维护端口；
- Image I/O representation version 1；
- 应用自有 Caches 内的安全路径、原子发布、命中验证和孤儿维护；
- 20 GiB 配额、5 GiB/5% 磁盘安全余量、LRU 淘汰；
- PrivacyInfo 增加 DiskSpace/`E174.1`；
- 临时 fixture、临时数据库和临时缓存根上的完整 TDD。

停止位置：不接 FSEvents、活动 projection、App/SwiftUI、Composition Root、Job/scheduler 或产品网格；不进入切片 5～7。

## 2. 可复制 CLI 命令

调用前先把 `<LAUNCH_HEAD>` 替换为 `git rev-parse HEAD` 的精确值。该调用必须创建全新 session；没有 `--resume` 或 `--continue`。

```bash
set -o pipefail
LAUNCH_HEAD="$(git rev-parse HEAD)"
PROMPT="$(
  awk '/^<!-- CURSOR_PROMPT_BEGIN -->$/{capture=1; next}
       /^<!-- CURSOR_PROMPT_END -->$/{capture=0}
       capture' docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md |
  sed '1d;$d' |
  sed "s/<LAUNCH_HEAD>/$LAUNCH_HEAD/g"
)"
agent -p --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" |
  tee /tmp/ImageAll-stage1-slice4.cursor.stream.jsonl
```

完整 `stream-json` 只写临时文件，不提交仓库。若 `system/init.model` 不是 `Composer 2.5 Fast`，立即终止，不接受交付。

## 3. 完整任务正文

<!-- CURSOR_PROMPT_BEGIN -->
```text
你是 ImageAll 的实现开发者 Cursor。现在开始全新的“阶段 1 / 切片 4”独立任务；不得恢复任何旧 session，不得使用 Cursor Task 子代理、MCP 或其他模型。实际模型必须由 stream-json 证明为 Composer 2.5 Fast。

角色边界：Codex 只负责产品/架构文档与复审；你只负责本交接单授权的 Swift、migration、测试、PrivacyInfo 和必要 Xcode 文件引用。不得修改或提交任何 Codex docs。

开工门：
- 当前必须是本地 main@<LAUNCH_HEAD>，分支 main。
- 上一批准实现是 bfa0cf0bcedd58ffbdc19dc163a065589f7aeb0d，Codex 接受 commit 是 bd87fc5e91b110d62dda8c1706b8ce573ad48d72；切片 3 已关闭，其 session 已退役。
- 工作区除项目所有者已有未跟踪 ?? user/ 外必须无其他变化。不得读取、修改、stage 或提交 user/。
- 不访问、stat、枚举或读取 /Volumes/HDD2；不读取真实 App 用户容器。测试只使用各测试创建并登记的临时根。
- 禁止 reset/checkout/restore/stash/clean/amend/push/rebase/cherry-pick/squash/history rewrite。

开工前按顺序完整读取：
1. AGENTS.md
2. docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md
3. docs/STAGE-1-IMPLEMENTATION-SPEC.md 第 1～3、5.3、6 节
4. docs/STAGE-1-BACKEND-ARCHITECTURE.md 第 3～4、7～8、11～13 节
5. docs/CURSOR-STAGE-1-SLICE-3-HANDOFF.md 中已批准的媒体、fingerprint、scope、只读与 generation 语义
6. docs/LOCAL-TEST-DATA-SAFETY.md
7. 当前 AppPaths、migration、snapshot/startup、授权、Reconcile、数据库和测试代码
8. .cursor/rules/codex-review-handoff.mdc

若权威文档之间冲突、当前 HEAD/工作区不符、必须修改已发布 migration/产品语义或可能触碰真实照片，立即停止并报告，不得自行解释。

唯一交付目标：
实现后端按需派生图缓存，不接任何 UI、Composition Root、FSEvents、活动 projection、scheduler 或 Job。

持久化合同：
- 新 migration ID 固定 v003_add_derived_image_cache；knownOrdered 为 v001、v002、v003。
- V001CreateCatalogCoreMigration.swift 和 V002AddStage1CatalogQuerySupportMigration.swift 零字节修改，v001/v002 原始 DDL/索引零变化。
- 新建 STRICT derived_image_cache_entry，精确列：
  id TEXT PK（与 v001 相同的小写 canonical UUID CHECK）；
  asset_id TEXT NOT NULL FK asset(id) ON DELETE CASCADE；
  content_revision INTEGER NOT NULL >=1；
  representation_version INTEGER NOT NULL >=1，本轮生产值 1；
  variant TEXT NOT NULL，只允许 gridSmall/gridRegular/preview；
  storage_format TEXT NOT NULL，只允许 jpeg/png；
  pixel_width/pixel_height INTEGER NOT NULL >0；
  byte_size INTEGER NOT NULL >0；
  encoded_sha256 BLOB NOT NULL 长度 32；
  created_at_ms/last_accessed_at_ms INTEGER NOT NULL >=0。
- 尺寸组合 CHECK：gridSmall=256x256；gridRegular=512x512；preview 正尺寸且最长边 <=2048。
- 数据库不存绝对/相对缓存路径、扩展名、bookmark、来源文件名或用户输入路径。
- 只建 derived_image_cache_key_uq(asset_id, content_revision, representation_version, variant) 和 derived_image_cache_lru_idx(last_accessed_at_ms, id)。
- 不建 trigger、settings/quota/Job/FTS/prediction/Photos/UI 表，不改现有业务表。
- snapshot/restore/future-schema/migration-prefix 只做 v003 最小更新。

Application 合同：
- 提供不含 GRDB/ImageIO/CoreGraphics/AppKit/URL/路径的异步 loadOrGenerate 端口。
- request 只含 assetID+variant。
- payload 只含 entryID、assetID、contentRevision、representationVersion=1、variant、jpeg/png、像素尺寸、encodedBytes、cacheHit/generated；不含路径/SQL/bookmark。
- 提供不接 scheduler 的显式异步 maintenance 端口，只返回 removedEntries/removedObjects/removedBytes/unsafeObjects 聚合。
- 封闭安全错误 raw value：derivedAssetNotFound、derivedAssetIneligible、derivedAuthorizationRequired、derivedSourceUnavailable、derivedSourceChanged、derivedDecodeFailed、derivedEncodeFailed、derivedCapacityUnavailable、derivedInsufficientSpace、derivedCacheUnsafePath、derivedCachePersistenceFailed。错误不含路径、文件名、bookmark、bytes、SQL、底层 NSError。

资格与只读 source：
- 只为存在的 current+available folder file、active Source、合法 relative path/file name、完整 size/mtime fingerprint、允许静态 UTI 生成。
- historical、Photos、missing/unreadable/unsupported、disabled/unavailable/authorizationRequired、缺 fingerprint 全部拒绝新生成。
- 精确 key 的已有有效 entry 可命中；状态变化不得伪造 key 或改写 Asset。
- 复用切片 2/3 已批准的 bookmark/stale/root/state/scope 服务，不复制权限状态机；所有路径 start/stop 配对。
- 从安全作用域根逐分量 no-follow 只读打开，不跟随 symlink/alias/package；Image I/O 消费已安全打开的句柄/数据提供者，不能检查后通过未锚定路径重开。
- source 零写入：无 create/write/truncate/remove/move/copy/rename/resource metadata/sidecar。

representation version 1：
- Image I/O 再确认 JPEG/PNG/HEIC/HEIF/TIFF/WebP canonical UTI 和单一静态主图。
- 方向 transform 覆盖 orientation 1～8。
- gridSmall 精确 256x256、gridRegular 精确 512x512：视觉居中 aspect-fill，对称 crop，小图高质量放大。
- preview 保持完整视觉比例、最长边 <=2048，较小源图不放大。
- 8-bit sRGB、高质量插值。
- 有影响视觉结果的 alpha 输出 PNG；其余 baseline JPEG quality 0.85。
- 不复制 EXIF/GPS/XMP/文件名/原始时间 metadata。
- 先在内存编码，计算 byte size/派生 SHA-256并重新 Image I/O 解码验证单图、format、尺寸、完整性；不得计算全库原图 SHA-256或回写 file_fingerprint.sha256。

缓存路径与发布：
- 根固定 AppPaths.cachesDirectory/DerivedImages/v1；子目录 staging 与 objects/<entry uuid 去连字符前两位>/<lower uuid>.<jpg|png>。
- 路径只由合法 entry UUID+封闭 format 推导，数据库不存 path。
- 生产根只能来自 AppPaths Caches；测试根必须是当前测试登记的临时根。
- 校验根和全部分量非 symlink，操作以打开目录句柄相对/no-follow 语义锚定；字符串 prefix 不能作为授权。无法证明即 derivedCacheUnsafePath，不触碰外部目标。
- 内存编码/验证 -> staging 排他创建/完整写入/sync -> staging 再验证 -> 同卷原子 rename 到全新对象 -> GRDB 最终重验证并发布 entry -> 返回；DB entry 永远晚于完整对象。
- 第 4 步后 DB 失败最多留下孤儿；绝不允许 entry 指向半文件。旧对象只在 entry 替换事务成功后 best-effort 删除。
- 同 key 并发在实例内合并/串行；跨实例 unique+事务选 winner，loser 不覆盖 winner并清理自己的对象。
- hit 验 regular/non-symlink、size、SHA、format、单图和尺寸；有效 hit touch LRU。缺失/损坏先移除 entry，再重建，不改 Asset。

source 稳定性：
- 生成前 current opened fingerprint 必须与 DB 快照相容。
- 生成后从同一打开对象与 current locator 再验证 size/mtime/resource ID。
- final DB 事务再验证 Asset current/available、Source active、relative locator、content_revision 和 fingerprint。
- 任一变化返回 derivedSourceChanged，删除本调用派生物，零 entry，且不改 Asset/fingerprint/revision/generation/Job。

配额与容量：
- 固定 published quota=20*1024^3，reserve=max(5*1024^3, volumeTotalCapacity/20)，所有运算防 overflow。
- 在派生卷只用 volumeAvailableCapacityForImportantUsageKey + volumeTotalCapacityKey；缺失、负数或 overflow fail closed 为 derivedCapacityUnavailable。
- encoding bytes 已知且 staging 之前执行 admission。
- published+incoming 超 20GiB时按 last_accessed_at_ms,id LRU 淘汰；available < reserve+incoming 时继续淘汰并在每个对象实际删除后重查 available。
- 淘汰先事务删 entry，再安全删对象；对象未删不得当作容量回升。无法满足返回 derivedInsufficientSpace，无 staging/新 entry。
- 只删 derived cache entry/object；绝不删 catalog、snapshot、backup、runtime、来源或事实。
- 不使用 UserDefaults，不做设置 UI。

维护：
- 提供显式、排他、幂等 maintenance，不接启动/scheduler。
- 删除缺失/损坏 entry、无引用 object、无活跃调用的遗留 staging；Asset cascade 后孤儿也收敛。
- 只遍历自有派生根；symlink/未知层级/无法证明归属不跟随、不触碰外部目标，聚合 unsafeObjects 或返回安全错误。
- 只返回数量和 bytes，不泄漏对象名/路径。

Privacy/依赖：
- PrivacyInfo 精确包含 FileTimestamp->3B52.1 和 DiskSpace->E174.1；不得有 UserDefaults/tracking/collected-data/其他类别。
- entitlement 保持 sandbox+user-selected read-only+app-scope bookmarks（Debug 可注入 get-task-allow）。
- 不新增 package；使用系统 Foundation/ImageIO/CoreGraphics/CryptoKit 与现有 GRDB。

强制 TDD，先保留七簇真实红灯摘要，再最小实现：
1. v003 migration/schema/STRICT/CHECK/FK/index introspection、sentinel upgrade、future guard、snapshot/restore、v001/v002零 diff。
2. Application 封闭契约、资格正反、exact key、scope 配对、分层无路径泄漏。
3. 六种允许格式、orientation 1～8像素级、grid crop/尺寸、preview fit、不放大、alpha PNG、opaque JPEG、sRGB、metadata stripping、动画/损坏反例；不得 skip。
4. pre/post/final 变化、有效 hit 不开 source、缺失/截断/同尺寸篡改/格式尺寸错误重建、revision key、同 key并发。
5. 真实生产 fault：staging create/write/sync/validate、rename、rename后DB前、DB insert/update/revalidation/LRU touch、旧对象删除；比较完整稳定事实和缓存树，证明最多孤儿、无半文件entry。
6. 20GiB、5GiB、5%、等于/差1/overflow、capacity unavailable、稳定 LRU、淘汰后重查、单对象过大、维护幂等、root/shard/object symlink与祖先/外部删除拒绝。
7. built PrivacyInfo、codesign、Swift6/macOS15/arm64、source write spy和树/bytes/mtime不变、现有499项回归、App/UI/CompositionRoot/scheme/entitlement/Package边界、HDD2/真实容器/user零访问。

不能用 skip、弱化断言、只测 fake/helper、只 grep 源码或未实际触发 fault 获得绿灯。若当前 SDK 无法生成某格式 fixture，先停下报告证据，不得静默删测试或改变批准格式。

绝对禁止：
- 修改 ImageAll/App、SwiftUI、RootView、Composition Root、Sidebar、网格、Inspector、菜单、活动入口或外观。
- FSEvents、活动 projection、scheduler/timer/background loop、新 Job kind。
- 修改 v001/v002、已批准标签/查询/对账语义，或进入切片5～7。
- PhotoKit、Vision/Core ML/OCR/AI/网络/跨来源去重/全库原图hash。
- source写入、sidecar、UserDefaults、新entitlement、新package。
- 修改 docs、docs/cursor-cli-tasks或user；访问HDD2/真实App容器。

完成验证：
- xcodebuild -scheme ImageAll -destination 'platform=macOS,arch=arm64' -configuration Debug -resultBundlePath /tmp/ImageAll-stage1-slice4.xcresult test
- xcodebuild -scheme ImageAll -destination 'platform=macOS,arch=arm64' -configuration Debug build
- git diff --check
- 从xcresult报告准确passed/failed/skipped；plutil built PrivacyInfo；codesign entitlements；Swift/macOS/arm64；v001/v002、Package.resolved、App/UI/CompositionRoot/scheme/entitlement零diff；生产import与source write检查。

提交要求：
- 只创建一个窄范围本地 commit，不 amend。
- author/committer Cursor Agent <cursoragent@cursor.com>。
- subject: feat(cursor): add derived image cache
- trailer: Agent-Role: implementation
- 无 Co-authored-by；不提交 docs/user；不 push。
- commit 后除既有 ?? user/ 外工作区干净。
- 按 .cursor/rules/codex-review-handoff.mdc 输出完整中文 Codex 复审材料，回传 system/init.model、全新 session ID、开工/交付 SHA、七簇红绿、准确测试数/build、manifest/codesign、diff边界、Git作者/trailer、HDD2/user/容器零访问。
- 明确停止于切片4，未进入FSEvents/活动projection或切片6～7 UI。
```
<!-- CURSOR_PROMPT_END -->

## 4. 禁止事项与停止位置

- 本次不是切片 3 返修，不得恢复 `3c43d530-d8d1-4bf9-b1a8-7493ee324d09` 或任何旧 session；
- Cursor 不得编辑本记录、权威 handoff 或其他 docs；
- 不访问 `user/`、`/Volumes/HDD2` 或真实 App 容器；
- 不 push、不改写历史；
- 不接 FSEvents、活动 projection 或产品 UI；
- 交付 commit 后停止，等待 Codex 逐项复审。

## 5. 调用与评审事实

调用后由 Codex 追加：

| 字段 | 实际值 |
|---|---|
| Launch HEAD | `416f9a3311972e48919a33b4a45e06b74065a7ee` |
| `system/init.model` | `Composer 2.5 Fast`（`stream-json` 第 1 条） |
| Cursor session ID | `cbb558ad-68b9-4286-8d16-54078021746b`（全新 session） |
| Cursor 初版交付 commit | `02f000390e0ec3451511f076aa879b2e2458d8b3` |
| 初版测试 | Cursor 与 Codex 独立结果均为 529 passed / 0 failed / 0 skipped；通过不等于验收矩阵充分 |
| 初版构建 | arm64 Debug `BUILD SUCCEEDED` |
| Git 归属 | `Cursor Agent <cursoragent@cursor.com>`；`feat(cursor): add derived image cache`；`Agent-Role: implementation` |
| 工作区 / push | `main`，仅既有 `?? user/`；未 push |
| Codex 初审 | **拒绝**；见第 6 节，必须沿用本 session 返修，禁止新 session 或 amend 初版 commit |

## 6. Codex 初审结论与返修要求

### 6.1 结论

`02f0003` 保持了 v001/v002、App/SwiftUI 与切片停止边界，v003 DDL 的主体形状也符合交接单；但实现与测试没有达到第 5～12 节的安全及行为门。529 项绿灯中，新增 30 项远不足以证明交接单明确要求的反例、fault matrix、路径对抗和像素行为，并出现了“测试名声称 LRU、实际不触发淘汰”的弱化断言。切片 4 不通过。

### 6.2 阻塞项

1. **有效 cache hit 被生成资格错误阻断。** `loadOrGenerate` 在查 exact-key entry 前先要求完整 generation context 且 `source.state = active`、Asset `available`；因此 Source 离线、disabled 或 authorizationRequired 时，即使同 revision 已有完整有效对象也无法返回。应先取得 Asset 当前 revision 并验证 exact hit；只有 miss/损坏后才进入新生成资格与授权流程。增加“生成后 Source 切换四种非 active 状态仍命中且 scope 不启动；同状态 miss 不生成”的运行测试。
2. **缓存文件操作没有满足目录句柄锚定/no-follow 合同。** `Data(contentsOf:)`、绝对路径 `rename`/`unlink`、`FileManager.enumerator` 和只检查 `versionRoot`/父目录会留下 staging、objects、shard 祖先被 symlink/rename 置换的 TOCTOU 窗口；尤其 `unlink` 会跟随被替换的父目录而可能删除派生根外同名文件。所有读、排他创建、sync、rename、删除和维护遍历必须锚定已打开且验证过的自有目录 FD，以相对组件和 no-follow 等价语义执行；逐层拒绝 root/staging/objects/shard/object symlink、未知层级及外部目标。测试必须放置外部哨兵并证明零触碰，不能只测 versionRoot symlink。
3. **source fingerprint 不是从已打开对象取得。** source bytes 从安全 FD 读取，但 size/mtime/resource ID 随后用拼接 URL 查询；post check 也重新按路径查询，不能证明“同一打开对象”稳定，存在 locator 替换窗口。改为对打开 FD `fstat`/等价句柄事实做 pre/post，再独立 no-follow 重开 current locator 比较 resource identity；最终 DB 事务继续重验证。用可控 seam 覆盖解码期间 size、mtime、resource ID、locator、revision、availability、Source state 和 fingerprint 分别变化，断言零 entry/零本次对象且事实不变。
4. **发布顺序与 fault coverage 不完整。** staging 写完后当前代码只重新校验内存 artifact，没有从 staging 文件复读 size/hash/解码；普通 `rename` 允许覆盖已存在目标。必须从 staging FD 复读验证，并采用“目标存在则失败”的同卷原子发布语义。逐一实际触发 staging create/write/sync/validate、rename、rename 后 DB 前、DB insert/update、final revalidation、LRU touch、替换旧 key 事务和旧对象删除 fault；比较稳定排序的完整 Asset/fingerprint/cache-entry 事实与缓存树。所有公开端口只泄出封闭 `DerivedImageError`，不得透传 `DerivedImageSecureIOError`、GRDB error 或带路径的底层 error。
5. **配额/LRU 测试被弱化。** `testPublishedQuotaEvictionUsesLRUOrder` 只生成一个小对象并断言总量小于 20 GiB，既不触发淘汰也不证明 `(last_accessed_at_ms,id)` 稳定顺序。建立可注入但仍走生产 admission/repository/store 的精确边界测试，覆盖 20 GiB、5 GiB、5%、等于/差 1、overflow、单对象超限、容量 nil/负值等价输入、淘汰后逐次重查、删除失败不算容量回升、稳定 LRU 与事实/来源零修改。
6. **视觉与缓存损坏矩阵不足。** orientation 1～8 目前只断言宽高，违反“非对称像素 fixture、不能只断言宽高”；缺 grid 居中 crop、gridRegular、preview 横/竖/方及 2048 边界、sRGB、alpha 位于首个 64×64 之外、metadata 的 GPS/XMP/文件名/日期、实际 UTI 漂移和多帧允许格式反例。cache 损坏只改数据库 byte_size，缺对象缺失、截断、同尺寸篡改、格式错误、尺寸错误和 revision/version/variant 独立 key 行为。补真实像素/文件行为断言，禁止通过提前 return 弱化。
7. **v003 约束与迁移回归证据不足。** 当前 constraint 测试只证明重复 key 与 cascade；必须覆盖 UUID 大写/结构/非 hex，revision/version、variant、format、三类尺寸组合、byte size、BLOB storage class/长度、timestamps 的正反例，以及不同 revision/version/variant 可共存。补真实 sqlite metadata 对列、STRICT、FK、两个 index key 的 introspection；补 future migration 在 v003 前拒绝、重开幂等、snapshot/restore/migration-prefix 与旧 snapshot 合法升级。`testV001AndV002DDLUnchangedAfterV003` 不得遗漏 `asset`，同时保留 migration 文件零字节 diff。
8. **生成资格、维护排他和错误/权限配对矩阵不足。** 生产资格未校验持久 `media_type` 是否在批准 UTI 集合；缺 missing/unreadable/unsupported、四类非 active Source、缺 fingerprint 反例。maintenance gate 只在 maintenance 之间互斥，不能阻止它清除正在生成的 staging；需与 active generate 协调，同时清理缺失/损坏 entry、cascade/publish/staging orphan 并验证二次幂等和聚合值。补 scope 在成功、hit、source change、decode/encode、capacity、文件发布与 DB failure 全部分支的 start/stop 配对。

### 6.3 返修执行约束

- 这是同一未验收交接单，必须 `--resume cbb558ad-68b9-4286-8d16-54078021746b`，仍显式使用 `--model composer-2.5-fast`；不得启动新 session、Task 子代理或 MCP。
- 当前基线必须为 `main@<REVIEW_HEAD>`，其中包含初版实现和本评审记录；工作区除 `?? user/` 外干净。
- 不 amend `02f0003`。返修以独立 `fix(cursor): harden derived image cache contract` commit 提交，保持 Cursor identity/trailer。
- 不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；不进入 FSEvents/Job/scheduler/UI。
- 不访问 `user/`、`/Volumes/HDD2` 或真实 App 容器；不 push、不改写历史。
- 完成后重新运行无管道的完整 arm64 Debug test 和 build，提供新 xcresult 精确计数、全部 fault/边界测试清单与最终工作区证据，再停止等待 Codex 复审。

### 6.4 首次返修 CLI 命令与完整正文

调用时将 `<REVIEW_HEAD>` 替换为包含第 6 节初审结论与本调用记录的精确 Codex commit：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份“阶段 1 / 切片 4”交接单的首次 Codex 返修。不要开始新任务，不要进入切片 5～7。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6 节的初审结论、8 项阻塞和返修约束；这些内容全部是本次验收合同，不得挑选或弱化。

开工门：当前必须是本地 main@<REVIEW_HEAD>；它在初版实现 02f000390e0ec3451511f076aa879b2e2458d8b3 后只增加 Codex 评审记录 commit；工作区只能有项目所有者既有 ?? user/。system/init.model 必须仍为 Composer 2.5 Fast。不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得 reset/checkout/restore/stash/clean/amend/push/rebase/cherry-pick/squash/history rewrite；不得使用 Cursor Task 子代理或 MCP。

逐项修正第 6.2 节全部问题。先用新增或加强后的运行测试真实复现每个缺口，再做最小生产修正；不得删除/改名以掩盖失败，不得提前 return、条件 skip、未触发 fault、只测 helper/fake、源码 grep 或“总量低于配额”之类弱断言换绿灯。特别要求：
- valid exact-key hit 必须先于生成资格/授权，非 active 或离线 Source 的 valid hit 不打开 source；miss 仍禁止生成。
- 缓存根的读写、排他创建、sync、同卷不覆盖发布、删除与维护必须以已验证目录 FD 相对/no-follow 锚定，逐层覆盖 root/staging/objects/shard/object 置换；外部哨兵必须保持不变。
- source pre/post 必须从同一已打开 FD 获取 fingerprint，再独立 no-follow 重开 current locator；final DB 逐字段重验证。
- staging 必须从已写文件复读 size/hash/单图/format/尺寸；所有 fault seam 实际触发，公开端口只返回封闭 DerivedImageError。
- 配额测试必须实际触发稳定 LRU 与容量重查，覆盖 20GiB/5GiB/5% 等于、差 1、overflow、单对象超限、删除失败。
- orientation 1～8 使用非对称像素断言；完整补齐 crop/fit/sRGB/alpha/metadata、cache corruption、v003 schema/migration、资格、scope 和 maintenance 排他矩阵。

不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement 或 Package.resolved；不新增 Job/FSEvents/scheduler/UI。允许仅为精确测试 seam 做最小内部注入，但不能改变产品常量或把生产安全逻辑移入测试。

完成后运行无管道的完整 arm64 Debug test（写入全新 /tmp/ImageAll-stage1-slice4-revision1.xcresult）、arm64 Debug build 和 git diff --check；从 xcresult 报告准确 passed/failed/skipped，并报告 built PrivacyInfo、codesign、Swift6/macOS15/arm64、v001/v002 与禁止边界零 diff。创建独立 commit，不 amend：Cursor Agent <cursoragent@cursor.com>；subject `fix(cursor): harden derived image cache contract`；trailer `Agent-Role: implementation`；不含 Co-authored-by；不提交 docs/user；不 push。提交后工作区只余既有 ?? user/，输出完整中文 Codex 复审材料并停止。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1.cursor.stream.jsonl
```

## 7. 首次返修中断与续接诊断

### 7.1 中断事实

- 首次返修实际从 `main@b6c1b6d4a184fb9bb64de10ae8180cf996dcc590` 启动；
- `system/init.model = Composer 2.5 Fast`，session 仍为 `cbb558ad-68b9-4286-8d16-54078021746b`；
- Cursor 在未提交的返修草稿中把维护枚举、`fstatat` 与删除失败逐步改成 `try?`/空数组/`unsafeObjects`，并尝试用 `O_RDWR | O_DIRECTORY` 打开目录；这会吞掉不能证明安全归属的错误，也错误理解了 `*at` 操作的权限语义；
- Codex 因此以 Ctrl-C 中断本次自动返修，未创建 Cursor commit。现场保留为 7 个实现/测试支持文件的未提交草稿；既有 `?? user/` 保持未读未改。

### 7.2 续接前必须先修复的根因

1. **不得以吞错换绿灯。** 维护枚举失败、合法对象的 `fstatat` 失败、合法目标的 `unlinkat` 失败不能统一降格为空集合或“unsafe 数量”；未知名称、symlink、非普通对象可以计为 `unsafeObjects` 并跳过，但已确认属于缓存的对象发生 I/O 失败必须映射为封闭 `DerivedImageError`。恢复 `sweepUnreferencedObjects` / `sweepStaging` 的显式 throws 语义。
2. **目录 FD 保持只读打开。** macOS 目录不能依赖 `O_RDWR` 打开；`mkdirat`、`unlinkat`、`renameatx_np` 使用 `O_RDONLY | O_DIRECTORY | O_NOFOLLOW` 的父目录 FD 即可，授权来自目录权限而非 FD 的 read/write mode。删除 `openMutableDirectory*` 与 `tryOpenMutableDirectory` 方向，不得经 `F_GETPATH` 或绝对路径重新打开。
3. **发布必须真正 no-replace。** 普通 `renameat` 会覆盖已有目标，不能命名为 `renameatNoReplace`。macOS 15 路径应使用 `renameatx_np(..., RENAME_EXCL)` 或同等原子排他原语；加入“预置目标内容保持不变、发布失败、staging/DB 状态符合合同”的真实测试。
4. **句柄生命周期必须真实。** `readStaging` 当前返回的 fd 已在函数内 `defer close`，不得返回无效句柄；改成只返回 bytes/size，或由调用方明确拥有并关闭有效 FD。`fdopendir` 成功后只由 `closedir` 关闭 duplicated fd；`readdir` 结束需区分 EOF 与 errno。
5. **维护只识别封闭命名空间。** shard 必须恰为两个小写十六进制字符；object 必须为小写 canonical UUID 加与封闭 format 对应的 `.jpg|.png`。未知目录/文件只计 unsafe 且绝不进入、删除或跟随；外部哨兵不变。删除无效 entry 后应基于仍有效 entries 重新计算引用集，使同一次维护可以收敛其对象。
6. **指纹必须兼容切片 3 的生产事实。** 不得只把测试 seed 改成 raw `fstat` 纳秒来迁就派生图实现。v002 生产 reconcile 通过 `FoundationFolderFileResourceReader.modifiedAtNs` 与 `resourceIdentifier` 持久化；句柄派生事实必须规范化为同一已批准表示，并用“真实生产 reconcile 建立 Asset/fingerprint，再生成派生图”的集成测试证明相容。pre/post 从同一 FD；reopen current locator 仍必须 no-follow 并从新 FD 取得 identity，不能再通过拼接 URL 查询 resource ID。
7. **不要扩大无测试实现。** 当前草稿没有新增第 6.2 节要求的行为测试。先把上述根因写成失败测试，再最小修复；随后继续补完原 8 项阻塞矩阵，不能只让原有 529 项恢复。

### 7.3 第二次续接命令

该命令仍属于同一份未验收交接单，必须恢复原 session。工作区会包含上次中断的实现草稿；不得 reset、restore、checkout、stash 或 clean，应逐行整理该草稿。

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 首次返修；上一次运行被 Codex 主动中断，因为实现开始用 try?/空集合吞掉维护错误，并错误尝试 O_RDWR 打开目录。不要新建 session，不要提交当前草稿，不要进入切片 5～7。

先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md、docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6 节和第 7 节。当前必须是本地 main@<REVIEW_HEAD>；除既有 ?? user/ 外，还会有上次中断留下的 7 个实现/测试支持文件未提交变化，这是同一任务草稿。不得 reset/checkout/restore/stash/clean 覆盖它们，也不得读取、修改、stage 或提交 user/。模型必须仍为 Composer 2.5 Fast；不得使用 Task 子代理或 MCP；不得访问 /Volumes/HDD2 或真实 App 容器；不得 push、amend 或改写历史。

先按第 7.2 节逐项纠正根因，并先增加会失败的真实测试：恢复维护显式错误语义；目录 FD 只读且所有操作继续 dirfd-relative/no-follow；用 renameatx_np+RENAME_EXCL 等价原语证明目标不覆盖；修正 staging/目录枚举 FD 生命周期；封闭 shard/object 名称且未知层级零触碰；删除无效 entry 后同次维护重算引用；用生产 reconcile 建立的 fingerprint 证明 handle facts 兼容，不得用 raw-fstat 测试 seed 代替生产语义。所有公共端口只泄出封闭 DerivedImageError。

这些根因稳定后，继续完成第 6.2 节原 8 项阻塞的完整 TDD 矩阵，尤其 valid hit 先于资格、source pre/post/reopen/final 变化、真实 staging/DB/LRU fault、实际触发 20GiB/reserve/LRU、orientation 1～8 非对称像素、cache corruption、v003 全约束/迁移、scope 配对与 generation/maintenance 排他。禁止删测、skip、提前 return、只测 helper/fake、源码 grep 或以未触发的断言换绿灯。

不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；不新增 Job/FSEvents/scheduler/UI。完成后运行无管道全量 arm64 Debug test（全新 xcresult）、arm64 Debug build、git diff --check 与全部边界核验；独立创建 fix(cursor): harden derived image cache contract commit，Cursor identity/trailer，禁止 amend/docs/user/push。输出完整复审材料后停止。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1b.cursor.stream.jsonl
```
