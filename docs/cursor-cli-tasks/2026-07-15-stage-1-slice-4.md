# Cursor CLI 任务记录：阶段 1 / 切片 4

> 任务 ID：`stage-1-slice-4`<br>
> 状态：Revision required — 初版 `02f0003` 未通过 Codex 复审，等待同 session 窄范围返修<br>
> 日期：2026-07-15<br>
> 权威交接单：[`CURSOR-STAGE-1-SLICE-4-HANDOFF.md`](../CURSOR-STAGE-1-SLICE-4-HANDOFF.md)<br>
> 上一批准实现：`main@bfa0cf0bcedd58ffbdc19dc163a065589f7aeb0d`<br>
> 上一 Codex 验收：`main@bd87fc5e91b110d62dda8c1706b8ce573ad48d72`<br>
> 实际开工 HEAD：`<LAUNCH_HEAD>`，由调用时替换为包含本记录的精确 Codex 文档 commit

## 1. 任务边界

本任务只实现阶段 1 / 切片 4：

- `v003_add_derived_image_cache`；
- 一个 STRICT `derived_image_cache_entry` 表和两个命名索引；
- Application 异步派生图/维护端口；
- Image I/O representation version 1；
- 应用自有 Caches 内的安全路径、原子发布、命中验证和孤儿维护；
- 20 GiB 配额、5 GiB/5% 磁盘安全余量、LRU 淘汰；
- PrivacyInfo 增加 DiskSpace/`E174.1`；
- 临时 fixture、临时数据库和临时缓存根上的完整 TDD。

停止位置：不接 FSEvents、活动 projection、App/SwiftUI、Composition Root、Job/scheduler 或产品网格；不进入切片 5～7。

## 2. 可复制 CLI 命令

调用前先把 `<LAUNCH_HEAD>` 替换为 `git rev-parse HEAD` 的精确值。该调用必须创建全新 session；没有 `--resume` 或 `--continue`。

```bash
set -o pipefail
LAUNCH_HEAD="$(git rev-parse HEAD)"
PROMPT="$(
  awk '/^<!-- CURSOR_PROMPT_BEGIN -->$/{capture=1; next}
       /^<!-- CURSOR_PROMPT_END -->$/{capture=0}
       capture' docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md |
  sed '1d;$d' |
  sed "s/<LAUNCH_HEAD>/$LAUNCH_HEAD/g"
)"
agent -p --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" |
  tee /tmp/ImageAll-stage1-slice4.cursor.stream.jsonl
```

完整 `stream-json` 只写临时文件，不提交仓库。若 `system/init.model` 不是 `Composer 2.5 Fast`，立即终止，不接受交付。

## 3. 完整任务正文

<!-- CURSOR_PROMPT_BEGIN -->
```text
你是 ImageAll 的实现开发者 Cursor。现在开始全新的“阶段 1 / 切片 4”独立任务；不得恢复任何旧 session，不得使用 Cursor Task 子代理、MCP 或其他模型。实际模型必须由 stream-json 证明为 Composer 2.5 Fast。

角色边界：Codex 只负责产品/架构文档与复审；你只负责本交接单授权的 Swift、migration、测试、PrivacyInfo 和必要 Xcode 文件引用。不得修改或提交任何 Codex docs。

开工门：
- 当前必须是本地 main@<LAUNCH_HEAD>，分支 main。
- 上一批准实现是 bfa0cf0bcedd58ffbdc19dc163a065589f7aeb0d，Codex 接受 commit 是 bd87fc5e91b110d62dda8c1706b8ce573ad48d72；切片 3 已关闭，其 session 已退役。
- 工作区除项目所有者已有未跟踪 ?? user/ 外必须无其他变化。不得读取、修改、stage 或提交 user/。
- 不访问、stat、枚举或读取 /Volumes/HDD2；不读取真实 App 用户容器。测试只使用各测试创建并登记的临时根。
- 禁止 reset/checkout/restore/stash/clean/amend/push/rebase/cherry-pick/squash/history rewrite。

开工前按顺序完整读取：
1. AGENTS.md
2. docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md
3. docs/STAGE-1-IMPLEMENTATION-SPEC.md 第 1～3、5.3、6 节
4. docs/STAGE-1-BACKEND-ARCHITECTURE.md 第 3～4、7～8、11～13 节
5. docs/CURSOR-STAGE-1-SLICE-3-HANDOFF.md 中已批准的媒体、fingerprint、scope、只读与 generation 语义
6. docs/LOCAL-TEST-DATA-SAFETY.md
7. 当前 AppPaths、migration、snapshot/startup、授权、Reconcile、数据库和测试代码
8. .cursor/rules/codex-review-handoff.mdc

若权威文档之间冲突、当前 HEAD/工作区不符、必须修改已发布 migration/产品语义或可能触碰真实照片，立即停止并报告，不得自行解释。

唯一交付目标：
实现后端按需派生图缓存，不接任何 UI、Composition Root、FSEvents、活动 projection、scheduler 或 Job。

持久化合同：
- 新 migration ID 固定 v003_add_derived_image_cache；knownOrdered 为 v001、v002、v003。
- V001CreateCatalogCoreMigration.swift 和 V002AddStage1CatalogQuerySupportMigration.swift 零字节修改，v001/v002 原始 DDL/索引零变化。
- 新建 STRICT derived_image_cache_entry，精确列：
  id TEXT PK（与 v001 相同的小写 canonical UUID CHECK）；
  asset_id TEXT NOT NULL FK asset(id) ON DELETE CASCADE；
  content_revision INTEGER NOT NULL >=1；
  representation_version INTEGER NOT NULL >=1，本轮生产值 1；
  variant TEXT NOT NULL，只允许 gridSmall/gridRegular/preview；
  storage_format TEXT NOT NULL，只允许 jpeg/png；
  pixel_width/pixel_height INTEGER NOT NULL >0；
  byte_size INTEGER NOT NULL >0；
  encoded_sha256 BLOB NOT NULL 长度 32；
  created_at_ms/last_accessed_at_ms INTEGER NOT NULL >=0。
- 尺寸组合 CHECK：gridSmall=256x256；gridRegular=512x512；preview 正尺寸且最长边 <=2048。
- 数据库不存绝对/相对缓存路径、扩展名、bookmark、来源文件名或用户输入路径。
- 只建 derived_image_cache_key_uq(asset_id, content_revision, representation_version, variant) 和 derived_image_cache_lru_idx(last_accessed_at_ms, id)。
- 不建 trigger、settings/quota/Job/FTS/prediction/Photos/UI 表，不改现有业务表。
- snapshot/restore/future-schema/migration-prefix 只做 v003 最小更新。

Application 合同：
- 提供不含 GRDB/ImageIO/CoreGraphics/AppKit/URL/路径的异步 loadOrGenerate 端口。
- request 只含 assetID+variant。
- payload 只含 entryID、assetID、contentRevision、representationVersion=1、variant、jpeg/png、像素尺寸、encodedBytes、cacheHit/generated；不含路径/SQL/bookmark。
- 提供不接 scheduler 的显式异步 maintenance 端口，只返回 removedEntries/removedObjects/removedBytes/unsafeObjects 聚合。
- 封闭安全错误 raw value：derivedAssetNotFound、derivedAssetIneligible、derivedAuthorizationRequired、derivedSourceUnavailable、derivedSourceChanged、derivedDecodeFailed、derivedEncodeFailed、derivedCapacityUnavailable、derivedInsufficientSpace、derivedCacheUnsafePath、derivedCachePersistenceFailed。错误不含路径、文件名、bookmark、bytes、SQL、底层 NSError。

资格与只读 source：
- 只为存在的 current+available folder file、active Source、合法 relative path/file name、完整 size/mtime fingerprint、允许静态 UTI 生成。
- historical、Photos、missing/unreadable/unsupported、disabled/unavailable/authorizationRequired、缺 fingerprint 全部拒绝新生成。
- 精确 key 的已有有效 entry 可命中；状态变化不得伪造 key 或改写 Asset。
- 复用切片 2/3 已批准的 bookmark/stale/root/state/scope 服务，不复制权限状态机；所有路径 start/stop 配对。
- 从安全作用域根逐分量 no-follow 只读打开，不跟随 symlink/alias/package；Image I/O 消费已安全打开的句柄/数据提供者，不能检查后通过未锚定路径重开。
- source 零写入：无 create/write/truncate/remove/move/copy/rename/resource metadata/sidecar。

representation version 1：
- Image I/O 再确认 JPEG/PNG/HEIC/HEIF/TIFF/WebP canonical UTI 和单一静态主图。
- 方向 transform 覆盖 orientation 1～8。
- gridSmall 精确 256x256、gridRegular 精确 512x512：视觉居中 aspect-fill，对称 crop，小图高质量放大。
- preview 保持完整视觉比例、最长边 <=2048，较小源图不放大。
- 8-bit sRGB、高质量插值。
- 有影响视觉结果的 alpha 输出 PNG；其余 baseline JPEG quality 0.85。
- 不复制 EXIF/GPS/XMP/文件名/原始时间 metadata。
- 先在内存编码，计算 byte size/派生 SHA-256并重新 Image I/O 解码验证单图、format、尺寸、完整性；不得计算全库原图 SHA-256或回写 file_fingerprint.sha256。

缓存路径与发布：
- 根固定 AppPaths.cachesDirectory/DerivedImages/v1；子目录 staging 与 objects/<entry uuid 去连字符前两位>/<lower uuid>.<jpg|png>。
- 路径只由合法 entry UUID+封闭 format 推导，数据库不存 path。
- 生产根只能来自 AppPaths Caches；测试根必须是当前测试登记的临时根。
- 校验根和全部分量非 symlink，操作以打开目录句柄相对/no-follow 语义锚定；字符串 prefix 不能作为授权。无法证明即 derivedCacheUnsafePath，不触碰外部目标。
- 内存编码/验证 -> staging 排他创建/完整写入/sync -> staging 再验证 -> 同卷原子 rename 到全新对象 -> GRDB 最终重验证并发布 entry -> 返回；DB entry 永远晚于完整对象。
- 第 4 步后 DB 失败最多留下孤儿；绝不允许 entry 指向半文件。旧对象只在 entry 替换事务成功后 best-effort 删除。
- 同 key 并发在实例内合并/串行；跨实例 unique+事务选 winner，loser 不覆盖 winner并清理自己的对象。
- hit 验 regular/non-symlink、size、SHA、format、单图和尺寸；有效 hit touch LRU。缺失/损坏先移除 entry，再重建，不改 Asset。

source 稳定性：
- 生成前 current opened fingerprint 必须与 DB 快照相容。
- 生成后从同一打开对象与 current locator 再验证 size/mtime/resource ID。
- final DB 事务再验证 Asset current/available、Source active、relative locator、content_revision 和 fingerprint。
- 任一变化返回 derivedSourceChanged，删除本调用派生物，零 entry，且不改 Asset/fingerprint/revision/generation/Job。

配额与容量：
- 固定 published quota=20*1024^3，reserve=max(5*1024^3, volumeTotalCapacity/20)，所有运算防 overflow。
- 在派生卷只用 volumeAvailableCapacityForImportantUsageKey + volumeTotalCapacityKey；缺失、负数或 overflow fail closed 为 derivedCapacityUnavailable。
- encoding bytes 已知且 staging 之前执行 admission。
- published+incoming 超 20GiB时按 last_accessed_at_ms,id LRU 淘汰；available < reserve+incoming 时继续淘汰并在每个对象实际删除后重查 available。
- 淘汰先事务删 entry，再安全删对象；对象未删不得当作容量回升。无法满足返回 derivedInsufficientSpace，无 staging/新 entry。
- 只删 derived cache entry/object；绝不删 catalog、snapshot、backup、runtime、来源或事实。
- 不使用 UserDefaults，不做设置 UI。

维护：
- 提供显式、排他、幂等 maintenance，不接启动/scheduler。
- 删除缺失/损坏 entry、无引用 object、无活跃调用的遗留 staging；Asset cascade 后孤儿也收敛。
- 只遍历自有派生根；symlink/未知层级/无法证明归属不跟随、不触碰外部目标，聚合 unsafeObjects 或返回安全错误。
- 只返回数量和 bytes，不泄漏对象名/路径。

Privacy/依赖：
- PrivacyInfo 精确包含 FileTimestamp->3B52.1 和 DiskSpace->E174.1；不得有 UserDefaults/tracking/collected-data/其他类别。
- entitlement 保持 sandbox+user-selected read-only+app-scope bookmarks（Debug 可注入 get-task-allow）。
- 不新增 package；使用系统 Foundation/ImageIO/CoreGraphics/CryptoKit 与现有 GRDB。

强制 TDD，先保留七簇真实红灯摘要，再最小实现：
1. v003 migration/schema/STRICT/CHECK/FK/index introspection、sentinel upgrade、future guard、snapshot/restore、v001/v002零 diff。
2. Application 封闭契约、资格正反、exact key、scope 配对、分层无路径泄漏。
3. 六种允许格式、orientation 1～8像素级、grid crop/尺寸、preview fit、不放大、alpha PNG、opaque JPEG、sRGB、metadata stripping、动画/损坏反例；不得 skip。
4. pre/post/final 变化、有效 hit 不开 source、缺失/截断/同尺寸篡改/格式尺寸错误重建、revision key、同 key并发。
5. 真实生产 fault：staging create/write/sync/validate、rename、rename后DB前、DB insert/update/revalidation/LRU touch、旧对象删除；比较完整稳定事实和缓存树，证明最多孤儿、无半文件entry。
6. 20GiB、5GiB、5%、等于/差1/overflow、capacity unavailable、稳定 LRU、淘汰后重查、单对象过大、维护幂等、root/shard/object symlink与祖先/外部删除拒绝。
7. built PrivacyInfo、codesign、Swift6/macOS15/arm64、source write spy和树/bytes/mtime不变、现有499项回归、App/UI/CompositionRoot/scheme/entitlement/Package边界、HDD2/真实容器/user零访问。

不能用 skip、弱化断言、只测 fake/helper、只 grep 源码或未实际触发 fault 获得绿灯。若当前 SDK 无法生成某格式 fixture，先停下报告证据，不得静默删测试或改变批准格式。

绝对禁止：
- 修改 ImageAll/App、SwiftUI、RootView、Composition Root、Sidebar、网格、Inspector、菜单、活动入口或外观。
- FSEvents、活动 projection、scheduler/timer/background loop、新 Job kind。
- 修改 v001/v002、已批准标签/查询/对账语义，或进入切片5～7。
- PhotoKit、Vision/Core ML/OCR/AI/网络/跨来源去重/全库原图hash。
- source写入、sidecar、UserDefaults、新entitlement、新package。
- 修改 docs、docs/cursor-cli-tasks或user；访问HDD2/真实App容器。

完成验证：
- xcodebuild -scheme ImageAll -destination 'platform=macOS,arch=arm64' -configuration Debug -resultBundlePath /tmp/ImageAll-stage1-slice4.xcresult test
- xcodebuild -scheme ImageAll -destination 'platform=macOS,arch=arm64' -configuration Debug build
- git diff --check
- 从xcresult报告准确passed/failed/skipped；plutil built PrivacyInfo；codesign entitlements；Swift/macOS/arm64；v001/v002、Package.resolved、App/UI/CompositionRoot/scheme/entitlement零diff；生产import与source write检查。

提交要求：
- 只创建一个窄范围本地 commit，不 amend。
- author/committer Cursor Agent <cursoragent@cursor.com>。
- subject: feat(cursor): add derived image cache
- trailer: Agent-Role: implementation
- 无 Co-authored-by；不提交 docs/user；不 push。
- commit 后除既有 ?? user/ 外工作区干净。
- 按 .cursor/rules/codex-review-handoff.mdc 输出完整中文 Codex 复审材料，回传 system/init.model、全新 session ID、开工/交付 SHA、七簇红绿、准确测试数/build、manifest/codesign、diff边界、Git作者/trailer、HDD2/user/容器零访问。
- 明确停止于切片4，未进入FSEvents/活动projection或切片6～7 UI。
```
<!-- CURSOR_PROMPT_END -->

## 4. 禁止事项与停止位置

- 本次不是切片 3 返修，不得恢复 `3c43d530-d8d1-4bf9-b1a8-7493ee324d09` 或任何旧 session；
- Cursor 不得编辑本记录、权威 handoff 或其他 docs；
- 不访问 `user/`、`/Volumes/HDD2` 或真实 App 容器；
- 不 push、不改写历史；
- 不接 FSEvents、活动 projection 或产品 UI；
- 交付 commit 后停止，等待 Codex 逐项复审。

## 5. 调用与评审事实

调用后由 Codex 追加：

| 字段 | 实际值 |
|---|---|
| Launch HEAD | `416f9a3311972e48919a33b4a45e06b74065a7ee` |
| `system/init.model` | `Composer 2.5 Fast`（`stream-json` 第 1 条） |
| Cursor session ID | `cbb558ad-68b9-4286-8d16-54078021746b`（全新 session） |
| Cursor 初版交付 commit | `02f000390e0ec3451511f076aa879b2e2458d8b3` |
| 初版测试 | Cursor 与 Codex 独立结果均为 529 passed / 0 failed / 0 skipped；通过不等于验收矩阵充分 |
| 初版构建 | arm64 Debug `BUILD SUCCEEDED` |
| Git 归属 | `Cursor Agent <cursoragent@cursor.com>`；`feat(cursor): add derived image cache`；`Agent-Role: implementation` |
| 工作区 / push | `main`，仅既有 `?? user/`；未 push |
| Codex 初审 | **拒绝**；见第 6 节，必须沿用本 session 返修，禁止新 session 或 amend 初版 commit |

## 6. Codex 初审结论与返修要求

### 6.1 结论

`02f0003` 保持了 v001/v002、App/SwiftUI 与切片停止边界，v003 DDL 的主体形状也符合交接单；但实现与测试没有达到第 5～12 节的安全及行为门。529 项绿灯中，新增 30 项远不足以证明交接单明确要求的反例、fault matrix、路径对抗和像素行为，并出现了“测试名声称 LRU、实际不触发淘汰”的弱化断言。切片 4 不通过。

### 6.2 阻塞项

1. **有效 cache hit 被生成资格错误阻断。** `loadOrGenerate` 在查 exact-key entry 前先要求完整 generation context 且 `source.state = active`、Asset `available`；因此 Source 离线、disabled 或 authorizationRequired 时，即使同 revision 已有完整有效对象也无法返回。应先取得 Asset 当前 revision 并验证 exact hit；只有 miss/损坏后才进入新生成资格与授权流程。增加“生成后 Source 切换四种非 active 状态仍命中且 scope 不启动；同状态 miss 不生成”的运行测试。
2. **缓存文件操作没有满足目录句柄锚定/no-follow 合同。** `Data(contentsOf:)`、绝对路径 `rename`/`unlink`、`FileManager.enumerator` 和只检查 `versionRoot`/父目录会留下 staging、objects、shard 祖先被 symlink/rename 置换的 TOCTOU 窗口；尤其 `unlink` 会跟随被替换的父目录而可能删除派生根外同名文件。所有读、排他创建、sync、rename、删除和维护遍历必须锚定已打开且验证过的自有目录 FD，以相对组件和 no-follow 等价语义执行；逐层拒绝 root/staging/objects/shard/object symlink、未知层级及外部目标。测试必须放置外部哨兵并证明零触碰，不能只测 versionRoot symlink。
3. **source fingerprint 不是从已打开对象取得。** source bytes 从安全 FD 读取，但 size/mtime/resource ID 随后用拼接 URL 查询；post check 也重新按路径查询，不能证明“同一打开对象”稳定，存在 locator 替换窗口。改为对打开 FD `fstat`/等价句柄事实做 pre/post，再独立 no-follow 重开 current locator 比较 resource identity；最终 DB 事务继续重验证。用可控 seam 覆盖解码期间 size、mtime、resource ID、locator、revision、availability、Source state 和 fingerprint 分别变化，断言零 entry/零本次对象且事实不变。
4. **发布顺序与 fault coverage 不完整。** staging 写完后当前代码只重新校验内存 artifact，没有从 staging 文件复读 size/hash/解码；普通 `rename` 允许覆盖已存在目标。必须从 staging FD 复读验证，并采用“目标存在则失败”的同卷原子发布语义。逐一实际触发 staging create/write/sync/validate、rename、rename 后 DB 前、DB insert/update、final revalidation、LRU touch、替换旧 key 事务和旧对象删除 fault；比较稳定排序的完整 Asset/fingerprint/cache-entry 事实与缓存树。所有公开端口只泄出封闭 `DerivedImageError`，不得透传 `DerivedImageSecureIOError`、GRDB error 或带路径的底层 error。
5. **配额/LRU 测试被弱化。** `testPublishedQuotaEvictionUsesLRUOrder` 只生成一个小对象并断言总量小于 20 GiB，既不触发淘汰也不证明 `(last_accessed_at_ms,id)` 稳定顺序。建立可注入但仍走生产 admission/repository/store 的精确边界测试，覆盖 20 GiB、5 GiB、5%、等于/差 1、overflow、单对象超限、容量 nil/负值等价输入、淘汰后逐次重查、删除失败不算容量回升、稳定 LRU 与事实/来源零修改。
6. **视觉与缓存损坏矩阵不足。** orientation 1～8 目前只断言宽高，违反“非对称像素 fixture、不能只断言宽高”；缺 grid 居中 crop、gridRegular、preview 横/竖/方及 2048 边界、sRGB、alpha 位于首个 64×64 之外、metadata 的 GPS/XMP/文件名/日期、实际 UTI 漂移和多帧允许格式反例。cache 损坏只改数据库 byte_size，缺对象缺失、截断、同尺寸篡改、格式错误、尺寸错误和 revision/version/variant 独立 key 行为。补真实像素/文件行为断言，禁止通过提前 return 弱化。
7. **v003 约束与迁移回归证据不足。** 当前 constraint 测试只证明重复 key 与 cascade；必须覆盖 UUID 大写/结构/非 hex，revision/version、variant、format、三类尺寸组合、byte size、BLOB storage class/长度、timestamps 的正反例，以及不同 revision/version/variant 可共存。补真实 sqlite metadata 对列、STRICT、FK、两个 index key 的 introspection；补 future migration 在 v003 前拒绝、重开幂等、snapshot/restore/migration-prefix 与旧 snapshot 合法升级。`testV001AndV002DDLUnchangedAfterV003` 不得遗漏 `asset`，同时保留 migration 文件零字节 diff。
8. **生成资格、维护排他和错误/权限配对矩阵不足。** 生产资格未校验持久 `media_type` 是否在批准 UTI 集合；缺 missing/unreadable/unsupported、四类非 active Source、缺 fingerprint 反例。maintenance gate 只在 maintenance 之间互斥，不能阻止它清除正在生成的 staging；需与 active generate 协调，同时清理缺失/损坏 entry、cascade/publish/staging orphan 并验证二次幂等和聚合值。补 scope 在成功、hit、source change、decode/encode、capacity、文件发布与 DB failure 全部分支的 start/stop 配对。

### 6.3 返修执行约束

- 这是同一未验收交接单，必须 `--resume cbb558ad-68b9-4286-8d16-54078021746b`，仍显式使用 `--model composer-2.5-fast`；不得启动新 session、Task 子代理或 MCP。
- 当前基线必须为 `main@<REVIEW_HEAD>`，其中包含初版实现和本评审记录；工作区除 `?? user/` 外干净。
- 不 amend `02f0003`。返修以独立 `fix(cursor): harden derived image cache contract` commit 提交，保持 Cursor identity/trailer。
- 不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；不进入 FSEvents/Job/scheduler/UI。
- 不访问 `user/`、`/Volumes/HDD2` 或真实 App 容器；不 push、不改写历史。
- 完成后重新运行无管道的完整 arm64 Debug test 和 build，提供新 xcresult 精确计数、全部 fault/边界测试清单与最终工作区证据，再停止等待 Codex 复审。

### 6.4 首次返修 CLI 命令与完整正文

调用时将 `<REVIEW_HEAD>` 替换为包含第 6 节初审结论与本调用记录的精确 Codex commit：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份“阶段 1 / 切片 4”交接单的首次 Codex 返修。不要开始新任务，不要进入切片 5～7。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6 节的初审结论、8 项阻塞和返修约束；这些内容全部是本次验收合同，不得挑选或弱化。

开工门：当前必须是本地 main@<REVIEW_HEAD>；它在初版实现 02f000390e0ec3451511f076aa879b2e2458d8b3 后只增加 Codex 评审记录 commit；工作区只能有项目所有者既有 ?? user/。system/init.model 必须仍为 Composer 2.5 Fast。不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得 reset/checkout/restore/stash/clean/amend/push/rebase/cherry-pick/squash/history rewrite；不得使用 Cursor Task 子代理或 MCP。

逐项修正第 6.2 节全部问题。先用新增或加强后的运行测试真实复现每个缺口，再做最小生产修正；不得删除/改名以掩盖失败，不得提前 return、条件 skip、未触发 fault、只测 helper/fake、源码 grep 或“总量低于配额”之类弱断言换绿灯。特别要求：
- valid exact-key hit 必须先于生成资格/授权，非 active 或离线 Source 的 valid hit 不打开 source；miss 仍禁止生成。
- 缓存根的读写、排他创建、sync、同卷不覆盖发布、删除与维护必须以已验证目录 FD 相对/no-follow 锚定，逐层覆盖 root/staging/objects/shard/object 置换；外部哨兵必须保持不变。
- source pre/post 必须从同一已打开 FD 获取 fingerprint，再独立 no-follow 重开 current locator；final DB 逐字段重验证。
- staging 必须从已写文件复读 size/hash/单图/format/尺寸；所有 fault seam 实际触发，公开端口只返回封闭 DerivedImageError。
- 配额测试必须实际触发稳定 LRU 与容量重查，覆盖 20GiB/5GiB/5% 等于、差 1、overflow、单对象超限、删除失败。
- orientation 1～8 使用非对称像素断言；完整补齐 crop/fit/sRGB/alpha/metadata、cache corruption、v003 schema/migration、资格、scope 和 maintenance 排他矩阵。

不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement 或 Package.resolved；不新增 Job/FSEvents/scheduler/UI。允许仅为精确测试 seam 做最小内部注入，但不能改变产品常量或把生产安全逻辑移入测试。

完成后运行无管道的完整 arm64 Debug test（写入全新 /tmp/ImageAll-stage1-slice4-revision1.xcresult）、arm64 Debug build 和 git diff --check；从 xcresult 报告准确 passed/failed/skipped，并报告 built PrivacyInfo、codesign、Swift6/macOS15/arm64、v001/v002 与禁止边界零 diff。创建独立 commit，不 amend：Cursor Agent <cursoragent@cursor.com>；subject `fix(cursor): harden derived image cache contract`；trailer `Agent-Role: implementation`；不含 Co-authored-by；不提交 docs/user；不 push。提交后工作区只余既有 ?? user/，输出完整中文 Codex 复审材料并停止。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1.cursor.stream.jsonl
```

## 7. 首次返修中断与续接诊断

### 7.1 中断事实

- 首次返修实际从 `main@b6c1b6d4a184fb9bb64de10ae8180cf996dcc590` 启动；
- `system/init.model = Composer 2.5 Fast`，session 仍为 `cbb558ad-68b9-4286-8d16-54078021746b`；
- Cursor 在未提交的返修草稿中把维护枚举、`fstatat` 与删除失败逐步改成 `try?`/空数组/`unsafeObjects`，并尝试用 `O_RDWR | O_DIRECTORY` 打开目录；这会吞掉不能证明安全归属的错误，也错误理解了 `*at` 操作的权限语义；
- Codex 因此以 Ctrl-C 中断本次自动返修，未创建 Cursor commit。现场保留为 7 个实现/测试支持文件的未提交草稿；既有 `?? user/` 保持未读未改。

### 7.2 续接前必须先修复的根因

1. **不得以吞错换绿灯。** 维护枚举失败、合法对象的 `fstatat` 失败、合法目标的 `unlinkat` 失败不能统一降格为空集合或“unsafe 数量”；未知名称、symlink、非普通对象可以计为 `unsafeObjects` 并跳过，但已确认属于缓存的对象发生 I/O 失败必须映射为封闭 `DerivedImageError`。恢复 `sweepUnreferencedObjects` / `sweepStaging` 的显式 throws 语义。
2. **目录 FD 保持只读打开。** macOS 目录不能依赖 `O_RDWR` 打开；`mkdirat`、`unlinkat`、`renameatx_np` 使用 `O_RDONLY | O_DIRECTORY | O_NOFOLLOW` 的父目录 FD 即可，授权来自目录权限而非 FD 的 read/write mode。删除 `openMutableDirectory*` 与 `tryOpenMutableDirectory` 方向，不得经 `F_GETPATH` 或绝对路径重新打开。
3. **发布必须真正 no-replace。** 普通 `renameat` 会覆盖已有目标，不能命名为 `renameatNoReplace`。macOS 15 路径应使用 `renameatx_np(..., RENAME_EXCL)` 或同等原子排他原语；加入“预置目标内容保持不变、发布失败、staging/DB 状态符合合同”的真实测试。
4. **句柄生命周期必须真实。** `readStaging` 当前返回的 fd 已在函数内 `defer close`，不得返回无效句柄；改成只返回 bytes/size，或由调用方明确拥有并关闭有效 FD。`fdopendir` 成功后只由 `closedir` 关闭 duplicated fd；`readdir` 结束需区分 EOF 与 errno。
5. **维护只识别封闭命名空间。** shard 必须恰为两个小写十六进制字符；object 必须为小写 canonical UUID 加与封闭 format 对应的 `.jpg|.png`。未知目录/文件只计 unsafe 且绝不进入、删除或跟随；外部哨兵不变。删除无效 entry 后应基于仍有效 entries 重新计算引用集，使同一次维护可以收敛其对象。
6. **指纹必须兼容切片 3 的生产事实。** 不得只把测试 seed 改成 raw `fstat` 纳秒来迁就派生图实现。v002 生产 reconcile 通过 `FoundationFolderFileResourceReader.modifiedAtNs` 与 `resourceIdentifier` 持久化；句柄派生事实必须规范化为同一已批准表示，并用“真实生产 reconcile 建立 Asset/fingerprint，再生成派生图”的集成测试证明相容。pre/post 从同一 FD；reopen current locator 仍必须 no-follow 并从新 FD 取得 identity，不能再通过拼接 URL 查询 resource ID。
7. **不要扩大无测试实现。** 当前草稿没有新增第 6.2 节要求的行为测试。先把上述根因写成失败测试，再最小修复；随后继续补完原 8 项阻塞矩阵，不能只让原有 529 项恢复。

### 7.3 第二次续接命令

该命令仍属于同一份未验收交接单，必须恢复原 session。工作区会包含上次中断的实现草稿；不得 reset、restore、checkout、stash 或 clean，应逐行整理该草稿。

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 首次返修；上一次运行被 Codex 主动中断，因为实现开始用 try?/空集合吞掉维护错误，并错误尝试 O_RDWR 打开目录。不要新建 session，不要提交当前草稿，不要进入切片 5～7。

先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md、docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6 节和第 7 节。当前必须是本地 main@<REVIEW_HEAD>；除既有 ?? user/ 外，还会有上次中断留下的 7 个实现/测试支持文件未提交变化，这是同一任务草稿。不得 reset/checkout/restore/stash/clean 覆盖它们，也不得读取、修改、stage 或提交 user/。模型必须仍为 Composer 2.5 Fast；不得使用 Task 子代理或 MCP；不得访问 /Volumes/HDD2 或真实 App 容器；不得 push、amend 或改写历史。

先按第 7.2 节逐项纠正根因，并先增加会失败的真实测试：恢复维护显式错误语义；目录 FD 只读且所有操作继续 dirfd-relative/no-follow；用 renameatx_np+RENAME_EXCL 等价原语证明目标不覆盖；修正 staging/目录枚举 FD 生命周期；封闭 shard/object 名称且未知层级零触碰；删除无效 entry 后同次维护重算引用；用生产 reconcile 建立的 fingerprint 证明 handle facts 兼容，不得用 raw-fstat 测试 seed 代替生产语义。所有公共端口只泄出封闭 DerivedImageError。

这些根因稳定后，继续完成第 6.2 节原 8 项阻塞的完整 TDD 矩阵，尤其 valid hit 先于资格、source pre/post/reopen/final 变化、真实 staging/DB/LRU fault、实际触发 20GiB/reserve/LRU、orientation 1～8 非对称像素、cache corruption、v003 全约束/迁移、scope 配对与 generation/maintenance 排他。禁止删测、skip、提前 return、只测 helper/fake、源码 grep 或以未触发的断言换绿灯。

不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；不新增 Job/FSEvents/scheduler/UI。完成后运行无管道全量 arm64 Debug test（全新 xcresult）、arm64 Debug build、git diff --check 与全部边界核验；独立创建 fix(cursor): harden derived image cache contract commit，Cursor identity/trailer，禁止 amend/docs/user/push。输出完整复审材料后停止。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1b.cursor.stream.jsonl
```

## 8. 第二次中断：dirent 名称读取根因

第二次续接已让生产 reconcile 指纹集成测试通过，并增加了 no-replace、valid-hit-first、未知媒体、维护等关键测试；但 Cursor 在目录枚举诊断中确认真实 `77` 被读成 `7.`、UUID 文件名被读成无效 UTF-8 后，开始转向 `getdirentries` 和手写 ABI offset。Codex 再次中断，避免引入不必要且脆弱的 Darwin ABI 解析。

根因是 Swift 表达式对 `entry.pointee.d_name.0` 取地址时，只取得 tuple 第一个元素的临时标量地址；以 `d_namlen` 从该地址继续读取会越过这个临时值，而不是读取 `dirent` 内的整个 `d_name` buffer。正确方向是：

- 保留 `readdir`、`d_namlen` 和 `fdopendir`；
- 对完整 `entry.pointee.d_name` tuple 取地址，再在该闭包内将它的原始内存绑定/转换为 `CChar` 或 `UInt8` buffer，严格读取 `d_namlen` 个字节；
- 禁止再对 `.0` 单独取地址；
- 禁止引入 `getdirentries`、手写 `dirent` 字节 offset、F_GETPATH 或绝对路径枚举；
- 用短分片名、完整 UUID 文件名、`.`/`..` 过滤、非 ASCII 非法名称与 errno/EOF 测试证明该 helper，而不是留下调试打印或调试 API。

第三次续接仍恢复同一 session。修复上述单点后先运行当前 3 个失败测试，删除所有临时 debug seam/断言文本，再完成第 6.2 节尚未覆盖的矩阵。当前约 538 项测试只增加了少量 hardening 用例，不能作为最终验收数量。

### 8.1 第三次续接命令

该命令仍属于同一份未验收交接单，必须恢复原 session，并保留前两次中断形成的同任务草稿。调用时将 `<REVIEW_HEAD>` 替换为包含本节的精确 Codex commit：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 返修，不是新任务；恢复原 Cursor session，禁止进入切片 5～7。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6～8 节。当前必须是本地 main@<REVIEW_HEAD>；工作区会保留同一返修任务的未提交实现/测试草稿以及项目所有者既有 ?? user/。不得 reset、checkout、restore、stash、clean 或覆盖这些草稿；不得读取、修改、stage 或提交 user/。system/init.model 必须仍为 Composer 2.5 Fast；不得使用 Task 子代理或 MCP；不得访问 /Volumes/HDD2 或真实 App 容器；不得 push、amend 或改写历史。

第一步只修复第 8 节确认的目录项名称读取根因：继续使用 fdopendir/readdir/d_namlen；对完整 entry.pointee.d_name tuple 取地址，在该闭包内把其原始内存绑定为 CChar 或 UInt8，严格读取 d_namlen 字节；绝对禁止对 d_name.0 单独取地址，禁止 getdirentries、手写 dirent ABI offset、F_GETPATH、绝对路径枚举或调试输出。以短分片名、完整 canonical UUID 文件名、点目录过滤、非法非 ASCII 名称和 errno/EOF 行为测试证明这个边界。

随后先运行并修复当前三个已知失败行为，不得弱化断言：DerivedImageFaultTests/testAfterRenameBeforeDBFaultLeavesOrphanOnly、DerivedImageHardeningTests/testInvalidEntryRemovedAndObjectSweptSameMaintenancePass、DerivedImageHardeningTests/testExternalSentinelUntouchedDuringMaintenance。确认这些用例绿灯后，删除临时 debug seam、打印和只为调查存在的断言文本。

然后继续按行为逐个 RED→GREEN 补齐第 6.2 节全部 8 项阻塞矩阵；不要把约 538 项局部绿灯当作完成。必须真实覆盖：valid exact-key hit 先于四类非 active Source 的生成资格且 hit 不开启 scope；source 同一 FD pre/post、no-follow reopen 与 final DB 每个事实变化；staging 复读、no-replace、DB/LRU/旧对象删除的真实 fault；20GiB/5GiB/5% 等于与差1、overflow、capacity unavailable、单对象超限、稳定 LRU、删除失败不计容量回升；orientation 1～8 非对称像素、grid crop、preview fit、sRGB、64×64 外 alpha、metadata stripping、实际格式/尺寸/多帧反例；对象缺失/截断/同尺寸篡改/格式尺寸错误与 key 独立；v003 全部 CHECK/BLOB/UUID/metadata/index/future/snapshot/prefix；generation/maintenance 排他、维护幂等、完整 scope start/stop 和封闭错误。每个循环都先写或加强一个可观察行为测试，再做最小生产修正；禁止 skip、提前 return、只测 fake/helper、源码 grep、未实际触发 fault 或篡改产品常量换绿灯。

所有生产缓存 I/O 继续以已验证目录 FD 相对/no-follow 锚定；已确认属于缓存的 I/O 失败必须映射封闭 DerivedImageError，未知或 symlink 名称只计 unsafe 且外部哨兵零触碰。对象相对路径必须同时验证 shard 为 UUID 前两位。删除无效 entry 后同次维护重算引用；removedBytes 必须来自实际已删对象大小，不能信任损坏 DB byte_size。source fingerprint 必须与切片 3 生产 reconcile 事实兼容，不能回退为测试专用 raw-fstat seed。

不修改 v001/v002、Codex docs、App/SwiftUI、Composition Root、scheme、entitlement 或 Package.resolved；不新增 Job/FSEvents/scheduler/UI。完成后运行无管道全量 arm64 Debug test并写入全新 /tmp/ImageAll-stage1-slice4-revision1c.xcresult，运行 arm64 Debug build 与 git diff --check；从 xcresult 报告准确 passed/failed/skipped，并提供 built PrivacyInfo、codesign、Swift6/macOS15/arm64、v001/v002、App/UI/CompositionRoot/scheme/entitlement/Package 零 diff 证据。

最终只创建一个独立 Cursor 返修 commit，不 amend：author/committer Cursor Agent <cursoragent@cursor.com>，subject `fix(cursor): harden derived image cache contract`，trailer `Agent-Role: implementation`，无 Co-authored-by，不提交 docs/user，不 push。提交后除既有 ?? user/ 外工作区干净，输出 .cursor/rules/codex-review-handoff.mdc 要求的完整中文复审材料并停止。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1c.cursor.stream.jsonl
```

## 9. 第三次中断：弱矩阵测试不得冒充验收证据

第三次续接已完成并定向证明：

- `readdir` 名称读取改为对完整 `d_name` tuple 取地址，不再读取 `.0` 临时标量；
- 原 3 个失败行为已通过：rename 后 DB 前只留孤儿、无效 entry 同轮清理、外部 symlink 哨兵零触碰；
- 短 shard、canonical UUID、点目录、空目录 EOF、无效 FD 的目录枚举测试通过；
- macOS/APFS 对无效 UTF-8 文件名的 `openat` 与 `renameat` 均以 `EILSEQ` 拒绝，无法把这种名称物化为真实磁盘 fixture；合法非 ASCII 名称读取可作为真实文件系统边界，损坏字节解码不能靠伪造“成功创建”断言；
- shard 与 UUID 前缀一致性、实际删除字节计数已加入当前草稿。

随后 Cursor 开始一次性创建 `DerivedImageMatrixTests.swift`，其中出现了与名称不符的弱证据：

1. `testMaintenanceWaitsForActiveGenerationThenConverges` 没有并发生成、阻塞点或“维护尚未完成”的断言；
2. `testGridSmallExactDimensionsAndCenterCrop` 只断言 256×256，没有任何 crop 像素事实；
3. orientation 合同要求 1～8 非对称像素，草稿只测试 orientation 1 且只比较两个像素不相等；
4. 已有 `DerivedImageHardeningTests` 已对三个非 active Source state 的 hit 做循环，新增 unavailable 单例只是重复；
5. `testInsufficientSpaceWhenEvictDeleteFails` 尚未证明实际进入 eviction/delete fault，Cursor 自己也确认参数可能直接通过或在错误阶段失败；
6. 一次性计划 30～40 个测试违反当前返修所需的逐行为 RED→GREEN，会把未验证假设扩散进大文件。

Codex 因此第三次 Ctrl-C 中断。`DerivedImageMatrixTests.swift` 是本次 Cursor 运行刚创建、尚未纳入工程且未提交的弱草稿，可以由 Cursor 明确删除；这不是对项目所有者文件的清理。其他同任务草稿继续保留，禁止 git restore/reset/clean。

### 9.1 第四次续接：只完成资格与维护排他簇

本轮是同一未验收返修的受控检查点，只完成下面两个行为簇并停止，不提交最终 Cursor commit，不扩展视觉、配额、v003 或其他矩阵。调用时以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 返修的受控检查点，不是新任务；恢复原 session，禁止进入切片 5～7。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6～9 节。当前必须是本地 main@<REVIEW_HEAD>，工作区包含同一任务草稿和既有 ?? user/。不得 reset、checkout、restore、stash、clean；不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

先删除本 session 刚创建、未提交且未纳入工程的 ImageAllTests/DerivedImage/DerivedImageMatrixTests.swift；它已被 Codex 判定为弱矩阵草稿。手工删除仅为该文件增加、现已无使用者的测试 seam/import，但保留已经定向通过的完整 d_name tuple 修复、目录测试、renameatx_np no-replace、生产 reconcile 指纹、valid-hit-first、维护同轮收敛、外部哨兵、shard/UUID 前缀和实际 removedBytes 修正。不得用 git restore/reset/clean 处理。

随后只做两个行为簇，并严格一次一个测试 RED→GREEN：

行为簇 A，生成资格与 scope。复用并加强现有 DerivedImageHardeningTests，不建重复矩阵文件。精确证明三个非 active Source state（disabled、unavailable、authorizationRequired）各自满足：已有 exact-key 有效 hit 返回且 scope start/stop 计数不增加；miss 返回封闭 ineligible 且 scope 不启动。再分别证明 Asset unavailable、missing、unreadable、unsupported media type、historical locator、Photos locator、缺 fingerprint 不生成、不留 staging/entry/object，并断言源文件及稳定 Asset/fingerprint 事实不变。对成功生成、hit、source changed、decode failure、capacity unavailable、文件发布 failure 和 repository failure，逐个实际触发分支并证明 scope start/stop 配对；不要添加尚无实际 fault 的 encode failure，留给后续 fault 簇。

行为簇 B，generation/maintenance 排他。写一个真实并发行为测试：通过最小内部阻塞 seam 让生成在 staging 已受 operation gate 保护且尚未发布的位置暂停；启动 maintenance，使用 expectation/actor/锁证明在生成释放前 maintenance 没有完成且受保护 staging 未被删；释放生成后，二者都完成，entry/object 正确且第二次 maintenance 幂等。再做反向顺序：maintenance 持有同一排他 gate 时，generation 不进入 staging；释放后生成成功。测试必须经 DerivedImageCacheService 公共 async 端口，不得直接只测 gate/helper，不得 Thread.sleep 作为唯一同步证据。seam 只表达可控阻塞点，不能变成通用 debug API。

每添加一个测试：先只运行该测试确认真实红灯；做最小生产修正；再只运行该测试和相关簇确认绿灯；然后才进入下一个。保留简短红灯原因清单。禁止一次性再建 30～40 项测试、测试名夸大覆盖、skip、提前 return、弱化断言、只测 fake/helper 或没有实际命中预期 fault/并发点。

完成两个簇后，运行 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests 及新排他测试所在 suite 的无管道定向测试；不要运行最终全量，不要创建 Cursor commit。输出：实际新增/修改的测试名、每个 RED→GREEN 证据、定向 passed/failed/skipped、git diff --check、禁止边界状态，然后停止等待 Codex 检查。不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved，不得扩展视觉/配额/v003/fault 其他簇。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1d.cursor.stream.jsonl
```

## 10. 行为簇 A/B 受控检查点复审

第四次续接实际仍使用 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`。Cursor 删除了已拒绝的弱矩阵草稿，
保留目录枚举等既有修正，并让
`DerivedImageHardeningTests`、`DerivedImageDirectoryEntryTests`、
`DerivedImageOperationExclusivityTests` 共 30 项定向测试通过；按检查点要求未提交、
未跑全量，HEAD 仍为 `608e1a659a62ee43ff34c8ae1d1f902bc25a4e13`。

该检查点尚不能批准，需先完成以下窄范围修正：

1. `AssetAvailability` 的封闭值只有 `available`、`missing`、`unreadable`、
   `unsupported`，不存在 `unavailable`。当前
   `testAssetUnavailableLeavesNoArtifacts` 实际只设置 `missing`；损坏 bytes 的
   decode 失败与 `availability = unreadable` 不是同一事实，未知 UTI 与
   `availability = unsupported` 也不是同一事实。应分别覆盖 catalog 的三种非
   available 值，并保留物理文件消失、损坏 bytes、未知 UTI 为不同运行场景；测试名
   不得混淆 Source 的 `unavailable` 与 Asset availability。
2. 物理 source 文件在 reconcile 后消失或暂时不能只读打开，属于
   `derivedSourceUnavailable`；`derivedCachePersistenceFailed` 只用于派生缓存原子发布
   或 cache-entry 事务失败。当前 source reader 的 `ioFailure` 被通用 catch 错映射为
   cache persistence，必须按 source/cache I/O 边界修正并锁定封闭错误。
3. `PublishStagingCheckpoint.reachedStagingName` 与
   `MaintenanceHoldCheckpoint.maintenanceHeld` 虽写入时持锁，测试轮询读取时没有持锁；
   `@unchecked Sendable` 不能使该访问安全。改为锁/condition/semaphore/actor 支持的
   显式 reached 等待和锁内快照，不得用 `Task.yield()` 轮询未同步属性。
4. 当前 inverted expectation 在 gate 释放后的正常 completion 中仍会被 fulfill。
   应只在“release 标记尚未设置时完成”才 fulfill early-completion expectation，或改用
   等价锁/actor completion probe；测试必须继续证明释放前未完成、staging 未删除，
   释放后双方成功。测试 checkpoint 只提供同步信号，不应把并发正确性藏在 helper。
5. 资格拒绝用例的事实快照必须在设置目标状态后取得，并覆盖足以证明 generation
   没有改写的完整 Asset/fingerprint 关键字段；当前只比 revision/size/mtime，且
   `missing` 用例在状态改变前取快照，不能支持测试名称声称的完整事实不变。
6. 删除 `DerivedImageCacheService` 中已经转交给 repository/store、此后不再读取的
   `repositoryFaultInjector`、`publishCheckpoint` 存储属性，保持最小测试 seam。

### 10.1 第五次续接：只收敛 A/B 复审项

本轮仍是同一未验收交接单的返修，不是新任务。完成后仅运行 A/B 定向测试并停止，
不得进入视觉、fingerprint 变化矩阵、配额、v003 或完整 fault matrix，不得提交最终
Cursor commit。调用时以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 返修的行为簇 A/B 受控检查点，不是新任务；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，禁止进入切片 5～7。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 6～10 节。当前必须是本地 main@<REVIEW_HEAD>，工作区包含同一任务草稿和既有 ?? user/。不得 reset、checkout、restore、stash、clean；不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

本次只修正第 10 节六项复审问题，继续逐行为 RED→GREEN，不新增大矩阵文件：

1. 用准确名称分别增加/修正 catalog `availability = missing`、`unreadable`、`unsupported` 三个资格拒绝测试，全部断言 scope 不启动、零 staging/entry/object、源文件 bytes 不变、设置目标状态后的完整关键 Asset/fingerprint 事实不变。保留并明确区分：物理 source 文件消失、可用记录但损坏 bytes 导致 decode failure、可用记录但未知 media UTI 三个不同场景。不得再称不存在的 Asset `unavailable`。
2. 先让物理 source 文件消失测试以 `derivedSourceUnavailable` 为预期红灯，再最小修正 source read/open I/O 的封闭错误映射；不得把 cache store/publish/DB I/O 的 persistence 错误一并改掉。若 source locator 被 symlink/类型置换，按 handoff 的 source changed/unsafe 边界写清测试，不得泄露底层路径或 NSError。
3. 重写两个 checkpoint 的观察接口：所有状态读写必须在同一锁、condition、semaphore 或 actor 隔离下；测试不得直接轮询 `private(set)` 可变属性，不得用 `Task.yield()` 轮询未同步状态。提供明确的 reached handshake 和锁内 stagingName 快照。
4. 并发测试的 early-completion inverted expectation 只能在 gate release 标记尚未设置时被 fulfill；正常 release 后完成不得再 fulfill inverted expectation。也可用等价 lock/actor completion probe。继续经 DerivedImageCacheService 公共 async 端口证明：release 前 operation 未完成且 staging/entry/object 状态正确，release 后双方完成，第二次 maintenance 幂等。
5. 事实快照在测试设置目标 catalog/source 状态后取得，并至少覆盖 locator kind/state、media type、content revision、availability、relative path/file name 与 fingerprint size/mtime/resource ID；需要时包含 Source state。对运行前后做相等比较，不能只比较三列或用 `_ = sourceBefore` 冒充断言。
6. 删除 DerivedImageCacheService 中未再读取的 repositoryFaultInjector、publishCheckpoint 存储属性；保留构造注入到真实 repository/store 的必要参数和 maintenance checkpoint。

每个修正先运行单项确认真实红灯，再最小实现并单项绿灯。完成后只运行 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests 的无管道定向测试；报告 passed/failed/skipped、每个红灯原因、git diff --check 和禁止边界。不要跑最终全量，不要创建 Cursor commit。不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved，不得扩展视觉/配额/v003/其他 fault 簇。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1e.cursor.stream.jsonl
```

## 11. A/B 最后一项错误域修正

第五次续接仍为 session `cbb558ad-68b9-4286-8d16-54078021746b`，模型证据仍为
`Composer 2.5 Fast`。三类 catalog availability、物理文件消失错误、完整事实快照
和同步 checkpoint 已收敛，32 项定向测试通过；但新增的
`DerivedImageSecureIOError.unsafePath -> derivedCacheUnsafePath` 映射混淆了两个安全域。

`derivedCacheUnsafePath` 的批准语义是“不能证明操作严格位于应用自有派生根”；source
locator 在 reconcile 后被替换为 symlink、非普通文件或不再满足批准相对路径时，是
source/current locator 变化，不是缓存根故障。该路径应拒绝跟随、保持外部目标零触碰，
并返回 `derivedSourceChanged`。这也是第 10.1 节已经要求但 Cursor 未增加的运行测试。

### 11.1 第六次续接命令

本次只修这个单点并再次运行 A/B 定向测试，不提交、不进入其他矩阵。调用时以包含本节
的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 A/B 受控返修，恢复 session cbb558ad-68b9-4286-8d16-54078021746b；不是新任务，禁止进入切片 5～7。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 4.2、5 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 10～11 节。当前必须是本地 main@<REVIEW_HEAD>，保留同一任务草稿和既有 ?? user/；不得 reset/checkout/restore/stash/clean，不得读改 stage/提交 user/，不得访问 /Volumes/HDD2 或真实容器，不 push/amend/改写历史，不使用 Task 子代理/MCP。system/init.model 必须仍为 Composer 2.5 Fast。

只修第 11 节错误域单点，严格 RED→GREEN：在临时 fixture 内先按正常 file Asset/fingerprint 建档，再将 current source file 替换为指向 sourceRoot 外部哨兵文件的 symlink；从 DerivedImageCacheService 公共 loadOrGenerate 端口调用，预期 `derivedSourceChanged`，证明 scope start/stop 配对、零 staging/entry/object、完整 catalog/fingerprint 事实不变、外部哨兵 bytes/mtime 不变且 symlink 未被跟随或改写。测试必须真实失败于当前 `derivedCacheUnsafePath` 或 `derivedSourceUnavailable` 映射，然后做最小生产修正。source reader 的 unsafePath 在这个源读取边界映射为 `derivedSourceChanged`；物理文件消失的 ioFailure 继续为 `derivedSourceUnavailable`；cache store/root 的 unsafePath 继续为 `derivedCacheUnsafePath`，不得全局替换或弱化。

单项绿灯后，再无管道运行 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests；报告准确 passed/failed/skipped、红灯原因、git diff --check 和禁止边界。不要跑全量，不要创建 Cursor commit，不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved，不得扩展视觉/配额/v003/fault 其他簇。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1f.cursor.stream.jsonl
```

## 12. 行为簇 A/B 批准结论

第六次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，开工 HEAD 为
`c45ab93ed9e0cda888f2d4308ec88fdfd49c7573`。Cursor 未创建 commit，未跑全量，
按要求停在 A/B 检查点。

Codex 独立检查确认：

- `testSourceLocatorSymlinkReplacementReturnsSourceChanged` 通过
  `DerivedImageCacheService.loadOrGenerate` 真实公共端口执行；source current locator 被
  symlink 替换后返回 `derivedSourceChanged`，scope 配对，缓存 entry/object/staging 均为
  0，完整 catalog/fingerprint 快照不变；
- 测试直接读取 sourceRoot 外 fixture 哨兵 URL，而不是通过 symlink 读取；哨兵 bytes 与
  mtime 不变，locator 操作前后均由 `FileAttributeType.typeSymbolicLink` 证明仍为链接；
- source reader 的 `ioFailure` 只在 source 读取边界映射为
  `derivedSourceUnavailable`，`unsafePath` 只在该边界映射为
  `derivedSourceChanged`；cache store/maintenance 的 `unsafePath` 仍映射为
  `derivedCacheUnsafePath`；
- `openRelativeReadOnlyNoFollow` 对 final component 先做
  `fstatat(..., AT_SYMLINK_NOFOLLOW)` 类型检查，实际打开仍使用 `O_NOFOLLOW`，打开后再以
  `fstat` 证明为普通文件；父级组件继续以 dirfd-relative、`O_DIRECTORY | O_NOFOLLOW`
  打开；
- 测试文件没有遗留调试打印、提前 `return`、skip 或弱化错误断言；物理 source 文件消失
  回归仍明确通过 `derivedSourceUnavailable`。

Codex 独立执行：

```text
xcodebuild -scheme ImageAll -destination 'platform=macOS,arch=arm64' \
  -configuration Debug \
  -only-testing:ImageAllTests/DerivedImageHardeningTests \
  -only-testing:ImageAllTests/DerivedImageDirectoryEntryTests \
  -only-testing:ImageAllTests/DerivedImageOperationExclusivityTests test

passed=33, failed=0, skipped=0, TEST SUCCEEDED
xcresult=/Users/liguowei/Library/Developer/Xcode/DerivedData/ImageAll-czpiafylsyrfkeeurevmprpmnley/Logs/Test/Test-ImageAll-2026.07.15_15-57-53-+0800.xcresult
```

`git diff --check` 无输出；禁止边界无新增变化；既有 `?? user/` 未读、未改、未 stage。
因此行为簇 A（资格、scope、错误域）与 B（generation/maintenance 排他）批准，可以进入
下一受控检查点；这不代表切片 4 整体验收通过。

## 13. 第七次续接：只完成 source 与 final revalidation 变化矩阵

本轮仍属于同一未验收切片 4 返修，恢复原 session，只实现
`CURSOR-STAGE-1-SLICE-4-HANDOFF.md` 第 8、12.4 节的 source/final
revalidation 行为；不进入视觉、缓存损坏、同 key 并发、fault、配额、v003 或最终提交。
调用时以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 返修的 source/final revalidation 受控检查点；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务，禁止进入切片 5～7。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 5、7.2、8、12.4 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 10～13 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一返修任务草稿和既有 ?? user/。不得 reset、checkout、restore、stash、clean；不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

本次只补齐 source 打开事实、生成期间 source 变化和 final GRDB revalidation 的真实行为矩阵。继续一次一个测试 RED→GREEN，优先加强现有 DerivedImageFingerprintTests 和 DerivedImageTestSupport，不创建大而混杂的 matrix 文件。

第一簇，生成前和同一已打开 FD：保留 stored/current fingerprint 不一致的现有行为，但把断言加强为 scope 配对、零 staging/entry/object、完整 catalog/fingerprint 快照与 source bytes 不被生成器改写。随后分别真实证明同一已打开 FD 在读取期间 size、mtime 变化会返回 derivedSourceChanged；resource ID 也必须成为 pre/post source fingerprint 的比较事实。测试必须经 DerivedImageCacheService 公共 async 端口，并使用锁/condition/semaphore 的明确 reached/release handshake；不得依靠 Thread.sleep、无锁轮询或只测 fingerprint helper。size/mtime 优先在临时 fixture 上实际变更已打开文件；若 macOS 无法在同一 inode 上真实改变 resource ID，允许通过现有 FolderFileResourceReading 注入封闭、脚本化的 resource-ID 事实，但仍须经真实 source FD 读取和公共 service 端口，且测试名称与报告明确该边界。不得为了测试把 production resource ID 永久丢为 nil。

第二簇，current locator 重开：让生成在已从原 FD 读完且尚未 reopen locator 的精确内部检查点暂停，用同步 handshake 将相对路径指向的文件原子替换为另一个真实普通文件；释放后必须由 no-follow reopen 检测 size、mtime 或 resource ID 不一致并返回 derivedSourceChanged。断言 scope 配对、零 entry/object/staging、生成器不改写替换文件、完整 catalog/fingerprint 仍保持调用前数据库事实；checkpoint 只表达这一稳定性阶段，不得成为通用 debug API。

第三簇，final GRDB transaction：在完整派生对象已发布但 repository 最终 revalidation 尚未开始的精确检查点暂停。分别对真实临时数据库只改变一个事实并释放：content_revision、locator_state、relative_path（同时维持合法 file_name/路径形状）、availability、Source state、fingerprint size、fingerprint mtime、fingerprint resource ID。每一项都必须由最终事务重新读取并拒绝旧 context，公共端口返回 derivedSourceChanged，entry 为 0，本调用 final object 被删除而不是残留，staging 为 0；被测试主动改写的那一个 catalog 事实保留为新值，其余 Asset/fingerprint/Source 稳定事实不被生成器改写。不得把 repository fault injector 的 revalidation 异常当成事实变化测试，不得只直接调用 revalidate helper。若 relative_path 测试需要避免数据库唯一约束，使用同一临时 source 下另一个合法、存在的 fixture 路径并保持 source 安全。

所有 checkpoint 状态读写都必须同步；inverted/early-completion 只在 release 前完成时触发，正常 release 后不得 fulfill。生产实现只增加当前行为必要的最小 seam 和比较事实；Application/Domain 不得新增 Foundation URL、GRDB、ImageIO 或路径泄漏。任一拒绝路径都不得修改 source bytes、mtime、外部文件、Tag、Job 或非目标 catalog 事实。

每添加一个测试先单项运行并记录真实红灯原因，再最小修正并单项绿灯。完成后只运行 DerivedImageFingerprintTests 及为本矩阵新建或直接相关的精确 suite，再回归已批准的 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests；报告准确 passed/failed/skipped、红灯摘要、git diff --check 和禁止边界。不要运行最终全量，不要创建 Cursor commit。

不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；不得扩展视觉、缓存损坏、同 key 并发、配额、v003 或其他 fault matrix。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1g.cursor.stream.jsonl
```

## 14. 第七次续接中断：生产 seam 先于首个红灯

第七次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`b7b6558cddfc87bb9e8338af64070bac08ac9e24`。Cursor 在尚未创建或运行本簇第一个失败
测试时，已经：

- 新建未提交的 `DerivedImageGenerationCheckpoints.swift`，包含 source-read、locator-reopen
  和 final-revalidation 三个生产 checkpoint 协议；
- 把 `handleFacts` 的 `resourceID` 从 `nil` 改为 reader 事实，并修改
  `matchesHandleFacts`；
- 在 `DerivedImageSourceReader` 接入 read checkpoint；
- 在 `DerivedImageCacheService` 接入三个 checkpoint、file resource reader，并删除原有
  `sourceReader` 注入参数。

这违反第 13 节“一次一个测试 RED→GREEN”的明确准入门。Codex 在 TestSupport 与
FingerprintTests 尚未写入本簇测试前以 Ctrl-C 中断，进程退出码 130；未运行测试、未
提交。以上都是本次 session 刚产生的未提交增量，可以由 Cursor 手工撤销；不得用
`git restore/reset/checkout/clean`，以免覆盖同一切片 4 的其他已批准草稿。

下一次续接只修复该执行顺序并完成第一个 resource-ID 行为，完成后停止。调用时以包含
本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 source/final revalidation 返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 8、12.4 节和 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 12～14 节。当前必须是本地 main@<REVIEW_HEAD>；工作区包含此前已批准的切片 4 草稿、本次被中断的未测试增量和既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改 stage/提交 user/；不得访问 /Volumes/HDD2 或真实容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

第一步只手工撤销第 14 节列出的本次未测试增量，保留此前 A/B 已批准草稿。具体恢复到第七次续接前形状：删除本 session 新建的未提交 DerivedImageGenerationCheckpoints.swift 以及若有的 pbxproj 引用；DerivedImageSourceFingerprintFacts.handleFacts 暂时恢复 resourceID: nil，reopenedLocatorFacts 恢复单独读取 reader.resourceIdentifier；GRDBDerivedImageCacheRepository.matchesHandleFacts 暂时恢复只比较 size/mtime，matches 继续额外比较 resource ID；DerivedImageSourceReader 移除 readCheckpoint 属性、参数与调用；DerivedImageCacheService 移除本次三个 checkpoint 属性/参数/调用和 fileResourceReader 参数，恢复原有 `sourceReader: DerivedImageSourceReader = DerivedImageSourceReader()` 构造注入及 `self.sourceReader = sourceReader`。只撤销这些本轮增量；不得触碰之前 d_name、secure I/O、A/B、operation gate、事实快照等修正。撤销后先运行已批准的 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests，必须仍为 33/33。

第二步只完成一个新行为：同一真实 source FD 的 pre/post resource ID 变化必须返回 derivedSourceChanged。先在 DerivedImageFingerprintTests/TestSupport 写测试支持：使用现有 `sourceReader` 构造注入，把一个线程安全的 FolderFileResourceReading 包装器注入 DerivedImageSourceReader；第一次 resourceIdentifier 返回数据库持久化的真实 resource ID，第二次针对同一 FD URL 返回不同的脚本值，size/mtime 始终委托 Foundation reader。测试必须经 DerivedImageCacheService.loadOrGenerate 公共 async 端口和真实临时 source FD/bytes；预期 derivedSourceChanged、scope 配对、零 staging/entry/object、完整 catalog/fingerprint 快照与 source bytes 不变。测试名必须明确 resource ID 使用 injected reader boundary。此测试在任何生产 resource-ID 修正前单独运行，必须以“调用未被拒绝或错误不匹配”形成真实红灯；保存命令与失败摘要。

红灯后才做最小生产修正：handleFacts 从同一 FD reader 取得 best-effort resource ID；pre/post Equatable 比较自然包含它；matchesHandleFacts 同时比较 persisted resource ID。reopenedLocatorFacts 可复用 handleFacts，不重复读取。不得接入 source-read、reopen 或 final checkpoint，不得实现 size/mtime、locator replacement 或 final DB 矩阵。

随后只运行新 resource-ID 测试、DerivedImageFingerprintTests 全 suite 和已批准 A/B 三 suite；报告准确 passed/failed/skipped、红灯和绿灯命令、git diff --check、禁止边界。不要跑全量，不要提交。不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1h.cursor.stream.jsonl
```

## 15. Resource-ID 检查点结果与 Codex 复审

第八次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`4b7087d3be620c358b4083098538ffbfd8f9f753`。Cursor 先手工撤销第 14 节中断的
未测试 checkpoint 增量，已批准 A/B 三套件仍为 33/33；随后按 TDD 只实现同一真实
source FD 的 pre/post resource-ID 变化行为：

- 红灯：新增公共 async service 测试后，生成未被拒绝，证明 resource ID 尚未进入同一
  FD 的 pre/post 比较；
- 绿灯：`handleFacts` 从注入的 `FolderFileResourceReading` 读取 best-effort resource
  ID，`matchesHandleFacts` 同时比较持久化 resource ID，locator 重开复用同一事实读取；
- 测试边界：size/mtime 委托 Foundation reader，第一次 resource-ID 调用返回数据库事实，
  第二次调用返回不同脚本值；真实 source FD 与 source bytes 仍参与公共服务调用；
- 拒绝结果：`derivedSourceChanged`、scope 成对、entry/object/staging 均为 0、完整 catalog
  事实与 source bytes 不变。

Cursor 定向结果为 37 passed、0 failed、0 skipped。Codex 独立用同一四套件再次运行，
结果同为 37 passed、0 failed、0 skipped，`** TEST SUCCEEDED **`；
`git diff --check` 无输出。未发现提前接入 source-read、locator-reopen 或 final-DB
checkpoint；`DerivedImageGenerationCheckpoints.swift` 及工程引用均不存在。

该资源标识行为批准。`matches(_:)` 当前在已包含 resource ID 的
`matchesHandleFacts(_:)` 之后重复比较一次，下一受控检查点做等价的最小去重，不改变
语义。

## 16. 第九次续接：同 FD size/mtime 与 locator 原子替换

仍恢复同一未验收切片 4 session。优先复用现有 `FolderFileResourceReading` 注入边界，在
测试适配器的确定调用点修改临时 fixture；若现有生产比较已经正确，测试先行后允许直接
得到 characterization green，不得为制造红灯而增加生产 debug/checkpoint seam。调用时
以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 source/final revalidation 返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 5、7.2、8、12.4 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 13～16 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 已批准草稿和既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

本次只完成三个 source 稳定性测试：同一已打开 FD 的 size 变化、同一已打开 FD 的 mtime 变化、以及 source bytes 读完后 current locator 被另一个真实普通文件原子替换。不要进入 final GRDB revalidation、视觉、缓存损坏、同 key 并发、fault、配额、v003、全量测试或提交。

先做一项等价清理：`matchesHandleFacts(_:)` 已比较 resource ID，因此把 `matches(_:)` 化简为只返回 `matchesHandleFacts(opened)`；不得改变其他生产逻辑。

随后测试先行，优先只扩充 DerivedImageFingerprintTests 与 DerivedImageTestSupport，并复用现有 `FolderFileResourceReading` 注入边界，不新增 production checkpoint 协议：

1. size：测试 reader 在第一次 resourceIdentifier 调用时，先取得并返回原 FD 的真实 resource ID，再对已知临时 source locator 做一次确定的实际 size 变化，使 pre facts 已捕获而 `readAllBytes`/post facts 看到变化。公共 `loadOrGenerate` 必须返回 `derivedSourceChanged`。保留测试主动改变后的 source bytes/size，证明生成器没有再改写。
2. mtime：同样在第一次 resourceIdentifier 调用时，在不改变 bytes/size 的前提下把临时 source 的 mtime 设置为一个与 pre/persisted 明确不同且可精确读取的值。公共端口必须返回 `derivedSourceChanged`，并保留测试设置的 bytes/mtime。
3. locator replacement：在第二次 resourceIdentifier 调用（同一 FD post facts 的最后一步）先捕获并返回原 FD 事实，同时把 current locator 原子替换为同一临时 source root 下另一个有效、真实普通 JPEG；这样同一 FD pre/post 保持一致，但后续 no-follow reopen 必须看到新 locator 事实并返回 `derivedSourceChanged`。替换文件必须有确定不同的 size、mtime 或 resource ID；释放后验证替换文件 bytes/mtime 未被生成器改写。

测试适配器的状态与一次性副作用必须由锁保护；不得 Thread.sleep、无锁轮询或测试 helper 直测。每个测试都必须经过 `DerivedImageCacheService.loadOrGenerate` 公共 async 端口，并断言：准确错误为 `derivedSourceChanged`、bookmark scope 成对、entry/object/staging 全为 0、数据库完整 generation catalog/fingerprint 快照保持调用前事实；source 文件差异只允许是测试明确施加的变化。测试使用真实临时 fixture，不得访问受保护真实数据。

每个测试在写完后先单项运行。若现有生产语义已使测试直接通过，记录为 test-first characterization green，禁止为了人为制造红灯添加生产 seam；若失败，只做满足该行为的最小生产修正并记录真实失败原因。现有 resource-ID 测试不得回退。完成后运行 DerivedImageFingerprintTests 全 suite，再回归 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests；报告准确 passed/failed/skipped、每项首次结果、git diff --check 和禁止边界。不要运行全量，不要创建 Cursor commit。

不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；不得新增 source-read、locator-reopen、final-revalidation production checkpoint，除非先停止并向 Codex 证明现有 reader 注入边界无法表达该行为。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1i.cursor.stream.jsonl
```

## 17. Source 稳定性检查点初审：同 FD 时序证据不足

第九次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`e44cfc54452db83c5481fd172fb147f3a01f8bbf`。Cursor 未提交，新增三个公共服务测试，
并以 DB-compatible Foundation facts 与同 FD exact `fstat` facts 分层解决 mtime 精度差异；
定向四套件结果为 40 passed、0 failed、0 skipped，`git diff --check` 无输出。

本检查点暂不批准，原因不是最终结果错误，而是测试没有证明拒绝发生在声明的稳定性阶段：

- size/mtime reader 在第一次 `resourceIdentifier` 调用中改变 source；
- 当前 `readSourceBytes` 先调用 `handleFacts`，副作用已经发生，之后才采集
  `preHandleFstat`；因此 pre/post exact fstat 可能都看到改变后的同一值；
- 即使同 FD guard 没有拒绝，后续 locator reopen 仍会看到路径事实变化并返回同一个
  `derivedSourceChanged`，所以现有错误、零 artifact 与 catalog 断言无法区分拒绝阶段；
- 三个 reader 都维护私有调用次数，但测试未断言调用次数。size/mtime/resource-ID 在同
  FD 门拒绝时应恰好读取 resource ID 两次；locator 原子替换必须继续到 reopen，恰好三次。

因此需要窄修采集顺序和可观察证据；不允许仅以现有 40/40 进入 final DB 检查点。

## 18. 第十次续接：封闭同 FD 与 reopen 阶段证据

本轮仍是同一未验收切片 4 的同一交接单返修。只修第 17 节时序证据，完成后停止；调用
时以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 source 稳定性返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 8、12.4 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 15～18 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 草稿和既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 不批准上一轮 40/40 作为时序证据。当前 `readSourceBytes` 在 `handleFacts` 的第一次 resourceIdentifier 副作用之后才采集 `preHandleFstat`，所以 size/mtime 测试可能只是 locator reopen 拒绝。此次只封闭这个缺口；不得进入 final GRDB revalidation、其他矩阵、全量测试或提交。

测试先行：先把四个测试 reader 的 resource-ID 调用次数以锁保护的只读属性暴露给测试；在现有四个测试增加准确阶段断言：resource-ID flip、same-FD size、same-FD mtime 都必须在公共服务返回后恰好为 2；locator 原子替换必须恰好为 3。先只运行 size 与 mtime 测试；当前实现预期因达到 reopen 而出现 callCount=3 的真实红灯，保存失败摘要。若实际不是 3，停止并报告，不得猜测修改。

红灯后做最小生产整理，推荐并优先采用以下精确时序，除非编译约束证明不可行：
1. 打开真实 source FD 后，先用 `fstatRegularFile` 采集 exact pre handle facts；
2. 再调用一次 `handleFacts` 取得与 DB 持久化兼容的 initial fingerprint；该调用末尾第一次 resourceIdentifier 可触发测试副作用；
3. 从同一 FD 读取 bytes；
4. 用 `fstatRegularFile` 采集 exact post handle facts；
5. 直接从同一 fd URL 调用 reader.resourceIdentifier 取得 post resource ID（第二次调用），无需再构造一个 size/mtime 含义混杂的 post fingerprint；
6. result 明确携带 initial fingerprint、pre/post exact handle facts、post resource ID；service 先校验 initial fingerprint 与 DB，再校验 exact handle facts 相等且 initial/post resource ID 相等；
7. 只有这些通过后才 render 和 no-follow reopen。reopen 继续通过 `handleFacts` 取得第三次 locator facts。

允许把 `DerivedImageSourceReadResult` 的字段命名和极小的 value type 调整为上述单一含义；不得新增 checkpoint 协议、通用 debug API 或跨层抽象。若 `Equatable` 没有消费者，可删除无用途的 conformance；若保留，facts 必须是明确 struct。不得把 fstat 精度写进 DB，也不得改 Reconcile 持久化契约。

绿灯验收：四个测试全部通过且准确 callCount 分别为 2/2/2/3；size/mtime 测试仍验证测试施加的真实文件变化被保留，locator 测试仍验证真实原子替换后的 bytes/mtime/resource identity 未被生成器改写；scope、零 artifact、完整 DB 快照断言不得弱化。随后运行 DerivedImageFingerprintTests 与已批准 A/B 三套件，必须从 xcresult 报告准确 passed/failed/skipped。运行 git diff --check，报告禁止边界。不要跑全量，不要 commit。

不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1j.cursor.stream.jsonl
```

## 19. Source 稳定性时序返修结果与批准结论

第十次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`411e4725965057292c8d898fef5da3c661bd1c27`。Cursor 未创建 commit、未跑全量，按要求
停在 source 稳定性检查点。

测试先暴露四个 reader 的锁保护调用计数，并增加阶段断言。修复前只运行 size 与 mtime
两例，均以 `resourceIdentifierCallCount` 实际为 3、预期为 2 形成红灯，证明旧实现绕过了
同一 FD 门并走到 locator reopen。

最小生产整理后，读取顺序为：

1. 在任何测试 reader 副作用前，以 `fstatRegularFile` 取得 exact pre handle facts；
2. 从同一 FD 取得与目录库持久化合同兼容的 initial fingerprint；
3. 从同一 FD 读取 bytes；
4. 再以 `fstatRegularFile` 取得 exact post handle facts，并直接读取 post resource ID；
5. initial fingerprint 先与目录库事实比较，再比较 exact pre/post size、mtime 与
   initial/post resource ID；全部稳定后才允许 render 与 locator reopen。

Codex 独立检查确认：

- `DerivedImageSourceReadResult` 的 initial、pre/post exact facts 与 post resource ID 各自只有
  单一含义；无消费者的 `Equatable` 已移除；
- resource-ID flip、same-FD size、same-FD mtime 三例都准确停在第 2 次 resource-ID 读取，
  locator 原子替换例准确走到 reopen 的第 3 次读取；
- 四例继续断言准确错误、scope 配对、零 entry/object/staging、完整目录库事实不被生成器
  改写；size/mtime/locator 由测试施加的真实临时文件变化被保留；
- 未新增 checkpoint/debug API，未改变 Reconcile 持久化 fingerprint 精度或合同。

Codex 使用全新 xcresult 独立执行四套件：

```text
DerivedImageFingerprintTests
DerivedImageHardeningTests
DerivedImageDirectoryEntryTests
DerivedImageOperationExclusivityTests

total=40, passed=40, failed=0, skipped=0, result=Passed
```

`git diff --check` 无输出；既有 `?? user/` 未读、未改、未 stage；未访问受保护真实数据。
因此 source 同 FD 与 locator reopen 稳定性检查点批准，可以进入 final GRDB revalidation；
这仍不代表切片 4 整体验收通过。

## 20. 第十一次续接：只完成 final GRDB revalidation 事实矩阵

本轮仍属于同一未验收切片 4 交接单，恢复原 session。只在本调用 final object 已经原子
发布、最终 repository 写事务尚未开始的精确边界建立同步检查点，并证明最终事务拒绝八类
目录库事实变化。完成后停止，不进入视觉、cache/fault/quota/v003 或最终提交。

调用时以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 source/final revalidation 返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、8、12.4 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 13、19～20 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 已批准草稿和既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读取、修改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

本次只完成 final GRDB revalidation 的真实事实变化矩阵；不得进入视觉、cache hit/corruption、同 key 并发、fault matrix、配额、v003、全量测试或提交。

先只写 content_revision 一例及其测试支持，不先改生产代码。测试必须通过 DerivedImageCacheService.loadOrGenerate 公共 async 端口和真实临时文件数据库启动生成，并要求一个“本调用 final object 已完成原子发布、repository.publishEntryReplacingKey 最终事务尚未开始”的同步点。若必要的 production seam 尚不存在，先以测试/测试支持无法编译形成真实红灯并记录；随后才增加最小 seam。生产 seam 只表达这个精确阶段，例如内部 Sendable checkpoint 协议及默认 no-op 实现，由 service 在 store.publish 成功返回之后、构造/调用最终 repository 写入之前同步调用；不得把 GRDB、测试闭包或通用 debug API泄漏到 Application/Domain，也不得复用“staging 已写但尚未 rename”的旧 checkpoint 冒充此阶段。

测试 checkpoint 必须以 NSCondition/锁与 semaphore 等明确 reached/release handshake 实现，所有状态都受同步保护；禁止 Thread.sleep、无锁轮询、依靠调度概率或错误使用 inverted expectation。测试在 release 前必须取得本调用 entryID/storage format 或等价可证明 identity，断言对应 final object 确实存在且 staging 已消失，然后在真实临时目录库只把 asset.content_revision 改为合法不同值。release 后公共端口必须准确返回 derivedSourceChanged；目录库 cache entry 为 0；该 entryID 对应的本调用 final object已删除；staging 为 0；测试改写后的 revision 保留；其余 Asset/fingerprint/Source 完整稳定事实、source bytes 与 mtime 不被生成器改写。先单项运行，保存红灯和绿灯证据；content_revision 绿灯后才扩展其余矩阵。

随后以八个清楚命名的公共服务测试覆盖以下每次只改变一个语义事实：
1. content_revision 增加；
2. locator_state 从 current 改为 historical；
3. relative locator 改为同一临时 source 下另一个合法且真实存在的图片路径，同时把 file_name 改成该路径的合法末段；这一对字段视为一个不可分割 locator 事实，原路径与新路径 bytes/mtime 均不得被生成器改写；
4. availability 从 available 改为 missing；
5. Source state 从 active 改为 disabled；
6. file_fingerprint.size_bytes 改为另一个合法非负值；
7. file_fingerprint.modified_at_ns 改为另一个合法值；
8. file_fingerprint.resource_id 改为与原值不同的合法 BLOB 或 nil/BLOB 对应值。

每例都必须在 final object 已存在且最终事务未开始时改真实数据库；不得用 repository fault injector、直接调用 revalidate/helper 或只检查行数代替。每例 release 后都断言：derivedSourceChanged；entry=0；捕获的本调用 final object不存在；staging=0；被测试改写的目标事实精确保留；其余 `GenerationCatalogFacts` 字段逐项与调用前相同；source 文件 bytes/mtime 不变（relative locator 例同时检查两个 fixture）。Tag、Job 与非目标目录库事实不得变化。允许抽取一个窄的共享断言 helper，但八个测试名和每项数据库 mutation 必须可直接审阅，不得用一个循环吞掉 XCTest 行为身份。

当前 repository 的最终写事务已经重新读取完整 `DerivedImageAssetGenerationContext` 并与 expected 比较；除非测试证明缺陷，不得重写查询或扩展比较事实。source-read 与 locator-reopen 已批准逻辑及准确 callCount 断言不得回退。完成后只运行新增 final-revalidation suite、DerivedImageFingerprintTests，以及已批准的 DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests；从全新 xcresult 报告准确 total/passed/failed/skipped，运行 git diff --check 并报告禁止边界。不要跑最终全量，不要创建 Cursor commit。

不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved；除把新测试/生产 Swift 文件加入既有 target 所需的最小工程引用外，不得改 pbxproj。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1k.cursor.stream.jsonl
```

## 21. 第十一次续接中断：final checkpoint 再次先于首个测试

第十一次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`5b0eeed4cd962e609ab20e2c0127c08b8a212bad`。Cursor 尚未创建
`DerivedImageFinalRevalidationTests`、尚未修改 TestSupport 或工程测试引用、也尚未运行首个
content-revision 测试，却已先写入以下 production 增量：

- `DerivedImageCacheStore.swift`：`DerivedImageFinalPublishCheckpointing` 协议与默认 no-op；
- `DerivedImageCacheService.swift`：checkpoint 属性、构造参数、赋值，以及
  `store.publish` 返回后的调用。

这与第 20 节“先只写 content_revision 测试；必要 seam 不存在时先取得编译红灯”的明确
TDD 门不符。Codex 在 production 调用写入后立即以 Ctrl-C 中断，CLI 退出码为 1；未运行
测试、未提交。以上五处是本次 session 新产生的未测试增量，下一次续接必须手工删除；
禁止使用 restore/reset/checkout/clean，以免覆盖同一切片的其他已批准草稿。

## 22. 第十二次续接：撤销未测试增量，只完成 content-revision 首例

本轮只恢复第 21 节前基线，再严格完成一个 final revalidation 行为。content-revision 首例
绿灯后停止，七项剩余矩阵待 Codex 先审查 checkpoint 阶段与测试证据。

调用时以包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 final GRDB revalidation 返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、8、12.4 节和 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 19～22 节。当前必须是本地 main@<REVIEW_HEAD>，工作区包含此前已批准的切片 4 草稿、第 21 节刚被中断的未测试 production 增量和既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改 stage/提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

第一步只手工删除第 21 节本 session 的五处未测试增量，恢复到第十一次续接前形状：从 DerivedImageCacheStore.swift 删除 `DerivedImageFinalPublishCheckpointing` 协议和 `NoDerivedImageFinalPublishCheckpoint`；从 DerivedImageCacheService.swift 删除 `finalPublishCheckpoint` 属性、构造参数、`self.finalPublishCheckpoint` 赋值和 `blockAfterFinalObjectPublished` 调用。不得触碰此前 source/fingerprint、secure I/O、operation gate、fault/cache/quota/v003 等草稿。确认仓库中没有 FinalPublish/FinalRevalidation 符号后，用全新 xcresult 跑已批准的 DerivedImageFingerprintTests、DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests，必须仍为 40/40。

第二步只完成 content_revision 一个行为，严格测试先行。先创建清楚命名的 `DerivedImageFinalRevalidationTests.swift`，加入测试 target 所需的最小 pbxproj 引用；在 DerivedImageTestSupport 增加仅此测试需要的同步 FinalPublishCheckpoint、final object/staging 查询、makeService 注入参数与最小事实断言。此时 production 协议、默认实现、service 参数和调用都必须仍不存在。测试须经公共 async `loadOrGenerate` 和真实临时文件数据库：启动生成，要求在本调用 final object 已原子发布而 repository 最终事务未开始时阻塞；release 前捕获 entryID/storageFormat/stagingName，断言对应 final object 存在、该 staging 文件不存在；只把 asset.content_revision 从 1 改为 2；release 后期望 derivedSourceChanged、entry=0、该 final object 删除、staging=0、revision=2 保留、其余 GenerationCatalogFacts 完全不变、source bytes/mtime 不变、scope 配对，Tag/Job 事实不变。

测试 checkpoint 必须用 NSCondition/锁和 semaphore 的 reached/release handshake，全部状态同步；禁止 Thread.sleep、无锁轮询、调度概率或 inverted expectation。先运行唯一 content-revision 测试；它必须因缺少 production `DerivedImageFinalPublishCheckpointing`/service 注入而编译失败。保存精确命令和关键编译错误；如果意外编译或测试运行，停止报告，不得继续。

只有取得该红灯后，才增加最小 production seam：内部 Sendable checkpoint 协议与默认 no-op（可与现有 checkpoint 协议内聚）；service 构造注入；在 `store.publish` 成功返回、final object 已存在之后，且在创建/调用 `repository.publishEntryReplacingKey` 最终事务之前同步调用并传递本调用 identity。不得修改 repository 查询/比较，不得引入 GRDB/测试闭包到 Application/Domain，不得复用 staging checkpoint 冒充该阶段。

随后只运行 content-revision 单测至绿灯，再运行同一新 suite（此时仅一例）和前述四个已批准 suite；从全新 xcresult 报告准确 total/passed/failed/skipped，运行 git diff --check。不得添加剩余七例，不得进入视觉、cache/fault/quota/v003、全量测试或提交。不得修改 docs、v001/v002、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved。完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1l.cursor.stream.jsonl
```

## 23. Content-revision 首例结果与复审阻塞项

第十二次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`ed54e3ff379b2c4a83ceb1611030c511efc38865`。Cursor 未创建 commit、未跑全量，也未扩展
其余七项。

Cursor 先手工删除第 21 节五处未测试 production 增量；四个已批准 suite 仍为 40/40。
随后只增加 content-revision 测试、测试支持与工程引用，在 production seam 不存在时取得
真实编译红灯，关键错误为：

```text
cannot find type 'DerivedImageFinalPublishCheckpointing' in scope
cannot find 'NoDerivedImageFinalPublishCheckpoint' in scope
extra argument 'finalPublishCheckpoint' in call
```

红灯后加入最小内部 seam。Codex 独立检查确认 checkpoint 的 production 调用位置在
`store.publish` 成功返回之后、`repository.publishEntryReplacingKey` 及其最终 GRDB 事务之前；
release 前能捕获本调用 entry ID、格式和 staging name，并证明 final object 已存在、对应
staging 已消失。真实数据库把 `content_revision` 从 1 改为 2 后，公共端口返回
`derivedSourceChanged`，且 revision 2 保留、cache entry/staging/本调用 object 清零，其余目录库
事实、source bytes/mtime、scope、Tag 与 Job 事实不变。Codex 使用全新 xcresult 独立运行新
suite 与四个已批准 suite：

```text
total=41, passed=41, failed=0, skipped=0, result=Passed
```

行为与 production seam 本身正确，但测试 checkpoint 仍有可靠性阻塞项：测试在启动
`loadTask` 后，先执行可能抛错的 wait、文件断言和数据库 mutation，直到正常路径才调用
`releaseFinalPublish()`。如果上述任一步提前失败，后台生成任务将永久阻塞；现有 release
也不是幂等的，无法安全地同时用于正常 release 与 `defer` 兜底。未封闭该失败路径前，
content-revision 检查点暂不批准扩展七项。

## 24. 第十三次续接：封闭 checkpoint 失败路径后停止

本轮仍只修 content-revision 首例的测试同步可靠性，不扩展其余矩阵。调用时以包含本节的
精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 final GRDB revalidation 返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、8、12.4 节和 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 20～24 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改 stage/提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已确认 content_revision 的 production seam 位置与数据库拒绝行为正确，但测试在 checkpoint release 前任一步失败时会留下永久阻塞的 loadTask。本次只封闭这一测试可靠性缺口，完成后停止；不得添加其余七例，不得改 production seam、repository、source/fingerprint、secure I/O、视觉、cache/fault/quota/v003，也不得跑全量或 commit。

只修改 `ImageAllTests/DerivedImage/DerivedImageTestSupport.swift` 与 `ImageAllTests/DerivedImage/DerivedImageFinalRevalidationTests.swift` 中 FinalPublishCheckpoint/首例相关代码：
1. 让 `releaseFinalPublish()` 在同步保护下严格幂等，最多向 semaphore signal 一次；
2. `loadTask` 创建后立即建立无条件 release 兜底，使 wait timeout、release 前文件断言、数据库 mutation 或其他提前 throw 都不会留下阻塞任务；正常路径仍必须在数据库 mutation 完成后显式 release，再 await loadTask；兜底与显式 release 可安全重复调用；
3. 不使用 sleep、轮询、inverted expectation、异步 detached 清理或降低既有断言；不要增加通用 debug API。

这是 test-only 生命周期修正，不需要伪造 production 红灯。完成后先运行唯一 content-revision 测试，再用全新 xcresult 运行 DerivedImageFinalRevalidationTests、DerivedImageFingerprintTests、DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests；必须仍为 41/41，并运行 git diff --check。报告精确改动和证据后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1m.cursor.stream.jsonl
```

## 25. Checkpoint 生命周期修正结果与批准结论

第十三次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`34c6042baf364da446c688fa0cbd7ae77512f57e`。Cursor 只修改首例及其 test-only
`FinalPublishCheckpoint`，未改 production、未增加矩阵测试、未创建 commit。

`releaseFinalPublish()` 现在在同步保护下最多 signal 一次；首例在创建 `loadTask` 后立即注册
无条件 release 兜底，同时正常路径仍严格在真实数据库 mutation 完成后显式 release，再
await 任务结果。提前超时或 throw 不再留下永久阻塞任务，正常路径的第二次 release 为安全
幂等调用；未引入 sleep、轮询、detached 清理或通用 debug API。

Codex 使用全新 xcresult 独立运行首例、新 suite 与四个已批准 suite：

```text
total=41, passed=41, failed=0, skipped=0, result=Passed
```

因此 content-revision 首例与 final-publish checkpoint 获得批准，可以复用同一精确 seam
扩展其余七项。该批准不代表整个 final revalidation 矩阵或切片 4 通过。

## 26. 第十四次续接：扩展其余七项 final revalidation 事实

本轮只扩展第 20 节剩余七项，不改已批准 production seam。调用时以包含本节的精确 Codex
commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 final GRDB revalidation 测试矩阵；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、8、12.4 节和 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 20、23～26 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改 stage/提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已批准 content_revision 首例、production final-publish seam 位置与幂等测试 checkpoint。本次只在 `DerivedImageFinalRevalidationTests.swift` 与必要的 `DerivedImageTestSupport.swift` 中增加其余七个真实数据库事实变化测试；不得改 production、repository、source/fingerprint、secure I/O、视觉、cache/fault/quota/v003、pbxproj 或 docs，不得跑全量或 commit。若任一新测试证明当前 repository 行为失败，停止并回传该红灯与诊断，不得自行改 production。

先把 `GenerationCatalogFacts` 补齐为最终 repository `DerivedImageAssetGenerationContext` 的完整可审阅快照：除现有字段外必须包含并断言 asset ID、source ID、source kind；不得把 record 时间戳或其他不参与最终 revalidation 的字段伪装成该合同。content_revision 首例也必须继续通过完整快照断言。

随后增加七个清楚命名、彼此独立的 async XCTest；每个测试都必须通过公共 `DerivedImageCacheService.loadOrGenerate`、真实临时文件数据库和已批准 `FinalPublishCheckpoint`，不得用循环吞掉测试身份。允许抽取窄共享运行/清理断言 helper，但每个测试中的目标 SQL mutation 和预期目标事实必须直接可审阅：

1. `testFinalRevalidationLocatorStateChangeReturnsSourceChanged`：只把 locator_state 从 current 改为 historical；
2. `testFinalRevalidationRelativeLocatorChangeReturnsSourceChanged`：预先在同一临时 source 建立另一个合法真实图片文件；在 checkpoint 只把 relative_path 与 file_name 一起改为新路径/合法末段，这一对是一个不可分割 locator 事实；
3. `testFinalRevalidationAvailabilityChangeReturnsSourceChanged`：只把 availability 从 available 改为 missing；
4. `testFinalRevalidationSourceStateChangeReturnsSourceChanged`：只把当前 source.state 从 active 改为 disabled；
5. `testFinalRevalidationFingerprintSizeChangeReturnsSourceChanged`：只把 file_fingerprint.size_bytes 改为不同的合法非负值；
6. `testFinalRevalidationFingerprintMtimeChangeReturnsSourceChanged`：只把 modified_at_ns 改为不同合法值；
7. `testFinalRevalidationFingerprintResourceIDChangeReturnsSourceChanged`：只把 resource_id 改为不同的合法 BLOB 或合法 nil/BLOB 对应值。

每例均须：在 final object 已存在、对应 staging 已消失且最终事务尚未开始时执行目标 mutation；mutation 后显式 release 并 await；公共端口准确返回 `derivedSourceChanged`；cache entry=0、捕获 entry ID 的 final object 不存在、staging=0；目标事实精确保留，完整快照其余字段逐项不变；source bytes 与精确 mtime 不变；scope start/stop 配对，Tag/Job 数量不变。relative locator 例必须同时保存并核对原、新两个 fixture 的 bytes 与精确 mtime。每个 loadTask 创建后立即注册同样的幂等 release 兜底，禁止 sleep、轮询、inverted expectation 或调度概率。

按测试名逐项运行；当前 repository 已比较完整 context，预期各例直接绿灯。若有失败，保存全新 xcresult 摘要并停止。七例全部绿后，用全新 xcresult 运行 DerivedImageFinalRevalidationTests、DerivedImageFingerprintTests、DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests，预期准确总数 48、failed=0、skipped=0。运行 git diff --check，报告每项 mutation、断言与禁止边界后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1n.cursor.stream.jsonl
```

## 27. Final GRDB revalidation 八项矩阵结果与批准结论

第十四次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`acde0d524ca766a46c956ae19e966d418da6338a`。Cursor 未改 production、repository、工程
引用或 docs，未创建 commit。

测试快照已补齐 `assetID`、`sourceID` 与 `sourceKind`，现在与最终事务比较的
`DerivedImageAssetGenerationContext` 字段一一对应。除已批准 content-revision 首例外，新增
七个独立公共服务测试，在已批准 checkpoint 分别真实修改 locator state、relative locator
与 file name、availability、source state、fingerprint size、mtime 和 resource ID。每例都
证明目标事实保留、其他完整 context 不变、结构化 `derivedSourceChanged`、零 cache entry、
本调用 object/staging 删除、scope 配对以及 Tag/Job 不变；relative locator 同时核对原、新
两个真实临时 fixture 的 bytes 与精确 mtime。

Cursor 逐项运行八例均通过。Codex 使用全新 xcresult 独立运行 final revalidation suite 与
此前批准的四个 suite：

```text
total=48, passed=48, failed=0, skipped=0, result=Passed
```

`git diff --check` 无输出，未访问受保护真实数据。因此 final GRDB revalidation 八项矩阵
批准，下一验收门为 handoff 12.3 Image I/O 与视觉结果；切片 4 整体仍未通过。

## 28. 第十五次续接：Image I/O 与视觉结果 TDD 门

本轮只封闭 handoff 6、12.3 的 representation version 1 渲染合同，以及实际 UTI 与目录库
media type 漂移的公共服务拒绝。仍是后端，不接任何 SwiftUI。调用时以包含本节的精确
Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 1、4～6、8、12.3、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 25～28 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 已批准草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改 stage/提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已批准 eligibility/scope、operation exclusivity、source same-FD/reopen 稳定性和 final GRDB revalidation。此次只封闭 Image I/O/视觉结果；不得进入 cache hit/corruption/concurrency、fault、quota、v003、privacy manifest、全量测试或提交，不得改 docs、pbxproj、Application/Domain 契约、App/SwiftUI、Composition Root、scheme、entitlement、Package.resolved、v001/v002。除测试 fixture/support 外，production 只允许在新测试红灯证明需要时窄改 `DerivedImageRenderer.swift`，以及为“目录库 media_type 与实际 canonical UTI 漂移”所需的最小 `DerivedImageCacheService.swift` 调用契约；不得重写 source reader、repository 或 cache store。

严格测试先行。先只重写/扩展 `DerivedImageRenderingTests.swift` 及其局部 test-only fixture/helper；优先把视觉 fixture 保持在该测试文件或 `DerivedImageTestSupport.swift`，不要为了方便改跨切片 Reconcile production。此时不得先改 Renderer/Service。测试须使用真实 Image I/O 编解码、可审计合成像素和内存 bytes，不得源码 grep、snapshot 文本、skip、提前 return 或“fixture 不可用则算通过”。目标环境是批准的 macOS 15+、Apple Silicon，本机不能编码或解码批准格式就是测试失败。

把 rendering suite 整理为下列 22 个清楚命名的 XCTest（可有窄 helper，但行为身份不可由一个循环完全吞掉）：

1～6. JPEG、PNG、HEIC、HEIF、TIFF、WebP 各一个静态输入生成测试；每个先证明 source 的 Image I/O canonical UTI 等于预期，再断言生成 artifact 可单图完整解码、尺寸/格式/bytes/hash 自洽。六类 fixture 不得共享“空数据也继续”的路径。
7. orientation 1～8 视觉像素测试：使用非方形、四角/四象限颜色不同的 opaque fixture；先证明 source orientation metadata 与 raw 视觉基准，再对每个方向采样远离 JPEG 边界的内部像素并按 EXIF 1～8 的独立期望映射比较，带清楚 orientation 标签和合理有损容差。只断言宽高交换不合格，期望不得通过调用 production transform 生成。
8～9. gridSmall 256×256、gridRegular 512×512 的居中 aspect-fill：使用可区分左右裁切的非对称条带/象限，既断言精确尺寸也断言两侧保留内容证明对称居中 crop。
10. grid 对小图高质量放大并仍得到精确方形，像素内容非空且没有错误 letterbox/matte。
11～13. preview 横、竖、方形大图分别保持完整比例且最长边精确不超过/达到 2048；使用可控尺寸，分别验证宽高。
14. preview 小图不放大，输出宽高等于方向校正后的 source。
15. meaningful alpha 位于 64×64 采样范围之外：至少 128×128 alpha-capable 输入，把透明 patch 放在右下内部；必须输出 PNG，解码后对应内部采样 alpha 仍非 opaque。该测试必须能让当前“只扫前 64×64”实现先红。
16. 带 alpha channel 但所有像素完全 opaque 的输入必须输出 JPEG。
17. 输出解码后为 8-bit sRGB，不依赖显示器色彩空间。
18. artifact 的真实 byte count、SHA-256、storage UTI、单帧、像素尺寸与完整解码全部一致。
19. metadata 隐私：构造并先证明 source 中存在唯一 EXIF 日期、GPS、IPTC/XMP 或等价 XMP metadata sentinel、文件名/时间 sentinel；输出必须证明这些 source sentinel 在属性/metadata/bytes 中均不存在。不得因输出字典不存在而提前 return，也不得只检查两个 EXIF key。
20. 允许 UTI 的真实 multi-frame TIFF 必须 `derivedDecodeFailed` 且不生成 artifact。
21. animated GIF 必须结构化拒绝；继续保留它与 allowed multi-frame TIFF 的不同边界。
22. 公共 service 实际 UTI 漂移：真实临时 DB 把 Asset media_type 记为 JPEG，但 source bytes/fingerprint 来自合法 PNG；通过 `loadOrGenerate` 必须返回 `derivedSourceChanged`，entry/object/staging 为 0，完整 GenerationCatalogFacts、source bytes/mtime、scope、Tag/Job 均不被生成器改写。这个测试不得直接调用 renderer helper 冒充服务行为。

先运行整个 `DerivedImageRenderingTests` 并保留准确红灯。预期至少：右下 alpha 测试揭示当前 64×64 扫描缺陷；实际 UTI 漂移揭示当前 service 未比较目录库 media type 与 Image I/O canonical UTI。其他失败先判断是 production 缺陷还是 fixture/独立期望错误；不得为迁就当前输出改 EXIF 映射或放宽关键断言。若 suite 意外全部通过，停止报告，不得改 production。

红灯后只做使这些合同成立的最小 production 修正。实际 canonical UTI 仍必须属于封闭六类，且必须与本次生成 context.mediaType 相等；不相等按 stale source fact 返回 `derivedSourceChanged`，不改目录库事实。alpha 检测必须覆盖整个输出图的实际像素，不得用固定 64×64 抽样；保持 representation version=1、JPEG quality=0.85、8-bit sRGB、高质量插值、PNG/JPEG策略和内存编码。不要新增 Accelerate/第三方依赖、测试闭包、通用 debug API 或可配置算法。

绿灯后用全新 xcresult 先运行准确 22 项 Rendering suite，再运行：DerivedImageRenderingTests、DerivedImageContractTests、DerivedImageFinalRevalidationTests、DerivedImageFingerprintTests、DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests。若 Rendering 最终确为 22 项，定向总数应为 77；如确因一个测试名覆盖多个明确 subcase 而数量不同，必须逐名解释，不能静默偏差。运行 git diff --check，报告 RED 失败、最小 production 修正、各格式/orientation/geometry/alpha/color/metadata/multiframe/UTI-drift 证据和禁止边界。不要跑全量，不要 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1o.cursor.stream.jsonl
```

## 29. 第十五次续接中止与视觉测试基准返修

第十五次续接实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`23aa02aa04a919e000881c8db5ac7e0a7ef7ae21`。Cursor 建立了要求的 22 个 Rendering
测试，但首轮红灯仍混有三个测试基准缺陷：grid 像素通道以 `UInt8` 直接求和导致 trap；
orientation 期望由与生产变换同构的映射 helper 推导；metadata fixture 使用 ImageIO 不保留的
非法日期/GPS 引用值，且输出证明允许提前 return。此时另外两项红灯是预期生产缺陷：仅扫描
左上 64×64 的 alpha 检测，以及公共服务未拒绝目录库 media type 与实际 canonical UTI 漂移。

Cursor 在收到纠正后仍继续追查已明确的 trap，并准备弱化像素断言。Codex 因此用 Ctrl-C 中止
CLI，未批准进入生产修正。中止前 Cursor 只继续改动 test support；本轮没有创建 commit。

仍属同一未通过交接单，下一调用继续恢复原 session。调用时以包含本节的精确 Codex commit
替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 Image I/O/视觉结果返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 6、8、12.3、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 27～29 节。当前必须是本地 main@<REVIEW_HEAD>，工作区保留同一切片 4 已批准草稿、未完成视觉测试草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

上次运行因测试基准误诊被 Codex 主动中止。本次先且只修复 22 项 DerivedImageRenderingTests 的三个测试缺陷；在整个 suite 的红灯准确收敛到 meaningful alpha 与 actual UTI drift 两项之前，严禁改任何 production 文件。不得弱化、删除或跳过断言，不得用生产 transform 或 ImageIO 自动方向变换生成期望真值，不得增加 sleep、轮询、通用 debug API，也不得进入 cache/fault/quota/v003/privacy/full-suite/commit。

测试基准必须如下修正：
1. `testGridUpscalesSmallSourceToExactSquareWithoutLetterbox` 保留 corner 像素验证，但将通道求和改为 `Int(corner.r) + Int(corner.g) + Int(corner.b)`；当前 signal trap 的明确原因是三个 UInt8 直接求和溢出，不再追 crash log，不得以只查 bytes/hash 代替无 matte 的像素证据。
2. orientation 测试移除 `mapDisplayToRaw` 及任何同构 affine helper，使用测试中直接写出的独立四角真值表。顺序固定为显示 TL、TR、BL、BR，颜色记号 R/G/B/Y：orientation 1=`R,G,B,Y`，2=`G,R,Y,B`，3=`Y,B,G,R`，4=`B,Y,R,G`，5=`R,B,G,Y`，6=`B,R,Y,G`，7=`Y,G,B,R`，8=`G,Y,R,B`。用不经有损编码的 PNG orientation fixture；每个 source 必须先由 ImageIO 属性证明对应 orientation 值存在，再由 renderer 输出四个远离边界的内部像素与显式表比较。若本机 ImageIO 不能在 PNG 中往返 orientation，先做临时 probe 并停止报告证据，不得静默改表或生产 renderer。
3. metadata fixture 只用 ImageIO 能合法往返的属性和值：EXIF UserComment 唯一 sentinel；GPS MapDatum sentinel、合法 N/E ref 与有区分度的 latitude/longitude；IPTC ObjectName 与 CaptionAbstract sentinel；TIFF DocumentName sentinel；合法且有区分度的 TIFF DateTime 与 EXIF DateTimeOriginal。source proof 必须解包 ImageIO properties 字典并逐项精确比较上述值；不可用 raw JPEG ASCII 作为 source proof，不得使用非法前缀日期或自定义 LatitudeRef。输出 proof 必须先要求 source/properties 可解码，然后检查 EXIF/GPS/IPTC/TIFF（以及存在时 XMP）字典中源 sentinel/精确定位与日期均不存在，同时 bytes 不含文本 sentinel；任何 guard 失败都 XCTFail/throw，禁止提前 return 算通过。
4. `rgbaPixel` 使用单个完整 8-bit sRGB RGBA buffer 绘制整张 image 一次，明确 top-left 测试坐标与 CoreGraphics row 原点的转换；不得为单像素创建/平移 context。先用 orientation 1 和 grid 固色断言证明 helper 本身的坐标与通道顺序。

完成后用全新 `/tmp/render-red-2.xcresult` 运行准确 22 项 suite，并用 xcresult 列出全部失败。只有当结果准确为 20 passed、2 failed，且失败仅是 `testMeaningfulAlphaOutsideLegacySampleRegionOutputsPNG` 与 `testServiceActualUTIDriftReturnsSourceChanged` 时，才允许进入生产修正；否则继续修 test-only fixture 或停止报告，不得改 production。

得到干净红灯后，只做两个最小 production 修正：alpha 检测覆盖完整实际输出像素；公共服务从同一 source bytes 的 ImageIO canonical UTI 得到封闭六类实际类型并与 generation context.mediaType 精确比较，不相等返回 `derivedSourceChanged`，不得改目录库事实。除 `DerivedImageRenderer.swift` 与实现该比较所必需的最小 `DerivedImageCacheService.swift` 外不得改 production；不得修改 orientation/geometry/metadata 路径，除非干净红灯之后由既有 22 项中的独立失败证明需要。

绿灯后以全新 xcresult 运行 Rendering suite，必须 22/22；再运行 DerivedImageRenderingTests、DerivedImageContractTests、DerivedImageFinalRevalidationTests、DerivedImageFingerprintTests、DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests，必须总计 77/77。运行 git diff --check，报告红灯 20/2、两项最小 production 修正、绿灯 22/22 与定向 77/77。不要跑全量，不要 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1p.cursor.stream.jsonl
```

## 30. Image I/O 门复审与第三次测试证据返修

第十五次续接返修实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`917ad26b87ae38c203586d8c515fe5b3912fd696`。Cursor 已取得严格 RED 20/2，随后只修改
`DerivedImageRenderer.swift` 的全图 alpha 扫描与 `DerivedImageCacheService.swift` 的实际 UTI
比较，并取得 Rendering 22/22、七套件 77/77。Codex 独立源码复审仍发现三项测试证据缺口：

1. 输出 bytes 通过 `String(data:encoding:.ascii) ?? ""` 检查 sentinel；JPEG 含任意非 ASCII
   字节时会把完整 bytes 退化为空字符串，不能证明 sentinel 的 UTF-8 字节序列不存在。
2. orientation 测试只证明 PNG orientation 属性可往返，没有在进入 1～8 renderer 循环前
   独立解码 orientation 1 source 并采样证明 raw visual baseline 为 R/G/B/Y；角点断言也没有
   orientation/corner 标签。
3. `kCGImagePropertyMakerAppleDictionary` 被命名为 XMP。此次 fixture 已用 IPTC 满足元数据族
   要求，不应把 MakerApple 字典当作 XMP 证据。

服务 UTI 边界还需收紧为：若 ImageIO 能取得 canonical UTI，则任何
`actualUTI != context.mediaType` 都是 `derivedSourceChanged`；只有无法取得类型的损坏字节继续
交给 renderer 映射为 `derivedDecodeFailed`。不能只在 actual UTI 也属于六类时比较，否则合法但
不受支持格式替换源文件会逃过 stale-fact 拒绝。

仍属同一未通过交接单。下一调用继续恢复原 session；调用时用包含本节的精确 Codex commit
替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 Image I/O/视觉结果返修；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 6、8、12.3、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 28～30 节。当前必须是本地 main@<REVIEW_HEAD>，保留同一切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 尚未批准 Image I/O 门。此次只修下列复审项，不进入 cache/fault/quota/v003/privacy/full-suite/commit，不改 docs、pbxproj、Application/Domain、App/SwiftUI、Composition Root、source reader、repository、cache store。测试改动仅限 DerivedImageRenderingTests.swift 与 DerivedImageTestSupport.swift；production 仅允许收紧 DerivedImageCacheService.swift 的 UTI 分支。不得弱化、删除、skip 或增加第 23 个 Rendering 测试；最终仍须准确 22 项。

1. 在 orientation 主循环前，用 orientation 1 的同一非方形四象限 PNG source：先断言 ImageIO orientation 属性为 1，再不启用 orientation transform 地解码其 raw image，并通过同一 rgbaPixel helper 在四个内部点明确证明 TL/TR/BL/BR 为 R/G/B/Y。该基准必须独立于 renderer。保留直接写出的 8 向真值表。让每个 renderer 结果的颜色失败消息携带 `orientation N corner TL|TR|BL|BR`；可给 assertColorsClose 增加 test-only message 参数，不得用 production transform 生成期望。
2. `assertOutputMetadataSentinelsAbsent` 不得把完整 JPEG 转 ASCII。对每个文本 sentinel（含日期）将其编码为 UTF-8 Data，并用真正的 byte-subsequence 搜索断言 output bytes 不包含；空/编码失败必须使测试失败。继续要求输出可解码并检查 EXIF/GPS/IPTC/TIFF properties。删除把 MakerAppleDictionary 当作 XMP 的分支或错误命名；当前 IPTC fixture 已满足 handoff 的 IPTC/XMP 族，不需要伪造 XMP。
3. UTI 分支改为：若同一 source bytes 可建立 CGImageSource 且可取得 actual canonical UTI，只要 `actualUTI != context.mediaType` 就抛 `derivedSourceChanged`，不限定 actualUTI 也在支持集合；若无法取得类型，则不在这里误报，继续让 renderer 对损坏字节给出 `derivedDecodeFailed`。保留现有 PNG-vs-JPEG 漂移与 corrupt-bytes 回归，禁止修改预期迁就实现。

完成后用全新 xcresult 运行准确 22 项 DerivedImageRenderingTests，必须 22/22；再运行 DerivedImageRenderingTests、DerivedImageContractTests、DerivedImageFinalRevalidationTests、DerivedImageFingerprintTests、DerivedImageHardeningTests、DerivedImageDirectoryEntryTests、DerivedImageOperationExclusivityTests，必须 77/77。运行 git diff --check。报告精确改动和证据，不跑全量、不 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1q.cursor.stream.jsonl
```

## 31. Image I/O 门批准与 cache hit/corruption/concurrency 续接

### 31.1 实际调用审计

第 30 节第一次启动因 Codex 的 shell 提取错误，只实际传入了三项编号修正，遗漏开工 HEAD 与
禁止边界。该调用已在仅出现 `system/init` 与 `user` 事件、尚无 assistant/tool call 时由 Codex
立即 Ctrl-C 中止，没有产生文件改动。随后以第 30 节完整正文重新恢复同一 session；实际
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`a153579724a7f5ec32325227624b38d3c949a9f7`。

完整调用完成了 orientation 1 raw visual baseline、UTF-8 byte-subsequence metadata sentinel
检查、角点失败标签与任意可识别 canonical UTI 漂移分支。Cursor 证据为 Rendering 22/22、
七套件 77/77、`git diff --check` 通过。Codex 又独立检查实现，并使用全新结果包复跑：

- `/tmp/codex-rendering-22.xcresult`：22 passed、0 failed；
- `/tmp/codex-directed-77.xcresult`：77 passed、0 failed；
- Rendering 文件准确包含 22 个 XCTest；
- 输出 bytes 对每个 sentinel（含日期）执行 UTF-8 `Data.range(of:)`；
- orientation 1 raw source 在 renderer 外独立证明 R/G/B/Y，8 向仍使用显式真值表；
- 可识别 UTI 与目录事实不等返回 `derivedSourceChanged`，损坏字节仍为 `derivedDecodeFailed`。

**Codex 结论：Image I/O 与视觉结果门通过。**

### 31.2 第十六次续接：缓存命中、损坏重建与并发

仍属同一未完成切片 4。下一调用只封闭 handoff 7.3、8、12.4 中尚未由已批准门覆盖的
cache hit/corruption/concurrency 行为；调用时用包含本节的精确 Codex commit 替换
`<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 1、7.3、8、12.4、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 27～31 节。当前必须是本地 main@<REVIEW_HEAD>，保留同一切片 4 已批准草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已批准 eligibility/scope、operation exclusivity、same-FD/reopen 稳定性、final GRDB revalidation 及 Image I/O/视觉结果。此次只封闭 cache hit、损坏/缺失重建、revision 隔离与同实例/跨实例并发；不得进入 fault matrix、quota/capacity/LRU eviction、maintenance sweep、v003 schema、privacy manifest、全量测试或提交，不得改 docs、Application/Domain 契约、App/SwiftUI、Composition Root、entitlement、scheme、Package.resolved、v001/v002/v003。若新建单一 test 文件，可只为该测试源修改 pbxproj 引用，不得改 build setting。production 只允许新红灯证明需要时窄改 DerivedImageCacheService、GRDBDerivedImageCacheRepository、DerivedImageCacheStore 或同目录既有模型；不得新增通用 debug API、第三方依赖或与本门无关抽象。

严格测试先行。先审计并复用 DerivedImageContractTests、DerivedImageFingerprintTests、DerivedImageHardeningTests 和现有 test support，避免复制已批准 same-FD/final revalidation。新增或强化独立运行测试，至少逐项覆盖：

1. 有效 exact-key hit：返回 `origin=cacheHit`、entryID/bytes/format/dimensions 与首次生成一致；使用更晚的注入 clock 证明 `last_accessed_at_ms` 被精确 touch；第二次请求不打开 source scope，且 Source/Asset/fingerprint/Tag/Job 不变。
2. entry 对象缺失：先移除当前测试 Caches 根内对象，命中必须删除旧 entry 并按当前 key 重建；最终一行、一个对象、无 staging，旧 entryID 消失。
3. 对象截断：真实截短对象，不能仅改 DB；命中验证失败并重建。
4. 同 byte-size 篡改：保持文件长度但改变 bytes，必须由 SHA 拒绝并重建。
5. storage-format 伪装：把合法另一格式 bytes 放入既有对象，并同步 DB byte_size/hash 使 size/hash gate 可过，但 DB storage_format 仍保持原值；必须由真实 UTI/解码合同拒绝并重建。
6. pixel-dimension 伪装：对象 bytes/hash/format 有效，但 DB pixel width/height 与实际图像不一致；必须拒绝并重建。
7. 上述每种无效 hit 都要证明最终 entry/object/staging 收敛，并比较完整 GenerationCatalogFacts、source bytes/mtime、scope、Tag/Job 不被缓存修复改写；不得只断言行数。
8. content_revision 增加后旧 key 不命中：新 revision 独立发布；旧/新两行和两个被引用对象均存在，旧对象未被新 key 覆盖。
9. 同一 service 同 key 并发：使用确定性 checkpoint/barrier 或现有 in-flight 行为，不用 sleep/poll；所有返回 bytes 一致，最终一行、一个对象、零 staging。
10. 两个独立 service 实例共享同一真实临时 DB 与同一测试 Caches 根并发：用现有 final-publish checkpoint 或窄 test-only barrier 让两者都到达事务竞争点，再释放；验证 unique/final transaction winner，loser 返回 winner bytes、不覆盖 winner，最终一行、一个被引用对象、零 staging、无额外 loser 对象。测试不得直接调用 repository 冒充公共 service 竞争。

缓存对象损坏只允许发生在当前测试登记的临时 Caches 根；不得改写 source 来模拟 cache corruption。所有等待必须有有限 timeout 并使超时失败；不得 Thread.sleep、无界轮询、条件 skip、源码 grep 或提前 return 算通过。每个行为身份保留清楚 XCTest 名称；可以新建 `DerivedImageCacheHitAndConcurrencyTests.swift`，也可在现有测试中做窄增强，但不得用一个参数循环吞掉缺失/截断/篡改/格式/尺寸五种身份。

先只改 tests/test support，运行本门 focused suite 并保留真实 RED 摘要。若全部已通过，停止报告“实现已满足、production 零修改”，不得为了制造红灯改生产；若有失败，分类为测试缺陷或生产缺陷，只做最小修正。绿灯后先运行本门 focused suite，再运行已批准 77 项定向套件加本门全部新增/强化测试；报告各 suite 准确计数、竞争证据、完整不变事实、git diff --check。不要跑 fault/quota/v003/privacy/full suite，不要 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1r.cursor.stream.jsonl
```

## 32. 缓存门中止审计与跨实例目录创建竞态返修

### 32.1 第十六次续接的实际结果

第 31.2 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`2097aca7ec6486da0d358fc2d23b399a0f66df3e`。Cursor 新增了
`DerivedImageCacheHitAndConcurrencyTests.swift`，实际形成 9 个具名 XCTest：有效命中、对象缺失、
截断、同尺寸篡改、格式伪装、尺寸伪装、revision 隔离、同 service 并发、跨实例并发；五种无效
hit 各自通过共享断言检查最终 entry/object/staging 收敛与目录事实不变，未把行为身份吞入一个参数循环。

红灯阶段先发现并修正两项 test-only 缺陷：有效命中原先比较了不同 service 的 scope 计数器；
尺寸伪装原先给固定尺寸 `gridSmall` 写入 schema 本就拒绝的尺寸。生产红灯还证明：对象被外部移除后，
缓存修复清理对 `unlinkat` 的 `ENOENT` 映射为 `ioFailure`，因此实现已窄改为把“目标已不存在”视为
幂等清理成功。此后 focused suite 为 8 passed、1 failed；唯一失败是两个独立 service 共享同一临时
数据库与 Caches 根的竞争测试，只有一个参与者能到达 staging/final-publish 屏障。

Cursor 为定位该失败把屏障扩为 staging 与 final 两阶段，但再次只有一个参与者到达 staging，随后开始
偏离已收敛的故障边界。Codex 因此主动 Ctrl-C 中止此次 CLI，保留草稿，不视为门通过。独立源码审计
定位到 `DerivedImageSecureIO.ensureSubdirectory` 的目录创建 TOCTOU：两个实例均可先由 `fstatat`
观察到 `ENOENT`，winner 的 `mkdirat` 成功，loser 的 `mkdirat` 得到 `EEXIST` 后却被直接映射为
`ioFailure`，因此 loser 永远到不了测试屏障。测试本身另有两项必须同时修正：

1. `CrossInstanceRaceBarrier` 的 checkpoint callback 使用无界 `condition.wait()`，不符合本门所有等待
   必须有限的约束；timeout 不能用 `preconditionFailure` 杀进程，而应记录失败、释放全部 waiter，
   再由测试侧确定性失败。
2. loser 最终通过 `validateHit(winner)` 返回 winner payload，不能从两个 service 的返回 payload 推断
   loser provisional entryID；两个返回 entryID 应相同，独立 provisional ID 必须由 barrier 在 final
   publish 前捕获。

### 32.2 第十七次续接：只修跨实例竞争与完成缓存门

仍属同一未通过交接单。下一调用只修上述竞态和测试证据，不扩大 production 范围；调用时用包含本节的
精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 cache hit/corruption/concurrency 门；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 1、7.3、8、12.4、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 31～32 节。当前必须是本地 main@<REVIEW_HEAD>，保留同一切片 4 已批准草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

上次 cache focused suite 已收敛到 8 passed、1 failed；唯一失败是两个独立 service 共享同一真实临时 DB 与 Caches 根的并发。Codex 已定位 production 根因为 DerivedImageSecureIO.ensureSubdirectory 的 `fstatat == ENOENT` 到 `mkdirat` TOCTOU：两个实例同时创建同一目录时，loser 得到 EEXIST 后被错误映射为 ioFailure，无法到达 staging/final checkpoint。此次只做下列窄范围返修，不进入 fault/quota/v003/privacy/full-suite/commit，不改 docs、Application/Domain、App/SwiftUI、Composition Root、entitlement、scheme、Package.resolved、migration。production 只允许修改 DerivedImageSecureIO.swift；tests/test support 只允许修改现有 DerivedImageCacheHitAndConcurrencyTests.swift、DerivedImageTestSupport.swift，和删除由本门变得无用的 test helper。pbxproj 不应再变化。

1. 先保留并报告当前 focused 8/1 红灯证据。将 ensureSubdirectory 改为安全处理并发创建：若初次 fstatat 为 ENOENT，而 mkdirat 失败为 EEXIST，不得盲目接受；必须通过 `openat(parentFD,name,O_RDONLY|O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC)` 重新打开并以实际 directory FD 为证据。若竞态对手放入 symlink 或非目录，继续返回 `derivedCacheUnsafePath`；其他 errno 保持既有 persistence/io 映射。保持目录 FD 所有权与 close 语义，不添加路径字符串 fallback。为证明修复，跨实例测试必须从全新的空 Caches 根开始，让两者真实竞争创建 version/staging/objects 路径，不得先预热目录。
2. 将 CrossInstanceRaceBarrier 中 staging/final callback 的所有等待改成有限 deadline。protocol callback 不能 throw 时，timeout 应原子记录 test-only failure、释放全部 waiter，并让测试侧的有限 `waitUntil...` 或窄 `failure()` 查询产生 XCTest/throw 失败；禁止 preconditionFailure、fatalError、Thread.sleep、无界 wait/poll。任何失败路径都必须 release-all，不能遗留挂起 Tasks；测试用 defer 或等价结构保证清理。
3. 修正跨实例胜负证据：barrier 在 final publish 前捕获两个 distinct provisional entryID。完成后应证明恰有一个 `.generated` 与一个 `.cacheHit`，两个返回 payload 的 bytes 与 entryID 都等于 winner；不得期望 loser 返回自身 provisional ID。两个 provisional ID 必须不同；其中 winner ID 对应唯一 DB row/最终对象，另一个 provisional loser 对象不存在；最终准确一行、一个被引用对象、零 staging、无额外 loser 对象。公共 service 仍是竞争入口，不得直接调用 repository 冒充。
4. 保留前八个已通过行为及 production `unlinkat ENOENT` 幂等修复。检查 PairedFinalPublishCheckpoint 等旧 helper 的实际引用；若已无引用，只删除本门造成的孤儿，不清理无关代码。不要增加通用 debug API 或新抽象。

先运行本门准确 9 项 focused suite，必须 9/9。然后用全新 xcresult 运行已批准七套件 77 项加本门 9 项，必须准确 86/86，并用 xcresult 明确计数；不是 87。运行 git diff --check。报告 ensureSubdirectory 的 EEXIST 安全重开、全部有限等待、provisional/winner 证据、focused 9/9 与 aggregate 86/86。不得跑 fault/quota/v003/privacy/full suite，不得 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1s.cursor.stream.jsonl
```

## 33. 缓存门第二次复审与安全错误映射／Task 收集返修

### 33.1 调用与测试结果

第 32.2 节第一次 CLI 只输出 `system/init` 与 `user` 后持续空转，无 assistant/tool 事件、文件
改动或测试进程；Codex 在约两分半后 Ctrl-C 中止。随后使用完全相同的开工 HEAD、session 与任务正文
重试，输出记录为 `/tmp/ImageAll-stage1-slice4-revision1s-retry.cursor.stream.jsonl`。重试实际
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`bc7606a7ea6f21bef89a0e3a8f260f4461298139`。

Cursor 已按根因修复 `ensureSubdirectory` 的并发创建，使 `mkdirat` 的 `EEXIST` 继续通过
`openat(O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC)` 和 `verifyDirectoryFD` 重开；同时把跨实例屏障改为
staging/final callback 均有 deadline，捕获两个 provisional ID，并修正 winner/loser 断言。证据为：

- `/tmp/cache-hit-focused-9.xcresult`：9 passed、0 failed；
- `/tmp/cache-hit-aggregate-86.xcresult`：86 passed、0 failed；
- `git diff --check` 通过；
- 无 commit，未进入后续门。

### 33.2 Codex 复审结论

本门仍有两个阻塞项，暂不批准：

1. `openVerifiedSubdirectoryFD` 在 `openat` 失败时统一抛 `ioFailure`。若 `fstatat == ENOENT`
   与 `mkdirat == EEXIST` 之间竞争对手放入 symlink 或普通文件，Darwin 通常通过 `ELOOP` 或
   `ENOTDIR` 拒绝 `O_NOFOLLOW|O_DIRECTORY`，当前会被上层错误映射为
   `derivedCachePersistenceFailed`，没有保持第 32.2 节要求的 `derivedCacheUnsafePath`。
2. 跨实例测试只用 `defer { barrier.releaseAll() }` 解除 checkpoint。若
   `waitUntilStagingBlocked` 或 `waitUntilFinalBlocked` 抛错，测试会在没有读取 `taskA.value`、
   `taskB.value` 的情况下退出；Swift `Task` 句柄析构不会自动等待或取消，临时环境随后 cleanup，
   仍可能留下访问已删除目录的后台任务。必须在正常和异常路径都收集两个任务的结果后再退出测试。

### 33.3 第十八次续接：封闭两个阻塞项

下一调用继续同一 session，只修这两个问题；调用时用包含本节的精确 Codex commit 替换
`<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 cache hit/corruption/concurrency 门；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.3、8、12.4、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 32～33 节。当前必须是本地 main@<REVIEW_HEAD>，保留切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已核实 cache focused 9/9 与 aggregate 86/86，但缓存门尚未批准。此次只封闭以下两个阻塞项，不进入 fault/quota/v003/privacy/full-suite/commit。production 仅允许修改 DerivedImageSecureIO.swift；test/test support 仅允许修改 DerivedImageCacheHitAndConcurrencyTests.swift，若确有必要可在 DerivedImageTestSupport.swift 做最小调整；不得改 docs、pbxproj、其他 production、Application/Domain、App/SwiftUI、Composition Root、entitlement、scheme、Package.resolved 或 migration。

1. 安全错误映射：`openVerifiedSubdirectoryFD` 的 `openat(O_RDONLY|O_DIRECTORY|O_NOFOLLOW|O_CLOEXEC)` 若因 `ELOOP` 或 `ENOTDIR` 失败，必须抛 `unsafePath`，使 service 映射为 `derivedCacheUnsafePath`；其他 errno 保持 `ioFailure`。不能移除 O_NOFOLLOW/O_DIRECTORY、不能先 follow path、不能把所有 open 失败都误报 unsafe。补最小 test-only 覆盖确保 symlink 与普通文件仍被分类为 unsafe；若无法在不加 production checkpoint/debug API 的情况下确定性命中 EEXIST 后的 race 分支，可直接对 internal `ensureSubdirectory` 的可观察分类做窄测试并在报告中说明边界，禁止为测试增加 production hook。
2. Task 生命周期：重构跨实例测试的 orchestration，使任何 barrier wait、断言准备或 task 本身失败时都先 release-all，并确定性收集 taskA、taskB 后再让测试退出；不得只依赖 Task 句柄析构。可使用 async helper/result capture 或 structured task group，但不得 sleep、无界等待或吞掉原始错误。若 orchestration 失败，两个 task 的错误也需被收集，最后抛出最有诊断价值的原始 barrier/task 错误；成功路径保留一个 generated、一个 cacheHit、相同 winner payload、两个 distinct provisional ID、loser 对象不存在以及 1 row/1 object/0 staging 的全部断言。

先运行本门准确 9 项 focused suite；若新增具名安全分类测试，准确报告新计数并相应调整 aggregate 总数，否则仍应 9/9 与 86/86。用全新 xcresult 运行 focused 和既有七套件加本门，明确列出 passed/failed/total；运行 git diff --check。报告 ELOOP/ENOTDIR 分类和异常路径 Task 收集方式。不得跑后续门，不得 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1t.cursor.stream.jsonl
```

## 34. Codex 独立聚合复跑发现偶发 source facts 失败

### 34.1 第十八次续接结果与源码复审

第 33.3 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`aee60331e3bdcc1bf1b5ff86d9aefe676ec6dd89`。Cursor 已把
`openVerifiedSubdirectoryFD` 的 `ELOOP`/`ENOTDIR` 窄映射为 `unsafePath`，新增 symlink、
普通文件、合法目录三个 internal 分类测试，并在跨实例测试退出前 release-all 后读取两个
`Task.result`。Cursor 结果为 focused 12/12、aggregate 89/89。

Codex 源码复审核实 production errno 分类正确，屏障全部等待有限，Task 在临时环境 cleanup 前会被
收集。但当前跨实例测试成功路径先收集一次 task，函数末尾又重复收集一次；
`preferredCrossInstanceFailure` 还接收却不读取 `outcomes`。行为安全，但可以直接收敛成“先完成或
捕获 barrier orchestration，release-all，准确收集一次两 task，再校验结果”的更小结构。

### 34.2 独立测试证据与阻塞结论

Codex 使用全新 xcresult 独立运行同一组测试：

- `/tmp/codex-cache-focused-12.xcresult`：12 passed、0 failed；
- `/tmp/codex-cache-aggregate-89.xcresult`：88 passed、1 failed；唯一失败为
  `DerivedImageFingerprintTests.testCorruptCacheEntryRebuildsWithoutChangingAsset`，错误是
  `derivedSourceChanged`；
- 该用例随后单独运行通过；
- 同一 89 项聚合第二次 `/tmp/codex-cache-aggregate-89-rerun.xcresult` 为 89/89。

因此不是确定性 production 回归，而是旧 cache-corruption 正向测试在并行系统压力下直接依赖
Foundation `/dev/fd` resource facts 的偶发失败。这个测试的身份是“损坏 entry 重建且 Asset
revision 不变”，不是验证 Foundation resource identifier；其他 fingerprint 专项测试已覆盖真实
reader 与变化拒绝。缓存门仍不批准，不能以一次重跑通过掩盖 88/89。

### 34.3 第十九次续接：消除正向测试偶发并简化 Task 收集

下一调用继续同一 session，只改 test/test support；调用时用包含本节的精确 Codex commit替换
`<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 cache hit/corruption/concurrency 门；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.3、8、12.4、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 33～34 节。当前必须是本地 main@<REVIEW_HEAD>，保留切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已独立确认 production ELOOP/ENOTDIR 映射正确，但缓存门仍未批准。独立 focused `/tmp/codex-cache-focused-12.xcresult` 为 12/12；首次 aggregate `/tmp/codex-cache-aggregate-89.xcresult` 为 88/89，唯一失败 `DerivedImageFingerprintTests.testCorruptCacheEntryRebuildsWithoutChangingAsset()` 抛 `derivedSourceChanged`；该用例单独重跑与第二次 aggregate 又通过。这是必须消除的 test nondeterminism，不能以 rerun pass 关闭。此次禁止修改任何 production；只允许窄改 DerivedImageFingerprintTests.swift、DerivedImageCacheHitAndConcurrencyTests.swift，若需要单一 test-only reader 可最小改 DerivedImageTestSupport.swift。不得改 docs、pbxproj、其他 tests、Application/Domain、App/SwiftUI、Composition Root、entitlement、scheme、Package.resolved 或 migration；不得进入 fault/quota/v003/privacy/full-suite/commit。

1. 保留这次 88/89 作为 RED。`testCorruptCacheEntryRebuildsWithoutChangingAsset` 的测试身份是 cache entry byte_size corruption 被删除并重建、Asset revision/事实和 source 不变，不是 Foundation `/dev/fd` 可选 resource ID 集成。让该正向测试用 test-only source-facts reader 精确返回 seed 后持久化的 size/mtime/resourceID，或采用等价的最小确定性 fixture；实际 source bytes 仍必须由公共 service 从真实临时文件 FD 读取，不能 fake renderer/service/repository。不要放宽 production fail-closed fingerprint 比较，也不要把其他真实 fingerprint 变化测试改成固定 facts。加强该用例至少比较完整 GenerationCatalogFacts（除 cache entry 外目录事实不变）、source bytes/mtime、最终新 entry/object/staging 收敛，而不是只看 content_revision。
2. 把跨实例测试简化为单次 Task 收集：先执行 barrier staging/final orchestration并捕获其原始错误与 provisional IDs；无论成功失败都 release-all；随后并发读取 taskA.result/taskB.result，且只读取/封装一次；若 orchestration 失败优先保留其原始 barrier/synthetic 错误，否则抛 task 原始错误；只有二者成功后执行 winner/loser 事实断言。删除未使用的 outcomes 参数/辅助和重复收集，不新增通用 abstraction。保持全部有限 timeout、两个 distinct provisional、1 generated+1 cacheHit、相同 winner payload、loser 无对象、1 row/1 object/0 staging。

先运行 cache focused suite（当前应准确 12 项）和 Fingerprint suite，报告准确计数。随后用三个全新 xcresult 连续运行同一 89 项 aggregate，三次都必须 89/89；不得只循环失败用例或只报告最后一次。运行 git diff --check。报告 RED 88/89、确定性 source-facts 边界、简化后的单次 Task 收集以及三次 aggregate 证据。不得改 production、不得跑后续门、不得 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1u.cursor.stream.jsonl
```

## 35. 缓存门批准与 fault matrix 续接

### 35.1 第十九次续接结果与 Codex 独立复审

第 34.3 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`d72a35fe5064b4a3d53c50ab0b7c0b1a04cdd6eb`。Cursor 只修改 test/test support：

- `testCorruptCacheEntryRebuildsWithoutChangingAsset` 使用 seed 后持久化的 size、mtime、resource ID
  构造 test-only reader；公共 service 仍从真实临时文件 FD 读取 source bytes；
- 该用例增加完整 `GenerationCatalogFacts`、source bytes/mtime、scope、Tag/Job 和最终
  1 entry/1 object/0 staging 收敛检查；
- 跨实例测试改为先捕获 barrier orchestration，再 unconditional release-all，随后准确一次并发取得
  `taskA.result`/`taskB.result`，不再重复收集或遗留后台 Task。

Cursor 证据为 cache focused 12/12、Fingerprint 7/7，以及同一 89 项 aggregate 连续三次
89/89。Codex 源码复核未发现 production 变化，并用全新结果包
`/tmp/codex-cache-stable-89.xcresult` 独立运行同一聚合：89 passed、0 failed、total 89；
`git diff --check` 通过。

**Codex 结论：cache hit、损坏重建、revision 隔离与同实例/跨实例并发门通过。**

### 35.2 第二十次续接：原子发布与 fault matrix

仍属同一未完成切片 4。下一调用只封闭 handoff 7.2 与 12.5，不进入配额、v003、隐私或
全量收尾；调用时用包含本节的精确 Codex commit 替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先完整重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 1、7.2、10、12.5、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 34～35 节。当前必须是本地 main@<REVIEW_HEAD>，保留同一切片 4 已批准草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2、真实 Photos Library 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 已批准 eligibility/scope、operation exclusivity、same-FD/reopen 稳定性、final GRDB revalidation、Image I/O/视觉结果，以及 cache hit/corruption/concurrency。此次只封闭原子发布与真实 production adapter fault matrix；不得进入 quota/capacity/LRU eviction 的正常策略验证、路径攻击矩阵、v003 migration/schema、privacy manifest、全量测试/build/commit。不得改 docs、Application/Domain 契约、App/SwiftUI、Composition Root、entitlement、scheme、Package.resolved 或任何 migration。优先只改 DerivedImageFaultTests.swift 与 DerivedImageTestSupport.swift；只有真实 RED 证明 fault seam 缺失、不在实际失败位置或无法证明事务回滚时，才允许窄改 DerivedImageCacheStore.swift、DerivedImageCacheService.swift、GRDBDerivedImageCacheRepository.swift 或其既有 DerivedImage production collaborator。不得增加 UI、第三方依赖、通用 debug API 或与本门无关抽象；pbxproj 不应变化。

严格测试先行。先审计每个现有 `DerivedImageCacheStoreFaultPoint`、`DerivedImageRepositoryFaultPoint` 的生产调用路径、触发时机和可达性，记录未覆盖、过早触发、重复检查、永远不可达的 seam。先只改 tests/test support，运行 fault focused suite，保留真实 RED。测试必须证明 fault injector 在期望点准确触发一次；不得只是安装 injector 后观察某个任意失败。可把 test-only injector 增强为线程安全 call log/count，但不要把生产路径或底层错误文本暴露到 Application。

用清楚、独立的 XCTest 行为身份至少逐项覆盖：

1. staging 排他创建失败：零 cache entry、零 final object、零遗留 staging。
2. staging 写中断：必须在 production write seam 实际产生/模拟部分写入后失败并由本调用清理，不能只在 create/write 前立即抛错冒充“写中断”；最终零 entry/object/staging。
3. staging sync 失败：完整写后、final rename 前失败，零 entry/object/staging。
4. staging 自校验失败：staging 已写并到达真实 size/hash/decode validation seam，失败后零 entry/object/staging；不能只测 fake renderer。
5. final rename 失败：完整 staging 已验证但未发布 final，最终零 entry/object/staging。
6. final 已发布、DB transaction 开始前中断：恰有一个未引用完整 orphan、零 entry、零 staging；维护删除该 orphan，第二次维护幂等零变化。
7. DB insert/update 的后段失败：fault 必须位于真实 GRDB 写事务内、在至少一个 DML 已执行后抛出，以证明事务回滚；不得在 `pool.write` 前立即抛错。失败后绝无 entry 指向对象，最多一个完整 orphan；完整目录事实不变；维护可清理 orphan 且幂等。
8. final Asset revalidation 失败：既覆盖真实事实变化返回 `derivedSourceChanged` 的已批准行为，又在本门证明 repository fault seam 位于最终事务内且确实触发；事务不发布 entry，最多 orphan，维护收敛。不要复制八个已批准事实变化用例。
9. LRU touch 后段失败：先生成有效 entry，再以更晚 clock 命中；fault 必须在 touch UPDATE 已执行后的同一事务内抛出，证明 `last_accessed_at_ms` 回滚为原值、entry/object 保持有效，source scope 未重新打开。若当前 seam 在 UPDATE 前触发，RED 后最小移动/细分到事务内，不得放宽为“任意 persistence failure”。
10. 替换旧 key entry 的事务后段失败：必须从真实可达 production 路径证明旧 entry/旧对象仍保持一致，不能只构造 repository helper 的不可能状态或把并发 winner 当 replacement。若审计证明当前 `replacedEntry`/`oldObjectDelete` 分支对公共 service 永远不可达，先把该事实作为 RED/架构缺口报告，再做满足 7.2/12.5 的最小语义修正；不得保留死 seam 后写只调用 internal helper 的假通过测试。修正不得破坏已批准规则：有效同 key 跨实例竞争仍由既有 entry 胜出，loser 不能覆盖 winner；损坏 entry 重建不能改 Asset。
11. 旧对象删除失败：数据库成功提交新 entry 后，实际触发一次旧对象删除 fault；调用成功返回新 payload，新 entry/新对象有效，旧对象只能成为 orphan，绝不能回滚为指向半文件；维护清理旧 orphan并二次幂等。必须与第 10 项同一真实 replacement 语义相容。
12. 重开真实临时文件数据库：成功发布后关闭原 service 语境，以 `CatalogDatabase.open(at:)` 重开同一文件库并构造独立 service；exact-key hit 能验证并读取同一 entry/bytes/format/dimensions，且不重新打开 source scope。强化现有 reopen 测试，而不是只看 `origin`。

每个故障测试都必须保存并比较以下稳定事实，按场景明确允许变化的 cache 部分：完整 `GenerationCatalogFacts`；稳定排序的全部 cache-entry 列（id、key、format、dimensions、byte size、hash、created/accessed）；稳定排序的 cache tree（relative component/type/size/hash，区分 staging、对象和未知项，绝对路径不得进入失败消息/生产错误）；source 文件树/bytes/mtime；Tag/Job 数；security scope start/stop。不得只断言行数。所有测试只能使用当前测试登记的唯一临时 Caches 根与临时数据库，不得把 `/tmp` 本身当 production cache root，不得读真实数据。

状态不变量：第 4 步前任何失败必须无 final object/无 entry；第 4 步后失败最多留下完整未引用 orphan，绝无 entry 指向半文件；GRDB 后段故障必须以完整 before/after 行事实证明回滚；cleanup 自身失败不能覆盖原始业务错误。maintenance 对本门制造的 staging/final orphan 均应清理且第二次幂等。若现有 defer 立即清理了全部 pre-step4 staging，这是允许的；若要验证遗留 staging 的 maintenance，只能通过受控模拟进程中断的 test-only fixture在当前临时根制造合法 staging，不得削弱正常调用清理。

禁止 sleep、无界 wait/poll、条件 skip、提前 return 算通过、源码 grep 代替运行、一个参数循环吞掉所有行为身份，或用直接 SQL 绕过被验收的 service 行为；仅为精确验证 repository 事务内部 rollback 时，可以在独立 repository adapter test 中调用生产 repository，但仍必须与至少一个公共 service 端到端故障测试配对。若某项在当前合同下确实互相矛盾或无法从公共路径合法到达，停止并报告精确调用图与最小架构选择，不要静默发明产品语义。

绿灯后先运行 DerivedImageFaultTests focused suite，并用 xcresult 报告准确 passed/failed/total；再运行已批准 89 项聚合加本门全部 fault tests（同名 suite 重叠只计一次），用全新 xcresult 报告准确总数。运行 git diff --check。报告每个 fault seam 的准确 production 位置/触发次数、RED 摘要、事务与文件系统 before/after 证据、maintenance 幂等和 reopen 证据。不得跑 quota/v003/privacy/full suite，不得 build、不得 commit；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1v.cursor.stream.jsonl
```

## 36. Fault matrix 调用图纠正与合同消歧

### 36.1 主动中止原因

第 35.2 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`0f6dc8007a5df550bf2787f923b5c7e2cbb3d345`。Cursor 完成只读审计后误判“损坏 cache
重建会让 `publishEntryReplacingKey` 返回 `replacedEntry`”；Codex 对生产调用图复核确认：当前
`validateHit` 在对象缺失/损坏时先单独删除 entry 和旧对象，再生成；跨实例已有同 key 时则返回
`lostRaceToExisting`。因此 `replacedEntry` 与 `oldObjectDelete` 对公共 service 实际不可达，不能按
该误判继续写测试。Codex 在 Cursor 尚未执行 edit/test tool call 前 Ctrl-C 中止本次运行，没有接受
其后续实现设想。

这也暴露出 handoff 7.3“先删除无效 entry 再重建”与 12.5“旧 key 最终事务替换失败回滚／提交后
旧对象删除失败”互相冲突。架构合同已在 handoff 7.3 收敛为：无效 entry 不可返回，但作为
replacement candidate 保留到最终事务原子替换；失败时可以保留无效旧行但不得作为 hit；成功后才
best-effort 删除旧对象；跨实例有效 winner 仍不可被覆盖。

### 36.2 第二十一次续接：按消歧合同完成 fault matrix

仍是第 35.2 节同一未通过任务。以下纠正替代其中关于“损坏 entry 先删除”的旧假设，其余范围、测试
事实矩阵、禁止事项和停止位置全部继续有效；调用时替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的原子发布与 fault matrix 门；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、刚更新的 docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、7.3、10、12.5、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 35～36 节。当前必须是本地 main@<REVIEW_HEAD>，保留切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage 或提交 user/；不得访问 /Volumes/HDD2、真实 Photos Library 或真实 App 容器；不得使用 Task 子代理/MCP，不 push/amend/改写历史。system/init.model 必须仍为 Composer 2.5 Fast。

Codex 主动中止了上次只读推演，因为“损坏 cache 重建能直接到达 replacedEntry/oldObjectDelete”是错误调用图：当前 validateHit 会先单独删除无效 row/object，跨实例同 key 则走 lostRaceToExisting。上次没有发生 edit/test tool call。现在以修订后的 handoff 7.3 为权威合同：缺失/损坏 entry 不可返回为 hit，但应作为 replacement candidate 保留到最终 GRDB 写事务；重建成功后在该事务内重新验证 candidate、Asset/Source/revision/fingerprint 并原子替换，事务成功后才 best-effort 删除旧对象。若重建或事务失败，原无效旧行可以保留，但每次请求都必须重新验证，绝不能返回它；新 final 最多成为未引用完整 orphan。跨实例若另一份有效同 key entry 已胜出，loser 仍返回 winner 且不覆盖 winner。

继续执行第 35.2 节全部 fault matrix 要求，尤其 staging create/partial write/sync/validation/final rename、after-final-before-DB、事务内 DML 后故障、final revalidation、UPDATE 后 LRU touch 回滚、replacement transaction 回滚、旧对象删除失败、maintenance 幂等和真实文件库重开。先 tests/test support 形成 RED，再做最小 production 修正。允许为上述 replacement 合同窄改 DerivedImageCacheService.swift 与 GRDBDerivedImageCacheRepository.swift：invalid hit 应传递精确 candidate identity 到最终事务，不得仅凭 key 盲删；事务只有在 candidate 仍匹配时才替换。若 candidate 已被另一实例替换为有效 winner，必须走 winner 语义。不得新增 force-regenerate API、额外 Application 契约或 debug API。

必须新增回归证据：缺失/截断/同尺寸篡改/格式伪装/尺寸伪装五种已批准 cache 修复测试继续通过；无效旧 entry 在重建最终事务前绝不作为 payload 返回；replacement 的 DML 后故障完整回滚旧 row，旧对象保持；正常 replacement 提交后新 row/new object 有效，旧对象删除失败只留 orphan；maintenance 清理 orphan 后二次幂等。故障 injector 必须记录期望 seam 准确触发一次。所有 before/after 仍比较完整 GenerationCatalogFacts、稳定排序 cache-entry 全列、稳定 cache tree、source bytes/mtime、Tag/Job 与 scope，不得退回行数断言。

范围仍只限 fault matrix：不得进入 quota/capacity/LRU 正常淘汰策略、路径攻击矩阵、v003/schema、privacy、全量测试/build/commit；不得改 docs、pbxproj、Application/Domain、App/SwiftUI、Composition Root、entitlement、scheme、Package.resolved 或 migration。禁止 sleep、无界等待、skip、提前 return、源码 grep 代替行为、直接 SQL 冒充公共 service。先报告 RED；绿灯后运行 DerivedImageFaultTests focused suite，再运行已批准 89 项加全部 fault tests并准确用 xcresult 计数，运行 git diff --check；完成后停止等待 Codex 复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1w.cursor.stream.jsonl
```

## 37. Fault matrix 强制拆分 RED 与生产修复

### 37.1 两次中止审计

第 36.2 节第一次调用确认 `system/init.model = Composer 2.5 Fast` 与 HEAD
`ad3a648df4165cc11693b3f425b31ef46b543b3c`，只执行 HEAD/文档/生产文件读取后持续空转超过两分钟；
Codex Ctrl-C 中止，无 edit/test。完全相同正文重试后，Cursor 已正确理解 replacement candidate、
精确 candidate ID、并发 winner、LRU UPDATE 后 fault、旧对象 best-effort delete 等目标，但随后明确计划
“先完成生产变更，再执行全面测试”。Codex 在任何 edit tool call 前再次中止。两次调用均未形成新文件
修改，不能作为 RED 证据。

### 37.2 第二十二次续接：只完成 Phase A RED

下一调用仍恢复同一 session，但只允许 tests/test support；完成真实 RED 后必须停止，不得接着修生产。
调用时替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 的 fault matrix，恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、7.3、12.5、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 35～37 节。当前必须是本地 main@<REVIEW_HEAD>，保留所有切片 4 草稿与 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage/commit user/，不得访问 /Volumes/HDD2 或真实 App 容器，不 push/amend，不使用子代理/MCP。system/init.model 必须是 Composer 2.5 Fast。

这是 fault matrix 的 Phase A RED，范围刻意缩小：只能修改 ImageAllTests/DerivedImage/DerivedImageFaultTests.swift 与 ImageAllTests/DerivedImage/DerivedImageTestSupport.swift。禁止修改任何 ImageAll/ production、pbxproj、docs、其他 tests、migration、manifest、UI 或配置；禁止 build/aggregate/commit。你上次“先生产后测试”的计划已被 Codex 中止，不能继续沿用。

先用现有 production API 与现有 fault enum 写出具名测试和最小 test-only support，覆盖第 35.2 与 36.2 已列的 12 类行为。测试支持至少提供：线程安全 store/repository fault 调用日志与准确 call count；稳定排序的 cache-entry 全列快照；只含相对 component/type/size/hash 的 cache tree 快照；完整 GenerationCatalogFacts、source bytes/mtime、Tag/Job、scope before/after 聚合断言。不得把绝对路径放进 snapshot 或失败消息。不要复制五个已批准 corruption tests，但必须新增 replacement transaction failure、old object delete failure 与 invalid candidate 不返回旧 payload 的端到端 service 测试。

RED 必须针对当前真实缺陷，不得为制造编译错误引用尚不存在的 production case/type/API，也不得先改 production：
- 自定义 store injector 在 `.stagingWrite` 回调时观察当前临时 cache tree，断言调用发生时已经存在一个非零但小于最终 artifact 的 staging regular file；当前 seam 在 create/write 前，应行为失败。
- replacement 事务故障使用现有 `.insert`：先生成旧 entry、真实损坏其 cache object 或现有 DB 校验字段使其不可命中，再请求公共 service；期望旧 entry 全列与旧对象在失败后保持、无新 entry 指向对象、最多新 orphan。当前 validateHit 会先删旧 entry/object，应失败。
- old object delete 使用现有 `.oldObjectDelete` 并从同一公共 invalid-candidate replacement 路径期望准确触发一次；当前死分支应失败。
- LRU touch 测试至少证明 fault 调用一次、返回 persistence failure、原 row/object 完整保持且 source scope 未打开；在测试名/注释明确“当前 RED 尚不能证明 UPDATE 后触发”，留给下一生产修复阶段用最小后置 seam/ordering证据补强，禁止把当前前置失败误报为通过最终合同。
- staging create/sync/validation/final rename/afterRenameBeforeDB/revalidation/reopen 等其余测试可先以现有 seam 写出并强化完整事实断言；单项若当前通过，要准确报告为既有 GREEN，不能伪造 RED。

只运行 `DerivedImageFaultTests` focused suite到自然完成，保存一个全新 xcresult，使用 xcresulttool 报告准确 passed/failed/total 与每个失败测试名。允许编译器先暴露 test-only 代码缺陷并修正到测试可运行，但最终 Phase A 必须是“测试目标成功编译、行为用例真实失败”，不是编译失败。运行 git diff --check。报告哪些用例 RED、哪些既有 GREEN、fault call count 和快照证据。然后立即停止，不得修改 production、不得为变绿继续实现、不得运行 89 项 aggregate、不得 commit，等待 Codex 复审 RED 质量。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1x.cursor.stream.jsonl
```

## 38. Phase A RED 复审与事实矩阵补正

### 38.1 第二十二次续接结果

第 37.2 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`491d0326571fa77b8e370876c6783ea45f948ad1`。Cursor 严格只修改
`DerivedImageFaultTests.swift` 与 `DerivedImageTestSupport.swift`，没有修改 production、运行聚合、
构建或提交。

`/tmp/fault-phase-a-red.xcresult` 的真实结果为 13 total、8 passed、5 failed。五个行为 RED 为：

- `.stagingWrite` 回调时没有非零且小于最终 artifact 的部分 staging 文件；
- 两个 replacement `.insert` 用例在故障后旧 entry 已被删除；
- invalid candidate 重建失败后旧 entry/object 已被删除；
- invalid-candidate 公共路径没有触发 `.oldObjectDelete`。

既有 staging create/sync/validate/final rename、after-rename-before-DB、final revalidation、当前前置
LRU fault 和文件库重开共八项为 GREEN。`git diff --check` 通过。测试命令使用管道且没有
`pipefail`，所以 shell 显示 exit 0；验收以 xcresult 的 5 failed 为准，后续命令必须保留
`xcodebuild` 的真实退出码。

### 38.2 Codex 复审阻塞项

五个 RED 的 production 根因映射正确，但 Phase A 尚未批准：

1. `FaultMatrixStableFacts` 直接保存含绝对 `URL` 的 `SourceFileSnapshot`，违反“快照/失败消息不含
   绝对路径”；也只保存单文件而非稳定相对 source tree。
2. `captureCacheTree` 对不可读项静默 `continue`，`unknown` 类型永远不会产生，并通过
   `Data(contentsOf:)` 读取目录项，不能证明链接/非普通项不会被跟随或遗漏。
3. staging-write 用例只验证部分 staging 的观察，没有要求调用结束后旧 invalid candidate 的 entry
   全列和旧对象树精确恢复；生产若先删除旧行/旧对象仍可能在补上部分写 seam 后误通过。
4. replacement helper 与 invalid-candidate 用例丢弃了实际重建 service 的 bookmark port，却比较
   baseline port；reopen 用例也丢弃重开 service 的 port。它们没有证明被验收调用的 scope 平衡或
   cache hit 零 source access。
5. `tamperedObjectBytes` 在篡改前读取，不能证明无效字节没有作为 payload 返回。
6. 两个 replacement 用例完全重复；第 35.2 节第 7 项 fresh insert 的事务回滚身份缺失。由于现有
   `.insert` seam 在 `pool.write` 前，纯黑盒 Phase A 只能先形成 before/after 事实测试并明确标注
   “尚不能证明 DML 后触发”，Phase B 必须由生产位置与同一测试共同封闭。
7. cleanup 失败不得覆盖原始业务错误，以及受控合法 staging orphan 被 maintenance 清理并二次幂等，
   尚无具名测试。
8. `assertFaultMatrixStableFactsUnchanged` 名称暗示比较全部事实，实际不比较 cache entries/tree；各场景
   应显式声明允许变化的 cache 部分，避免虚假“完整快照不变”。

### 38.3 第二十三次续接：只修 Phase A 测试质量

仍属同一未通过 Phase A；继续只改两个测试文件，不得进入 production。调用时替换
`<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 fault matrix 的 Phase A RED；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。先重读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、7.3、12.5、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 35～38 节。当前必须是本地 main@<REVIEW_HEAD>，保留全部切片 4 草稿与既有 ?? user/。不得 reset、restore、checkout、stash、clean；不得读改、stage/commit user/；不得访问 /Volumes/HDD2、真实 Photos Library 或真实 App 容器；不 push/amend，不用子代理/MCP。system/init.model 必须为 Composer 2.5 Fast。

Codex 已确认 `/tmp/fault-phase-a-red.xcresult` 为编译成功后的真实 13 total / 8 passed / 5 failed，五个 RED 方向正确，但事实矩阵仍有八项测试质量阻塞。此次仍只能修改 ImageAllTests/DerivedImage/DerivedImageFaultTests.swift 与 ImageAllTests/DerivedImage/DerivedImageTestSupport.swift；禁止修改任何 ImageAll/ production、pbxproj、docs、其他 tests、migration、manifest、UI 或配置；禁止 aggregate/build/commit。完成后仍停在 Phase A。

逐项补正：

1. 从 `FaultMatrixStableFacts` 移除含绝对 URL 的 `SourceFileSnapshot`。改为稳定排序的 path-free source tree item：只含相对 component、entry type、byte size、mtime、内容 hash；测试失败消息和 snapshot 绝不能包含绝对路径。当前 fixture 至少精确覆盖源文件 bytes/hash/mtime；未知或非普通 source 项也不得被静默遗漏或跟随。
2. cache tree 捕获必须稳定列出当前 cache version root 下所有目录项并区分 staging regular file、object regular file、unknown；使用 no-follow 的 entry facts，symlink、目录、命名不合法或不可读项记为 unknown，绝不能用 `Data(contentsOf:)` 跟随 symlink，也不能 `try? ... continue` 静默消失。regular file 才读取 size/hash。相对 component 必须稳定，仍不得含绝对路径。
3. staging-write RED 在 fault 返回后除验证 scope/catalog/source/Tag/Job 外，还必须精确断言 cache-entry 全列与 cache tree 回到 before：旧 invalid candidate row 和被篡改旧对象仍保持，部分 staging 已由本次调用清理。当前 production 会先删旧 row/object，因此该用例应继续是同一个真实 RED，而不是只因 partial size 为 nil 失败。
4. replacement helper 与 invalid-candidate 用例必须保留实际重建 service 返回的 bookmark port，并验证该 port 在调用后 start/stop 平衡；不得拿 baseline port 代替。reopen 测试把独立 reopened service 的 bookmark port 保存为变量，exact-key hit 后明确为 0 start / 0 stop；原 service port 另行保持平衡。
5. invalid candidate 用例在真实篡改完成后再读取 corrupted bytes；若公共调用意外返回，必须证明返回 payload 不是这些 corrupted bytes且不是 cacheHit。不要把正常确定性重编码出的原始合法字节误当 corrupted bytes。
6. 删除两个完全重复的 replacement `.insert` 身份。保留一个 invalid-candidate replacement rollback RED；把另一个改成 fresh generation 的 `testFreshInsertFaultRollsBackEntryAndLeavesOneCompleteOrphan...`：无旧 entry，从公共 service 触发现有 `.insert`，验证完整 catalog/source/Tag/Job/scope 不变、0 entry、恰 1 个完整 final orphan、0 staging，maintenance 清掉 orphan且二次幂等。测试名/注释明确当前 Phase A 只能证明 rollback 外部事实，尚不能证明 fault 在 DML 后；Phase B 必须把 seam 放到事务内至少一个 DML 后并由源码位置加同一 before/after 测试封闭，禁止误报最终合同已通过。
7. 新增两个独立具名用例：a) 在当前唯一临时 cache 根以 test-only fault injector/临时权限或等价受控方式让 staging cleanup 本身失败，同时触发一个更早的既有业务 fault，最终对外仍是原始 `derivedCachePersistenceFailed`，cleanup 错误不能覆盖；测试必须恢复权限/状态后再清理，不能污染其他用例。b) 在当前合法临时 version root 通过 production anchored session 或等价 no-follow test fixture 制造一个合法 staging orphan，`performMaintenance` 删除它，第二次 maintenance 为 0；不得直接写 `/tmp` 根或真实容器。若 macOS 权限模型使 cleanup-failure fixture 无法确定性构造，停止并报告精确原因与最小 test seam 选择，不得用无效断言冒充覆盖。
8. 重命名/拆分快照断言，让 helper 名称与实际比较字段一致。每个用例显式比较允许不变的 cache entries/tree，或显式断言该场景允许的单 orphan/替换变化；不得构造含 cache 数组却被 helper 忽略的 `FaultMatrixStableFacts` 来声称“全部不变”。oldObjectDelete 用例保留预期 cache 变化，但 catalog/source/Tag/Job 与实际 service scope 必须不变/平衡。

保留已有五个 production RED，不得因为修测试把缺陷放宽。最终只运行 `DerivedImageFaultTests` focused suite，命令使用 `set -o pipefail` 或不经会吞退出码的管道，写入全新 `/tmp/fault-phase-a-red-review.xcresult`；用 xcresulttool 准确报告 passed/failed/total、失败测试名与每项 fault call count。允许新增 GREEN 后总数变化，但预期至少 stagingWrite、replacement rollback、invalid candidate、oldObjectDelete 仍是行为 RED；fresh insert 明确 partial GREEN。运行 git diff --check。不得修改 production、不得跑 89 项、不得 build/commit；完成后停止等 Codex 再审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1y.cursor.stream.jsonl
```

## 39. Phase A 测试质量续接中断与恢复编译

### 39.1 第二十三次续接实际结果

第 38.3 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`4cf7ec25c621051ddfaf16f096a215617e7792e0`。Cursor 只对
`DerivedImageTestSupport.swift` 做了部分 test-only 编辑：增加 source tree、no-follow cache object read、
合法 staging orphan fixture 的入口，并把快照声明改名为 `FaultMatrixCatalogSnapshot`。随后在计划大段替换
fault-matrix helper 时持续空转，Codex 在超过两分半无有效输出后 Ctrl-C 中止。

本次没有修改 production，没有运行测试、聚合或构建，也没有创建 commit。中断时
`DerivedImageFaultTests.swift` 仍完整引用旧 `FaultMatrixStableFacts` /
`captureFaultMatrixStableFacts` API，而 test support 已移除旧类型声明但尚未替换旧 helper，因此当前草稿
是明确的编译不一致中间态，不能作为 Phase A RED 证据。下一次必须先以最小编辑恢复测试目标编译，再完成
第 38.2 节的事实矩阵修正；不得继续规划通用递归抽象。

### 39.2 第二十四次续接：最小闭合两份测试文件

仍属同一未通过 Phase A，仅续接原 session。调用时替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 fault matrix 的 Phase A RED；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。当前必须是本地 main@<REVIEW_HEAD>。先读 AGENTS.md、docs/CURSOR-STAGE-1-SLICE-4-HANDOFF.md 第 7.2、7.3、12.5、13～15 节，以及 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 38～39 节。保留全部切片 4 草稿与既有 ?? user/；不得 reset、restore、checkout、stash、clean；不得读改、stage/commit user/；不得访问 /Volumes/HDD2、真实 Photos Library 或真实 App 容器；不 push/amend，不用子代理/MCP。system/init.model 必须为 Composer 2.5 Fast。

上次在 test support 做到一半后被 Codex 中止。当前 DerivedImageTestSupport.swift 已声明 FaultMatrixCatalogSnapshot 和三个 TempEnvironment helper，但旧 capture/assert helper 及 DerivedImageFaultTests.swift 仍引用已不存在的 FaultMatrixStableFacts，测试目标处于编译不一致中间态。此次仍只能修改 ImageAllTests/DerivedImage/DerivedImageTestSupport.swift 与 ImageAllTests/DerivedImage/DerivedImageFaultTests.swift；禁止修改任何 ImageAll/ production、pbxproj、docs、其他 tests、migration、manifest、UI 或配置；禁止 aggregate/build/commit。

第一优先级是最小恢复编译：完成现有重命名，删除/改写旧 helper 引用，先运行一次 DerivedImageFaultTests 确认测试目标能编译；不要重新审计整个项目，不要设计通用文件树框架。随后完成第 38.2 节八项阻塞的最小测试实现：

1. FaultMatrix 快照不得持有 SourceFileSnapshot 或绝对 URL。source tree 只递归当前 fixture 的 sourceRoot，输出稳定排序的相对 component/type/size/mtime/hash；目录记 directory 后递归，symlink/未知项记 unknown 且不跟随。旧 SourceFileSnapshot 可保留给其他既有测试，但 fault matrix 不得使用。
2. cache tree 只检查当前 fixture version root。`staging`、`objects` 和合法 shard 目录作为预期结构容器递归，不把这些容器本身误记 unknown；其子项按 staging regular、object regular 或 unknown 输出。意外顶层项、非法 shard、symlink/目录/其他类型记 unknown且不跟随；regular file 才读取 size/hash，不得静默 continue。
3. stagingWrite 测试在 fault 回调观察非零且小于最终 artifact 的 staging regular file，调用后精确比较旧 invalid candidate 的 entry 全列与被篡改旧对象树仍与 before 相同，0 staging；使用实际重建 service 的 bookmark port并验证平衡。
4. replacement/invalid/reopen 均保存实际被验收 service 的 bookmark port；reopened exact-key hit 为 0 start/0 stop。corrupted bytes 必须在 tamper 完成后读取，意外 payload 不得等于 corrupted bytes且不得是 cacheHit。
5. 删除重复 replacement 测试，改为 fresh insert `.insert` fault：0 entry、恰一完整 final orphan、0 staging，maintenance 清理且第二次为 0；明确这是外部事实 partial GREEN，Phase B 才把 seam 放到事务内 DML 后。
6. helper 名必须准确说明只比较 catalog/source/Tag/Job/scope；各测试单独显式断言 cache entries/tree 的不变或允许变化。
7. 新增合法 staging orphan maintenance 清理及二次幂等测试，只用现有 test-only 临时 cache root。
8. cleanup failure fixture保持最小且确定：可在 `.stagingSync` fault 回调中只枚举当前 test-only version root 的唯一 staging regular file，对其 `chflags(UF_IMMUTABLE)` 后返回 true；defer 必须清 flag并删除临时根。测试确认对外仍是原始 `derivedCachePersistenceFailed` 且 cleanup 失败时 staging 实际保留，恢复 flag 后清理。若本机/APFS 上该方式不能确定性让 unlink 失败，明确报告并暂时省略这一项，不得循环尝试或空转；其余七项必须完成。

完成后用 `set -o pipefail` 只运行 DerivedImageFaultTests，输出到全新 `/tmp/fault-phase-a-red-review2.xcresult`，用 xcresulttool报告准确 total/passed/failed及失败名和 fault call count。至少 stagingWrite、replacement rollback、invalid candidate、oldObjectDelete 应保持真实行为 RED；fresh insert为明确 partial GREEN。运行 git diff --check。不得修改 production、不得运行89项、不得build/commit；报告后立即停止等待Codex复审。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1z.cursor.stream.jsonl
```

## 40. Phase A RED 独立复审与 orphan 合同修正

### 40.1 第二十四次续接结果

第 39.2 节实际恢复 session `cbb558ad-68b9-4286-8d16-54078021746b`，
`system/init.model = Composer 2.5 Fast`，精确开工 HEAD 为
`87c3df961cdb50bdf2e2c95787e3af99f3878afe`。Cursor 只修改两份授权测试文件，恢复测试目标编译，
完成 path-free source tree、no-follow cache tree、实际 service scope、fresh insert partial GREEN、cleanup
失败和合法 staging orphan maintenance 等测试支持。cleanup fixture 最终通过
`URLResourceValues.isUserImmutable` 在本机确定性保留 staging，恢复后能清理。

Cursor `/tmp/fault-phase-a-red-review2.xcresult` 与 Codex 独立
`/tmp/codex-fault-phase-a-review.xcresult` 均为 15 total、11 passed、4 failed。四个稳定 RED 为：

- `.stagingWrite` seam 看不到非零部分 staging；
- invalid candidate rebuild failure 后旧 entry 被提前删除；
- replacement `.insert` failure 后旧 entry 被提前删除；
- invalid-candidate 正常 replacement 不触发 `.oldObjectDelete`。

cleanup failure、合法 staging orphan maintenance、fresh insert 外部 rollback、pre-final faults、final 已发布
orphan、当前前置 LRU fault、revalidation fault 与真实文件库重开均通过。两次结果均无 skip，
`git diff --check` 通过；本次没有 production 修改、aggregate、build 或 commit。Cursor 输出结果后持续空等，
Codex Ctrl-C 结束进程，不影响已有 xcresult。

### 40.2 Codex 复审阻塞

Phase A 还需三个窄修正，不能直接进入生产：

1. 两个 invalid-candidate `.insert` failure 用例在正确事务实现下都会在 DB rollback 后留下一个完整、
   未引用的新 final orphan；它们当前调用 `assertCacheTreeSnapshotsEqual(before, after)`，错误要求 cache
   tree 完全不变，与 handoff 12.5“最多一个完整 orphan”冲突。
2. 上述用例必须分别证明：旧被篡改 object 的 relative component、size、hash 精确保持；旧 entry 全列保持；
   0 staging / 0 unknown；恰多一个不同 relative component 的完整 object，size/hash 与本次确定性合法重编码
   artifact 一致；数据库中没有指向该新 object 的 entry。不得只断言 object count。
3. final revalidation 用例当前断言 `.insert` 被探测 1 次，这是旧的事务外顺序。正确顺序应为：事务内实际
   revalidate 成功后触发 `.revalidation`，该故障立即回滚/退出，尚未执行或探测 `.insert`，所以当前测试
   应期待 `.insert` call count 为 0；在旧 production 上这会形成新增 ordering RED。

### 40.3 第二十五次续接：只收敛最终 RED

仍是同一 Phase A，仅允许修两份测试文件。调用时替换 `<REVIEW_HEAD>`：

```bash
REVIEW_HEAD="$(git rev-parse HEAD)"
PROMPT="$(cat <<'CURSOR_PROMPT'
继续同一份阶段 1 / 切片 4 fault matrix Phase A；恢复 session cbb558ad-68b9-4286-8d16-54078021746b，不是新任务。当前必须是本地 main@<REVIEW_HEAD>。先读 AGENTS.md 与 docs/cursor-cli-tasks/2026-07-15-stage-1-slice-4.md 第 40 节。保留全部切片 4 草稿与 ?? user/；不得 reset/restore/checkout/stash/clean，不得读改/stage/commit user/，不得访问 /Volumes/HDD2 或真实容器，不 push/amend，不用子代理/MCP。system/init.model 必须为 Composer 2.5 Fast。

此次只能修改 ImageAllTests/DerivedImage/DerivedImageFaultTests.swift 与必要的最小 ImageAllTests/DerivedImage/DerivedImageTestSupport.swift；禁止 production、其他测试、pbxproj、docs、build、aggregate、commit。不要重写现有快照框架。

只做三项：
1. 在 testInvalidCandidateReplacementRollbackPreservesOldEntryColumnsAndOldObject 与 testInvalidCandidateNeverReturnedAsPayloadAndEntryPreservedOnRebuildFailure 中移除错误的 cache-tree 完全相等断言。对每个用例保存 tamper 后的旧 object tree item，并在失败后证明旧 entry 全列不变、旧 object relative component/size/hash 精确不变、0 staging、0 unknown、恰多一个不同 component 的完整 object；新 object size/hash 必须等于 baseline first.encodedBytes 的 count/SHA256，且 entriesAfter 不含对应新 object ID。可加一个很小的 test helper解析合法 object relative component/UUID，或直接在用例中比较；不得只比较 count。允许的新 orphan正是 final publish 后 DB 事务回滚的合同结果。
2. testFinalRevalidationRepositoryFaultTriggersOnceAndLeavesOrphanWithoutEntry 改为断言 `.revalidation == 1` 且 `.insert == 0`，说明 revalidation fault 必须发生在实际 revalidate 后、任何 insert DML/探测前；旧 production 应由 `.insert == 1` 形成 ordering RED。
3. 保留其余测试与四个既有 RED，不得放宽 stagingWrite、old row/object 或 oldObjectDelete 预期。

用 `set -o pipefail` 只运行 DerivedImageFaultTests 到全新 `/tmp/fault-phase-a-final-red.xcresult`，用 xcresulttool 报准确 total/passed/failed和失败名；预期 15 total、10 passed、5 failed（原四项加 revalidation ordering）。运行 git diff --check，随后立即停止，不改 production、不commit。
CURSOR_PROMPT
)"
PROMPT="${PROMPT//<REVIEW_HEAD>/$REVIEW_HEAD}"
agent -p --resume cbb558ad-68b9-4286-8d16-54078021746b \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  "$PROMPT" | tee /tmp/ImageAll-stage1-slice4-revision1aa.cursor.stream.jsonl
```
