# Cursor CLI 任务记录：阶段 0 / 切片 6

## 任务元数据

| 字段 | 值 |
|---|---|
| Task ID | `stage-0-slice-6` |
| 状态 | `accepted` |
| 权威 handoff | [`../CURSOR-STAGE-0-SLICE-6-HANDOFF.md`](../CURSOR-STAGE-0-SLICE-6-HANDOFF.md) |
| 上一批准实现 | `main@562f77885957a46d3f74c978785d8535356bf484` |
| 实际开工 HEAD | `658698b9693ee15895acfdccdf174e9a84773bde` |
| Cursor session | `2f994f17-ec69-4b2d-a9cc-c8652ace0713` |
| 实际模型 | `system/init.model = Composer 2.5 Fast` |
| 首轮交付 commit | `56425f4416497d78dffb79a43d539288ec653e85` |
| 返修 1 交付 commit | `a629279315e76fbcf3b5858af9bb9dbc65756e27` |
| 返修 2 交付 commit | `7984ff9aad05011120eeb9dd1591c9e98e0775ec` |
| 返修 3 交付 commit | `892f4e29e1ebf492c1540c5a29d9c54abc05a78f` |
| Codex 评审 | 通过；切片 6 与阶段 0 关闭，Cursor session 已退役 |

## 调用规则

- 这是切片 6 的新任务，必须启动全新 Cursor CLI session；禁止 `--resume`；
- 调用前 `git status --short --branch` 必须为干净的本地 `main`；
- `<LAUNCH_HEAD>` 必须替换为调用前 `git rev-parse HEAD` 的完整 SHA；
- 只允许模型 `composer-2.5-fast`；看到 `system/init.model` 不是 `Composer 2.5 Fast` 时立即停止；
- 不访问 `/Volumes/HDD2`，不 push，不修改历史。

## 完整 CLI 命令模板

```bash
agent -p --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下节完整任务正文；将其中 <LAUNCH_HEAD> 替换为实际 SHA>'
```

不使用 `--resume`。实际执行的 shell quoting 可以等价调整，但任务正文不得删减；实际 SHA、模型与 session ID 在交付后补记。

## 完整任务正文

```text
你是 ImageAll 的实现开发者 Cursor。本次是“阶段 0 / 切片 6——安全启动集成”的全新独立任务，不得恢复任何旧 session，不得使用 Cursor 子代理或 MCP 代做。

开工事实：
- 工作区：/Volumes/SSD1/ImageAll
- 分支：main
- 精确开工 HEAD：<LAUNCH_HEAD>
- 上一批准实现：562f77885957a46d3f74c978785d8535356bf484（阶段 0 切片 5 已由 Codex 通过）
- 本任务包含其后的 Codex 协作规则、切片 6 handoff 与任务记录提交；这不是实现代码。
- 既有独立验收基线：224 tests passed，Debug build passed。

开始前必须：
1. 读取根 AGENTS.md、.cursor/rules/codex-review-handoff.mdc、docs/STAGE-0-IMPLEMENTATION-SPEC.md、docs/CURSOR-STAGE-0-SLICE-6-HANDOFF.md、docs/LOCAL-TEST-DATA-SAFETY.md 和 docs/cursor-cli-tasks/2026-07-14-stage-0-slice-6.md。
2. 打印并核对 git rev-parse HEAD 恰好为 <LAUNCH_HEAD>，分支为 main，工作区干净；有来源不明改动立即停止报告，禁止 reset/checkout/stash/clean。
3. 在 stream-json 的 system/init 中核对模型显示为 Composer 2.5 Fast；否则停止。

实施任务：
- 严格按 docs/CURSOR-STAGE-0-SLICE-6-HANDOFF.md 以 TDD 实现 AppPaths、真实 advisory process lock、安全数据库 bootstrap/migration、Job crash recovery readiness gate、可观察启动状态及 Composition Root/SwiftUI 接线。
- 先写每簇失败测试，再写最少实现；保留全部既有测试。
- 新库和旧 schema 的 migration 只能在同卷候选/工作副本，正式数据库路径不得原地迁移。
- CatalogReady 只能在正式库最终打开并 recoverInterruptedRunningJobs 成功后发布。
- foundationReady 的既有语义和值不得改变；当前不实现 scheduler loop 或任何真实 handler。
- blocking filesystem/database/snapshot/migration/recovery 工作不得运行在 MainActor。
- 自动化测试只使用每测试独立临时目录；手工 smoke 只允许非破坏地使用 App 自己的 sandbox container。

绝对禁止：
- 不得读取、列举、遍历或修改 /Volumes/HDD2，包括 Photos Library.photoslibrary 和年份目录；
- 不得删除或重置已有 App container 数据；
- 不得改 v001_create_catalog_core，不得新增第三方依赖、target、scheme、entitlement；
- 不得进入阶段 1，不得实现文件夹/PhotoKit/扫描/缩略图/hash/OCR/embedding/模型/产品标签 UI；
- 不得修改 Codex handoff 或 docs/cursor-cli-tasks 记录；
- 不得 push、amend、squash、reset、clean 或改写历史。

验收：
- 运行 handoff 第 11 节全部命令；提供准确测试总数和 Debug build 结果。
- 依 handoff 第 9 节做非破坏 smoke；受权限限制时用允许的组合证据，不伪造。
- 创建一个窄范围本地实现 commit。必须用 Cursor Agent <cursoragent@cursor.com>，主题 feat(cursor): integrate safe catalog startup，trailer Agent-Role: implementation；不得包含 Co-authored-by。
- commit 后工作区必须干净，不 push。
- 最终按 .cursor/rules/codex-review-handoff.mdc 与 handoff 第 12 节输出完整中文 Codex 复审材料，并原样给出 author/subject/trailer 命令结果。

完成切片 6 后停止，等待 Codex 独立复审，不得进入阶段 1。
```

## 禁止事项与停止位置

以 handoff 第 1、2、9、10 节和上述任务正文为准。特别强调：`/Volumes/HDD2` 零访问；App container 只允许非破坏 smoke；任务结束停在阶段 0 切片 6。

## 执行与评审结果

### 首轮实际调用

```bash
START_HEAD=$(git rev-parse HEAD)
TASK=$(sed -n '/^```text$/,/^```$/p' docs/cursor-cli-tasks/2026-07-14-stage-0-slice-6.md | sed '1d;$d' | sed "s/<LAUNCH_HEAD>/$START_HEAD/g")
agent -p --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll "$TASK"
```

- `system/init.model`：`Composer 2.5 Fast`；
- session：`2f994f17-ec69-4b2d-a9cc-c8652ace0713`；
- 开工 HEAD：`658698b9693ee15895acfdccdf174e9a84773bde`；
- 首轮交付：`56425f4416497d78dffb79a43d539288ec653e85`，作者 `Cursor Agent <cursoragent@cursor.com>`，主题 `feat(cursor): integrate safe catalog startup`，trailer `Agent-Role: implementation`；
- Cursor 报告 247 tests passed、Debug build passed；Codex 独立重跑同样得到 247 passed、0 failed、Debug build passed；
- 工作区干净、未 push、受保护真实数据零访问。

### Codex 首轮评审结论

未通过，阻塞项如下：

1. 旧 schema 路径复制实现了替换流程，但替换后校验失败只抛错，没有复用切片 5 的自动回滚与 retained-backup 校验；不满足安全替换契约。
2. 容量公式实现为 `max(2 * footprint, 1 MiB) + 64 MiB`，规格要求 `2 * max(footprint, 1 MiB) + 64 MiB`。
3. 并发测试由测试自己把 coordinator 放到后台 queue，未证明生产 `CatalogStartupModel` 的 bootstrap I/O 确实离开主线程。
4. 生产 value types/dependency bag 广泛使用 `@unchecked Sendable`，缺少逐项同步证明；只允许保留在具有明确同步不变量的最小引用类型上。
5. `openCurrentSchema` 在验证抛错时没有显式关闭已创建 pool；`flock` 错误分支在保存 `errno` 前关闭 fd。两者都需要收紧资源时序。
6. handoff 要求的 snapshot、工作副本 migration、替换后校验/回滚、final open 等失败反例覆盖不足。

## 返修 1

这是同一交接单尚未通过验收的窄范围返修，允许且只允许恢复上述 session。返修开始前的实际 HEAD 由调用时把 `<CORRECTION_HEAD>` 替换为精确 SHA。

### 返修 CLI 命令模板

```bash
agent -p --resume 2f994f17-ec69-4b2d-a9cc-c8652ace0713 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下节完整返修任务正文；将 <CORRECTION_HEAD> 替换为实际 SHA>'
```

### 完整返修任务正文

```text
继续同一 ImageAll 阶段 0 / 切片 6 交接单的第一次窄范围返修。Codex 尚未通过首轮实现；不得进入新任务或阶段 1。

开工事实：
- 工作区 /Volumes/SSD1/ImageAll，分支 main。
- 当前精确 HEAD：<CORRECTION_HEAD>。它应包含 Cursor 首轮实现 56425f4416497d78dffb79a43d539288ec653e85 和其后的 Codex 评审记录，只新增文档事实，不是实现变更。
- 必须恢复 session 2f994f17-ec69-4b2d-a9cc-c8652ace0713，并继续显式使用 composer-2.5-fast；stream-json 必须再次证明 system/init.model = Composer 2.5 Fast。
- 开工前核对 HEAD、main、工作区干净；不符合即停止，不得 reset/checkout/stash/clean。
- 重新读取 AGENTS.md、.cursor/rules/codex-review-handoff.mdc、docs/CURSOR-STAGE-0-SLICE-6-HANDOFF.md 和本任务记录的“返修 1”。

仅修复以下 Codex 阻塞项，先补失败测试再做最少实现：

1. 旧 schema 安全替换：不要在 CatalogBootstrapCoordinator 复制一套弱化的 replacement。复用切片 5 的 CatalogDatabaseRestoreCoordinator（或抽取并复用同一套既有 retained-backup、post-replace validation、automatic rollback primitive），使旧库流程严格为：pre-migration snapshot → snapshot 工作副本 migration → retained original backup → post-replace validation；校验失败必须自动回滚到原事实，回滚结果按既有安全错误分类。补真实文件测试证明 post-replace validator 失败时原库 migration history/事实恢复、不会 ready；补回滚失败进入安全不可用而不是继续 final open。不要修改 v001。

2. 容量公式：实现并测试 `2 * max(sourceFootprint, 1 MiB) + 64 MiB`，覆盖 footprint=0、恰好 1 MiB、超过 1 MiB、恰好足够、少 1 byte 和溢出。算术顺序必须与公式一致。

3. Swift 6 并发边界：移除生产 value types 和 CatalogBootstrapDependencies/CatalogRuntimeToken/路径解析器/容量组件上无同步证明的 `@unchecked Sendable`。只可在确有 NSLock 等同步不变量的最小引用类型保留，并在代码旁简述安全理由；不要用 unchecked 让整个依赖包绕过检查。保持 Swift 6 clean build。

4. 资源时序：CatalogDatabase.openCurrentSchema 在创建 pool 后任何验证失败都必须在抛出前关闭 pool；补测试。DarwinCatalogProcessLock 必须在 close(fd) 前保存 flock 的 errno，并据保存值区分 contention/I/O。bootstrap 失败必须遵守先关闭已打开数据库、后释放 lock。

5. 测试矩阵缺口：补最小、真实文件或窄 seam 反例，至少覆盖 snapshot failure、工作副本 migration failure、initial replacement failure、post-replace validation failure + successful rollback、rollback failure、final current-schema open failure、recovery failure；每项证明不发布 ready、正式原事实按契约保留、锁/连接按契约处理。新库候选失败不得留下正式空库。不要为测试引入大而泛化的生产抽象。

6. 生产并发证据：改写/补充测试，通过 CatalogStartupModel 的真实 startBootstrap 路径注入线程探针并等待展示结果，证明路径/数据库/migration/recovery 的阻塞入口不是 main thread。测试自己在 DispatchQueue.global 直接调用 coordinator 不能作为唯一证据。

7. 保持首轮正确边界：foundationReady 语义不变；未拿锁不得 inspect/open formal DB；CatalogReady 仍严格晚于 final open + recovery；不启动 scheduler；不改依赖、target、scheme、entitlement、v001；不访问 /Volumes/HDD2；不删除或重置 App container；不修改 Codex 文档或任务记录；不 push、不 amend、不改历史。

验证并交付：
- 运行完整 Debug tests 和 Debug build，报告准确总数；运行 git diff --check。
- 创建新的窄范围本地 commit，不 amend 首轮提交。作者 Cursor Agent <cursoragent@cursor.com>，主题 fix(cursor): address slice 6 startup review blockers，trailer Agent-Role: implementation，不含 Co-authored-by。
- commit 后工作区干净并停止，输出完整中文 Codex 复审材料，逐项对应以上 7 点以及精确 author/subject/trailer。
```

### 返修 1 实际调用与结果

```bash
CORRECTION_HEAD=$(git rev-parse HEAD)
TASK=$(awk '/^### 完整返修任务正文$/{found=1;next} found && /^```text$/{inblock=1;next} inblock && /^```$/{exit} inblock{print}' docs/cursor-cli-tasks/2026-07-14-stage-0-slice-6.md | sed "s/<CORRECTION_HEAD>/$CORRECTION_HEAD/g")
agent -p --resume 2f994f17-ec69-4b2d-a9cc-c8652ace0713 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll "$TASK"
```

- `system/init.model`：`Composer 2.5 Fast`；
- session：`2f994f17-ec69-4b2d-a9cc-c8652ace0713`；
- 返修开工 HEAD：`a02ea1a05bdb295ed8e1b0560a84e62ee3fcaca7`；
- 返修 1 交付：`a629279315e76fbcf3b5858af9bb9dbc65756e27`，作者 `Cursor Agent <cursoragent@cursor.com>`，主题 `fix(cursor): address slice 6 startup review blockers`，trailer `Agent-Role: implementation`；
- Cursor 报告 263 tests passed、Debug build passed、工作区干净、未 push。

### Codex 返修 1 评审结论

未通过，仍有三个安全阻塞项：

1. `migrateOldSchemaDatabase` 在 snapshot 后直接调用 `removeSidecarsIfPresent` 删除正式库 WAL/SHM；这可能删除尚未 checkpoint 的持久事实，违反 handoff 第 6.2 节以及阶段规格第 4.6、7.2 节的替换纪律。
2. “工作副本 migration 失败”测试只在 migration 前通过新增 hook 人工抛出错误，没有让真实 GRDB migration 在工作副本失败；为此还修改了已通过的切片 5 restore primitive，证据不足且扩大生产 seam。
3. recovery 失败时 `pool.close()` 异常被吞掉，随后仍释放 lock；不能证明“全部数据库连接成功关闭后才释放锁”。

另外，返修测试只用空库 migration history 代表“原事实”；返修 2 应加入一个旧 schema sentinel 事实，证明 migration/replacement/rollback 前后真正的数据事实未丢失。

## 返修 2

这是同一交接单尚未通过验收的第二次窄范围返修，继续恢复同一 session。调用时把 `<CORRECTION_HEAD>` 替换为精确 HEAD。

### 返修 2 CLI 命令模板

```bash
agent -p --resume 2f994f17-ec69-4b2d-a9cc-c8652ace0713 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下节完整返修 2 任务正文；将 <CORRECTION_HEAD> 替换为实际 SHA>'
```

### 完整返修 2 任务正文

```text
继续同一 ImageAll 阶段 0 / 切片 6 交接单的第二次窄范围返修。返修 1 仍未通过 Codex 安全验收；不得进入新任务或阶段 1。

开工事实：
- 工作区 /Volumes/SSD1/ImageAll，分支 main，当前精确 HEAD <CORRECTION_HEAD>。
- HEAD 应包含 Cursor 返修 1 实现 a629279315e76fbcf3b5858af9bb9dbc65756e27 和其后的 Codex 返修 2 评审记录；Codex 提交只含文档，不是实现。
- 必须恢复 session 2f994f17-ec69-4b2d-a9cc-c8652ace0713，并显式使用 composer-2.5-fast；stream-json 再次证明 system/init.model = Composer 2.5 Fast。
- 开工前核对 HEAD、main、工作区干净；不符合就停止。重新读取 AGENTS.md、切片 6 handoff、本任务记录“返修 2”和切片 5 handoff 第 7.2 节。

仅处理以下阻塞项，先补会失败的测试，再做最小实现：

1. 正式旧库的 WAL/SHM 绝不能被盲删。删除 CatalogBootstrapCoordinator 中 snapshot 后直接 `removeSidecarsIfPresent(formalURL)` 的路径。snapshot 验证并关闭只读 source 后，在仍持有进程 lock 的前提下，以“不运行 migration”的可写连接打开正式旧库，复用已通过的 `checkpointAndCloseForReplacement()`（或严格等价的既有 primitive），确保 TRUNCATE checkpoint 成功、所有该管线连接关闭、journal 收敛和 sidecar 前置条件成立后，才调用 restore coordinator。checkpoint/close/convergence 任一失败必须在 replacement 前停止、不 ready、不删除未合并 WAL。不得原地运行 migration。

2. 增加旧 schema 的真实 sentinel 事实（例如测试专用 legacy 表中的一行，不修改 v001），覆盖：正常旧库升级后事实仍存在；initial replacement 失败后原事实仍存在；post-replace validation 失败且自动回滚后原事实仍存在。至少一个集成测试要验证启动管线确实调用了安全 checkpoint/close 前置，而不是靠直接删 sidecar 满足 restore precondition。所有 fixture 都在每测试临时目录。

3. 用真实 migration 引擎制造“工作副本 migration failure”：在旧 schema 临时库创建会与 v001 DDL 冲突的 legacy 结构，使 snapshot 工作副本上的 GRDB migration 确实失败；断言正式库 sentinel 事实和 migration history 未变、不 ready、不进入 replacement/final open。移除返修 1 为伪造此失败而加入的 `beforeWorkCopyPreparationHook` / `restoreBeforeWorkCopyHook` 及其对切片 5 restore primitive、dependency bag 和测试支持的修改，除非能证明它还服务于其他既有已批准契约。

4. recovery 失败清理必须严格先成功关闭数据库、后释放 process lock；禁止吞掉 close error 后照常 release。为这一顺序建立窄测试 seam/记录器即可，不要做通用资源框架。默认生产路径仍调用真实 `DatabasePool.close()`。如果 close 意外失败，选择 fail-safe：不得显式释放 lock，也不得让 token 的 deinit 隐式释放；锁可保持到进程退出并返回安全不可用。`CatalogProcessLockToken` 的生命周期要与 handoff“显式释放或进程退出”一致，并补测试证明 close 失败时 release handler 未被调用、close 成功时才调用。

5. 保留返修 1 已正确的容量公式、openCurrentSchema 关闭、errno、生产 CatalogStartupModel 非主线程证据和 263 项回归。不要修改 v001、依赖、target、scheme、entitlement、foundationReady 或 CatalogReady 顺序；不启动 scheduler；不访问 /Volumes/HDD2；不删除/reset App container；不改 Codex 文档/任务记录；不 push、不 amend、不改写历史。

验证并交付：
- 运行完整 Debug tests、Debug build、git diff --check，报告准确测试总数。
- 创建新的窄范围本地 commit，不 amend。作者 Cursor Agent <cursoragent@cursor.com>；主题 fix(cursor): close slice 6 safety gaps；trailer Agent-Role: implementation；不含 Co-authored-by。
- commit 后工作区干净，输出中文复审材料，逐项列出 WAL checkpoint/close/sidecar 顺序、真实 migration failure、sentinel 事实、recovery close/release 次序及精确提交归属，然后停止。
```

### 返修 2 实际调用与结果

```bash
CORRECTION_HEAD=$(git rev-parse HEAD)
TASK=$(awk '/^### 完整返修 2 任务正文$/{found=1;next} found && /^```text$/{inblock=1;next} inblock && /^```$/{exit} inblock{print}' docs/cursor-cli-tasks/2026-07-14-stage-0-slice-6.md | sed "s/<CORRECTION_HEAD>/$CORRECTION_HEAD/g")
agent -p --resume 2f994f17-ec69-4b2d-a9cc-c8652ace0713 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll "$TASK"
```

- `system/init.model`：`Composer 2.5 Fast`；
- session：`2f994f17-ec69-4b2d-a9cc-c8652ace0713`；
- 返修开工 HEAD：`aff3bdc2ea445db0c36e9be700596cbf9d16cebf`；
- 返修 2 交付：`7984ff9aad05011120eeb9dd1591c9e98e0775ec`，作者 `Cursor Agent <cursoragent@cursor.com>`，主题 `fix(cursor): close slice 6 safety gaps`，trailer `Agent-Role: implementation`；
- Cursor 报告 266 tests passed、Debug build passed、工作区干净、未 push；Codex 使用 `.xcresult` 独立汇总得到 267 passed、0 failed，Debug build passed。

### Codex 返修 2 评审结论

未通过，剩余两类可验证缺口：

1. 正式旧库 snapshot 已完成后，`checkpointAndCloseFormalDatabase` 抛出的 `.closeFailed` 仍被通用 `mapMigrationFailure` 归类为 `.snapshotFailed`；这与实际阶段不符，也与 Cursor 交付所称 checkpoint/close/convergence 均映射为 `.publicationFailed` 不一致。bootstrap 层没有 checkpoint、close、convergence 失败的反例证明 replacement/final open 未发生、正式 sentinel 未变。
2. 真实工作副本 migration failure 测试证明了正式 sentinel 和 migration history 未变，但没有按返修 2 正文直接断言 `replacementCallCount == 0` 与 final current-schema open 次数为 0。

## 返修 3

这是同一交接单尚未通过验收的第三次窄范围返修，继续恢复同一 session。调用时把 `<CORRECTION_HEAD>` 替换为精确 HEAD。

### 返修 3 CLI 命令模板

```bash
agent -p --resume 2f994f17-ec69-4b2d-a9cc-c8652ace0713 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll \
  '<下节完整返修 3 任务正文；将 <CORRECTION_HEAD> 替换为实际 SHA>'
```

### 完整返修 3 任务正文

```text
继续同一 ImageAll 阶段 0 / 切片 6 交接单的第三次窄范围返修。返修 2 仍未通过 Codex 最终验收；不得进入新任务或阶段 1。

开工事实：
- 工作区 /Volumes/SSD1/ImageAll，分支 main，当前精确 HEAD <CORRECTION_HEAD>。
- HEAD 应包含 Cursor 返修 2 实现 7984ff9aad05011120eeb9dd1591c9e98e0775ec 和其后的 Codex 返修 3 评审记录；Codex 提交只含文档，不是实现。
- 必须恢复 session 2f994f17-ec69-4b2d-a9cc-c8652ace0713，并显式使用 composer-2.5-fast；stream-json 再次证明 system/init.model = Composer 2.5 Fast。
- 开工前核对 HEAD、main、工作区干净；不符合就停止。重新读取 AGENTS.md、切片 6 handoff、本任务记录“返修 3”。

只修复以下剩余验收缺口，先补失败测试再做最少实现：

1. 为正式旧库 snapshot 完成后的 checkpoint/close/convergence 准备建立正确的 bootstrap 失败语义。`checkpointAndCloseFormalDatabase` 抛出 `.checkpointFailed`、`.closeFailed` 或 `.sidecarConvergenceFailed` 时，都属于 replacement/publication 前置失败，必须返回 `.unavailable(.publicationFailed)`，不得误报 `.snapshotFailed`。不要改变 snapshot 创建阶段自身 `.closeFailed -> .snapshotFailed` 的语义；用最窄的阶段上下文映射解决，不要扩大通用错误框架。

2. 增加 bootstrap 层参数化或等价的最小反例，至少覆盖 `.checkpointFailed`、`.closeFailed`、`.sidecarConvergenceFailed`。每个反例必须直接证明：不 ready；正式库 sentinel 和 migration history 未变；replacement 调用次数为 0；final current-schema open 次数为 0。使用现有临时文件 fixture、`FaultInjectingCatalogDatabaseFileReplacer.replacementCallCount` 和窄线程安全 counter；不得访问真实数据。

3. 收紧 `testWorkCopyMigrationFailureDoesNotBecomeReady`：保留真实冲突 `source` 表触发 GRDB migration 失败，并直接断言 replacement 调用次数为 0、final current-schema open 次数为 0；不能以 sentinel/history 未变替代这两个调用边界断言。

4. 保持返修 1–2 已正确的 WAL checkpoint/close 顺序、sentinel 三路径、真实 migration failure、recovery close/release fail-safe、容量公式、资源关闭、errno 与生产非主线程证据。不要修改 v001、依赖、target、scheme、entitlement、foundationReady、CatalogReady 顺序、Codex 文档或任务记录；不启动 scheduler；不访问 /Volumes/HDD2；不删除/reset App container；不 push、不 amend、不改写历史。

验证并交付：
- 运行完整 Debug tests、Debug build、git diff --check。测试总数必须以 `xcrun xcresulttool get test-results summary` 或等价的 `.xcresult` 权威汇总为准，不再用日志行数估算。
- 创建新的窄范围本地 commit，不 amend。作者 Cursor Agent <cursoragent@cursor.com>；主题 `fix(cursor): complete slice 6 failure evidence`；trailer `Agent-Role: implementation`；不含 Co-authored-by。
- commit 后工作区干净，输出中文复审材料，逐项对应以上缺口并停止，等待 Codex 最终验收。
```

### 返修 3 实际调用与结果

```bash
CORRECTION_HEAD=$(git rev-parse HEAD)
TASK=$(awk '/^### 完整返修 3 任务正文$/{found=1;next} found && /^```text$/{inblock=1;next} inblock && /^```$/{exit} inblock{print}' docs/cursor-cli-tasks/2026-07-14-stage-0-slice-6.md | sed "s/<CORRECTION_HEAD>/$CORRECTION_HEAD/g")
agent -p --resume 2f994f17-ec69-4b2d-a9cc-c8652ace0713 \
  --model composer-2.5-fast --force --sandbox disabled --trust \
  --output-format stream-json --workspace /Volumes/SSD1/ImageAll "$TASK"
```

- `system/init.model`：`Composer 2.5 Fast`；
- session：`2f994f17-ec69-4b2d-a9cc-c8652ace0713`；
- 返修开工 HEAD：`c72f8733c38460dfa9d7bdd12121f655a2fa630a`；
- 返修 3 交付：`892f4e29e1ebf492c1540c5a29d9c54abc05a78f`，作者 `Cursor Agent <cursoragent@cursor.com>`，主题 `fix(cursor): complete slice 6 failure evidence`，trailer `Agent-Role: implementation`；
- Cursor 的 `.xcresult` 汇总为 268 passed、0 failed，Debug build passed、工作区干净、未 push。

### Codex 最终独立验收

- 审查 `c72f873..892f4e2`：只修改 bootstrap 阶段错误映射与对应测试；提交作者/trailer 正确；
- post-snapshot checkpoint 的 `.checkpointFailed`、`.closeFailed`、`.sidecarConvergenceFailed` 均归类为 `.publicationFailed`，snapshot 创建阶段 `.closeFailed -> .snapshotFailed` 未变；
- 三个 checkpoint 失败分支和真实 work-copy migration failure 均直接证明 `replacementCallCount == 0`、final open count 为 0、正式 sentinel 与 migration history 未变；
- Codex 独立 `.xcresult`：268 passed、0 failed、0 skipped；Debug build passed；`git diff --check` 无输出；
- 双实例非破坏 smoke：首实例 `catalogReady`，第二实例 `anotherInstanceRunning`，第一实例保持存活；容器只出现阶段 0 规定的 Catalog、Backups、Runtime 布局；
- 未改 v001、依赖、target、scheme、entitlement，未进入阶段 1，未访问 `/Volumes/HDD2`，未清理 App container，未 push。

结论：切片 6 通过，阶段 0 完成。Cursor session `2f994f17-ec69-4b2d-a9cc-c8652ace0713` 自此退役，任何后续阶段必须创建新的任务记录和全新 Cursor CLI session。完整汇总见 [阶段 0 验收证据](../STAGE-0-EVIDENCE.md)。
