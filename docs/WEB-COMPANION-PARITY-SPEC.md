# Web Companion 与 Mac 端功能对齐规格

> 状态：实施中
> 更新：2026-08-10
> 决策来源：`docs/ADR-048-MAC-HOST-WEB-COMPANION.md`

## 目标

让用户在网页端完成 ImageAll 日常工作时，感觉仍在使用 Mac 端：信息层级一致、操作语义
一致、状态不会因后台刷新丢失，且 Host 是唯一数据与写操作权威。

“对齐”不等于在浏览器复制 AppKit / SwiftUI 控件。网页应复用 Mac 端的任务名称、决定
语义、快捷键和视觉层级，并为触控和窄屏提供等价操作。

## 不可退让的交互契约

1. 普通点击选择单项；Command 点击增减选择；Shift 点击连续选择；Command-A 全选当前
   已载入结果；空白区域拖动可框选。
2. 方向键按网格移动，Page Up / Page Down 按可见页移动，Space 打开或关闭预览。
3. 照片与视频是两个独立工作会话，分别保留来源、筛选、搜索、排序、分页、选择和滚动
   位置。
4. 来源、筛选、搜索或排序改变时才重置分页。Host 事件、账号轮询、手动刷新和标签写入
   必须保留已载入窗口与滚动上下文。
5. 审核快捷键与 Mac 一致：P 确认、X 拒绝、U 稍后。U 只把当前项移到本次队列后面，
   不清除或写入标签决定。
6. 单选检查器显示单张资产详情；多选检查器显示所有活动标签的确认、拒绝、未决定聚合
   状态，并允许直接批量决定。
7. 浏览器只保存非敏感工作区偏好。账号密码、配对令牌和长期认证材料不得写入
   `localStorage`、URL 或日志。

## 能力边界

| 类别 | 网页行为 | 例子 |
| --- | --- | --- |
| Web 直接完成 | 调用 Host 权威 API，网页显示真实结果 | 浏览、筛选、多选、标签、审核、任务控制 |
| Web 发起，Mac 确认 | 网页创建请求，Mac 展示原生授权或风险确认，结果回传网页 | 选择新来源、Photos 授权、受保护空间写入 |
| Mac 保留 | 网页解释限制并指向 Mac，不提供伪入口 | 系统权限授予、Host 网络与账号白名单配置 |

任何写操作都必须遵循现有原子性、幂等、来源能力和照片保护契约；不得为了网页功能绕过
Mac 端校验。

## 分阶段对齐清单

状态含义：`已实现待运行验收` 表示代码与静态契约已落地，但仍需新版 Mac Host 和已登录
浏览器完成端到端验收；`待协议` 表示 Host 还没有足够的远程命令或投影。

| 优先级 | 能力 | 当前状态 | 验收要点 |
| --- | --- | --- | --- |
| P0 | 正式审核路由与 P/X/U 语义 | 已实现，合成浏览器已验收 | P/X 按队列顺序批量持久化；U 不写决定、不重排且前进到下一未选项 |
| P0 | 照片/视频主模式与独立会话 | 完整会话恢复已完成合成验收，待真实运行验收 | 来回切换不丢来源、筛选、排序、选择、检查器、已载入分页窗口和滚动 |
| P0 | 网格密度、侧栏/检查器显隐 | 图库、审核与瘦身共享布局已完成合成验收，待真实运行验收 | 宽屏三栏保持 Mac 层级；审核队列复用 Mac 同一密度和原比例状态，偏好仅本机保存 |
| P0 | 全屏工作区返回与浏览器历史 | 五工作区与双向嵌套已完成合成验收，待真实运行验收 | Back、Forward、显式返回与 Escape 同层级；返回后图库和工作区上下文不变 |
| P0 | 键盘导航、Command/Shift 多选、框选 | 三网格键盘已完成合成验收；图库修饰键点击部分运行验收 | 图库、审核与瘦身使用同一 Mac 选择语义，框选过程无整页重绘 |
| P0 | 标签侧栏、修饰键筛选、上下文菜单与分组折叠 | 已实现，合成浏览器已验收 | 并集/交集/排除进入 Host 查询；标签与非系统分组右键操作复用权威命令；折叠刷新保持 |
| P0 | 高级筛选即时组合操作 | 已实现，合成浏览器已验收 | 与 Mac 一样即时切换标签状态、标签关系、多个可用状态和多个格式组；面板与焦点保持，补齐 JPEG 2000、RAW |
| P0 | 搜索与筛选零结果恢复 | 已实现，合成浏览器已验收 | 像 Mac 空状态一样原位清除搜索、标签、状态/格式或全部条件，保留媒体与来源范围 |
| P0 | 常驻筛选摘要、命令动作与审核全屏 | 已实现，合成浏览器已验收 | 当前范围始终可见；标签/来源动作走 Host；全屏保留红心、P/X/U 与视频播放 |
| P0 | 图库与审核缩略图直接红心 | 已实现，合成浏览器已验收 | 与 Mac 一样悬停/选中可直接切换；触控常驻，不改变选择、预览、滚动、P/X/U 或视频悬停 |
| P0 | 单选/多选检查器、标签搜索与直接整理 | 已实现，合成浏览器已验收 | 与 Mac 同组显示；左键确认、右键清除；标签可直接拖放/键盘排序，多选读取全部活动标签聚合状态 |
| P0 | 单张检查器 AI 建议与直接决定 | 已实现，合成浏览器已验收 | 待审标签及四轨来源与 Mac 同源；属于/不属于写入后重新读取权威详情 |
| P0 | 单图中心画布与常驻检查器 | 已实现，合成浏览器已验收 | 桌面像 Mac 一样只替换中心图库，右侧检查器可继续搜索/决定标签和使用模型；窄屏保持全屏模态 |
| P0 | 图库与审核自动分页、局部事件刷新 | 已实现；图库已运行验收，审核合成浏览器已验收 | 不缩回首屏，不重入，不刷新无关投影；审核末项可见时像 Mac 一样自动续页 |
| P0 | 训练、瘦身、回收、来源与存储操作的应用内确认 | 已实现，合成浏览器已验收 | 不出现浏览器原生确认框；危险等级、说明、取消/确认及焦点恢复与 Mac 同层级；Escape 只关闭最上层确认并保留底层上下文，Host 继续执行最终安全校验 |
| P0 | 全工作区命令面板连续性 | 已实现，合成浏览器已验收 | 图库、审核、训练、瘦身、照片世界、总览和全屏预览均可用 ⌘K；审核与瘦身直接提供上下文全选、预览、红心和核心处理动作；取消恢复原焦点，跨工作区命令复用浏览器历史 |
| P0 | 自适应工具栏与动作连续性 | 已实现，合成浏览器已验收 | 按真实内容和窗口宽度自动收纳；390px 只保留命令、活动、连接与更多，桌面空间恢复后完整展开，动态任务仍可见可取消 |
| P0 | 工具栏持续帮助与图标可发现性 | 已实现，合成浏览器已验收 | 像 Mac `persistentHelp` 一样解释动作范围、快捷键和 Mac 确认边界；悬停与键盘焦点均可达，不抢焦点、不越出视口 |
| P1 | 原始宽高比与视频播放 | 图库既有运行验收；审核真实比例与共享切换已完成合成验收 | 原比例优先读取 Host 已缓存的完整宽高比缩略图，旧 Host 或缓存缺失安全回退；视频可播放和定位 |
| P1 | 视频卡时长与悬停静音预览 | 已实现，合成浏览器已验收 | 复用受保护媒体路由；单播放器，离开/滚动/切换立即释放且不改变选择与滚动 |
| P1 | 标签新增、重命名、归档、分组与双通道撤销 | 已实现，合成浏览器已验收 | 标签与审核各保留最近一步；互不覆盖，重放保持幂等 |
| P1 | 选中项目特征准备与相似项查找 | 已实现，合成浏览器已验收 | 复用 Mac Embedding Cache 与瘦身种子分析；进度可见、可取消、媒体范围由 Host 复核 |
| P1 | 抽取样本并生成个人建议 | 已实现，合成浏览器已验收 | 多选按网格顺序、无选区从全图库抽样；复用 Mac 个人模型并进入同一审核队列 |
| P1 | 按标签生成全库个人建议 | 已实现，合成浏览器已验收 | 标签资格、个人/超级个人阈值、Top N 与来源均由 Host 投影；可取消、可恢复并回流同一审核队列 |
| P1 | 建议阈值、每标签上限与卡片内刷新 | 已实现，合成浏览器已验收 | 与 Mac 同一仓储；三轨独立、可继承/采用参考值，审核卡内可编辑并按生效门槛清理低分待审项 |
| P1 | 图库总览、媒体账本与统计下钻 | 已实现，合成浏览器已验收 | 与 Mac `Library Atlas` 同源；来源/标签/媒体卡可回到真实筛选，刷新不形成循环 |
| P1 | 审核总览、标签/来源统计与队列恢复 | 分组层级、多来源范围与闭环已完成合成验收，待真实运行验收 | 数量与 Mac 同源，按标签组折叠，任意来源组合独立于图库位置，刷新后保留模式与位置 |
| P1 | 审核本地模型服务与全库建议任务 | 已实现，合成浏览器已验收 | 服务状态、标准/个人模型能力、全库进度和暂停/继续/取消均来自 Host；来源范围与审核总览一致 |
| P1 | 活动面板手动刷新与连续操作 | 已实现，合成浏览器已验收 | 与 Mac 同名；只重读任务投影，成功、失败和离线重试均保持任务选择、动作焦点与列表滚动 |
| P1 | 完整检查器元数据与来源动作 | 已运行验收（本机 Chrome） | 文件信息、时间、尺寸、状态与 Mac 一致 |
| P1 | 应用存储、缓存维护与用户数据导出 | 已实现，风险说明与合成浏览器已验收 | 用量与位置状态同源；路径脱敏；两类清理先显示与 Mac 同语义的网页说明，再由 Mac 最终确认；导出与迁移继续使用 Mac 原生选择器 |
| P2 | 训练数据集、配置、运行与进度 | 配置、关联、恢复和长记录导航已完成合成验收，待真实运行验收 | Run/三槽/配置同源；启动、任务控制与恢复均复用 Host 权威状态机，长记录选择和刷新不丢阅读位置 |
| P2 | 图库个人模型快捷入口 | 已实现，合成浏览器已验收 | 与 Mac 同名；已确认标签、当前选区/全库范围自动带入同一训练设置 |
| P2 | 世界地图、地点聚合与照片下钻 | 地图、地点图库、位置回填、地点标签与照片条红心已实现，合成浏览器已验收 | 复用 Mac 本地渲染器、目录聚合和地点解析事务；地点详情可用 Host 精确范围打开完整图库，照片条可直接红心，位置检查、搜索、键盘候选导航与确认均由 Host 执行 |
| P3 | 图库瘦身分析、候选组与回收站 | 直接红心、框选、成员菜单、预览显式删除、共享缩略图布局、来源刷新/索引及失败恢复动作已完成合成验收，待真实运行验收 | 记录、候选组、三种分析范围、阈值、来源维护和任务控制同源；回收失败按 Mac 权威决议分别请求权限、刷新来源、重新检查或返回分析，说明与保护原则由 Host 投影；删除仍复用 Mac 证明、确认与核验 |
| P3 | 图库瘦身独立相似阈值编辑 | 已实现，合成浏览器已验收 | 从“分析选项”直接读取并编辑 Mac 权威阈值；应用或恢复默认不启动分析、不重建当前记录/分组/选区/滚动，极限模式警告与 Mac 一致 |
| P4 | 来源新增/移除、全部更新、批量缓存、批量权限、Host 管理 | 已实现，侧栏直达连接、全部更新、批量缓存、批量权限、前置说明与上下文菜单合成验收 | Web 发起、Mac 原生确认；连接 Photos、重绑图库、完整修复和移除来源先显示与 Mac 同语义的应用内说明，所有入口复用同一确认与焦点恢复；缓存和授权任务复用逐来源进度；缓存进度可从工具栏直接取消；Host 安全设置仍在 Mac |
| P4 | 当前来源立即同步/重扫与持续进度 | 已实现，合成浏览器已验收 | 与网页只读重载严格区分；Photos、文件夹和全部来源分别复用 sync、rescan、refreshAll Host 命令；扫描期间显示来源和计数且只轮询任务投影 |

## 本轮实现范围

本轮修改 Web Companion 静态资源、Remote 协议/Host 路由与对应测试、本规格和 ADR-048；
图库瘦身接入已有分析服务的权威投影、受限命令和可恢复回收站，来源管理接入“Web 发起、
Mac 原生确认”的异步请求；应用存储维护接入脱敏用量投影和 Mac 原生选择/确认；网页不直接访问
文件系统或 Photos Library，所有恢复、协调和永久删除仍由 Mac 既有领域服务执行。

新增的工作区能力包括：

- 照片/视频分段切换和各自会话状态；
- 九档网格密度、侧栏/检查器显隐；
- 标签导航、无标签入口、检查器标签搜索和多选标签聚合；
- 单张与多选检查器按 Host 标签组分段和折叠；标签主体复用 Mac 的左键确认、右键清除语义，
  同时保留显式三态按钮，并在折叠或异步写入后按稳定 ID 恢复焦点；
- 高级筛选中的可用状态与文件格式由单选下拉改为 Mac 式多选列表；多个状态、多个格式组可在同一
  查询中组合，JPEG 2000 与 RAW 使用 Mac 相同 UTI 分组，视频继续显示 MP4 / MOV。筛选徽标按
  已选状态和格式组逐项计数，常驻摘要使用用户可读组名而非底层 UTI；Host 继续接收既有数组参数。
- 高级筛选不再维护等待“应用”的第二份交互状态：状态、格式、标签状态、标签条件及 ALL / ANY
  关系变化后立即更新权威查询，120 ms 内的连续操作合并为最后一次。旧网格在请求期间保持可读，
  结果只在查询签名仍匹配时替换；筛选面板、当前控件焦点和媒体会话保持。“完成”只关闭面板，
  “清除全部”立即生效，状态行原位反馈正在更新、已更新或失败。
- 单张检查器从 Review 仓储投影该资产全部待审 AI 建议和四轨来源，默认显示前 5 条并可展开；
  “属于/不属于”继续复用原子标签决定，成功后由 Host 详情决定哪些建议消失；
- Command/Shift/Command-A、方向键、Page Up / Page Down、Space、空白框选；
- Command-K 命令面板、快捷键说明和资产右键菜单；
- 审核正式 API 路径与 P/X/U；
- Intersection Observer 自动续页、按事件类型合并刷新，以及配对会话的 15 秒一致性兜底轮询；
- 图库与审核队列按稳定 ID 原位协调，手动刷新、后台刷新和写入后刷新都保留已载入窗口。
- 新增与 Mac `GalleryOverviewView` 同源的“图库总览 / Library Atlas”：四项关键指标、照片/
  视频媒体账本、精确摘要覆盖、来源构成、可用状态、正样本标签和最近 16 个年份均由 Host
  `/v1/gallery-overview` 一次性只读投影；来源、标签和媒体卡可下钻回真实图库筛选，不在浏览器
  复制统计逻辑或伪造结果。
- 检查器请求按工作区与请求代次验收；无变化的静默轮询不重载详情，瞬时失败进入合并重试。
- 标签侧栏按 Host 标签组投影分组；管理面板支持标签重命名、移动分组、归档，以及标签组
  新增、重命名和删除，所有写入均使用幂等操作 ID。
- 标签与审核决定成功后分别显示各自最近一步撤销；两个 Host 槽互不覆盖，均只对当前进程中的
  本通道最近一步有效。浏览器在传输失败后重试同一次撤销时复用原操作 ID，不把旧幂等账本迁移成
  不可验证的新格式。
- 审核首页按标签显示待审总数、建议来源构成、已确认/已拒绝样本数和任务状态；照片/视频
  与当前来源范围由 Host 权威查询，从总览进入队列后后台刷新不会覆盖队列摘要。
- 账号模式只拉取进入或接近可视区的缩略图，并把检查器大图请求设为高优先级；点击照片时
  先从当前缩略图生成本地渐进占位，大图完成后原位替换，不再用长时间空白转圈阻塞详情。
- Host 新增受保护的视频媒体路由，完整响应和单段 HTTP Range 均以 256 KiB 有界分块发送，
  返回真实视频 MIME、`Accept-Ranges`、`Content-Range`，并正确拒绝越界或多段 Range。
- 文件夹视频复用来源访问租约和 no-follow 文件描述符；Photos 视频只通过 PhotoKit 请求本机
  原始 AV 资源 URL，每个响应独立持有并在完成、失败或取消后释放资源。
- 检查器使用浏览器原生视频控制器播放、暂停和定位；网格增加“适应/填充”宽高比切换。
- 普通图库视频卡常驻播放标识和时长；细指针停留 180 ms 后用同一受保护媒体路由静音循环播放。
  页面同时只保留一个悬停播放器，离开、滚动离屏、切换媒体、进入单图、窗口失焦或退出即释放；
  触控与减少动态效果环境不自动播放，悬停不改变选择、分页、检查器目标或滚动位置。
  账号 Basic 仅通过不缓存内容的同源 Service Worker 内存桥接到媒体 GET/HEAD，不进入 URL
  或持久存储；Chromium 原生视频请求缺少 `clientId` 时回退询问同源受控窗口，并以同源模式
  重建请求，避免 `no-cors` 剥离 `Authorization`。
- 后台增量刷新若仍是同一视频和同一内容版本，会复用现有 `<video>` 实例，保持播放/暂停和
  定位进度，不因账号轮询重载媒体。
- 单张检查器补齐视频时长与文件大小，和既有来源、相对位置、媒体、格式、尺寸、拍摄时间、
  修改时间、可用状态共同使用 Mac `AssetInspectorDetail` 权威投影；网页不接收绝对路径。
- 检查器新增“在 Mac 上用‘预览’打开原图/用系统播放器打开”动作，受保护的同源 POST 只
  委派 Mac 现有只读原始资产打开器，不提供浏览器下载或绕过来源授权。
- 新增训练工程只读工作台：按照片/视频和方法读取 Mac `TrainingWorkspacePort` 的真实 Run 与
  三种模型槽位，左侧记录、右侧台账详情和移动端上下分栏沿用 Mac 信息结构；任务事件与
  账号轮询保留当前筛选和选中 Run。
- Host 在训练投影边界移除历史 JSON 中可能含本机路径/书签/locator 的字段，并拒绝把绝对
  artifact 引用发送给网页。
- 新增训练配置与启动命令：Host 返回可训练标签、正反样本数、活动来源和方法可用性，并在
  接收任务时再次执行 Mac 端样本门槛、媒体隔离、范围与单运行校验；相同 `operationID` 重试
  只执行一次。
- 网页“新建任务”面板沿用 Mac 的方法、标签、来源/当前选择和提交确认层级；个人模型继续
  复用现有训练快照、Embedding Cache 与 Centroid / AdamW Rebuilder，任务活动原位显示在
  Run 工作台。网页不接触原始训练文件，也不以本地状态伪造任务成功。
- 个人训练活动只显示 Host 返回的取消动作，取消请求落到同一个 Rebuilder 并返回取消终态；
  Feature Print Run 可打开关联后台任务，建议任务的暂停/继续/取消走审核领域端口，继续时
  唤醒 Host Runner。
- 失败或取消 Run 提供重新配置入口。可验证的历史标签会预选；旧记录缺少标签 ID 时保留原
  方法但清空标签，强制用户重新选择后才能提交。
- 普通图库选区补齐 Mac 的“准备特征”和“查找相似”工具。特征准备复用同一
  `AppSelectedAssetEmbeddingCaching`，Host 逐项复核媒体类型与 content revision，网页只显示
  生成、缓存命中、仅云端和失败计数，并可取消；相似项直接以冻结选区启动既有瘦身种子分析。
  宽屏批量栏、窄屏多选检查器与 Command-K 命令面板提供同名入口。
- 新增世界地图工作区：Host 通过 `/v1/world-map/snapshot` 和 `/v1/world-map/selection`
  投影 Mac 现有 `WorldMapCatalog` 聚合与同桶照片；浏览器复用 App bundle 内的 MapLibre / deck.gl /
  Natural Earth 资源，不从 CDN 执行代码，也不接收路径、locator 或 PhotoKit identifier。
- 地图 iframe 只允许同源嵌入，根 Web 页面继续禁止被嵌入。相机停止后以 240 ms 合并视口查询，
  请求代次拒绝过期响应；点击照片塔载入地点卡片，缩略图和全屏预览继续走受保护媒体路由。
- 网页按 Mac 原有层级补齐“更新照片位置”面板：`/v1/world-map/location-backfill` 投影每个来源的
  状态、覆盖率、已定位/无坐标计数与 reconcile 进度，`.../requests` 以 operation ID 幂等开始、
  重试或取消。打开地图、打开/关闭面板和轮询均不隐式启动或取消；Host 只读来源元数据并仅更新
  ImageAll 自有目录。
- 网页补齐 Mac 的“补全地点”面板：`/v1/world-map/place-tags` 只读本地标签缓存，
  `.../requests` 仅在用户显式提交时搜索地点描述或确认 Host 候选。浏览器不接收照片、路径、
  Photos identifier 或资产 ID；Host 继续执行 Apple 地图→OpenStreetMap 回退、候选校验、GPS 优先
  和目录事务。自动化只用合成目录投影与合成浏览器 iframe，不读取受保护真实图库。

## 2026-07-30 验证记录

- JavaScript 语法与 lint、HTML ID/引用完整性、Swift 测试文件语法和 Git whitespace
  检查通过；
- `ImageAllRemoteProtocol` 14 项、`ImageAllRemoteClient` 37 项测试通过；
- 自动化未访问 `/Volumes/HDD2` 受保护真实数据；
- Mac Debug / Remote XCTest 与已登录浏览器矩阵仍待新版 Host 重新构建运行后验收；当前运行中
  Host 在本轮资源改动前启动，不能作为新界面的运行证据。

## 2026-08-01 验证记录

- `ImageAllRemoteProtocol` 18 项 DTO 往返测试通过，覆盖标签组、标签管理、一步撤销、审核
  总览，以及旧 Host 缺少标签分组/审核媒体类型字段时的兼容解码；Remote Facade 定向测试
  覆盖最近一步撤销、撤销重放、稳定兼容分组 ID 和 Mac 审核总览映射；
- Remote HTTP Server 直接运行 26 项时有 25 项通过；唯一未执行成功的是依赖 App
  `Bundle.main` 的打包资源测试，直接 `xctest` 环境没有 App Host。随后 Xcode App Host
  测试启动仍停在系统测试宿主阶段并人工停止，不记作断言失败或通过；
- JavaScript 语法/lint、185 个 HTML ID 与 180 个 JavaScript ID 引用完整性、Git whitespace、
  签名 Mac Debug 构建和 `build-for-testing` 均通过；源码、构建包和 `127.0.0.1:8788` 实际
  返回的 `index.html`、`app.css`、`app.js` SHA-256 分别一致；
- 新版签名 App 已重新启动且本机端口监听成功。本轮测试和文件命令未直接读取或写入
  `/Volumes/HDD2` 受保护路径，也未执行照片内容操作；当前无法接管用户已登录的 Chrome，
  账号态界面与写操作仍待浏览器运行验收。
- 随后使用 Computer Use 接管本机 Chrome，以密码管理器保存的白名单账号登录
  `127.0.0.1:8788` 并完成只读验收：筛选“猫”后可见结果均为猫或人与猫，已载入窗口从
  72 自动扩展到 144，跨过 12 秒账号轮询后仍保持 144；点击第 63 / 144 项不跳回顶部；
  Command-A 在焦点仍位于“选择”按钮时正确选中 72 项；标签管理面板和审核总览能显示
  Host 权威数据。
- Chrome Network 证据显示 PhotoKit 2048px 预览请求返回 `200`，实测一张照片约 8.2 秒；
  网页现改为先即时显示所选卡片的本地缩略图，再在后台无缝替换大图。最终签名 App 重启
  后，首张照片在点击后约 1 秒内已有可见预览，中段选图仍保留滚动位置。
- 最终检查通过 JavaScript 语法/lint、186 个 HTML ID 与 181 个 JavaScript ID 引用完整性、
  Git whitespace、签名 Mac Debug 构建和 `build-for-testing`。Computer Use 无法保持修饰键
  并同时发出鼠标点击，因此 Command-点击、Shift-点击和框选仍保留为后续人工运行验收；
  本轮未执行标签、审核或真实照片写操作。

## 2026-08-05 验证记录

- JavaScript 语法/lint、媒体 Service Worker 静态安全契约、Git whitespace、签名 Mac Debug
  普通构建和 `build-for-testing` 通过；普通运行 App 不包含测试 PlugIns，签名校验通过。
- Remote 定向测试覆盖浏览器 Range 形式、完整 GET、HEAD、206 单段 Range、416 越界 Range、
  真实 `video/mp4` MIME、准确长度/`Content-Range` 和网页安全头，3 项均通过。首次集成测试
  暴露 NWConnection 首段上下文未完成造成的流式死锁，修复后媒体用例在约 0.2 秒完成。
- `127.0.0.1:8788` 由新版单实例 Host 监听，实际返回的 `service-worker.js` 与构建源码一致；
  CSP 包含 `media-src 'self' blob:` 和 `worker-src 'self'`。测试宿主直接运行依赖 App
  `Bundle.main` 的打包资源用例仍受既有 runner 限制，等价静态资源断言由 Node 契约检查通过。
- Computer Use 只读验收确认“适应/填充”切换后已载入数量保持 72；首轮账号登录下打开文件夹
  MP4 与 Apple Photos MP4 均出现浏览器媒体失败。Host 对用户选定资产的 16 字节只读 Range
  探针返回 `206 video/mp4`，因此问题收敛到 Service Worker 生命周期：Chrome 回收 Worker 后，
  其全局内存凭据会丢失。现改为每次媒体请求可向发起页面临时索取当前内存凭据，不写入
  URL、日志或持久存储。
- 最终以 Chrome 已登录会话复测：Worker 版本确认更新，原生 `bytes=0-` 请求经 Worker 返回
  `206 video/mp4`；1280 × 720 视频 `readyState=4`，可播放、暂停，并可定位至约 10 秒。
  等待超过一次 10 秒账号轮询后，暂停态和 10.01 秒进度保持不变，没有重载归零。
- 向下滚动自动扩展到 216 项后点击后续视频，再等待一次轮询，载入数量、选中项和
  `scrollTop=1476` 均保持不变；没有跳回顶部或缩回首屏。组合键多选按用户要求继续留到
  最终人工验收。
- 完整检查器在已登录 Chrome 中通过照片与视频双模式验收：照片显示来源、媒体、尺寸、
  拍摄/修改时间、格式和状态；视频另显示 `0:12` 时长、`4.5 MB` 文件大小及相对位置。
  “在 Mac 上用‘预览’打开原图”受保护 POST 成功返回，网页提示已交给 Mac 打开；等待超过
  一次账号轮询后，视频选中项、`1 / 72` 导航位置和详情均保持不变，控制台无错误。
- XCTest、构建和静态检查未直接遍历 `/Volumes/HDD2` 受保护目录；浏览器只执行用户授权的
  只读查看，没有标签、审核、文件或照片写操作。
- 训练工程只读工作台在已登录 Chrome 中读取到 37 条照片 Run；三种模型槽均显示已就绪，
  最近一条增强个人模型 Run 展示验证集口径、样本数、产物类型和安全相对引用。按
  Feature Print 筛选后得到 11 条记录，选择历史失败 Run 并跨过一次 10 秒账号轮询后，筛选
  与选中 Run 保持不变；视频 + Feature Print 的空结果安全显示 0 条和三个尚未训练槽位。
- 训练 DTO 往返、Facade 隐私清洗、Host 查询路由和打包网页契约定向测试通过；历史 JSON
  中路径/书签/locator/filename/original 字段及绝对 artifact 引用不会进入网页。浏览器控制台
  无错误，本轮没有启动训练或执行任何数据写入。
- 新版签名 Host 增加“新建训练任务”：已登录 Chrome 可读取三种方法、Host 权威标签资格、
  样本数和 13 个活动来源。切换快速个人模型并搜索“猫”时显示 92 个已确认样本；未选择时
  提交按钮禁用，选择后启用，取消选择后再次禁用。
- 真实界面验收发现连接轮询会误启用未满足本地选择门禁的写按钮；修复后跨过一次 10 秒轮询，
  “猫”的选择、启动摘要、面板和按钮状态均保持不变。390px 窄屏下方法横排、配置滚动区和
  固定提交栏可正常使用，控制台无 warning/error。本轮只验证到提交前，没有启动真实训练。
- 协议包 21 项、Remote Facade/HTTP 定向 43 项通过；HTTP 用例实际覆盖 setup、202 launch
  回执和相同 `operationID` 重放只调用一次 Host 命令。签名 Debug 构建、深度签名校验、
  JavaScript 语法/lint 和 whitespace 检查通过，普通运行 App 不包含测试 PlugIns。
- 训练控制切片新增活动取消协议/路由、建议任务恢复唤醒、Run→后台任务入口和失败 Run
  重新配置。协议包 21 项、Remote Facade/HTTP 定向 47 项通过；取消路由返回 Host 的
  `cancelled` 终态，建议任务恢复用例证明会唤醒 Runner。
- 已登录 Chrome 选择历史失败 Feature Print Run 后同时显示“查看关联任务”和“重新配置”；
  已结束关联任务给出保留 Run 的明确提示。该旧 Run 缺少标签 ID，重新配置后保持 Feature
  Print 方法、0 个标签选择和禁用提交；跨过 11 秒后台刷新仍保留选中 Run，控制台无异常。
  312px 等效窄屏无横向溢出。本轮未取消现有任务，也未启动真实训练。
- 图库瘦身新增只读三栏工作台，直接投影 Mac 的分析记录、候选分组和选中分组成员；照片/
  视频各自切换，候选组和成员按小窗口渐进展开，不在浏览器重新计算相似度。初始只渲染
  48 组与 96 个成员，用户可继续展开到分析结果真实总量，避免 8572 组一次性进入 DOM。
- 已登录本机 Chrome 实测照片分析为 1 条记录、8572 组、30981 个成员，视频为 1 条记录、
  1405 组、5403 个成员；照片候选从 48 逐步展开到 576，越过旧 500 组上限后再选择成员、
  继续展开并跨过一次账号轮询，仍保持 576 组、同一成员选择和打开的工作台。此前普通/
  Command/Shift 组合选择保留 3 项，双击可用成员 `IMG_0442.PNG` 成功打开预览；512px 等效
  窄屏无横向溢出，控制台无错误。本轮未启动分析、回收或删除任何项目。
- 当前协议包 22 项、Remote Facade 18 项、Remote HTTP 32 项全部通过；新增回归用例明确请求
  并返回 501 个候选组。签名 Debug 构建、深度签名、JavaScript 语法与 whitespace 检查通过。
- 图库瘦身新增 Mac 风格“新分析”面板：支持全部来源、当前筛选、按种子查找，活动来源全选/
  清除，以及 Feature Print Top-K/L2、DINO cosine、拍摄日分桶四组共享设置。全部来源提交为
  `sourceIDs: null`，显式空选保持禁用并由 Host 二次拒绝。
- 分析记录原位提供暂停、继续和删除记录；删除提示明确不会删除任何原始媒体。命令服务枚举
  Host 权威资产范围、复用现有分析队列和 Runner；启动、阈值更新与任务动作均有幂等门。
- 本切片协议包 23 项、Remote Facade 19 项、Remote HTTP 33 项全部通过；HTTP 用例覆盖 setup、
  202 launch 重放、阈值保存和删除记录路由。自动化只使用合成 stub，未读取受保护真实图库。
- 来源侧栏新增 Mac 风格管理面板，可发起连接文件夹、连接/重新绑定 Apple Photos、重新授权、
  文件夹重扫、Photos 同步/完整修复和删除来源。需要权限或破坏性确认的请求先返回 `202`，
  网页显示“等待 Mac”，真正的目录选择、TCC 和删除确认只在 Mac 原生界面执行。
- 来源请求使用同进程操作 ID 去重并限制为单个交互请求；完成、取消或失败后网页原位刷新来源
  和资产窗口。删除继续复用回收记录门禁，只清理 ImageAll 索引与 App 缓存，不删除原始媒体。
- Host 开关、公网地址、配对设备和网页白名单仍只在 Mac“网页访问”设置中管理；网页明确展示
  边界，不提供自授权或扩大 Host 暴露面的入口。
- 当前协议包 24 项、Remote Facade 20 项、Remote HTTP 34 项全部通过；HTTP 用例验证状态查询、
  `202` 异步回执和命令映射。测试使用合成 stub，没有弹出系统选择器或操作真实来源。
- 图库瘦身新增“分析 / 回收站”分段。回收站按照片/视频、来源和文件名读取 Mac 未解决记录，
  分批显示文件名、来源、状态与保留期限；响应不含原路径、隔离路径或 Photos 标识。
- 文件夹回收项根据 Host resolution 只显示恢复/永久删除、撤销授权前失败意图或重新检查状态。
  恢复、重新检查、永久删除使用异步请求并回到 Mac 原生确认；恢复缺少写授权时继续使用既有
  安全目录选择器。Photos 回收项只显示系统“最近删除”恢复说明。
- 本切片协议包 25 项、Remote Facade 21 项、Remote HTTP 35 项全部通过；合成浏览器验证三类
  动作矩阵、等待 Mac → 完成原位状态、390px 无横向溢出且无脚本错误。自动化没有调用真实
  回收、恢复或永久删除，也未读取受保护真实图库。
- 候选成员选择栏新增“移入可恢复回收站”和“立即释放空间”。提交冻结当前分析记录、候选簇、
  媒体类型和精确资产 ID；Host 再校验成员关系，Mac 原生窗口二次确认后才调用既有回收服务。
  网页原位显示复制/校验/删除/Photos 系统操作进度，以及成功隐藏、失败、待来源授权和 Photos
  未授权的逐项审计摘要；完成后重读权威工作区，不让回收成员在刷新后重新出现。
- 本切片协议包 26 项、Remote Facade 22 项、Remote HTTP 36 项全部通过。合成 Playwright 浏览器
  验证 2 项冻结快照、等待/运行/完成轮询、完成后 4→2 成员同步、390px `scrollWidth == 390`，
  控制台 0 错误；未向真实 Host 提交回收或删除命令，也未读取受保护真实图库。
- 分析记录补齐 Mac 的控制请求与阶段化进度：`正在暂停/正在取消` 不再退化成普通运行中，
  指纹、Feature Print、DINOv2 与聚类阶段显示独立进度；分析运行时明确提示一键清理保持锁定。
- 分析记录导航新增上一条/下一条、当前位置和方向键/Home/End；异步切换完成后恢复选中记录焦点，
  窄屏横向记录栏自动将当前记录滚入视野，不把成员网格或页面带回顶部。
- 完全相同项清理补齐 Mac 风格的删除后独立核验报告，展示目标保留、实际读取、当前可用、确认
  清理、完成组、剩余冗余和未决数量；这些值均由 Host 删除后再次读取产生，不用方案预估冒充结果。
- 本切片协议包 27 项通过；Facade/HTTP 定向测试覆盖控制请求、阶段映射和完整核验字段。合成
  Playwright 使用现有 Chrome 验证键盘导航、焦点恢复、报告弹窗和 390px `scrollWidth == 390`，
  控制台 0 错误，未连接或操作真实图库。
- 全局任务面板与图库瘦身分析记录新增双向定位：任务可以直接打开对应媒体类型和记录，瘦身
  状态栏也可以返回并聚焦同一后台任务。任务摘要只增加控制请求、尝试次数、安全错误码和结构化
  导航目标，不暴露原始错误、路径、哈希或 Photos 标识。
- 失败记录按 Host 安全码显示授权、来源、可恢复重试或尝试耗尽建议；可重试失败在原位提供继续
  分析。瘦身检查器补齐 Mac 端的任务、来源、范围、种子、已分析/待分析、策略版本、候选类型、
  成员、已选、结果和模型技术摘要；390px 下默认折叠并保持成员区域可滚动。
- 本切片协议包 28 项、Remote Facade 22 项、Remote HTTP 37 项全部通过。合成 Chrome 验证双向
  定位、失败恢复、选择后检查器同步和 390px `scrollWidth == 390`，控制台 0 错误；未连接或
  操作真实图库。

## 2026-08-06 世界地图网页切片验证记录

- Remote Protocol 29 项 DTO 往返测试通过；Remote Facade 与 HTTP Server 定向测试共 59 项通过，
  覆盖目录聚合/地点照片映射、鉴权路由、固定静态资源白名单，以及根页面 `DENY` 与地图 iframe
  `SAMEORIGIN` 的安全头差异。
- 合成 Chromium 端到端验证通过：打开世界地图、接收 renderer ready、按视口读取 1 座照片塔、
  点击“上海”载入同桶照片、打开/关闭全屏预览、Escape 返回图库；700 ms 稳定窗口内地图快照只
  请求 1 次，没有刷新循环、JavaScript 异常或 console error；另覆盖 Host 500 时持续显示可恢复
  错误态，不再只闪 toast 后留下空白地图。
- 真实目录最初暴露出地图资格 SQL 的相关 `NOT EXISTS` 会对回收记录重复扫描，两个计数各约
  12 秒并触发 Host 15 秒断连。改为等价、排除 `NULL` 的一次性集合查询后，同库只读测量约为
  0.02 秒、0.09 秒，聚合约 0.03 秒；新增回归覆盖 active recycle 排除和 purged `NULL` 记录。
- 新版签名 App 已部署并重启。真实 Host API 只读验收返回 200：全球快照 0.093 秒，25,599 张
  eligible、11,437 张已定位、14,162 张未定位、19 座照片塔；首座塔下钻 0.264 秒返回 36/3,890
  张元数据。重启清除了旧网页会话，Chrome 的系统密码确认无法由自动化代替，因此已登录页面
  的最终可见指标核对仍待用户恢复登录后完成。
- JavaScript 语法检查、Mac 主工程签名构建和定向测试通过。自动化未读取 `/Volumes/HDD2`、
  Apple Photos 原图或媒体字节；真实运行仅查询 ImageAll 自有目录数据库与只读 Host 元数据 API。
- 地点标签长列表进一步覆盖搜索中继续改词、较晚返回的缓存刷新和候选键盘导航。进行中的提示固定
  显示实际已提交查询，当前输入不会被旧响应覆盖；ArrowDown 从输入进入候选，ArrowUp、Home、End
  可连续移动，Enter 原位确认。刷新代次若落后于搜索/确认写入则只收束加载态，不覆盖新结果。
- Chromium 以 9 张地点卡、两项同名候选和刻意延迟的旧快照验证上述竞态；编辑框、焦点及可视位置
  保持，确认的是键盘当前候选，390px 无横向溢出。地点卡移除会造成高度漂移的延迟几何估算，长
  列表采用稳定布局；测试仍只使用合成元数据，未调用真实地点搜索或读取照片。

## 2026-08-06 图库总览网页切片验证记录

- 新增 `RemoteGalleryOverviewSnapshot` 及 `GET /v1/gallery-overview`，只映射 Mac 现有
  `GalleryOverviewSnapshot`；响应不含路径、locator、PhotoKit identifier 或媒体字节。
- 网页沿用 Mac `LIBRARY ATLAS` 的圆角卡片、照片蓝/视频橙和保守去重口径，并加入来源、标签、
  照片/视频卡的真实图库下钻、刷新错误态、焦点返回、Escape 关闭和 390px 响应式布局。
- Remote Protocol 30 项测试通过；Remote Facade 与 HTTP Server 定向 Xcode 测试通过。
  Python Playwright 合成浏览器验证总量 150、保守去重 139、正样本 20，来源和“猫”标签下钻
  请求参数正确，手动刷新后总览请求稳定为 2 次且无后台重复刷新；390px 无横向溢出，页面与
  console 均为 0 错误。
- 自动化使用合成目录投影，未读取 `/Volumes/HDD2`、Apple Photos 原图或任何媒体字节。签名
  App 已正常重启以消除“新网页资源 + 旧 Host 进程”的版本错配；`127.0.0.1:8788` 返回新缓存
  版本与总览入口，未登录访问总览 API 正确返回 401。重启会清除网页登录态，因此真实 Host 的
  可见统计留待用户重新登录后的浏览器验收。

## 2026-08-06 高频浏览与审核操作切片验证记录

- 网格上方新增与 Mac 工作区一致的常驻筛选摘要，明确显示“猫 已确认”“旅行 已拒绝”、
  无标签、可用状态与格式；多项正向条件可直接在“或 / 且”间切换，清除后重新查询 Host，
  不在浏览器本地隐藏结果。
- Command-K 命令面板在已有单选或多选时提供每个活动标签的确认、拒绝和清除决定；同时加入
  “连接文件夹来源”和当前来源的重扫/同步入口，均复用现有 Host 权威写入与 Mac 原生确认链。
- 审核全屏预览保留“属于 / 不属于 / 稍后”操作与 P/X/U 键盘路径；左右切换同步审核队列选中项，
  P/X 完成后留在全屏并前进到下一项，U 只移动队列不写决定。视频全屏复用受保护媒体路由和
  浏览器原生控制器，关闭或切换时会停止旧播放器。
- Python Playwright 合成浏览器已验证：两标签“且→或”查询、常驻摘要与清除、命令面板单图
  标签写入、文件夹来源请求、审核全屏 P/X/U、视频全屏 DOM/媒体路由，以及 390px 无横向溢出；
  11 次资产查询、1 次标签决定、2 次审核决定和 1 次来源请求均符合预期，console/page error 为 0。
- Web 静态资源契约裸 `xctest` 通过 1/1，JavaScript/Python 语法和相关 diff 检查通过；签名 Debug
  构建与深度签名校验通过。新版 App 已重启为 PID 59027，`127.0.0.1:8788` 正在监听并返回
  `20260806-16` 资源，未登录审核 API 正确返回 401。
- 自动化只使用合成元数据和 1 px 图片响应，未读取 `/Volumes/HDD2`、Apple Photos 原图或任何
  真实媒体字节；真实账号登录页面只留作新版 Host 重启后的人工验收。

## 2026-08-06 应用存储与数据维护切片验证记录

- 标题栏新增与 Mac 工作区同名的“应用存储与预览缓存”面板，展示预览缓存、长期 Photos 原图
  副本的条目数和登记用量，以及应用资料使用内置/外置位置与重启待生效状态；网页协议不返回
  应用支持目录、缓存目录或外置根目录的绝对路径。
- 网页可发起导出用户数据、选择外置应用存储、清理预览缓存和清理长期 Photos 原图副本；导出/
  迁移继续使用 Mac 目录选择器，两类清理继续使用 Mac 原生风险确认。相同操作 ID 幂等，操作
  互斥，网页轮询等待、运行、完成、取消和失败状态。
- 清理动作复用 Mac 既有服务和安全门禁：预览缓存仅删除可重建派生物；长期原图副本清理在图库
  瘦身分析活动时拒绝，且不修改 Apple Photos 原图、文件夹原件、标签、Feature Print 或模型。
- Remote Protocol 31 项测试通过；新增服务 3 项、Facade 1 项和 HTTP 1 项定向测试全部通过；
  Mac Debug build 与 test bundle 编译通过。Python Playwright 验证用量/状态、异步导出回执、
  完成轮询、路径脱敏、390px 无横向溢出和 footer 可达，console/page error 为 0。
- 浏览器自动化只使用合成用量和请求回执，未读取 `/Volumes/HDD2`、Apple Photos 原图或任何
  真实媒体字节。

## 2026-08-06 选中项目工具切片验证记录

- 网页宽屏批量栏、窄屏多选检查器和 Command-K 命令面板新增“准备特征 / 查找相似”。
  “查找相似”直接冻结当前照片或视频选区并启动既有瘦身种子分析，不复制相似度算法。
- Host 新增照片特征异步投影与命令：最多接受 5000 个去重资产 ID，逐项复核媒体类型和
  content revision，复用 Mac 的 Embedding Cache；进度显示新生成、缓存命中、仅云端与失败，
  支持取消，相同 operation ID 返回同一任务最新状态。
- Remote Protocol 32 项通过；服务、Facade、HTTP 和打包网页 4 项定向测试通过。合成 Chrome
  验证两项选择、特征任务运行到完成、按种子请求、自动打开对应瘦身记录，以及 390px 多选
  检查器入口可达且无横向溢出；console/page error 为 0。
- 合成测试只使用内存元数据和 1 px 图片，没有读取 `/Volumes/HDD2`、Apple Photos 原图或
  任何真实媒体字节，也没有向真实 Host 启动特征生成或瘦身分析。

## 2026-08-06 个人建议抽检切片验证记录

- 网页批量栏、窄屏多选检查器、Command-K 和建议审核标题栏补齐 Mac 的“抽取样本并生成建议”。
  有选区时按当前网格顺序取最多配置数量；无选区时由 Host 从完整图库抽取候选，浏览器不以当前
  已载入窗口冒充全图库。
- Host 复用 `AppPersonalSampleSuggesting`、Embedding Cache 和 `PersonalizationReviewPort`：逐项
  复核 asset ID、媒体类型与 content revision，激活个人模型能力并写入同一待审核队列。操作异步、
  可取消、同一 operation ID 幂等；刷新网页只恢复活动投影，不会重新启动生成。
- Remote Protocol 33 项通过；Host 服务幂等测试与 HTTP 查询/提交/取消路由测试共 2 项通过。
  合成 Chrome 验证两项多选按网格顺序提交、完成后进入审核、全图库请求提交空资产范围、390px
  多选入口可达，console/page error 为 0。
- 合成测试只使用内存元数据和 1 px 图片，没有读取 `/Volumes/HDD2`、Apple Photos 原图或任何
  真实媒体字节，也没有向真实 Host 启动个人建议生成。

## 2026-08-06 按标签生成全库个人建议切片验证记录

- 审核总览的每个合格标签新增与 Mac 同名的“个人模型 Top N / 超级个人 Top N”入口。弹窗显示
  Host 权威最低分数、最大待审数量和活动来源；当前来源模式只预选当前来源，空选不能提交。
- Host 复用 `AppPersonalTagLibrarySuggesting`、Embedding Cache 与同一审核仓储，按 500 项分页
  枚举尚未对目标标签做决定的完整来源范围。个人模型能力、标签资格、来源集合与阈值在提交时
  再校验；任务阶段、计数和停止动作可查询，同一 operation ID 重放不会重复扫描或写入。
- Remote Protocol 34 项 DTO 往返测试通过；服务测试覆盖来源冻结、候选分页、Host 阈值、Top N、
  写入审核队列和完成后幂等重放；HTTP 测试覆盖查询、202 提交与取消路由。
- 合成 Chrome 验证生成按钮不会误进审核队列，弹窗准确显示 `0.420` 与 `Top 25`，只提交用户保留
  的一个来源；轮询完成后自动进入“猫”的审核队列。390px 无横向溢出，console/page error 为 0。
  自动化仅使用内存元数据和 1 px 图片，没有读取受保护真实图库，也没有向真实 Host 启动扫描。

## 2026-08-06 Feature Print 审核—训练双向恢复切片验证记录

- 审核总览不再把全部标签铺成无层级网格；它复用 Mac 权威标签组和网页已有标签顺序，按组显示
  标签数与待审总数。分组可折叠，状态作为非敏感界面偏好保存在当前浏览器；方向键与 Home/End
  可遍历分组，异步重绘后焦点仍按稳定 group ID 返回。归档或旧数据中无法匹配的标签进入“其他”。
- 审核总览卡片补齐 Mac 的“生成特征向量 / 更新特征向量”入口，复用现有训练配置面板和
  `POST /v1/training/launch`；标签、生成/更新模式、活动来源和样本门槛仍由 Host 权威设置与
  提交时复核，浏览器不另建 Feature Print 任务模型。
- 运行中的 Feature Print 卡片原位显示 Host 返回的暂停、继续和取消动作，并可定位关联训练
  Run；训练详情反向提供“打开标签审核”，有待审项时进入同一标签队列，否则定位到总览卡片。
  任务动作完成后同步刷新任务、训练和审核投影，不依赖浏览器本地数组伪造终态。
- 合成 Chrome 串联验证审核组折叠/展开与焦点恢复、暂停态继续、运行态取消、按 job/tag 精确选中
  训练 Run、从训练返回同一标签审核、取消后重新打开“更新特征向量”配置并提交准确标签和两项
  来源；390px 无横向溢出，console/page error 为 0。测试仅使用内存元数据和 1 px 图片，没有
  访问或修改真实媒体。

## 2026-08-06 训练失败恢复与键盘连续性切片验证记录

- Feature Print 新运行把结构化 `tagID` 和本次冻结的来源 ID 写入训练账本；旧运行继续兼容读取。
  Remote 投影只从白名单字段生成恢复上下文，不把路径、书签、PhotoKit 标识或原始错误交给网页。
- 失败详情把安全技术码翻译为用户可执行的原因和下一步，同时保留技术码便于排查。“重新配置”
  恢复可确认的方法、标签和来源；历史来源已停用时明确剔除并提示，旧记录没有来源明细时清空
  来源选择，禁止把原来的选定范围静默扩大成全部来源。
- 训练记录列表补齐方向键、Home/End、roving tabindex 和 `aria-activedescendant`。点击或键盘选择
  重建列表后恢复焦点；Host 异步刷新时保持当前 Run 和焦点，并使用 `preventScroll` 避免工作区
  跳回顶部。
- Remote Protocol 35 项、Host 定向 2 项、Mac `build-for-testing` 均通过。合成 Chrome 验证失败
  指引、历史来源恢复、失效来源剔除、准确重新提交、键盘导航、异步刷新焦点与 390px 无横向
  溢出，console/page error 为 0；测试未连接真实 Host、未启动训练、未读取受保护真实图库。

## 2026-08-06 个人模型批次状态与重启收束切片验证记录

- 个人模型多标签任务保持 Mac 既有的“逐标签独立训练、单项失败后继续后续标签”语义。Host 为
  每个标签投影等待、准备样本、准备特征、训练、发布、成功、跳过、失败或取消状态；已成功标签
  不因同批其他标签失败而回滚，最终汇总明确区分完成、跳过与失败数量。
- 每个标签仍写入独立 Run 和模型产物，同时在安全样本摘要中记录同一批次 ID、标签顺序和当前
  序号。App 启动时只把进程内、尚未终结的个人模型 Run 收束为 `hostRestartInterrupted`；由持久
  队列驱动的 Feature Print Run 不受影响。已有发布模型不会被中断记录覆盖。
- 网页活动卡片逐标签显示真实进度；部分成功时“重新处理未完成标签”只回填失败、跳过或未开始
  标签。训练工作区新增 N 新建、R 刷新、V 打开标签审核、E 重新配置快捷键，并保留方向键和
  Home/End；取消活动不再计入进行中角标。
- Remote Protocol 35 项、Host 定向 3 项和 Mac `build-for-testing` 通过。隔离 Chrome 合成批次
  验证中间标签失败后继续、精确重试未完成标签、N/R/V/E、刷新保持与 390px 无横向溢出，
  console/page/resource error 为 0。测试没有连接真实 Host、启动真实训练或读取受保护图库。

## 2026-08-06 训练批次历史与记录栏对齐切片验证记录

- Host 不再在 12 秒后丢弃个人模型批次状态，而是在 App 自有 RemoteHost 目录保存最近 50 个
  脱敏批次。账本只含 operation ID、媒体、方法、时间、标签 ID/显示名、阶段、计数与安全错误码；
  文件使用原子写入、完整文件保护和 0600 权限，不保存资产、路径、书签、向量或模型内容。
- 单标签 `training_run` 与模型产物仍是训练结果的权威真相；辅助批次账本负责保留“尚未创建 Run
  就被跳过”等批次级事实。Host 重建时保留终态历史，把旧进程仍在运行的批次及标签明确收束为
  `hostRestartInterrupted`，不会自动重启或覆盖已有发布模型。
- 网页新增紧凑的“批次历史”横向账本：显示全部完成、部分完成、失败、取消或重启中断，保留逐
  标签结果，可打开关联 Run，并只重新处理未完成标签。顶部瞬时状态在 15 秒后收起，历史账本仍
  保留；新增与 Mac 一致的记录栏显示/隐藏按钮和 L 快捷键，布局偏好仅保存在浏览器本机。
- Remote Protocol 35 项、Host 定向 2 项与 Mac `build-for-testing` 通过。隔离 Chrome 验证终态
  超过瞬时窗口后历史仍可见、关联 Run 定位、精确重试、L 切换、刷新保持和 390px 无横向溢出，
  console/page/resource error 为 0；测试只使用临时文件和合成元数据。

## 2026-08-06 训练 Run 上下文与记录筛选切片验证记录

- Host 训练投影新增标签可读名、批次 ID/序号/总数和正负样本统计；标签已归档时仍从 Mac 权威
  标签仓储读取历史名称。主界面不再依赖解析任意 JSON 才能区分批次，旧 Host 缺少新字段时仍按
  单项记录安全显示，协议解码保持向后兼容。
- 网页训练记录栏新增“全部 / 批次 / 单项”筛选并与方法筛选组合，B 键可循环切换。筛选后列表
  数量、空状态、方向键、Home/End、批次关联定位和异步刷新都只作用于当前可见记录，不把选中项
  或焦点静默跳到页面根节点。
- Run 行和详情补齐标签、记录类型、批次位置、样本数、正负样本、开始/结束、耗时、数据范围及
  完整 Run/job/batch/tag 标识；技术详情继续默认折叠，避免高密度字段抢占 Mac 风格主信息层级。
- Remote Protocol 35 项、Host 安全投影定向 1 项与 Mac `build-for-testing` 通过。隔离 Chrome
  验证结构化批次、单项和旧载荷、B 循环、批次定位、折叠技术标识、刷新保持及 390px 无横向
  溢出，console/page/resource error 为 0；测试未连接真实 Host、启动训练或读取受保护图库。

## 2026-08-06 模型槽与训练/审核/任务往返切片验证记录

- 网页模型槽不再是不可操作的三张状态卡：已发布槽直接定位 Host 给出的 `publishedRunID`，并从
  同一 Run 投影补充样本数、短标识和完成时间；未发布槽打开对应方法的新建任务。没有新增第二份
  “模型版本”状态，模型槽与 Run 仍以 Mac 训练仓储为唯一真相。
- 模型槽支持 M 聚焦以及左右键、Home/End 导航。训练进入标签审核、审核卡片进入训练记录时保存
  结构化 run/tag/job 上下文；返回按钮明确显示“返回训练记录/返回建议审核”，正常往返不重建原
  工作区 DOM，因此滚动、选中项和焦点不再掉回图库或页面根节点。
- 关联任务弹层提升到训练/审核工作区之上，使用可含操作按钮的列表语义和 roving focus；J 打开
  当前 Run 的关联任务，方向键与 Home/End 移动，Enter 进入只读上下文动作或聚焦可用操作，Esc
  关闭后返回原 Run。鼠标点击没有取得焦点时仍优先按 job/tag ID 在当前 DOM 中恢复。
- 七组隔离 Chrome 回归全部通过，覆盖训练、审核桥接、筛选/预览、图库总览、选区工具、存储和
  标签全库建议；训练用例额外验证模型槽、双向嵌套返回、任务键盘导航、390px 和零 console/page/
  resource error。测试只使用合成元数据，未连接真实 Host、启动训练或读取受保护图库。

## 2026-08-06 通用设置与跨工作区焦点切片验证记录

- 网页新增与 Mac 同层级的通用设置面板，直接读写 Mac 当前持有的本地模型、空闲缩略图预热和
  工具栏显示设置。工具栏“仅图标 / 图标与名称”会同步影响 Mac 与网页；模型启用等待真实激活
  结果，预热开关同步当前图库 runtime，不在浏览器保存第二份偏好。
- 公网地址、Host 开关、配对设备和账号白名单继续只允许在 Mac 修改；网页明确说明该安全边界，
  不提供可绕过原生确认的假入口。设置补丁带 operation ID，Host 以持久幂等记录阻止网络重试
  重复执行。
- 设置支持 Command-,、Escape、分段按钮与方向键，关闭回到齿轮按钮；来源管理和存储面板打开
  后聚焦首个主操作，方向键可遍历，关闭回到触发按钮。训练、审核和任务弹层改用结构化 ID 恢复
  焦点，异步重绘不再把焦点丢到页面根节点。
- Remote Protocol 36 项通过，Host 设置 facade 测试目标通过编译；九组隔离 Chrome 流程覆盖设置
  三项写入、来源/存储键盘、训练/审核/任务往返、390px 与零 console/page/resource error。测试
  只使用合成元数据，没有读取受保护图库或执行真实媒体写入。

## 2026-08-06 建议阈值与设置生命周期切片验证记录

- 通用设置新增与 Mac 一致的三轨默认门槛，并以 0.05 为步进直接写入同一
  `SuggestionThresholdPort`。按标签覆盖页列出全部活动标签，支持搜索、单独设置、恢复继承默认，
  以及采用 Host 根据同轨正负样本生成的参考值；浏览器不横向比较或重算三种方法的分数。
- `GET /v1/settings/general` 每次刷新 Mac 权威阈值投影；`PUT` 增加单一阈值 mutation，并把动作、
  方法、标签和分数纳入 operation ID 幂等键。协议字段保持可选，旧 Host/旧快照仍可解码。
- 修复快速打开设置后立即 Escape 会遗留 `loading=true`、导致以后永久禁用的生命周期问题。标签
  编辑提交时按 tag/method 稳定键恢复焦点，搜索输入的方向键不再被弹窗按钮导航截获。
- Remote Protocol 37 项与 Mac `build-for-testing` 通过；八组 Python 隔离 Chrome 回归和世界地图
  Node 回归全部通过。阈值用例覆盖 7 次合成写入、搜索、覆盖、继承、采用参考值、快速关闭再打开、
  焦点恢复及 390px 无横向溢出，console/page/resource error 为 0；测试未连接真实 Host、启动训练
  或读取受保护图库。

## 2026-08-06 来源侧栏上下文与面板重开切片验证记录

- 来源侧栏新增与 Mac 使用习惯一致的右键和 Shift-F10 菜单；菜单按来源类型和状态显示查看、
  同步/重扫、恢复授权/重新绑定、完整修复、删除和打开来源管理，所有写动作继续复用既有 Host
  source-management 请求与 Mac 原生确认，不在浏览器直接操作目录或 Photos。
- 菜单支持上下方向键、Home/End、Escape，关闭后回到原来源；“在图库中查看”重绘来源列表后按
  source ID 恢复焦点。未建立 Remote 安全契约的缩略图预热和回收权限操作没有伪造网页入口。
- 来源管理与存储面板补齐加载代次取消：加载中快速 Escape 会清除 loading，再次打开重新读取
  Host，不会像旧设置生命周期那样永久禁用。
- 八组 Python 隔离 Chrome 回归与世界地图 Node 回归全部通过；新增用例覆盖右键菜单动作集合、
  Shift-F10、方向键、Escape、查看来源、快速关闭再打开及 390px，console/page/resource error 为
  0。测试只使用合成来源，没有读取受保护图库或执行真实来源写入。

## 2026-08-06 图库个人模型快捷入口切片验证记录

- 图库主工具栏新增紧凑个人模型菜单，提供与 Mac 同名的“重建个人模型”和
  “训练超级个人模型”；命令面板也可搜索相同动作，不用先进入训练工程。
- 快捷入口只读取当前“已确认”标签筛选；有多选时默认“图库当前选择”，无多选时
  默认“所有来源已确认样本”。标签与范围在原有训练设置页中可二次确认。
- 修复训练统一安全门：Host 报告方法不可用时，保留用户点击的方法并显示原因，但最终
  启动按钮必须禁用，不再可能提交一个 Host 已明确拒绝的方法。
- 训练回归覆盖标签带入、全库/当前选择、不可用 AdamW、命令搜索、方向键、Escape 和
  异步重绘后的结构化焦点恢复，并连续三次通过稳定性验证。八组 Python 隔离 Chrome 流程、
  世界地图 Node 回归、Mac Debug 增量签名构建和 8788 新资源检查通过；没有启动真实训练、
  读取受保护照片或写入真实标签。

## 2026-08-06 检查器标签分组与主动作切片验证记录

- 单张与多选检查器不再平铺全部活动标签，而是按 Host 标签组的稳定顺序显示可折叠分段；旧数据
  或未知分组归入“其他”。折叠状态只保存在浏览器工作区偏好，不保存标签事实。
- 标签主体左键确认、右键清除；聚焦主体后 X 拒绝、Delete/Backspace 清除。原有确认/拒绝/清除
  三态按钮保留，触控和需要明确拒绝的用户无需依赖右键或快捷键。
- 多选主体显示全部确认、全部拒绝、全部未决定或混合摘要，并继续通过同一批量端点提交完整选区。
  分组标题支持方向键、Home/End；折叠和异步标签写入重绘后焦点回到原 group/tag/action。
- 单选与多选两条合成 Playwright 流程覆盖分组顺序、折叠持久化、聚合 mixed、左键/右键/键盘写入、
  焦点恢复及 390px 无横向溢出；八组 Python 浏览器流程全部通过，世界地图 Node 回归退出码为 0。
  测试仅使用虚构 UUID 与 1×1 合成图片，没有访问受保护图库或真实 Host 标签。
- 组内顺序后来经架构复核确认为 Mac 设备本地偏好，而非远程图库事实；网页侧栏排序切片已按同一
  边界实现，跨组移动继续复用 Host 权威 `groupID` 命令，见后续验证记录。

## 2026-08-06 单张检查器 AI 建议切片验证记录

- `RemoteAssetDetail.pendingSuggestions` 以可选字段兼容旧 Host；新 Host 从现有 Review 端口读取每张
  资产的待审建议，映射标签 ID、显示名和特征向量/标准模型/个人模型/超级个人模型来源，不传分数、
  路径、向量或模型内容。
- 网页按 Mac 顺序在预览与元数据之间显示“AI 建议”，默认前 5 条，可展开全部；每项提供
  “属于 / 不属于”，复用现有标签决定和一步撤销链路。写入刷新后按 `tagID + origin + action` 恢复焦点；
  若建议已被 Host 清除则落到相邻建议或区标题。
- Remote Protocol 37 项全绿，覆盖新字段往返和旧 JSON 缺字段；Facade 定向非宿主测试 1 项全绿，
  证明四轨来源来自 Review 投影。合成 Chrome 流程覆盖 6 条建议、默认 5 条、展开、来源徽标、决定
  请求与焦点恢复，并经截图核对 330px 检查器不溢出。所有数据均为虚构 UUID 和 1×1 合成图片。

## 2026-08-06 iCloud-only 单图预览恢复切片验证记录

- Host 普通 `/preview` 保持 local-only，并把 PhotoKit `cloudOnly` 从通用 500 改为稳定 409；网页
  据此显示与 Mac 同义的云端占位和显式获取按钮，普通浏览、网格预取和后台刷新都不会触发下载。
- 新增的单图 `POST /cloud-preview` 直接复用 Mac `downloadCloudPreview`：Host 内部签发不透明
  grant，只缓存最大 2048px 标准预览，不向浏览器开放网络开关、路径或 Photos identifier。成功后
  网页重新读取普通预览，切图代次保护阻止旧结果覆盖当前检查器。
- Remote Protocol 37 项、Remote Client 38 项、Mac build-for-testing 与三项 Host 定向测试通过。
  合成 Chrome 流程覆盖 409 云端占位、第一次下载 500、原位重试、第二次成功和预览显示；仅使用
  1×1 合成 PNG/TIFF 与虚构 UUID，没有读取受保护图库或触发真实 iCloud 下载。

## 2026-08-06 首次使用与常用标签切片验证记录

- 无来源且无标签时，网页按 Mac 首次使用层级显示“连接照片文件夹 / 连接 Apple Photos / 添加
  常用标签”；连接动作继续进入已有来源管理与 Mac 原生确认，标签侧栏另保留同一常用入口。
- 常用标签由 Host 直接调用既有 `installPresetTags()` 安装人像、风景、美食、动物、植物、建筑、
  旅行、截图和文档。协议只提交 operation ID；安装不扫描媒体、不分析照片、不向任何资产写标签。
- Remote Protocol 37 项、Remote Client 39 项、Facade 与真实 HTTP 路由定向测试通过；HTTP 重放
  同一请求只调用一次 Catalog。合成 Chrome 流程覆盖首次空状态、来源请求、九标签安装、已存在
  空结果、命令面板、焦点恢复和 390px 无横向溢出，且无 Console/Page 错误。
- 浏览器仅使用空来源、虚构 UUID 与内存标签响应，没有连接真实 Host 来源、读取受保护图库或修改
  真实用户标签。宽屏截图核对继续沿用 Mac 三栏、系统按钮和紧凑留白，没有引入第二套视觉语言。

## 2026-08-06 侧栏来源与标签排序切片验证记录

- 来源行支持拖放和 Option-上/下，顺序只进入现有浏览器 workspace preferences；刷新后保持，
  不调用来源管理、不启动扫描、不改变图库筛选。Host 新来源仍按返回顺序追加，陈旧 ID 被忽略。
- 标签同组拖放和 Option-左/右只更新本地顺序；跨组拖放及 Option-上/下复用既有 Host move 端点。
  乐观显示成功后重读标签投影，409 等失败会恢复原 groupID 和两组顺序并把焦点留在移动标签。
- 侧栏、标签选择器、标签管理、单图检查器和多选检查器统一消费同一顺序。搜索过滤时标签不可拖，
  防止在局部结果中误排；拖放插入线和分组轮廓沿用现有 Mac 强调色与紧凑样式。
- 独立 Chrome 合成流程覆盖两来源排序、刷新持久化、同组标签排序零网络写入、跨组成功、跨组 409
  回滚、单选/多选检查器同步、Option 键盘路径、搜索门禁和 390px 无横向溢出。全部为虚构 UUID、
  空白合成图片和内存响应，没有读取受保护图库或修改真实标签。
- 全部十组 Python Chromium 回归和世界地图 Node 浏览器流程通过；训练→审核→训练的焦点恢复在
  负载下暴露首帧时序后改为有限帧重试，并连续三次定向通过。恢复器只强制首次目标恢复，之后若
  用户已主动聚焦其他控件便停止，不会持续抢焦点。

## 2026-08-06 标签分组折叠与检查器直接整理切片验证记录

- 标签侧栏的组标题补齐 Mac 同义的 disclosure、标签数量和折叠偏好；Arrow Up/Down、Home/End
  可在组标题间导航。刷新保留折叠状态，标签搜索只临时展开含命中项的组，清除搜索后恢复原偏好。
- 单图和多选检查器标签主体可直接拖放：组内只改浏览器本地顺序，跨组调用既有 Host move 命令；
  Option-上/下调整组内顺序，Option-左/右移到相邻组。侧栏、检查器、选择器和管理面板继续共享
  同一顺序，没有建立第二份标签事实。
- 合成 Chrome 流程覆盖折叠刷新持久化、搜索临时揭示、组标题键盘导航、单图跨组成功、既有 409
  失败回滚、单图键盘组内排序、多选直接拖动、操作面焦点恢复和 390px 无横向溢出；共发出三次
  跨组请求，其中一次按预期拒绝并恢复。全部使用虚构 UUID 与 1×1 合成图，没有连接真实图库。
- 十组 Python Chromium 回归和世界地图 Node 浏览器流程全部通过；`RemoteHTTPServerTests` 全组通过，
  签名 Debug App 已部署并由 `127.0.0.1:8788` 返回 Web v36。App 重启后原 Chrome 会话失效，保存的
  旧密码返回 `invalid credentials`，因此本切片不把真实账号页点击结果记作通过，待用户重新登录复验。

## 2026-08-06 标签侧栏修饰键与上下文菜单切片验证记录

- 标签普通点击切换并集筛选，Command/Ctrl 点击切换交集，Command/Ctrl-Option 点击切换排除；已确认
  与排除状态在 chip 上分别使用既有强调色和危险色，查询实际发送 `acceptedTagIDs`、
  `excludedTagIDs` 与需要时的 `tagMatchMode=all`，不是浏览器本地假状态。
- 标签右键和 Shift-F10 补齐仅筛选、排除/取消排除、重命名与归档；非系统分组标题补齐重命名与
  删除。编辑进入已有标签管理面板并预选、聚焦对应字段，归档/删除继续经过确认门和 Host 幂等命令。
  系统分组不暴露破坏性菜单。
- 上下文菜单支持方向键、Home/End 和 Escape；标签/分组管理关闭、确认取消及归档完成后按稳定 ID
  回焦。检查器拖动期间暂停批量汇总重绘，汇总加载不再禁用排序芯片；拖动结束另在 dragend 后
  重新稳定焦点，覆盖浏览器默认拖放收尾晚于首轮重绘的情况。
- 合成 Chrome 流程验证两个确认标签的交集查询、确认加排除、仅筛选、右键与 Shift-F10、菜单键盘
  导航、标签和分组重命名、删除取消、标签归档及 390px 无横向溢出。全部使用虚构 UUID、内存响应
  和合成图片，没有连接或修改真实照片库。
- 标签侧栏定向流程用真实鼠标按下/连续移动/松开轨迹连续运行 5 次通过；十组 Python Chromium 流程
  与世界地图 Node 流程 11 路并发全部通过。`RemoteHTTPServerTests` 44 项全组通过，签名构建包含
  `20260806-37` 静态资源，并已由本机 `127.0.0.1:8788` 实际返回。

## 2026-08-06 来源缩略图缓存预热切片验证记录

- 来源右键与来源管理面板按 Mac 状态机补齐“预热缩略图缓存”和“专门用于原比例的缓存”；活动
  预热期间其他来源写动作禁用，同一位置切换为“取消缓存预热”。
- Host 只通过目录分页与既有派生图缓存端口工作；原比例预热先排除已缓存资产。快照提供已完成、
  总数、成功与失败计数，单项失败不伪造全量成功，取消和 operation ID 重放保持幂等。
- 关闭来源面板不停止轮询：顶部紧凑状态按钮继续显示进度，点击可返回同一任务；完成、取消或失败
  后自动收起。界面沿用现有 Mac 面板、系统进度条和强调色，没有增加独立视觉体系。
- 合成 Chrome 流程覆盖两种入口、运行进度、面板关闭后的后台状态、重新打开和取消；服务定向测试
  覆盖逐项失败、完成计数、已缓存跳过、协作取消与幂等重放。全部使用虚构 UUID、合成响应与派生
  缓存桩，没有连接真实图库或读取原照片。
- 十组 Python Chromium 流程与世界地图 Node 流程全部通过，协议包 37 项和
  `RemoteHTTPServerTests` 44 项全组通过；签名 Debug App 已重启为新 Host 进程，本机
  `127.0.0.1:8788` 实际返回 `20260806-38`。重启使原网页会话失效，真实账号页只验证到 401
  登录门，不猜测或重放用户密码。

## 2026-08-06 来源授权恢复切片验证记录

- 活动 Apple Photos 来源补齐“请求照片写入权限…”，活动文件夹来源补齐“更新回收权限…”；右键、
  Shift-F10 与来源管理面板复用同一 Host 动作集合，界面继续沿用现有 Mac 风格和等待状态。
- 网页仅提交来源 ID、动作与 operation ID。Photos TCC 授权和文件夹系统选择器都由 Mac 原生界面
  执行；路径、安全作用域书签和 Photos 标识不进入浏览器响应。Host 在唤起原生界面前校验来源类型
  与状态，取消、拒绝、受限及成功均收束为可解释终态。
- 自动化只用虚构来源、授权桩和合成响应验证菜单、键盘路径、请求映射、幂等终态及取消路径；不连接
  真实图库、不触发真实 TCC 或文件夹选择器，也不读取受保护照片。
- 十组 Python Chromium 流程与世界地图 Node 流程全部通过，协议包 37 项、
  `RemoteHTTPServerTests` 44 项、来源服务 4 项及 facade 映射定向测试通过；签名 Debug App 已重启
  为新 Host 进程，本机 `127.0.0.1:8788` 实际返回 `20260806-39`。重启使原网页会话失效，真实账号
  页只验证到 401 登录门，不猜测或重放用户密码。

## 2026-08-06 来源空状态恢复切片验证记录

- 图库为空且没有额外筛选时，网页按当前来源类型和状态复用 Mac 语义：活动 Photos 立即同步、旧
  Photos 连接当前图库、待授权 Photos 重新授权，活动文件夹立即重扫、失效文件夹重新授权。
- 空页面的直接动作继续先打开来源管理，再提交既有 Host 幂等命令；需要权限或选择器的步骤仍在
  Mac 原生界面完成。网页只持有来源 ID、动作和 operation ID，不接收路径、书签或 Photos 标识。
- 修复来源投影已经更新、资产列表未变化时空页面文案不刷新的问题；来源状态变化现在独立触发空
  页面重绘，侧栏状态、恢复文案和按钮不会互相矛盾。
- 合成 Chromium 流程覆盖 Photos 活动/需授权/旧图库，以及文件夹活动/需授权五种状态和直接同步
  请求；全部使用虚构来源与空资产响应，不连接真实图库或触发系统选择器。
- 十组 Python Chromium 流程与世界地图 Node 流程全部通过，协议包 37 项和
  `RemoteHTTPServerTests` 44 项全组通过；签名 Debug App 已重启为新 Host 进程，本机
  `127.0.0.1:8788` 实际返回 `20260806-40`。重启使原网页会话失效，真实账号页只验证到 401
  登录门，不猜测或重放用户密码。

## 2026-08-06 单图连续导航与返回上下文切片验证记录

- 全屏预览新增与 Mac 单图模式一致的显式返回入口，并按图库、审核、图库瘦身和地图显示“返回网格、
  返回审核、返回分析、返回地图”；关闭按钮和 Escape 仍保留。
- 图库单图移动会同步底层主选中项、检查器和对应网格卡片，并把返回焦点更新到当前照片；关闭预览
  后不会跳回打开前的旧照片或网格顶部。
- 到达图库 72 项或审核 48 项窗口末尾时，只要 Host 仍返回游标，“下一张”继续可用并先载入下一
  页；位置显示区分“当前已载入”和“还有更多”。跨页后沿用同一筛选、排序、来源和媒体类型快照。
- 快速连续方向键在检查器同步期间按最后一次方向串行执行，不静默丢弃第二个按键；分页、媒体请求
  和检查器请求继续使用既有代次保护，关闭中的旧请求不能重新打开预览。
- 合成 Chromium 流程使用 72+2 项图库和 48+1 项审核队列，覆盖跨页、底层选中与检查器同步、返回
  焦点、快速右/左键、390px 布局和末页禁用；仅使用虚构 UUID 与 1px 合成图片。
- 十一组 Python Chromium 流程与世界地图 Node 流程全部通过，协议包 37 项和
  `RemoteHTTPServerTests` 44 项全组通过；签名 Debug App 已重启为新 Host 进程，本机
  `127.0.0.1:8788` 实际返回 `20260806-41`。重启使原网页会话失效，真实账号页只验证到 401
  登录门，不猜测或重放用户密码。

## 2026-08-06 审核多选与即时批量操作切片验证记录

- 审核队列补齐普通点击、Command/Ctrl 切换、Shift 连续区间、Command/Ctrl-Shift 追加区间、空白
  框选和 Command/Ctrl-A 全选当前已载入窗口；主选中项保留双层描边并继续驱动右侧预览，多选项
  使用与 Mac 选择态一致的强调色和勾选标记。
- P/X 对冻结选区按审核队列顺序一次提交既有批量决定端点；U 不发决定、不重排队列，只跳到下一
  未选项并收束为单选。刷新与分页保留仍有效的选区、锚点、主选中项和已载入窗口，不让旧异步请求
  覆盖用户刚完成的选择。
- 普通标签批量确认、拒绝、清除与新增并应用取消二次确认，恢复 Mac 的即时动作，并继续由 Host
  返回唯一最近一步撤销；不可逆回收、系统授权等既有确认门没有放宽。
- 合成 Chromium 流程使用 48 项审核队列验证框选、Command/Shift 三项选区、队列顺序批量拒绝、
  两项 U、Command-A、48+1 跨页单图导航和 390px 布局；标签流程同时断言批量决定期间没有浏览器
  对话框。全部使用虚构 UUID、内存响应和合成图片，不连接或读取真实图库。
- 十一组 Python Chromium 流程与世界地图 Node 流程全部通过，协议包 37 项和
  `RemoteHTTPServerTests` 44 项全组通过；签名 Debug App 已重启为新 Host 进程，本机
  `127.0.0.1:8788` 实际返回 `20260806-42`。重启使原网页会话失效，真实账号页只验证到 401
  登录门，不猜测或重放用户密码。

## 2026-08-06 标签与审核双通道撤销切片验证记录

- Host 把原先共享的最近撤销槽拆成标签和审核两个进程内槽；标签批量决定与新增并应用只更新标签
  槽，审核 P/X 只更新审核槽。`/v1/tag-decisions/undo` 与新增的
  `/v1/review/decisions/undo` 分别校验本通道凭据、恢复原决定快照并保持 operation ID 幂等。
- 网页桌面标题栏补齐与 Mac 同义的“撤销标签操作”和条件式“撤销审核操作”；全屏审核标题栏原位
  提供审核撤销，离开审核后全局入口仍可用。390px 把审核撤销收束为可见图标，命令面板保留两条
  同名命令；Toast 记录凭据类型，不再把审核撤销发送到标签端点。
- 合成 Chromium 流程在三项审核批量拒绝后验证审核按钮、端点、冻结 undo ID、刷新恢复和 390px
  可见边界；Facade 定向测试验证先做标签再做审核后两边仍可分别恢复一次。全部使用虚构 UUID、
  内存响应和 1px 合成图片，不连接或读取真实图库。
- 十一组 Python Chromium 流程与世界地图 Node 流程全部通过；协议包 38 项、
  `RemoteHTTPServerTests` 44 项和 `RemoteCatalogFacadeTests` 36 项全组通过。签名 Debug App 已重启
  为 PID 58675，本机 `127.0.0.1:8788` 实际返回 `20260806-43`；重启后的会话端点按预期返回 401
  登录门。

## 2026-08-06 视频卡悬停预览切片验证记录

- 普通视频图库卡补齐 Mac 已有的播放标识、时长和悬停静音播放。悬停只复用
  `/v1/assets/{id}/media` 与现有账号内存凭据桥，不新增下载协议或浏览器媒体缓存。
- 同时只允许一个悬停播放器；180 ms 延迟避免掠过时加载。离开、滚动离屏、切换媒体、打开单图、
  窗口失焦或退出会话均暂停、清空 `src` 并移除元素；减少动态效果与触控环境不自动播放。
- 合成 Chromium 使用可解码的小型 MP4 验证 `0:12` 徽标、静音循环、无控件自动播放、离开释放、
  选择和 `scrollTop` 保持，以及随后打开单图仍正常；测试使用虚构 UUID 和合成媒体，不连接或读取
  真实图库。
- 十一组 Python Chromium 流程与世界地图 Node 流程全部通过，打包网页契约 XCTest 通过；签名
  Debug App 已重启为 PID 61479，本机 `127.0.0.1:8788` 实际返回 `20260806-44`，重启后的会话
  端点按预期返回 401 登录门。

## 2026-08-06 世界地图位置回填切片验证记录

- Web 地图标题栏补齐与 Mac 同名的“更新照片位置”。面板沿用 Mac 的标题、安全说明、来源卡、
  阶段徽标、覆盖进度、已检查/已定位/无坐标指标，以及开始、重试、取消和完成状态；390px 下收束
  为单列可滚动面板，不另造一套交互语言。
- Host 新增只读状态与幂等命令路由，直接复用 `WorldMapLocationBackfillSnapshot` 和现有 reconcile
  控制。浏览器每 1.5 秒刷新已打开面板，关闭仅停止 UI 轮询；网络重试复用 operation ID，不重复
  开始或取消。打开地图或面板绝不自动扫描。
- Remote Protocol 38 项、Mac Debug 构建、Facade/HTTP 两项定向测试通过。十二组合成 Chromium
  流程全部通过；世界地图流程验证开始、取消、进度收束、关闭后停止轮询、重新打开恢复状态、
  原地图预览连续性和 390px 无溢出。首次并发运行时 onboarding 焦点断言受机器负载影响失败，
  独立重跑通过。所有媒体、来源、UUID 和进度均为合成数据，未连接或读取受保护真实图库。
- Apple Development 签名 Debug App 已重启为 PID 67042，本机 `127.0.0.1:8788` 实际返回
  `20260806-45`，位置回填端点和会话端点在重启后均按预期返回 401 登录门；未调用真实来源命令。

## 2026-08-06 世界地图地点标签切片验证记录

- 地图标题栏新增与 Mac 同名的“补全地点”，替换原来“仍需在 Mac 端确认”的功能断层。面板沿用
  Mac 的标题、隐私说明、标签/分组/照片数、五态徽标、地点描述、缓存来源提示、重新搜索与候选卡；
  搜索完成或确认候选后只替换当前卡并刷新地图聚合，不退出地图工作区。
- Host 新增只读 `/v1/world-map/place-tags` 与幂等 `/v1/world-map/place-tags/requests`。搜索请求只含
  operation ID、tag ID 和规范化地点描述；确认请求只含 Host 候选 place ID。响应不含照片、路径、
  Photos identifier、资产 ID 或原始 GPS。现有 Mac resolver、候选上限、GPS 优先和 GRDB 原子写入
  保持为唯一实现。
- Remote Protocol 38 项通过；Facade 地点搜索/确认/回放定向测试、HTTP 鉴权/路由测试和打包网页
  契约测试通过。十一组 Python Chromium 回归与世界地图 Node 流程全部通过；新流程覆盖 161 字
  描述留在浏览器、空白规范化、Enter 搜索、两个候选确认、九卡长列表 `scrollTop` 保持、输入焦点
  恢复、地图原位刷新和 390px 无横向溢出。全部为合成标签、候选和 UUID，未访问受保护真实图库。

## 2026-08-09 验证记录

- 审核来源筛选按 Mac `ReviewSourceFilterMenu` 对齐：`nil` 表示全部活动来源，菜单可任意组合来源，
  也允许暂时清空；审核范围不再绑定图库侧栏当前来源。空选择在网页本地显示空总览/空队列，避免
  远程协议把空数组解释成全来源；来源失效时只与当前活动来源求交集。
- 同一审核范围贯通总览、队列、按标签生成个人建议和 Feature Print 训练配置。训练入口显式提交
  `selectedSources` 范围，不再虽然携带来源 ID 却回落到全来源；从训练记录回到审核时仍可在目标
  标签不属于当前范围的情况下恢复全来源并定位。
- 菜单在请求期间保持打开，使用事件原始传播路径避免异步重绘被误判为外部点击；来源项重建后
  恢复同一键盘焦点，支持方向键、Home/End 与 Escape。宽屏和 390px 截图检查通过，窄屏以工具栏
  图标收敛，弹层左右均未越出视口。
- 审核标题栏补齐 Mac 的“每标签上限”步进器，范围继续由共享工作区偏好约束，步长 50；个人抽检、
  按标签个人建议、Feature Print 和标准模型共用同一上限。旧 Host 缺少可写字段时只读显示现有
  投影，不伪造写入。
- 每个审核标签卡按 Mac 增加“门槛与生成”折叠区：三轨生效门槛可直接输入或以 0.05 步进，显示
  继承/单独设置状态，可采用 Host 参考值或恢复全局默认。显式“刷新”通过幂等设置命令按当前生效
  门槛删除低分待审项，不修改门槛、不重新扫描图库；完成后重新读取审核总数并保持折叠、焦点和
  当前来源范围。
- 十一组 Python Chromium Web Companion 回归及世界地图 Node 流程全部通过；新增断言覆盖任意
  来源子集、空选择不发全库请求、全选恢复、总览/队列查询一致、个人建议预填、训练启动请求、
  卡片阈值编辑、参考/继承、低分刷新、异步焦点保持及 390px 无横向溢出。Remote Protocol 38 项、
  Facade 幂等映射与共享阈值仓储两项定向 XCTest 通过；JavaScript 语法、全部 Python 浏览器脚本
  语法、551 个 HTML ID / 531 个 JavaScript ID 引用完整性和 Git whitespace 检查通过。
- 本轮仅使用合成来源、标签、任务、Run、资产和临时静态服务；未启动或重启真实 App/8788 Host，
  未读取或写入 `/Volumes/HDD2` 受保护真实图库。新版 Host 的已登录真实运行验收仍待后续完成。

## 2026-08-09 审核本地模型面板验证记录

- 网页审核总览左侧按 Mac `ReviewLocalModelPanel` 补齐三层紧凑卡片：本地模型服务、标准模型全库
  建议、个人模型全库建议或 App 内抽检。视觉继续使用 Mac 风格的层级、边框、状态徽章和紧凑
  操作区；宽屏与 390px 均不横向溢出。
- 新增 `/v1/library-suggestions` 权威投影和 `/v1/library-suggestions/requests` 启动命令。浏览器只提交
  媒体类型、标准/个人轨道与顶部审核来源范围；Host 复核模型能力、标准标签包和个人模型身份，复用
  现有建议任务及通用任务动作。网页不启动服务、不下载模型、不读取本机文件，也不伪造完成态。
- 标准模型与个人模型全库任务显示真实扫描、建议、跳过和错误信息；运行态动作复用 Host 的
  暂停/继续/取消投影。旧 Host 不声明新 capability 时保留原 App 内抽检路径，避免兼容回退把空
  来源或缺失能力误解释为全库任务。
- Remote Protocol 40 项全部通过；新增两个服务级 XCTest 验证标准包安装、个人模型身份、来源子集
  与 operation ID 重放，两个 HTTP/资源 XCTest 验证 loopback GET/POST、动作投影和打包页面契约；
  Chromium 回归验证服务状态、标准/个人启动、进度、暂停/继续/取消以及 390px 布局。
- 本记录仍是合成 Host 与本机 loopback 自动化证据；运行中的 8788 App 未重启，因此不把它表述为
  新版真实图库或公网验收。后续重启新版 App 后还需在已登录网页复核真实任务状态与长时间刷新稳定性。

## 2026-08-09 单图红心操作验证记录

- 网页全屏工具栏补齐 Mac `SinglePhotoNavigationBar` 的红心按钮，位置位于标题右侧、关闭按钮之前；
  未收藏显示空心，已收藏显示红色实心，等待/失败同步沿用 Mac 的状态徽章语义。
- 图库上下文直接复用已载入资产的红心投影；审核、图库瘦身和地图上下文在进入或前后切换时按需读取
  `/v1/assets/{assetID}`，只更新全屏红心缓存，不改变图库选择、分页、来源范围、滚动或当前工作区。
- 点击继续复用 `/v1/favorites` 的幂等 Host 命令。收藏结果同步回网格、检查器和全屏；从“红心收藏”
  取消当前项时选择下一项继续预览，无剩余项才返回原工作区。加载中、离线或写入期间按钮禁用。
- Chromium 覆盖图库单图往返收藏和审核全屏按需详情两条路径；均验证全屏不关闭、标题/队列不跳转、
  单资产请求正确。打包资源 XCTest 与 JavaScript/Python 语法检查通过。

## 2026-08-09 网格直接红心验证记录

- 图库缩略图改为合法的可访问组合结构：每个网格单元由完整覆盖的主选择/预览按钮和独立红心按钮
  组成，不在原生按钮内嵌套按钮。主按钮继续承载文件名、来源、媒体类型、标签摘要、`aria-pressed`
  和键盘焦点，容器继续作为分页复用、框选命中和视频悬停的稳定身份。
- 红心位于右上角，选择勾移到左上角；已收藏、悬停、键盘聚焦、单选/多选时显示，390px 或粗指针
  设备上常驻。等待/失败同步沿用原状态徽章，离线和写入期间禁用。
- 点击或 Enter 只执行当前资产的幂等红心命令，不改变单选/多选、不开全屏或检查器、不改变已载入
  数量和滚动位置；“红心收藏”范围取消当前项后把焦点交给相邻网格主按钮。
- Chromium 三条回归覆盖鼠标、键盘、390px 触控路径、批量选择、自动分页、视频悬停和双击全屏；
  页面及控制台无错误。打包资源契约由直接 `xctest` 通过；标准 Xcode runner 完成编译后因等待本机
  测试 worker 未进入用例，已只停止该测试 Host，保留用户正在运行的 App。

## 2026-08-09 审核队列直接红心验证记录

- 审核队列卡片按 Mac `PersonalizationReviewUI` 改为合法的兄弟按钮结构：主按钮继续承载普通点击、
  Command/Shift 多选、框选、双击单图和键盘焦点，红心独立位于右上；选择勾移到左上，两个状态不
  重叠。悬停、选中、键盘聚焦或已收藏时显示，390px/粗指针设备常驻。
- 审核队列投影新增可选红心状态；新版 Host 批量读取当前页状态，旧 Host/旧 JSON 缺字段仍可解码。
  直接红心继续走共享 `/v1/favorites` 幂等命令，更新网格、审核、检查器与全屏缓存，不复制状态源。
- 鼠标点击和 Enter 往返均保持冻结审核选区、主项目、Shift 锚点、P/X/U 目标、右侧详情和队列
  `scrollTop`，也不会打开单图。队列后台刷新指纹包含红心及 Photos 同步状态，外部变化可原位呈现。
- Chromium 覆盖直接红心、审核 P/X/U、多选、框选、Command-A、撤销、跨页单图以及图库红心回归；
  390px 截图无横向溢出，页面/控制台无错误。Remote Protocol round-trip、Facade 投影、打包资源
  契约和 macOS `build-for-testing` 均通过。全部使用合成 UUID/图片，未连接或读取受保护真实图库。

## 2026-08-09 世界地图照片条直接红心验证记录

- 与 Mac `WorldMapPhotoCard` 对齐，地点照片条每个 76px 卡片由主预览按钮和右上独立红心组成；悬停、
  键盘聚焦或已收藏时显示，390px/粗指针设备常驻。文件名仍留在底部，红心不会截断地点照片标签。
- 地图选择投影新增可选红心状态，Host 对当前地点返回的有限资产批量读取；旧 JSON 缺字段仍可解码，
  旧 Host 不显示无依据按钮。直接操作复用 `/v1/favorites`，同步回地点条、全屏缓存及其他已载入表面。
- 鼠标和 Enter 往返保持照片塔选择、地点详情、横向 `scrollLeft` 与全屏关闭状态；点击主表面仍进入
  “返回地图”单图，关闭后焦点和地图上下文不变。
- 完整世界地图 Node Chromium 流程通过，同时覆盖 renderer ready、视口合并、地点照片下钻、位置
  回填、地点候选搜索/确认、长列表焦点和 390px；新增 2 次红心命令，页面与控制台无错误。Remote
  Protocol、World Map Facade、打包资源和增量 `build-for-testing` 通过，均使用合成数据。

## 2026-08-09 图库瘦身直接红心与框选验证记录

- 分析成员卡片改为合法的兄弟按钮结构：主表面继续负责普通点击、Command/Shift 多选、双击和空格
  单图；独立红心位于右上，视频时左上显示播放标识与时长。红心往返不改变选区、Shift 锚点、检查器、
  滚动或单图状态，悬停/选中/键盘聚焦时显示，390px 与粗指针设备常驻。
- 瘦身成员补齐与 Mac `LibraryGridMarqueeContainer` 同义的空白拖拽框选；拖动过程局部更新命中卡片，
  Command/Ctrl 可追加，结束后才刷新完整检查器。等待 Mac 确认和正在处理的回收成员显示同义遮罩，
  禁止选择或红心，避免在冻结删除目标上制造互相矛盾的操作反馈。
- 回收站缩略图补齐视频标识与直接红心。文件夹与 Photos 状态均来自共享 Host 收藏投影；按钮帮助和
  无障碍文案明确提示红心只用于整理，不会阻止恢复、回收或永久删除，Photos 的保留与永久删除继续
  由系统“照片”App 管理。红心操作不会触发恢复、永久删除确认或原始文件访问。
- Remote Protocol 的成员与回收 DTO 使用可选红心字段，旧 JSON 缺字段仍可解码；Facade 对当前有限
  成员/回收页批量读取权威状态。两项 round-trip、两项 Facade、打包资源 XCTest、macOS
  `build-for-testing` 和完整 selection-tools Chromium 流程通过；桌面与 390px 截图无横向溢出。
- 候选成员补齐与 Mac 同义的右键和 Shift-F10 菜单。打开菜单不改变现有多选；目标已选时回收动作
  复用冻结选区，目标未选时只在真正执行动作后切换为单项。菜单提供直接红心、可恢复回收和快速
  释放空间，方向键、Home/End、Escape 与焦点恢复均可用。
- 瘦身工作台复用图库同一九档密度和“适应/填充”偏好，切换时保持任务、分组、选区、Shift 锚点、
  滚动和回收状态。桌面滑杆具备稳定内在宽度；390px 收敛为单图标但保留完整无障碍名称，分析页和
  回收页标题栏均无截断或横向溢出。
- 自动化只使用两个合成资产、合成回收记录和临时 `8802` 静态服务；未重启运行中的 `8788` App，
  未读取或写入 `/Volumes/HDD2`。新版已登录真实 Host 的视觉与长时间刷新验收仍待后续完成。

## 2026-08-09 图库瘦身回收范围验证记录

- 回收站标题栏按 Mac `LibrarySlimmingRecycleScope` 补齐“全部 / Photos / 文件夹 / 待处理”四种范围及
  权威计数；范围与媒体类型、来源和文件名搜索组合，切换范围不会清空另外三个条件。
- Host 先按媒体、来源和搜索建立完整上下文并计算四种计数，再应用所选范围，最后才截取页大小；
  因此首批页面即使被其他范围项目占满，也不会把目标范围错误显示为空。待处理严格对应非
  `recycled` 状态，涵盖失败、未完成及等待协调项目。
- 旧 Host 缺少 `scopeCounts` 时网页回退到“全部”并禁用无权威依据的高级范围，不对当前已载入页
  伪造筛选。Photos 永久删除和恢复仍留在系统“照片”App，网页没有新增原始文件访问入口。
- Chromium fixture 覆盖四范围计数、鼠标与键盘切换、来源/搜索保持和查询参数；纯服务投影 XCTest
  以 Photos 首项、文件范围 `limit = 1` 证明先筛范围再分页。协议 round-trip、Facade/HTTP 路由、
  打包资源、增量 macOS `build-for-testing`、JavaScript 与 whitespace 检查通过。
- 自动化只使用合成回收记录；未重启运行中的 `8788` App，未读取或写入 `/Volumes/HDD2`。新版已登录
  真实 Host 下的四范围计数、长列表渐进载入和刷新保持仍待后续运行验收。

## 2026-08-09 图库瘦身分析导航显隐验证记录

- 分析标题栏补齐 Mac `showsAnalysisNavigator` 同义按钮，可整体隐藏/显示分析记录栏和候选分组栏；
  隐藏后成员检查器、选择工具栏与网格使用全部工作区宽度，回收站不显示无关控制。
- 显隐只改变布局，不重新请求 Host 或重建成员列表，因此当前任务、候选分组、选区、Shift 锚点、
  成员滚动、密度、宽高比和等待 Mac 确认状态均保持；按钮持续恢复焦点并更新可访问名称与帮助。
- Chromium 在 1440px 验证成员区域实际变宽、两侧栏隐藏/恢复及状态保持，在 390px 验证单行工具栏
  完整可达、成员区占满剩余高度且页面无横向溢出。打包资源契约、JavaScript/Python 语法、增量
  macOS `build-for-testing` 和 whitespace 检查通过。
- 自动化继续使用合成资产与临时 `8802` 服务；未重启运行中的 `8788` App，未访问受保护真实图库。

## 2026-08-09 图库瘦身 Delete 快速清理验证记录

- 分析工作台补齐 Mac 的 Delete / Forward Delete 快速清理入口。普通网格冻结当前选区；单图预览
  始终只冻结当前可见成员，不会误用打开预览前保留的多选。按钮、右键菜单和快捷键继续提交同一
  Host `releaseSourceSpace` 命令，并保留网页确认与 Mac 原生二次确认两层安全门。
- 网页为预览提交保存本次原分组顺序，但不伪造回收结果；只有 Host 审计明确返回当前资产已隐藏后，
  才优先进入原分组下一项，否则上一项。资产未隐藏、用户取消、Mac 取消或失败时预览保持原位；
  原分组不再存在或没有剩余成员时关闭预览，不跳回网格顶部。
- Chromium 用三项合成视频验证：旧多选包含两项时从中间项预览按 Delete，请求只含中间项；取消不
  提交且预览/选区原位；Host 完成后进入第三项并显示 `2 / 2`，网格同步选择替代项。纯替代算法同时
  覆盖下一项、末项回退和无剩余项，页面与控制台无错误。
- 本切片没有新增文件系统或 PhotoKit 入口；文件来源永久删除与 Photos“最近删除”仍完全由 Mac Host
  现有服务和原生确认执行。自动化使用空视频响应、合成 UUID 与临时 `8802` 服务，未重启真实 App。

## 2026-08-09 图库瘦身分组审阅与记录上下文验证记录

- 分析导航按 Mac `LibrarySlimmingClusterQueueScope` 补齐“待处理 / 已确认 / 已忽略”三条 Host 权威
  队列及完整计数；Host 在分页前按持久审阅状态筛选，网页不会从当前已载入分组推算总数。旧 Host
  缺少 `clusterScopeCounts` 时只保留待处理队列并禁用高级范围，不伪造确认或忽略状态。
- 每个候选分组右侧固定显示“已确认”和“忽略”，保存期间只锁定该分组。写入后重新读取当前队列，
  分组按 Host 终态移出；已确认或已忽略分组的详情标题显示状态并可“重新处理”回到待处理。所有写入
  使用 operation ID 幂等命令，并验证分组确实属于指定分析任务。
- 分析记录补齐与 Mac 同义的右键及 Shift-F10 菜单。打开菜单不会改变当前任务；取消恢复右键目标
  焦点，删除未选记录继续保留当前任务，删除当前记录则采用 Host 返回的第一条剩余记录。运行中的
  任务不暴露删除操作，删除记录仍明确不会删除任何原始媒体。
- Remote Protocol round-trip、Facade 权威范围与幂等写入、HTTP POST 路由、打包资源三项定向 XCTest
  和 macOS `build-for-testing` 通过。完整 selection-tools Chromium 流程验证鼠标/键盘上下文菜单、
  取消与删除焦点、三队列计数、确认、重新处理、Home/End、桌面与 390px 布局，页面及控制台无错误。
- 自动化只使用合成任务、分组、资产和临时 `8802` 服务；测试包装器已停止临时服务。未重启运行中的
  `8788` App，未读取或写入 `/Volumes/HDD2` 受保护真实图库。

## 2026-08-09 图库瘦身历史处理记录验证记录

- Host 在隐藏资产投影后保留已确认、已忽略分组的原始身份和原始成员数；即使当前只剩一项或零项
  可查看成员，仍会进入相应历史队列。待处理分组继续要求至少两个当前可比较成员，种子分析继续要求
  至少一个当前成员，不会因历史兼容放宽新候选条件。
- 远程分组新增可选 `originalMemberCount` 与 `isHistoricalProcessedRecord` 字段，旧 Host 缺少字段时网页
  仍按当前成员数显示。历史行显示“原 N 项 · 现 M 项可查看”；零成员时不请求已失效缩略图，并在
  详情展示与 Mac 同义的“历史处理记录”空状态。
- 历史记录仍可“重新处理”。当前不足两项时，Host 清除旧处置后该记录退出审阅队列，网页明确提示
  当前无需返回待处理；具备至少两个当前成员时则正常回到待处理队列。
- Remote Protocol round-trip、Facade 隐藏成员投影、打包网页资源契约、macOS `build-for-testing` 和
  selection-tools Chromium 流程通过。浏览器覆盖零成员已确认记录、一成员已忽略记录、历史空状态、
  原/现计数与重新处理，并目视检查桌面和 390px 布局。
- 自动化只使用合成资产和临时 `8802` 服务；未重启 `8788` App，未读取或写入 `/Volumes/HDD2`。

## 2026-08-09 图库瘦身完整分析历史验证记录

- Mac 会读取并展示当前媒体类型的全部持久分析记录；网页不再静默截断前 100 条。Host 接受有上限的
  `jobLimit` 并返回可选 `totalJobCount`，旧 Host 缺少总数字段时网页回退到当前返回数量。
- 网页初始读取 100 条，显示 Host 完整总数和剩余数量，可用“载入更多记录”按 100 条扩展；位于
  当前窗口末尾时按下一条会先扩展窗口，按 End 会扩展到完整历史再定位最后一条。已选任务、分组、
  成员选择和滚动在单纯扩展历史窗口时保持。
- 从任务面板深链到 100 条之外的旧记录时，Host 按原有顺序把前缀扩展到目标记录，不在列表末尾
  追加顺序错误的孤立记录；随后上一条、下一条和位置计数继续可信。
- Facade 使用 102 条合成记录验证两条初始窗口与第 102 条深链；Chromium 使用 121 条合成记录验证
  `2 → 102 → 121` 渐进载入、End 导航和完整总数。桌面截图确认 260px 记录栏的标题、位置和删除
  操作保持单行紧凑布局。
- Remote Protocol round-trip、Facade/资源契约 XCTest、macOS `build-for-testing` 与完整
  selection-tools 浏览器流程通过。自动化未访问 `/Volumes/HDD2`；真实已登录 Host 仍需单独验收。

## 2026-08-10 全屏预览缩放与拖拽验证记录

- 网页的共享全屏预览不再只是静态 `object-fit: contain`：图库、审核、世界地图和图库瘦身入口使用
  同一套 100%～800% 视口状态。图片可用工具栏加减按钮、滚轮 / 触控板、双击以及 `+`、`-`、
  `0` 快捷键缩放；放大后按住左键拖动，位移按适应后图像尺寸与可视区域约束，不允许把整张图片
  拖离画布。
- 切换上一张、下一张或关闭预览时缩放与位移归零，防止上一张的视口污染下一张。视频继续使用原生
  播放控件并隐藏图片缩放组；预览层采用与 Mac 图库瘦身大图查看一致的 96% 黑色遮罩。
- 图库瘦身预览工具栏补齐 Mac 同位置的红色删除入口，桌面显示“删除”，390px 收束为可辨识图标；
  它和 Delete 键共用既有当前预览删除契约，只冻结当前可见资产，并继续经过网页风险确认、Mac 原生
  二次确认和 Host 身份核验。提交期间入口立即禁用，完成后的下一项优先/末项回退语义不变。
- Chromium 使用 1200 × 900 合成 SVG 验证 125% 按钮缩放、滚轮连续缩放、双击 200%、真实指针
  水平拖拽、边界约束、`0` 归位、媒体切换重置和视频隐藏缩放组。390px 视口无横向溢出，工具栏
  全部位于可视范围，截图能直接看到放大及拖拽后的裁切结果。
- Web 资源契约 XCTest 与 macOS `build-for-testing` 通过。自动化只使用临时 `8800` 静态服务和
  合成媒体，未重启当前 `8788` App，未访问 `/Volumes/HDD2`；真实已登录 Host 仍需单独验收。

## 2026-08-10 全部来源更新验证记录

- 来源管理补齐 Mac 侧栏“立即更新所有来源”的跨来源动作，并明确区分“刷新状态”：前者提交新增的
  `refreshAll` Host 命令，后者仍只重新读取当前投影。命令面板也提供同名入口，不要求用户逐个来源
  判断“立即重扫”或“立即同步”。
- `refreshAll` 只接受 `sourceID = nil`，Host 提交时重新读取来源并只选择 `active` 项；全部活跃文件夹
  一次进入既有 reconcile 队列，全部活跃 Apple Photos 来源逐个进入既有同步队列，再分别启动原有
  runner。`unavailable`、`authorizationRequired` 和 `disabled` 来源不会被误报为已更新。
- Host 继续执行全局单活动来源请求约束；相同 `operationID` 重试返回原请求，不重复排队。网页在无
  活跃来源、离线、提交中或另一个来源请求运行时禁用入口，并显示 Host 返回的真实终态消息。
- Remote Protocol round-trip、Facade 映射、Host 服务定向 XCTest、Web 资源契约 XCTest、macOS
  `build-for-testing` 与 Chromium 来源管理流程通过；390px 中连接、Photos 与更新全部入口可见且无
  横向溢出。自动化未访问 `/Volumes/HDD2`，也未重启当前 `8788` App；新版真实 Host 仍待运行验收。

## 2026-08-10 全部来源缓存预热验证记录

- 来源管理和命令面板补齐与 Mac 侧栏同名的“缓存所有来源的缩略图”和“缓存所有来源的原比例缩略图”。
  两个动作均提交 `sourceID = nil`，Host 按来源既有顺序依次处理 `active` 与 `unavailable` 来源，跳过
  `disabled` 和 `authorizationRequired`；同一时刻继续只允许一个来源管理请求运行。
- Host 不把批量入口退化成逐个网页请求：它一次读取每个来源的完整资产窗口，复用正方形或原比例
  缓存清单，只生成当前可处理的缺失照片/视频。进度与 Mac 一样显示当前来源位置、当前来源生成数、
  已复用数、不可处理跳过数和失败数；单个资产或来源失败不阻断后续来源，终态汇总全部来源。
- 取消动作使用 `cancelPrewarm + sourceID=nil` 精确取消整批任务，停止当前及后续来源，并明确说明已经
  生成的缓存仍然保留。相同 operation ID 重试继续返回原请求，不重复生成缓存。
- Remote Protocol 41 项 round-trip、Host 来源管理 8 项 XCTest、Facade/资源契约定向 XCTest、macOS
  `build-for-testing` 和 Chromium 来源管理流程通过；390px 中三个全局来源动作完整可见且无横向
  溢出。自动化只使用合成资产与临时 `8800` 服务，未访问 `/Volumes/HDD2`，未重启当前 `8788` App；
  新版真实 Host 仍待运行验收。

## 2026-08-10 批量来源权限验证记录

- 来源管理补齐 Mac “批量处理权限”折叠区，避免把系统级操作铺成顶层按钮墙。访问授权入口显示当前
  需要重新授权的来源数，目标与 Mac 一致：`unavailable / authorizationRequired` 文件夹，以及
  `authorizationRequired / disabled` Apple Photos；文件夹回收权限入口只统计 `active` 文件夹。
- 两个批量动作分别提交 `reauthorizeAll` 和 `refreshAllFolderMutationAuthorizations`，都要求
  `sourceID = nil`。Host 按当前来源顺序逐个在 Mac 显示系统授权窗口，网页只显示当前来源与完成数量；
  取消任意窗口会停止后续来源，之前完成的授权继续有效。回收权限动作本身不移动、修改或删除照片。
- Apple Photos 写入权限继续使用系统图库统一的一次性授权，并在同一折叠区复用既有 Host 命令。
  删除来源没有进入批量协议，仍必须逐来源经过网页风险确认和 Mac 原生确认。
- Remote Protocol round-trip、Host 来源管理 11 项 XCTest、Facade/资源契约定向 XCTest、macOS
  `build-for-testing` 和 Chromium 来源管理流程通过；390px 展开批量权限区后无横向溢出，来源列表仍
  可独立滚动。自动化只使用合成来源，未访问 `/Volumes/HDD2`，未重启当前 `8788` App；真实系统
  选择器与新版 Host 仍待运行验收。

## 2026-08-10 审核队列共享缩略图布局验证记录

- Web 审核队列补齐 Mac 已有的网格密度和原比例入口，并与图库、图库瘦身共用同一份本机布局偏好；
  在任一页面调整后，三个页面的控件值、CSS 网格宽度和宽高比状态立即同步，不创建审核专属的第二套
  设置。切换只更新当前可见缩略图，不重建审核队列，当前标签、主选项、多选集合、锚点和滚动保持。
- 审核队列投影新增向后兼容的可选 `width / height`，Host 从现有资产记录同查询返回，网页据此让横版、
  竖版和宽银幕卡片像 Mac `ReviewThumbnailView` 一样使用真实画幅；旧 Host 缺少字段时继续使用方形卡片。
- `/v1/assets/{id}/thumbnail?aspect=original` 优先读取 Mac 已生成的原比例缓存，不在普通浏览请求里擅自
  扫描原片或生成持久缓存；缓存缺失或旧实现不支持时回退已有方形缩略图。图库、审核、瘦身成员和
  回收站的可见缩略图统一使用该请求语义。
- 390px 标题栏改为两行紧凑布局，审核标签、来源范围、每标签上限、原比例切换和刷新均通过真实元素
  边界断言保持可见可点；截图确认 4:3、3:4、16:9 卡片和详情面板没有横向溢出。
- Remote Protocol 41 项、Facade 原比例缓存/回退、Host HTTP 路由、GRDB 审核宽高投影、Web 资源契约、
  macOS `build-for-testing` 与 Chromium 完整主流程通过。自动化只使用临时 `8800` 服务与合成图像，
  未重启当前 `8788` App，未访问 `/Volumes/HDD2`；新版真实 Host 的原比例缓存仍待运行验收。

## 2026-08-10 照片与视频独立会话恢复验证记录

- Web 的照片与视频主模式各自冻结来源范围、图库范围、搜索、排序、完整筛选、已载入资产窗口与
  `nextCursor`、单选/多选、锚点、检查器详情和滚动位置；切换媒体后先同步恢复原 DOM，再向 Host
  读取权威增量，避免短暂回到首屏或把另一种媒体的选择带进来。
- Chromium 使用 18 张分两页照片和 17 个分两页视频建立两个不同会话：照片保留原筛选、原检查器
  和 240px 滚动，视频另设 `CLIP` 搜索、最早优先、选中详情和 180px 滚动；连续执行
  `照片 → 视频 → 照片 → 视频 → 照片`，每次均逐字段核对来源、筛选、排序、资产顺序、分页窗口、
  选区、锚点、检查器和实际滚动值完全恢复。
- 同一流程继续覆盖视频悬停静音播放、全屏视频、审核原比例布局、局部筛选和 390px 响应式界面；
  临时 `8800` 服务结束后已自动停止。自动化未重启 `8788` App，未访问 `/Volumes/HDD2`；真实已登录
  Host 上的大图库长距离滚动仍待运行验收。

## 2026-08-10 训练记录导航与并发启动保护验证记录

- 训练记录栏不再因点击 Run 重建 DOM 后跳回顶部。Web 按媒体、方法筛选和“全部 / 批次 / 单项”
  分别保存滚动位置；切换范围后返回、同一 Run 静默刷新以及关闭再打开训练工程均恢复原记录位置，
  同时保留选中行和 roving focus。
- 详情区区分“刷新同一 Run”和“进入另一 Run”：前者在重新投影数据、配置与结果后恢复原阅读位置，
  后者固定从概览顶部开始，避免用户打开新记录却落在上一条的技术 JSON 中间。
- Web 与 Mac 使用相同的单活动个人训练门：Host 投影存在进行中训练时，“新建任务”立即禁用，N
  快捷键和直接函数入口也不能绕过；完成、失败或取消后入口恢复。若设置窗口打开期间外部开始训练，
  启动按钮会禁用并解释原因。
- Chromium 用 39 条训练记录验证深滚动点击、同 Run 刷新、详情滚动、记录范围往返、关闭再打开和
  活动训练安全门，完整训练恢复/键盘流程通过且 console/page/resource error 为 0。测试只使用临时
  `8805` 服务和合成训练账本，服务结束后已停止；未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 来源侧栏直达入口验证记录

- Web 来源侧栏不再把首次连接入口只藏在“•••”管理面板。与 Mac 相同，来源列表下方始终直接显示
  “连接文件夹…”；尚无 Photos 来源时同时显示“连接 Apple Photos…”，已有活动 Photos 时原位显示
  “已连接 Apple Photos”。已有但待恢复的 Photos 不显示重复连接入口，继续从对应来源行处理。
- 两个侧栏入口不复制来源业务：点击后仍打开既有来源管理面板，提交相同 `connectFolder` 或
  `connectPhotos` 请求，并在 Mac 经过原生说明、系统选择器和权限确认。活动来源请求会禁用两个入口；
  关闭面板时，触发点可用则原位恢复焦点，否则回到同一“来源”标题旁的管理按钮。
- 侧栏“图库”顺序同步 Mac：图库总览、照片世界、全部媒体、红心收藏、无标签、待审核建议、训练工程、
  图库瘦身；不再把“世界地图”插在基础图库筛选之后，也使用 Mac 的“照片世界”用户名称。
- Chromium 空库流程验证侧栏文件夹与 Photos 各提交一次权威请求、活动 Photos 状态、首次使用动作、
  常用标签安装、焦点回退和 390px 抽屉边界，console/page error 为 0。桌面与窄屏截图已人工核对；
  临时 `8807` 服务结束后自动停止，未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 侧栏连续键盘导航验证记录

- Web 侧栏把图库入口、动态来源行和“连接文件夹 / Apple Photos”动作串成与 Mac `List` 一致的
  连续键盘路径；`↑ / ↓` 可跨分组逐项移动，`Home / End` 到达首末可用入口，跳过隐藏或禁用项目。
  这只移动焦点，不切换图库范围，按 Enter / Space 后才执行当前入口。
- 来源行既有 `Option-↑ / Option-↓` 排序保持不变；标签侧栏继续使用标签组、标签芯片自己的方向键
  语义，不被图库/来源导航接管。移动端抽屉打开后直接聚焦当前 `aria-current` 范围，使键盘和辅助
  技术用户无需从页面顶部重新 Tab。
- Chromium 空来源与双来源流程验证 `图库总览 → 照片世界`、图库末项与首个来源的跨界移动、
  `End → 连接来源`、`Home → 图库总览`，以及 390px 抽屉聚焦当前范围；两个流程均通过且临时
  `8806 / 8807` 服务已自动停止。自动化未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 图库个人建议直达入口与搜索语义验证记录

- 以当前运行 Mac 图库工具栏为准，Web“个人模型”菜单补齐第三个高频动作“抽 500 张生成建议”；
  Host 提供全库个人模型能力时同一位置改为“使用个人模型扫描全库”。入口不复制任务逻辑，分别
  复用既有抽检或全库建议请求，并进入同一待审核建议总览；视频、离线或已有活动任务时原位禁用。
- 图库搜索框和命令面板统一采用 Mac 文案“搜索文件名、路径、标签或来源”。这不是扩大网页私有
  搜索范围：查询继续传给 Host 的 `AssetPageFilter.searchText`，由目录仓储同时匹配文件名、相对路径、
  来源显示名和标签名。
- Chromium 验证个人模型菜单 `↑ / ↓` 可到达第三项，Enter 提交空资产选择和当前审核来源范围、
  打开审核总览并能正常返回，随后既有个人模型训练与 39 条长记录导航全流程继续通过；临时 `8805`
  服务结束后自动停止。自动化未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 Mac 全局状态横幅与上下文动作验证记录

- Web 通过 `workspaceNotices` 能力读取 Mac 当前全局提示，并按信息、成功、警告三种类型显示同层级
  状态横幅；投影只包含稳定版本 ID、预格式化安全文案和类型化动作，不暴露错误对象、绝对路径、
  书签或 Photos 标识。旧 Host 没有该能力或旧响应缺少 `actions` 时继续正常工作。
- Mac 的标签批量操作提示会投影“撤销”，由 Host 校验精确 notice 版本与当前撤销资格后执行；来源
  删除被回收项阻挡时投影“前往回收站”及来源 ID，由 Host 先校验提示仍有效，再让 Web 打开自身
  回收站并应用同一来源筛选。Web 不接受通用 URL、命令名或任意参数。
- 关闭或执行动作都绑定精确 notice ID；如果请求期间 Mac 产生新提示，旧请求返回 `performed=false`
  并保留新提示。动作进行中会原位禁用其他动作和关闭按钮，后台投影刷新不会覆盖正在处理的横幅；
  标签撤销后只增量刷新资产、标签和审核投影，不重建已载入分页窗口或滚动位置。
- Remote Protocol 42 项测试、5 项相关 XCTest、Debug `build-for-testing` 与完整 Chromium
  筛选/审核/预览流程通过。浏览器流程覆盖陈旧动作保护、精确来源回收筛选、标签撤销和 390px
  无横向溢出；临时 `8800` 服务结束后自动停止。未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 统一搜索与零结果恢复验证记录

- Web 零结果页面对齐 Mac `ContentUnavailableView` 恢复语义：只有搜索时直接“清除搜索”；
  组合搜索、标签条件、可用状态或文件格式时，可逐组清除或一次“清除全部条件”。
  每个动作只修改对应查询维度，保留当前照片/视频、来源和排序范围，完成后把焦点返回
  搜索框或筛选按钮。
- 修复了一个状态投影缺口：当两次权威查询都返回空数组、但查询条件已改变时，旧实现只比较
  资产指纹而不重绘空状态。现在同时跟踪已渲染查询签名，所以“搜索→搜索+标签+状态”即使仍为
  0 项，说明、图标与恢复按钮也会立即更新。
- Chromium 定向流程覆盖搜索单条件、搜索+标签+状态组合、逐组清除、一次清除全部、
  查询参数与焦点恢复；桌面和 390px 布局无横向溢出。本切片仅修改 Web 资源和合成浏览器测试，
  未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 全屏工作区统一返回与浏览器历史验证记录

- 图库总览、照片世界、建议审核、训练工程和图库瘦身统一采用 Mac 风格显式返回按钮；普通入口显示
  “返回图库”，训练与审核互相深链时继续显示“返回训练记录 / 返回建议审核”。按钮、Escape 与浏览器
  Back 共享同一历史返回路径，浏览器 Forward 可重新打开原工作区，不再出现页面视觉关闭但浏览器
  历史仍停留在旧层级的分叉行为。
- 每次登录工作区建立带会话代次的图库历史根；过期会话条目不会在新登录中重新打开旧工作区。
  连续返回按顺序处理，重复点击复用同一次返回，避免两个异步工作区同时恢复。总览来源/标签/收藏
  下钻和瘦身任务深链会先完成历史返回，再改变图库条件或打开任务，因此底层筛选、选区、检查器与
  滚动不会被全屏工作区关闭过程重建。
- Chromium 分别覆盖图库总览、照片世界、审核/训练双向嵌套和瘦身分析的 Back、Forward、显式按钮
  与 Escape；瘦身 Forward 额外逐字段核对任务、分组、成员选区、锚点以及底层图库选区和滚动。
  390px 瘦身标题栏改为两行紧凑工具栏，完整保留“返回图库”且刷新按钮不越界。图库总览、世界地图、
  训练、选择工具、筛选/审核/预览、通用设置、首次使用预设和单图连续导航共 8 套浏览器流程通过；
  临时服务均自动停止。未重启 `8788` App，未访问 `/Volumes/HDD2`。

## 2026-08-10 图库瘦身来源刷新与相似索引验证记录

- Web “分析选项”复用 Mac `LibrarySlimmingWorkspaceUI` 的来源维护语义：刷新只能提交当前活动来源的
  非空子集；初始化或重建相似索引必须精确选择一个活动来源，并按当前照片/视频类型分别读取状态。
  Host 负责目录重扫、Photos 同步和索引后台执行，网页只接收构建中、就绪、需要重建或失败状态及
  安全计数，不暴露路径、原始错误、资产标识、向量或索引内容。
- Remote Protocol、Facade、HTTP 和 Host Service 新增幂等 `source-maintenance` 纵向链路；同一
  operation ID 重放不会重复排队。刷新同时启动适用的文件夹与 Photos runner，索引构建复用 Mac
  自动 runner；旧 Host 缺少能力字段时隐藏索引动作而不伪造状态。
- 网页采用与 Mac 工具栏一致的紧凑分析选项弹层，来源勾选与“新分析”的目录范围共享选择状态；
  构建期间每 1.5 秒静默读取 Host 状态，关闭后停止轮询，Escape 把焦点恢复到触发按钮。390px
  实测弹层完整落在视口，刷新、来源选择、索引状态和初始化动作均可触达且无横向溢出。
- Remote Protocol 42/42、来源维护 Service 2/2、Facade 1/1、HTTP 1/1、`build-for-testing`、
  JavaScript/Python 语法和 whitespace 检查通过；图库总览、筛选/审核/预览、选择与瘦身、训练恢复、
  通用设置、首次使用、单图连续导航和照片世界共 8 套 Chromium 回归通过，临时服务均自动停止。
  自动化只使用合成来源与媒体，未访问 `/Volumes/HDD2`，未重启当前 App；新版已登录真实 Host 的
  长任务状态、来源计数及刷新保持仍待运行验收。

## 2026-08-10 照片上下文菜单键盘连续操作验证记录

- 普通照片卡片补齐与来源、标签和图库瘦身成员相同的键盘菜单语义：焦点位于卡片时可用
  `Shift-F10` 或 Context Menu 键打开，首个可用动作自动聚焦，方向键循环移动，Home/End 直达
  首末动作，Escape 关闭并按资产 ID 返回原卡片。菜单的无障碍名称包含当前文件名，异步刷新后
  不依赖可能失效的旧 DOM 节点恢复焦点。
- 鼠标右键继续复用同一菜单；打开、移动和取消菜单不会进入选择模式、改变现有选区、重载已载入
  窗口或移动图库滚动位置。执行选择或红心后返回当前仍可见的资产，来源筛选完成后返回对应来源行，
  单图查看仍沿用全屏预览自己的焦点返回链路。
- Chromium 筛选/审核/预览完整流程覆盖键盘打开、自动聚焦、End、方向键循环、Escape、焦点返回、
  零选区和滚动保持，89 次资产查询及既有审核、来源、媒体与红心回归通过；JavaScript/Python 语法
  和 whitespace 检查通过。测试仅使用合成媒体，临时 `8800` 服务已停止，未重启 App，未访问
  `/Volumes/HDD2`。

## 2026-08-10 视频全屏 Mac 系统播放器入口验证记录

- Web 视频全屏预览补齐 Mac `SinglePhotoView` 的“使用系统播放器打开”动作；浏览器内嵌播放器继续
  保留，新增入口只把当前视频的稳定资产 ID 提交给既有受保护 `open-original` Host 路由，由 Mac
  解析权威来源并交给系统播放器。网页不接触原始路径，也不新增文件或 Photos 读取能力。
- 入口仅在视频预览显示；离线、不可用、正在导航或已有打开请求时禁用。桌面显示紧凑“Mac 播放”，
  390px 收为播放图标，包含当前文件名的无障碍名称。成功或失败都保持当前全屏视频、位置、播放器、
  底层选择和滚动，不触发资产窗口重载；检查器与全屏入口共享同一并发门禁和状态文案。
- Chromium 完整筛选/审核/预览流程验证精确 POST、成功提示、全屏保持及 390px 无横向溢出；视觉
  截图确认返回、文件名、Mac 播放、红心和关闭操作完整可达。Web 资源 bare XCTest 1/1 通过，
  JavaScript/Python 语法和 whitespace 检查通过。测试只使用合成 MP4，未重启现有 App，未访问
  `/Volumes/HDD2`。

## 2026-08-10 活动面板手动刷新验证记录

- Web 顶部任务弹层与 Mac `jobActivityPanel` 对齐为“活动”，补齐同名说明、刷新按钮和空状态；
  任务标题、状态、进度、暂停/继续/取消及既有瘦身深链保持不变，没有另造第二套任务模型。
- 顶部工具栏与命令面板也使用 Mac 同名“活动”；每次打开自动读取一次最新投影，点击刷新或在弹层内
  按 `R` 同样只读取 `/v1/jobs`，不会连带重载图库、检查器、来源、审核或训练工作区。
  刷新前按稳定任务 ID 冻结当前任务、任务动作、非零 `scrollTop`；Host 返回后恢复同一上下文。任务
  消失时回退到仍存在的首个任务，无任务时焦点回到刷新按钮。
- 任务读取发生 5xx 时继续保留旧投影并按全局策略标记 Mac 离线；只读刷新按钮变成“重试刷新活动”
  且保持可用，下一次成功读取再恢复在线状态，全部写操作在此期间仍禁用。请求期间按钮显示旋转状态，
  成功和失败均恢复原任务动作或标题栏按钮焦点。
- Chromium 完整训练恢复/键盘流程覆盖按钮与 `R`、成功、503、离线重试、空状态、19 条任务滚动、
  训练工作区焦点返回及 390px 边界，console/page/resource 意外错误为 0；截图人工核对 Mac 风格紧凑
  层级。JavaScript/Python 语法、whitespace 和现有 bare Web 资源 XCTest 1/1 通过。独立
  `build-for-testing` 在包解析后无编译子进程且持续 0 CPU，已安全终止，因此新增 Swift 静态断言仍
  待下一次正常构建执行。测试使用临时 `8805` 服务，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 当前来源立即同步与重扫验证记录

- Web 右上角不再用一个含糊“刷新图库”图标同时暗示两种行为：Mac 同源来源动作单独显示“立即同步”
  或“立即重扫”；原网页投影刷新保留为独立 `⟳`，无障碍名称和命令面板明确改成“重新读取网页数据”。
- 选中 active Apple Photos 时直接提交 `syncPhotos + sourceID`，选中文件夹时提交
  `rescan + sourceID`；没有选中单一来源时按 Mac 全库语义提交既有 `refreshAll + sourceID=nil`，
  Host 继续负责活跃文件夹重扫与 Photos 同步。网页不读取路径，也不复制扫描实现。
- 当前来源不可用、待授权、网页离线或存在任一来源管理请求时，来源动作原位禁用；请求期间图标旋转、
  标题显示 Host 进度。只读网页重载仍可用于重新读取来源投影，因此恢复状态不会错误触发真实扫描。
- Chromium 通用设置/来源恢复完整流程精确验证 `refreshAll / syncPhotos / rescan` 三种请求、来源 ID、
  不可用门禁和原网页刷新语义；既有授权、空状态、上下文菜单和设置回归继续通过。390px 截图及真实
  元素边界确认来源更新、活动、设置、网页重载和退出均可达且无页面横向溢出。测试使用临时 `8806`
  服务和合成来源，JavaScript/Python 语法、whitespace 与现有 bare Web 资源 XCTest 1/1 通过；新增
  Swift 静态断言仍需正常重建测试包后执行。未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 来源扫描持续进度验证记录

- `RemoteJobSummary` 以向后兼容的可选字段投影来源 ID 与显示名称；Host 从任务的 `source_id` 关联
  当前来源摘要，来源读取失败时仍返回任务列表。网页只接收用户可见名称，不接收路径、PhotoKit
  identifier 或底层扫描错误细节。旧 Host 缺少两个字段时继续可解码并回退为通用任务名称。
- 顶部工具栏复用 Mac `catalogProgressTitle` 的语义：有总数时显示“来源 · 已完成/总数”，只有已检查
  数时显示“来源 · 已检查 N 张”，尚无计数时显示“正在扫描来源”。进度按钮直达并聚焦同一个“活动”
  任务；任务完成或失败后进度条退出，终态与失败摘要仍保留在活动列表，不伪造成功提示。
- 活跃扫描期间每秒只静默读取 `/v1/jobs`；后台读取不显示活动刷新转圈或 toast，不重新请求资产、
  来源、标签、检查器或任何工作区。任务列表按稳定任务 ID 恢复选择、动作焦点和非零滚动；图库保持
  已载入 ID/游标、来源、选区、检查器、选择模式与滚动位置。
- Chromium 通用设置流程验证进度从 18/72 变化到 36/72、宽屏和 390px 边界、进度深链及终态隐藏；
  390px 下进度暂时代替来源刷新文字入口，保留活动、设置、网页重载和退出。筛选/审核/预览流程另行
  冻结图库全部上下文并证明进度轮询没有增加 `/v1/assets` 请求。Remote Protocol 42/42、隔离
  `build-for-testing` 与 Job/Facade/Web 资源 3 项定向 XCTest 通过；测试只用合成来源与媒体，未重启
  当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 缩略图缓存工具栏直接取消验证记录

- Mac `sourceThumbnailPrewarmProgress` 在进度文字旁直接提供“取消”；Web 顶部现在使用同一紧凑分组，
  宽屏即使选择“仅图标”工具栏仍显示来源/总进度，取消按钮紧邻进度而不是要求先打开来源管理。
  点击进度仍可查看完整生成、复用、跳过和失败计数，两条入口共享同一个 Host 请求投影。
- 直接取消复用既有 `cancelPrewarm` 幂等命令和当前活动请求的 `sourceID`；单来源精确提交其 ID，全部
  来源预热提交 `nil`。请求期间只禁用取消重入，不关闭图库、选择、检查器或滚动，也不弹出来源管理；
  终态隐藏进度组并把键盘焦点移到“活动”，完整取消结果仍可在来源管理与任务状态中读取。
- 390px 下进度收为缓存图标与相邻取消按钮，并在任务期间让当前来源刷新入口暂时让位；活动、设置、
  网页重载和退出均保持在视口内且无横向滚动。Chromium 筛选/审核/预览完整流程验证单来源直接取消、
  后续全部来源缓存/原比例缓存与批量权限连续操作，79 次资产查询、标签/审核/媒体/红心回归均通过；
  宽屏与 390px 截图人工核对通过。隔离 `build-for-testing` 及 Job/Facade/Web 资源 3 项定向 XCTest
  通过；自动化只使用合成来源与媒体，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 Apple Photos 权限设置直达验证记录

- Mac 在 Photos 授权空状态同时提供“重新检查并同步”和“打开照片权限设置…”；Web 现在对
  `authorizationRequired / disabled` Photos 来源提供同层级恢复入口。点击后直接提交
  `openPhotosPrivacySettings + sourceID`，不要求先打开来源管理，也不会把网页按钮伪装成已经授权。
- Host 重新读取权威 Photos 来源并只接受该来源类型；命令在主线程打开 macOS
  `Privacy_Photos` 系统设置页。相同 operation ID 重放只返回原终态，不重复打开系统设置；打开失败
  返回失败而不是继续刷新图库或宣称权限恢复。该动作不传回路径、PhotoKit 标识或系统设置 URL。
- 离线或已有来源请求时入口原位禁用；请求只进入既有来源管理状态机，不重建图库、选择、检查器或
  滚动。390px 截图确认“重新授权… / 打开照片权限设置… / 打开来源管理…”三条恢复路径完整可达、
  无横向溢出，并继续使用 Mac 端紧凑按钮层级。
- Remote Protocol 42/42、Host/Facade/Web 资源 3 项定向 XCTest、完全隔离的 macOS
  `build-for-testing` 与 Chromium 通用设置流程通过；浏览器断言精确 action/sourceID、来源管理保持
  关闭及 390px 元素边界。测试使用合成 Photos 来源和临时 `8806` 服务，未重启当前 App，未访问
  `/Volumes/HDD2`；真实系统设置跳转和授权恢复仍待新版 App 运行验收。

## 2026-08-10 三网格框选边缘自动滚动验证记录

- 图库、审核队列和图库瘦身成员统一使用内容坐标框选：指针靠近滚动区域上下边缘时按距离持续滚动，
  已滚出视口的早期命中项继续保留在选区；指针离开边缘、释放或取消时立即停止，不重新读取资产、
  审核或瘦身投影。Command/Ctrl 追加、普通框选替换及既有点击/Shift 选择语义保持不变。
- 瘦身成员根据当前布局的真实 overflow 与尺寸选择滚动容器：桌面优先使用成员网格，390px 下使用
  成员面板。成员卡 Grid 隐式行改为内容高度，并移除会形成循环尺寸的 `height: 100%`，长列表不再
  把卡片压缩成不可点击的极窄行；红心异步刷新也按同一实际容器恢复滚动位置。
- Chromium 选择工具流程使用 99 项合成长列表，分别验证图库、审核、桌面瘦身与 390px 瘦身持续
  滚动、首屏和后续项目同时入选、释放后停止及后续点击/键盘/回收操作回归；筛选/审核/预览流程同步
  通过。完全隔离的 `build-for-testing`、Web 打包资源定向 XCTest、JavaScript/Python 语法与
  whitespace 检查通过，临时 `8800/8802` 服务均自动停止。测试未重启当前 App，未访问
  `/Volumes/HDD2`。

## 2026-08-10 审核队列自动续页验证记录

- Mac 审核网格在最后一张卡片出现时自动请求下一页；Web 现在复用同一连续工作语义：审核滚动区域
  距末尾 280px 内自动按现有游标追加，短首屏会自动补到填满视口或没有下一页，不再要求每 48 项
  中断审核去点击“载入更多”。原按钮继续保留为网络失败后的显式重试和键盘/辅助技术回退。
- 自动追加冻结当前主项、完整多选、锚点和非零滚动；只调用审核队列端点，不刷新总览、图库、检查器
  或其他工作区。相同或未前进游标不会形成请求循环；离开审核队列、已有请求或没有下一页时立即停止。
- Chromium 首屏合成响应只返回 2 项与第二页游标，验证末项可见后自动补齐第 3 项并清空游标；既有
  P/X/U、红心、框选、预览、来源筛选和 390px 回归继续通过。测试使用临时 `8800` 服务并自动停止，
  完全隔离的 `build-for-testing`、Web 打包资源定向 XCTest、JavaScript/Python 语法与 whitespace
  检查通过；未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 审核框选与后台刷新互斥验证记录

- 审核队列把一次指针框选视为不可被投影更新打断的用户事务：按下指针后到释放或取消前，账号轮询、
  Host 事件、手动刷新和自动续页只合并刷新意图，不启动会重绘审核网格的请求；框选矩形、候选卡、
  已跨页选区、锚点和自动滚动位置保持连续。
- 已经在途的审核请求也按框选代次验收。如果请求发出后用户开始并完成了框选，即使响应在释放后才
  到达，也不会用请求开始时的旧选区覆盖用户刚完成的结果；响应被丢弃，并在交互结束后合并为一次
  `preserveLoadedWindow` 权威刷新。即使 Host 返回不足 48 项的短页和后续游标，刷新也会持续取到
  原窗口目标或游标结束，再按稳定资产 ID 恢复有效选区并重新触发必要的自动续页。
- Chromium 回归使用 39 项合成审核队列，在真实边缘自动滚动期间主动请求刷新，断言请求被延迟、
  候选卡不消失、首屏和后续项目同时入选，释放后只执行合并刷新且选择仍在；随后恢复三项队列继续
  验证 Command-K 全选、红心和 P/X/U。测试只使用临时服务与合成数据，不读取受保护照片。

## 2026-08-10 照片世界完整地点图库验证记录

- 与 Mac `WorldMapGalleryScope` 对齐，地点详情在 Host 预览选择完成后显示“在图库中查看全部 N 张”。
  点击后不使用浏览器已载入的有限缩略图做伪筛选，而是把照片塔快照中 Host 下发的精确网格桶和可选
  视口范围带入普通 `/v1/assets` 分页；目录层继续强制可用照片、地点桶和回收排除条件。
- 地点范围复用现有图库的自动分页、排序、搜索、高级筛选、多选、检查器、红心和全屏预览。进入时像
  Mac 一样切回照片、清空旧来源与筛选；顶部显示地点名、精确范围和完整总数，提供“返回地图”与
  “查看全部照片”。浏览器 Back/Forward 在原照片塔与地点图库间往返，地图镜头和选中照片塔不丢失。
- Remote 资产请求新增可选 `worldMapSelection`，旧客户端缺字段仍为普通图库；GET 参数缺失、非有限
  数值或无效空间范围返回 400，不降级为全图库。Remote Protocol 42/42、Facade/HTTP/Web 资源 4 项
  定向 XCTest、完全隔离的 macOS `build-for-testing`、JavaScript 与 whitespace 检查通过。
- 世界地图 Chromium 流程验证精确参数、完整图库卡片、历史往返、清除范围及 390px 两个动作完整可达、
  无横向溢出；临时 `8800` 服务自动停止。测试使用合成元数据和像素图，未重启当前 App，未访问
  `/Volumes/HDD2`。
- 地点有限预览失败时不再误报为“没有可预览照片”：卡片原位显示 Host 安全错误和“重试”，隐藏
  “在图库中查看全部”，重试成功后恢复同一照片塔、地点条与完整图库入口。Chromium 用一次合成 500
  验证失败/恢复，预期网络错误从最终控制台断言中单独核验，页面无额外脚本错误。

## 2026-08-10 Mac 风格应用内确认验证记录

- 训练取消、瘦身分析记录删除、候选成员可恢复回收/快速删除，以及回收站永久删除/失败意图撤销，
  不再调用浏览器原生 `window.confirm`。这些入口统一使用网页既有确认窗口，按普通、警告、不可逆
  三种层级显示 Mac 风格图标、动作标题、完整影响范围和明确的取消/确认按钮。
- 确认窗口使用 `alertdialog`、标题与说明关联；Escape 和“取消”不提交 Host 请求，并按稳定训练
  operation、分析 job、回收 entry、瘦身成员或全屏预览资产恢复焦点。确认后仍只提交原有 Remote
  命令，Mac Host 保留冻结范围、来源身份核验和最终原生确认，网页没有扩大删除权限。
- 合成 Chromium 覆盖训练取消、分析记录删除、网格批量删除和全屏当前项删除的取消/确认双路径，
  并断言没有任何浏览器原生 dialog；标签/分组既有确认回归继续通过。390px 截图与元素边界确认标题、
  说明、取消和危险动作完整可达、无横向溢出。Web 打包资源定向 XCTest、完全隔离的 macOS
  `build-for-testing`、JavaScript/Python 语法与 whitespace 检查通过；测试仅使用合成媒体，临时
  `8802/8805/8808` 服务均自动停止，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 全工作区命令面板连续性验证记录

- Web 不再把建议审核、训练工程、图库瘦身、照片世界、图库总览或全屏预览误判为 ⌘K 的阻塞层；
  命令面板在这些工作区与普通图库中持续可用。页脚明确显示当前上下文，照片世界不展示不适用的
  视频切换，审核/训练/瘦身使用各自真实媒体状态。标签、新标签、特征准备与个人建议仍只在普通图库
  出现；审核队列与瘦身候选则显示各自可执行的选择和处理动作，不把图库状态误用到当前工作区。
- 审核队列和瘦身候选的 ⌘A 不再只有隐藏的键盘路径：命令面板按当前已载入窗口提供同义“全选”，
  并复用现有选择集合、主项和范围锚点。两个工作区都可从面板预览当前项、批量加入或取消红心；审核
  额外提供 P/X/U 三种决定，瘦身额外提供可恢复回收与快速释放空间，后两者继续进入既有 Mac 风格
  风险说明、冻结范围和 Host 最终确认，不新增旁路。
- Escape、再次按 ⌘K 或浏览器原生取消均通过同一关闭路径，按稳定元素恢复原搜索框、审核卡、
  训练 Run、瘦身/地图/总览工具栏或全屏预览控件；键盘上下移动会跳过 Host 当前禁用的命令。
- 跨工作区命令把会被审核入口重置的模式与标签写入当前历史条目；训练媒体/筛选/Run、瘦身任务/分组、
  地图照片塔和视口继续复用既有工作区持久状态，再通过同一历史状态机进入目标。Back/Forward 和显式
  “返回”可恢复原工作区，不在仍可见的全屏层背后偷偷修改图库。新增“打开图库瘦身”，刷新与照片/
  视频切换按当前工作区执行。
- 图库总览、筛选/审核/预览、训练恢复、选择与瘦身、照片世界五套 Chromium 流程覆盖六个工作区、
  全屏预览、媒体切换、跨工作区返回、历史恢复和焦点回退；390px 命令面板完整位于视口且上下文与
  键盘提示不重叠。审核和瘦身流程进一步执行了命令面板全选与批量红心，验证选区、主项和后续操作
  连续；核心决定/处理命令仅在适用上下文出现。Web 资源契约、JavaScript/Python 语法与 whitespace
  检查通过。测试只使用合成投影和临时本地服务，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 自适应工具栏验证记录

- 工具栏不再只依赖固定断点。每当窗口、标题、工具栏显示模式、连接状态或任务状态变化时，Web 会
  临时测量完整工具栏的真实固有宽度；空间不足时像 Mac 自适应工具栏一样，只保留当前桌面层级可用的
  侧栏/品牌、⌘K、活动、连接点和“更多”。≤720px 继续作为硬收纳门槛，避免首次渲染闪烁。
- 820px 合成分屏验证确认右侧动作收纳时仍保留桌面侧栏按钮、App 标记、标题和检查器按钮；1440px
  空间恢复后自动展开完整工具栏。28px 回差避免临界宽度反复切换；从打开的菜单直接扩宽窗口会关闭
  菜单并把焦点交给仍可见的 ⌘K，而不是留在已经隐藏的“更多”按钮。
- “更多”菜单直接代理既有权威按钮，不复制来源扫描、设置、工作区导航或退出逻辑。菜单按状态与
  撤销、工作区、Mac 与数据、设置分组，显示 Host 与连接摘要；来源扫描和缓存任务会置亮工具栏小点，
  菜单原位展示动态进度及取消入口。
- 菜单支持上下方向键、Home/End、Enter 与 Escape；执行动作前把焦点稳定落回“更多”，因此活动、
  设置、存储和各全屏工作区关闭后不会试图聚焦已隐藏的代理项。点击外部、打开命令面板或从窄屏
  切回桌面都会清理菜单状态。
- 合成 Chromium 覆盖 820px 自动收纳、1440px 自动展开、390px 无横向溢出、动态扫描提示、菜单
  边界、键盘首尾导航、进入活动、关闭/扩宽后焦点恢复、任务结束后提示消失及来源更新仍可达；截图
  确认中等宽度仍保留 Mac 桌面身份，窄屏菜单层级与现有紧凑控件一致。
  测试使用临时 `8806` 服务并自动停止，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 单张照片本地模型预览验证记录

- 单张照片检查器补齐 Mac 的按需本地模型入口，使用“标准场景”和“个人标签”两条明确轨道；请求由
  Host 权威执行，网页只显示标签名称、建议状态和可执行决定，不暴露分数、向量、模型、权重、路径或
  catalog 标识。预览不可用时原位说明需先在 Mac 下载 iCloud 原片，不把缺少本机媒体误报为模型失败。
- 标准场景结果继续进入既有待审建议并刷新标签/检查器投影；个人标签结果可在当前检查器逐项接受或
  拒绝。切换资产、来源或工作区会推进请求代次，旧响应不得覆盖新照片；分析及决定都保持已载入窗口、
  当前选择、排序和滚动位置。
- Host 以 `operationID` 保证同一请求不重复调用本地模型，完成结果只保留最近 64 项回放窗口；成功后
  立即移除进行中命令，避免长时间浏览时幂等注册表无界增长。Remote 响应继续只投影脱敏结果。
- Remote Protocol 43/43、Facade 标准/个人与回放窗口、HTTP 路由、Web 资源契约和 macOS
  `build-for-testing` 通过。合成 Chromium 使用 24 项已载入图库验证标准结果、个人决定、切换照片时
  丢弃延迟旧响应、选择/滚动保持及 390px 无横向溢出；临时 `8807` 服务已停止，未重启当前 App，
  未访问 `/Volumes/HDD2`。

## 2026-08-10 单图中心画布与常驻检查器验证记录

- 宽屏图库单图不再把整个 Web App 变成不可交互的全屏遮罩。它按 `libraryPane` 的实时边界只替换
  中心图库画布，保留 Mac 同层级的标题栏、来源侧栏和右侧检查器；来源与中心图库在单图期间冻结，
  检查器继续可搜索标签、提交决定、使用红心、原图打开、AI 建议和本地模型，不需要退出大图。
- 上一张/下一张继续同步底层主选中项和权威检查器详情，跨页仍只追加下一页；关闭后恢复当前网格卡片
  焦点、非零滚动和已载入窗口。Tab 焦点环把单图工具栏与检查器连成一个连续区域，不会掉到被冻结的
  侧栏或图库。
- ≤720px 自动退回完整模态单图，避免窄屏同时挤压画布和检查器；窗口在单图期间跨过断点会重新计算
  隔离范围和 `aria-modal`。审核、图库瘦身与照片世界仍保持各自既有全屏上下文，不受图库布局改变。
- 合成 Chromium 使用 72+2 项图库和 48+1 项审核队列验证中心画布精确边界、检查器标签搜索、Tab
  循环、跨页导航、焦点/滚动恢复、390px 模态回退及审核自动续页两种时序；截图确认桌面层级与 Mac
  一致。临时 `8810` 服务已停止，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 工具栏持续帮助验证记录

- 主标题栏和图库高频动作补齐与 Mac `.persistentHelp` 同层级的标题与说明，包括侧栏/检查器、命令、
  快捷键、双通道撤销、地图、训练、瘦身、审核、存储、活动、设置、来源更新、网页重读、筛选、多选、
  个人模型、宽高比和九档密度。说明明确动作影响范围、快捷键以及需要 Mac 原生确认的安全边界。
- 鼠标停留 650ms 后显示，键盘 `:focus-visible` 160ms 后显示；帮助层不接收指针或焦点。点击、按键、
  移出、焦点离开、滚动、调整窗口或关闭工作区时立即收起，并恢复原生 `title`，不会改变当前选择、
  来源、筛选、已载入窗口或滚动。
- 显示期间为当前控件追加 `aria-describedby`，收起时只移除自身 ID，保留控件已有的其他描述关系。
  定位使用不受入场 transform 影响的布局宽高，在控件上下选择可用空间并按 8px 安全边距夹紧。
- `general-settings-keyboard` 合成 Chromium 回归覆盖宽屏悬停、键盘 Tab 聚焦、原生标题抑制/恢复、
  无障碍关联、动作后收起、390px 边界和无横向溢出，并继续通过既有设置、来源管理、授权恢复、活动、
  自适应工具栏和阈值流程；截图为 `/tmp/imageall-persistent-help-synthetic.png` 与
  `/tmp/imageall-persistent-help-390.png`。临时 `8806` 服务已停止，未重启 App，未访问
  `/Volumes/HDD2`。

## 2026-08-10 图库瘦身独立阈值编辑验证记录

- 对照 Mac `LibrarySlimmingWorkspaceView.analysisOptionsMenu` 与
  `LibrarySlimmingThresholdEditor`，网页“分析选项”新增同层级“调整相似阈值…”入口，不再要求先进入
  “新建分析”才能修改共享阈值；编辑器读取 `/v1/library-slimming/setup` 的当前值与工厂值，并继续通过
  既有 `/v1/library-slimming/thresholds` 幂等写入。
- Feature Print Top-K / 全部候选、L2 半径 / 不限、DINOv2 余弦下限 / 不限、拍摄日分桶及自动激活数量
  均与 Mac 同语义；极限设置显示完整性能警告。恢复默认立即写入工厂值，应用写入当前草稿，成功文案均明确
  “下次分析生效”。
- 合成浏览器覆盖当前值读取、极限模式门禁与警告、应用、恢复默认、Escape 焦点恢复，以及任务、候选组、
  成员选区和两处滚动位置保持；390px 编辑器完全位于视口且不产生页面横向滚动，截图保存为
  `/tmp/imageall-slimming-thresholds-390.png`。

## 2026-08-10 图库瘦身回收恢复动作验证记录

- Remote Protocol 不再把 Mac 的 `reinspectFileLocations`、`updateFolderAuthorization`、
  `refreshSourceBeforeRetry`、`requestPhotosAuthorization` 与 `retryFromAnalysis` 压扁为单一
  `inspect`；网页获得完整恢复决议、问题类型，以及 Host 编写的当前状态、保护原则和解释文本。旧 Host
  缺失这些可选字段时仍可解码，旧 `inspect` 值也继续兼容。
- 回收列表与 Mac 同语义显示“继续协调/继续清理”“重新检查”“更新回收权限”“刷新来源”“请求照片权限”
  和“返回分析结果”；文件夹授权、来源重扫/Photos 同步和 Photos 写权限复用既有来源管理命令，回收重试
  仍通过原回收命令及 Mac 原生确认，不在浏览器实施任何文件操作。
- “说明”使用紧凑 Mac 风格应用内面板展示 Host 状态、保护原则和下一步，完成或 Escape 后回到原条目；
  来源动作提交和轮询保持回收范围、来源筛选、搜索、已载入数量与滚动位置。
- 合成 Chromium 覆盖 7 类代表性回收记录、5 条 Host 动作路径、移除失败记录确认、说明焦点恢复、返回分析
  和 390px 无横向溢出；截图为 `/tmp/imageall-slimming-recycle-recovery.png` 与
  `/tmp/imageall-slimming-recycle-recovery-390.png`。`build-for-testing`、Protocol 往返、Facade 安全投影、
  HTTP 回收路由和 Web 静态资源 4 项定向测试通过；未重启 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 来源高风险操作前置说明验证记录

- 对照 Mac 的来源连接、系统图库重绑和完整修复说明，网页在把请求交给 Host 前统一显示应用内确认：
  连接 Photos 说明日常只读、索引位置、iCloud 原图和“最近删除”边界；系统图库重绑说明保留旧索引、人工标签
  与历史；完整修复说明全库重扫成本、后台执行以及对现有浏览的影响。
- 侧栏直达、来源空状态、来源管理列表和来源右键菜单均复用 `requestSourceManagementAction`。修复了右键菜单
  仍直接提交完整修复和删除来源的旁路；取消不会产生 Host 请求，确认后才提交，结束后焦点回到同一来源动作。
- Photos 权限空状态与 Mac 同名显示“重新检查并同步”；被停用的 Photos 来源在管理列表和右键菜单显示
  “重新启用…”，授权缺失的来源仍显示“重新授权…”，不再把三种恢复语义压成同一个标签。
- 合成 Chromium 覆盖连接 Photos、重绑系统图库、完整修复、删除来源的取消/确认、精确请求载荷与焦点
  恢复；完整修复确认截图保存为 `/tmp/imageall-source-full-repair-confirmation.png`。测试使用合成来源与 API，
  不读取真实照片。

## 2026-08-10 存储清理风险说明与焦点连续性验证记录

- 对照 Mac `previewCachePanel` 的两项 `confirmationDialog`，网页清理预览缓存前明确说明只删除可重建
  缩略图和单图预览，不影响原照片、标签、Feature Print 或个人模型；清理长期 Photos 原图副本前明确
  说明只删除 ImageAll 自有副本和缓存索引，不修改 Apple Photos、人工标签或相同检测结果。
- 取消确认不发送请求并立即回到原清理按钮；确认后才向 Host 提交稳定操作，等待 Mac 最终确认期间焦点
  停留在存储面板，终态轮询完成后自动回到触发按钮。导出和选择外置位置不增加无意义确认，仍直接进入
  Mac 原生目录选择器。
- 合成 Chromium 覆盖两项清理的取消、确认、请求载荷、等待/完成状态与异步焦点恢复；长期原图确认
  截图保存为 `/tmp/imageall-storage-originals-confirmation.png`。测试仅使用合成用量和回执，不访问真实媒体。

## 2026-08-10 嵌套弹窗 Escape 层级验证记录

- 修正全局 Escape 的关闭优先级：应用内确认始终先于来源管理、存储维护、设置或全屏工作区关闭；回收恢复
  “说明”也被纳入阻塞弹窗集合并先于图库瘦身工作区关闭，避免键盘取消时底层上下文突然消失。
- 取消继续执行原有焦点恢复契约：来源完整修复回到同一来源动作、存储清理回到同一清理按钮、回收说明回到
  原记录说明按钮；底层选区、筛选、已载入窗口和滚动均不重建。
- 三套合成 Chromium 回归分别在来源确认、存储确认和回收说明中实际按 Escape，均验证最上层弹窗关闭、
  底层面板继续可见且未发送取消前请求。并发浏览器关闭曾出现一次测试基础设施挂起，串行复跑完整通过。

## 2026-08-10 审核与瘦身触控多选验证记录

- 审核队列和图库瘦身成员在窄屏或粗指针设备上新增显式“选择 / 全选 / 完成”入口，复用现有 Mac
  蓝色强调、选中描边和圆形勾选状态；桌面 Command/Ctrl、Shift、Command-A 与空白框选语义不变，
  宽屏不会增加冗余工具栏控件。
- 进入选择模式后普通触控点击按追加/移出处理，无需模拟修饰键；全选覆盖当前已载入的 Host 权威窗口。
  “完成”或 Escape 只退出选择模式，并将多选收束到最后活动主项，不离开审核或瘦身工作区。选择模式
  内的双击不打开全屏，避免触控双击误触；收藏按钮继续独立于选择状态。
- Chromium 在 390px 下分别验证审核与瘦身的无修饰键多选、全选、完成、Escape、误双击保护、焦点恢复
  和页面无横向溢出；活动态截图为 `/tmp/imageall-review-touch-selection-active-390.png` 与
  `/tmp/imageall-slimming-touch-selection-active-390.png`。筛选/审核/预览与选择工具完整流程均通过；
  完全隔离的 `build-for-testing` 和 Web 打包资源定向 XCTest 通过。测试使用合成媒体，未重启当前 App，
  未访问 `/Volumes/HDD2`。

## 2026-08-10 审核与瘦身网格键盘连续浏览验证记录

- 审核队列和图库瘦身成员现在与 Mac 媒体网格复用完整键盘语义：左右移动相邻项，上下按当前真实
  列数移动，Home/End 到首尾，Page Up/Down 按滚动区域当前可见行数跨页；密度、原比例与响应式列数
  变化后无需维护另一套固定列数。
- Shift 配合方向、首尾或翻页键从稳定锚点扩展区间；无 Shift 时收束到新的主项。移动后主按钮取得
  焦点并只执行 `nearest` 滚动，右侧详情、已载入窗口和队列顺序不重建。Space 从当前主项打开单图，
  再按 Space 返回同一卡片；审核 P/X/U 与瘦身工作区级 Delete、Command-A 继续保持原优先级。
- 合成 Chromium 分别验证审核与瘦身的左右、Shift 扩展、Home、End、Page Up、Page Down、Space
  预览与焦点恢复；既有审核决策、红心、框选、瘦身回收和布局回归继续通过。测试只读取合成投影，
  临时 `8800/8802` 服务自动停止，未访问受保护真实图库。

## 2026-08-10 主图库网格键盘连续多选验证记录

- 主图库补齐与审核、瘦身一致的 Home/End、Page Up/Down、方向键和 Shift 区间语义。选择状态明确区分
  固定锚点与当前主项：Shift 移动持续从锚点扩展，普通移动收束到目标项，Command-A 保留当前主项，
  不再因键盘移动把范围错误重置到旧锚点。
- Space 在单选或多选状态下都打开当前主项，而不是只允许恰好选择一项；返回后焦点回到同一网格卡片，
  批量标签、红心、特征准备和相似查找继续作用于完整选区。卡片辅助说明与快捷键面板同步公开首尾、
  翻页和 Shift 连续选择操作。
- 合成 Chromium 实际验证 End、Shift+Home、普通 End、Page Up、Page Down、Home、Space 预览及焦点恢复，
  并继续跑通标签聚合、红心、建议生成、瘦身、回收和框选完整回归；活动选区截图保存为
  `/tmp/imageall-gallery-keyboard-range-selection.png`。测试只使用合成媒体，临时 `8802` 服务自动停止，
  未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 完全相同媒体清理决策信息对齐验证记录

- 网页“一键清理完全相同媒体”确认页与 Mac 原生确认页使用同一运行时方案：五项指标完整显示
  已核验、会保留、红心保留、将清理和处理分组；补充普通保留、全组红心安全跳过及“红心资产
  不会进入自动删除计划”的保护说明。网页不得用总数相减猜测红心状态。
- `LibrarySlimmingIdenticalCleanupPlanSnapshot` 从领域方案投影 `favoriteRetainedAssetCount`、
  `ordinaryRetainedAssetCount` 与 `protectedSkippedAssetCount`；Remote DTO 以可选字段向后兼容旧 Host。
  旧 Host 未提供拆分时网页显示明确降级说明和占位值，实际方案仍由 Mac 按红心保护规则核验。
- 数据概览复刻 Mac 的去留环图、2/3/4/5+ 完全相同组规模柱图，以及 Apple Photos / 文件夹来源
  横条图；快速删除、Photos 系统确认和安全跳过分别显示独立风险提示。图表使用 Host 权威计数和
  可读 `aria-label`，不引入第三方图表运行时。
- 聚焦 Chromium 回归通过真实计划 POST 验证新/旧 Host 两种响应、五项指标、三类图表、风险文案、
  无控制台或资源错误以及 1440px / 390px 无横向溢出；截图保存为
  `/tmp/imageall-identical-cleanup-mac-parity.png` 与
  `/tmp/imageall-identical-cleanup-mac-parity-390.png`。Remote 协议 round-trip、Host HTTP 路由、
  Web 打包资源定向 XCTest 和隔离 `build-for-testing` 通过；测试只使用合成统计，不访问真实媒体。

## 2026-08-10 完全相同媒体清理全局执行锁定验证记录

- Mac Host 将一键清理的实际执行状态投影为 `validatingPlan`、`recyclingAssets`、
  `requestingAuthorization`、`refreshingState` 与 `verifyingResult` 五个权威阶段；Remote DTO 使用
  可选 `executionStage` 向后兼容旧 Host，网页不解析中文消息来猜测状态。
- 网页在登录完成后同时读取照片和视频的清理请求，并在等待 Mac 确认及实际运行期间持续轮询，
  不再因离开图库瘦身工作区而停止。`awaitingMac` 不妨碍正常浏览；进入 `running` 后显示与 Mac
  相同标题、说明、实际处理进度、删除冲突锁定和退出警告的不可关闭模态层。
- 执行期间鼠标和全局键盘命令均由浏览器顶层模态语义阻断；结束后原工作区、滚动和焦点保持，
  已有删除后核验报告继续显示。页面退出或会话重置会可靠停止轮询并移除锁定层。
- 聚焦 Chromium 回归覆盖等待确认、五个执行阶段、`3 / 8` 确定进度、`⌘K` 阻断、结束后搜索框
  焦点恢复以及 1440px / 390px 无横向溢出；截图为
  `/tmp/imageall-identical-cleanup-blocking.png` 与
  `/tmp/imageall-identical-cleanup-blocking-390.png`。Remote 协议兼容 round-trip、Host HTTP 路由、
  Web 打包资源定向 XCTest 和隔离 `build-for-testing` 均通过；全部使用合成请求，不访问真实媒体。

## 2026-08-10 来源删除回收阻塞导航验证记录

- 网页发起删除来源后，Host 若发现该来源仍有真实回收项、可撤销的授权失败或必须人工检查的条目，
  不再只把来源请求标记为失败。来源命令通过独立的 notice recording 端口，把结构化来源 ID、显示名
  与三类阻塞计数写回 `LibraryWorkspaceModel`，继续复用 Mac 的同一提示文案、严重级别和动作投影。
- 来源命令服务不获得关闭提示或执行提示动作的权限；Web 仍经既有 `workspaceNotices` 能力读取当前
  Host 权威提示。删除请求首次进入终态时会立即刷新提示，显示“尚未删除，且没有在后台继续”、
  原照片未修改及“前往回收站”，无需等待其他目录事件或手动刷新页面。
- “前往回收站”继续使用带 notice revision 的防陈旧动作；成功后打开图库瘦身回收站并精确筛选原
  来源，失败时保留 Host 的更新提示。来源管理面板、主图库已载入窗口、选择和滚动不因提示刷新重建。
- 新增服务级 XCTest 证明六条合成阻塞记录会产生完整 notice recording 且不会报告删除成功；完整
  筛选/审核/预览 Chromium 流程覆盖终态请求、提示即时出现、动作 POST 与来源筛选，89 次资产查询
  及既有标签、审核、来源、媒体和红心流程继续通过。截图为
  `/tmp/imageall-source-delete-blocker.png`；隔离 `build-for-testing`、JavaScript/Python 语法与
  whitespace 检查通过。测试只使用合成来源，未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 存储操作跨面板连续性验证记录

- 网页提交便携数据导出、外部存储位置选择或缓存清理后，关闭存储面板不再终止状态轮询。标题栏
  “存储”按钮继续投影 Host 权威的“等待 Mac”或“存储处理中”，通过 `aria-busy`、完整帮助文案与
  紧凑工具栏菜单保持可发现，同时允许用户继续浏览图库、审核和其他工作区。
- 操作进入完成、取消或失败终态后，网页恢复普通“存储”状态并显示结果提示；重新打开面板时先保留
  已有用量、活动请求和历史快照，再静默读取最新状态，不再出现面板内容短暂清空或退回加载骨架。
- 退出登录仍会清理存储轮询、请求代次与本地状态，旧会话响应不能写入新会话。面板关闭后的后台
  跟踪只读取 Host 状态，不绕过 Mac 对目录选择、迁移与破坏性清理的确认边界。
- Chromium 合成流程覆盖导出等待、关闭面板、继续轮询、标题栏状态、完成提示、无闪空重开、历史
  恢复、两种清理确认及 390px 可达性；截图为 `/tmp/imageall-storage-background-status.png`。
  测试未重启当前 App，未访问 `/Volumes/HDD2`。

## 2026-08-10 多选主项检查器连续性验证记录

- 网页进入多选后不再用批量标签完全替换当前照片语境。检查器在批量动作与标签区之间保留当前
  主项的受保护缩略图、选区位置、文件名、来源、相对位置、媒体类型、尺寸、文件大小、拍摄/修改
  时间、格式和可用状态，与 Mac 多选检查器继续显示当前主项的层级一致。
- 主项只从 Host 已载入窗口和当前真实选区中解析；框选测试或旧 DOM 残留的未知 ID 不会触发资产
  详情请求。详情以 `assetID + contentRevision` 复用，并用工作区与请求代次拒绝快速换项后的过期
  响应；详情读取失败时继续显示资产摘要和缩略图，不中断批量标签操作。
- 点击主项预览复用既有单图灯箱，关闭后完整多选、主项与锚点保持，不重建资产窗口或滚动到顶部。
  Chromium 专用合成回归覆盖两项 Command 多选、主项详情、灯箱往返、选区保持、无控制台错误及
  390px 无横向溢出；截图为 `/tmp/imageall-selection-primary-inspector.png`。测试不读取真实媒体。

## 2026-08-10 iCloud 预览进度与取消验证记录

- Mac 已有的 iCloud 单图下载不再在网页中退化为不可中断的阻塞请求。新增可协商的
  `cloudPreviewLifecycle` 能力以及开始、状态、取消三条 Host 路由；进度由真实下载回调投影，网页
  显示百分比、确定进度条和“取消”，不根据等待时间伪造进度。
- 请求使用客户端 operation ID。Host 对同一 operation ID 幂等重放，对同一资产的并发不同操作
  返回冲突；网页以资产 ID、operation ID 和请求代次拒绝换图、退出检查器或重试后的过期响应。
  切换照片会取消仍在进行的下载，刷新或重新选择则恢复 Host 已有活动，而不是重复启动。
- 完成后继续通过普通受保护预览路由读取缓存；取消和失败均回到可重试状态。未声明新能力的旧 Host
  继续走原有同步 POST，网页在此期间隐藏取消入口，保持向后兼容。恢复层使用现有 Mac 检查器颜色
  token，修复暗色预览画布上说明文字不可读的问题。
- Chromium 合成回归覆盖 `42%` 进度、取消、不同 operation ID 重试、完成后载入缓存预览和 390px
  无横向溢出；截图为 `/tmp/imageall-cloud-preview-progress-cancel.png`。旧 Host 的完整筛选、审核与
  灯箱回归继续通过。Remote 协议 round-trip、Host 生命周期/打包资源两个定向 XCTest 和隔离
  `build-for-testing` 均通过；测试只使用合成资产，未重启当前 App，未访问真实媒体。

## 2026-08-10 主图库双分栏宽度验证记录

- Web 主图库补齐 Mac `NavigationSplitView` 与检查器可调列宽的高频操作。桌面端左侧栏可在
  180–300px、右检查器可在 240–380px 内拖动；边界取自 Mac 当前源码，不建立网页自定义的大跨度
  布局。命中区保持 9px，静止时不增加视觉噪声，悬停、键盘焦点和拖动时才显示系统强调色细线。
- 两个分隔器都是可聚焦的纵向 `separator`，公开当前像素宽度。方向键按分隔线的视觉移动方向微调，
  Shift 加速，Home/End 到达边界，双击恢复 Mac 理想宽度 220/300px；不要求精确鼠标操作才能使用。
- 宽度只写入既有非敏感 workspace preferences，与侧栏/检查器显隐、密度和比例偏好同域保存。拖动
  只修改 CSS 网格变量，不重建资产窗口、检查器、选区、锚点或滚动；隐藏再显示仍恢复原宽度。
  700px 以下继续使用既有侧栏抽屉和全屏检查器，分隔器自动移除，不造成窄屏横向溢出。
- 扩展 Chromium 主项检查器回归覆盖双侧拖动、ARIA 值、选区/主项/滚动保持、键盘边界、刷新持久化、
  显隐恢复、双击复位与 390px 降级；截图为 `/tmp/imageall-split-view-resizing.png`。测试只使用两张
  合成资产，不访问真实媒体。

## 2026-08-10 审核工作区双分栏宽度验证记录

- 审核总览补齐 Mac `HSplitView` 的本地模型分隔器。本地模型面板严格限制为 248–320px，默认
  288px；审核队列继续保留与 Mac 检查器同源的详情和 P/X/U 决策入口，但由原先约 62% 固定详情栏
  改为 240–380px、默认 300px 的检查器，剩余空间全部交给待审核网格。
- 两处分隔器复用主图库已经验证的 9px 隐形命中区、系统强调色焦点线和统一指针生命周期；方向键、
  Shift、Home/End 与双击复位均可用，并通过 `separator` 的 ARIA 当前值公开精确像素宽度。
- 宽度进入既有非敏感 workspace preferences。拖动只更新 CSS 网格变量，不重新请求审核总览或队列，
  不改变待审项目、主项、完整选区、下一页游标及两侧滚动。700px 以下继续使用原有上下堆叠布局并
  隐藏分隔器，触控选择、详情和决定动作保持可达。
- Chromium 完整筛选/审核/灯箱回归覆盖两处分隔器的鼠标上限、键盘边界、偏好恢复、双击复位、
  非零滚动与审核状态保持、390px 无横向溢出；截图为 `/tmp/imageall-review-overview-split.png` 和
  `/tmp/imageall-review-queue-split.png`。测试使用合成媒体，不访问真实照片。

## 2026-08-10 图库瘦身单导航栏验证记录

- 网页图库瘦身不再把“分析记录”和“候选分组”拆成两列。与 Mac
  `analysisHistoryAndClusters` 的单一 `List` 一致，两组内容改在同一个导航栏中纵向排列并共用
  滚动轴，桌面宽度严格采用 Mac 当前的 196–244px 与 22% 比例，不再占用约 430–560px 挤压媒体网格。
- 原分析记录、上一条/下一条、载入更多、审阅队列范围、候选分组与分组操作保持原有语义；切换记录
  或追加历史只更新列表内容，导航滚动由外层单一容器持有。隐藏导航仍一次隐藏记录与候选分组，且
  不改变当前分析、分组、成员选区、锚点或成员滚动。
- 700px 以下保留网页触控友好的上下布局，但导航本身仍是一条 Mac 风格纵向列表，不再把记录与
  候选组变成两个横向卡片带。导航占用一段连续高度，成员区使用剩余空间，页面不产生横向溢出。
- Chromium 合成回归覆盖桌面两列总布局、导航精确宽度、两组内容同轴、显隐连续性、121 条历史
  渐进载入时非零导航滚动保持，以及 390px 上下布局；截图为
  `/tmp/imageall-slimming-single-navigator.png`。测试不访问真实媒体。

## 2026-08-10 图库瘦身紧凑详情栏验证记录

- Mac 的候选详情在一条紧凑标题栏中显示类型、成员、选中数、来源、审阅状态和相似度，随后立即进入
  媒体网格。Web 保留已有分析诊断数据，但不再默认展开四列检查器挤压网格；诊断改为标题栏内的
  “分析概览”按需浮层，默认关闭且打开时覆盖内容，不增加布局轨道或改变网格高度。
- 标题和副标题继续显示与 Mac 同层级的候选类型、结果、成员窗口与来源；已选数量、审阅状态及
  “重新处理”仍在同一标题栏。浮层保留任务状态、来源、分析范围、策略和技术摘要，因此空间对齐
  不以删除已有诊断功能为代价。
- 浮层通过原生 `details/summary` 提供键盘焦点，Escape 关闭并返回触发器。打开、关闭和后台重绘均
  不改变当前分析记录、候选分组、成员选区、锚点、成员网格尺寸或滚动。390px 下浮层限制在标题栏
  可用宽度内并保持两列字段，不形成页面横向溢出。
- Chromium 瘦身完整回归覆盖默认关闭、打开后网格几何与选区精确保持、Escape 焦点恢复及截图
  `/tmp/imageall-slimming-inspector-overlay.png`；测试只使用合成媒体。

## 2026-08-10 图库瘦身回收站信息架构验证记录

- Web 回收站恢复 Mac `recycleBinHeader` 的独立层级：橙色回收图标、“回收站”标题、照片/视频项目
  总数及待关注数摘要、正在处理状态、来源范围提示、四类范围按钮和文件名搜索。来源范围提示可直接
  “显示全部来源”，搜索输入显示真实结果数并可一键清除；两者都复用 Host 查询，不做浏览器假筛选。
- 回收条目由原整宽表格行改为与 Mac `LazyVGrid` 相同的 380–640px 自适应卡片。桌面宽屏自动形成
  多列，卡片上半区显示 96×84 缩略图、名称、来源、日期和生命周期，下半区显示保护策略与 Host
  允许的恢复、权限修复、重新检查、说明或永久删除动作；红心与视频标记继续保留。
- 空结果按 Mac 区分回收站为空、文件名无匹配、来源无项目及当前范围为空，并提供“清除搜索”、
  “显示全部来源”或“查看全部”。恢复动作完成后焦点返回搜索、来源选择或全部范围，不能留下失效
  焦点；请求、筛选、红心和恢复刷新继续保持回收站滚动。
- 390px 下标题摘要和四类范围保持可触控，卡片退为单列但不退化为无层级表格；搜索、来源、策略和
  全部安全动作均在视口内。Chromium 合成回归覆盖桌面/窄屏卡片几何、摘要、来源横幅、结果计数、
  空结果恢复、真实 source/search/scope 组合过滤及既有恢复流程；截图为
  `/tmp/imageall-slimming-recycle-mac-layout.png` 与
  `/tmp/imageall-slimming-recycle-mac-layout-390.png`，不访问真实媒体。

## 2026-08-10 来源管理列表与当前来源详情验证记录

- Mac 通过侧栏来源列表和当前来源上下文菜单组织操作，不会把所有来源的全部命令同时铺成按钮墙。
  Web 来源管理因此改为同层级的“来源列表 + 当前来源详情”：左侧只显示名称、类型与可用状态，右侧
  只显示当前来源，并按“更新与缓存”“权限与修复”“移除来源”分组呈现 Host 允许的动作。
- 当前图库来源、正在处理的来源和上次在面板中选择的来源依次作为恢复锚点。点击来源或使用
  Up/Down/Home/End 会切换详情，Right 进入详情动作、Left 返回来源列表；切换只重绘管理面板，
  不改变图库、选择、检查器或滚动。
- 面板补齐 Mac 来源上下文中的“在图库中查看”。执行后关闭面板、切换到精确来源并把焦点恢复到
  侧栏来源；从侧栏右键“打开来源管理”会直接选中同一个来源，避免用户再次查找。
- 原有连接、全部更新、两种批量缓存、批量权限、逐来源恢复与 Mac 原生确认边界保持不变。390px
  将来源列表叠放在详情上方，两部分均可独立滚动且所有动作仍可达。Chromium 回归截图为
  `/tmp/imageall-source-manager-mac-layout.png` 与
  `/tmp/imageall-source-manager-mac-layout-390.png`，只使用合成来源。
- Mac 侧栏标题只用省略号菜单承载跨来源命令，因此 Web 顶部进一步只保留“连接文件夹”和
  “连接 Apple Photos”两个新增来源入口；全部更新、两种批量缓存和批量权限收进“所有来源操作”。
  菜单执行任一命令后自动收起，方向键可纵向遍历，Left/Escape 返回菜单触发器；运行状态仍显示在
  面板状态区和全局工具栏，不因菜单收起而消失。390px 菜单限制在面板内，不形成五按钮竖墙。

## 2026-08-10 图库照片卡片持续详情验证记录

- Mac 图库照片卡片通过 `LibraryAssetDetailText.hoverText` 持续显示文件名、来源、相对位置、尺寸、
  格式、拍摄/修改时间、标签统计与可用状态。Web 图片卡片复用既有 `persistentHelp` 呈现同一组
  Host 权威事实；标题使用文件名，正文保留逐行信息，不再依赖短暂且不可控的浏览器原生 tooltip。
- `RemoteAssetSummary` 新增可选 `relativePath`、`mediaModifiedAtMs` 与 `durationMs`，Facade 从
  `AssetGridItemProjection` 原样映射。旧 Host 缺少字段时继续解码为 `nil`，网页只省略对应行，不能
  为位置、修改时间或时长编造值。
- 图片卡片鼠标停留 650ms 或键盘可见焦点 160ms 后显示详情；移出、点击、按键、滚动、焦点离开或
  窗口变化立即收起，并恢复原有 `title` / `aria-describedby`。显示期间不重建网格，不改变分页、选区、
  主项、检查器或滚动。
- 视频卡片继续以悬停静音播放为优先，不挂载持续帮助层；既有原生标题、视频徽标、双击预览和滚动
  连续性保持。Chromium 合成回归覆盖桌面与 390px 视口边界以及视频无覆盖播放，截图为
  `/tmp/imageall-asset-persistent-help.png` 与 `/tmp/imageall-asset-persistent-help-390.png`。

## 2026-08-10 检查器内联新增标签验证记录

- Mac 单张检查器把“新标签名称”输入框常驻在人工标签区，Return 或右侧加号会创建标签并立即确认；
  Web 单张和多选检查器现在使用相同的内联结构，不再要求先打开独立模态框。侧栏标签管理和批量工具栏
  仍可使用原有模态入口，避免改变管理型流程。
- 两个内联入口都复用 Host 的 `/v1/tags/create-and-apply` 原子命令，提交前冻结当前单张或完整选区。
  同名失败重试保留同一个 `operationID`，用户修改输入后才生成新 ID；Host 成功后刷新标签、当前检查器
  与批量聚合，但保持已载入窗口、主项、选区、锚点和滚动。
- Return 提交成功后清空输入并把焦点留在原输入框，便于连续录入；Escape 只清除草稿或错误，不退出
  选择模式、关闭检查器或改变图库。合成 Chromium 回归覆盖一次 409 失败后的同 ID 重试、单张确认、
  两项批量确认、无模态框及 390px 边界，截图为 `/tmp/imageall-inspector-inline-tag.png` 与
  `/tmp/imageall-selection-inline-tag-390.png`，不访问真实媒体。

## 2026-08-10 检查器标签持续帮助与分组菜单验证记录

- Mac 检查器的标签芯片会按已确认、混合或未决定状态说明左键、右键和拖动语义，分组标题也可直接
  右键重命名或删除。Web 动态标签控件现在接入既有持续帮助层：单张与多选芯片显示 Host 决定状态，
  并说明左键确认、右键清除、X 拒绝、Delete 清除、拖动及 Option + 方向键排序/跨组移动。
- 分组标题显示展开/折叠状态、标签数量、方向键/Home/End 导航；非系统分组可通过右键、Context Menu
  键或 Shift-F10 打开“重命名分组 / 删除分组”。关闭菜单、取消标签管理或取消确认后，焦点返回原检查器
  分组，而不是错误跳回侧栏；系统分组明确显示不可重命名或删除。
- 确认、拒绝和未决定按钮分别说明当前是否已生效及作用范围，帮助层仍在指针移出、按键、滚动和窗口
  变化时立即收起，不会执行标签操作。合成 Chromium 回归覆盖桌面状态化说明、键盘/右键分组菜单、
  标签管理焦点往返及 390px 视口边界，截图为 `/tmp/imageall-inspector-tag-help.png` 与
  `/tmp/imageall-selection-tag-help-390.png`，不访问真实媒体。

## 2026-08-10 侧栏来源与标签持续帮助验证记录

- Mac 侧栏会按来源可用、离线、需授权和停用状态解释当前边界，也会按标签未筛选、并集、交集和排除
  状态解释点击语义。Web 来源行、标签分组与标签芯片现在接入同一个持续帮助层，不再只依赖浏览器短暂
  `title`；标题、状态和动作均来自当前 Host 投影与网页筛选状态。
- 来源帮助说明点击范围、Photos 同步或文件夹重扫、右键/Context Menu/Shift-F10 动作，以及拖动或
  Option + 上/下排序。标签帮助说明并集、交集、排除、右键管理和 Option + 方向键排序/跨组移动；搜索
  期间明确提示清除搜索后才能排序。分组标题同步显示展开状态、标签数、方向键导航和系统分组保护。
- 鼠标停留与键盘可见焦点均可触发，打开来源或标签菜单后 Escape 返回原行；帮助本身不改变来源、
  筛选、已载入窗口、选区或滚动。合成 Chromium 回归覆盖桌面状态变化、键盘菜单焦点往返和 390px
  视口边界，截图为 `/tmp/imageall-sidebar-source-tag-help.png` 与
  `/tmp/imageall-sidebar-source-tag-help-390.png`，不访问真实媒体。

## 2026-08-10 标签分组共享折叠状态验证记录

- Mac 侧栏和检查器都调用同一份 `isTagGroupCollapsed`，分组折叠不是各表面独立偏好。Web 现在也只
  维护一份 `collapsedTagGroupIDs`：从侧栏折叠会立即同步单张或多选检查器，从任一检查器展开也会同步
  侧栏，不再出现同一个分组左右状态相反或切换选择后突然展开。
- 新偏好首次载入时合并旧 `collapsedSidebarTagGroupIDs` 与 `collapsedInspectorTagGroupIDs`，避免升级后
  丢失任一侧既有折叠；保存时继续同步写入两个旧键，旧版回退也不会再次分裂状态。审核总览仍保留独立
  折叠集合，因为它是不同工作区的队列展示，不参与图库人工标签编辑。
- 合成 Chromium 回归覆盖侧栏到单张、刷新恢复、单张到侧栏、390px 侧栏到多选及多选到侧栏的双向
  同步，并确认组内排序、跨组移动、筛选、管理菜单和焦点回归保持。截图为
  `/tmp/imageall-shared-tag-group-collapse.png` 与 `/tmp/imageall-shared-tag-group-collapse-390.png`，不访问
  真实媒体。

## 验收矩阵

### 自动化

- Web 静态资源契约测试覆盖新增界面 ID、正式审核路由和关键交互函数；
- JavaScript 语法、lint 与 Git whitespace 检查通过；
- Remote 定向测试和 Mac Debug 构建需通过后方可完成运行验收；
- 自动化测试不读取 `/Volumes/HDD2` 受保护真实数据。

### 已登录桌面浏览器

1. 在照片模式筛选“猫”，载入至少两页，等待轮询或触发刷新；结果数、滚动位置和筛选保持。
2. 切换视频再返回照片；原来源、筛选、选择和滚动位置恢复。
3. 调整九档密度，隐藏再显示侧栏和检查器；刷新页面后非敏感布局偏好保留。
4. 验证普通点击、Command 点击、Shift 点击、Command-A 和空白框选。
5. 用方向键与 Page Down 移动，Space 预览；关闭预览后列表位置不跳顶。
6. 多选至少两张照片，确认标签聚合状态与各照片真实决定一致，再执行一项批量更新。
7. 审核页分别执行 P、X、U；U 后项目只出现一次且原标签决定不变。
8. 让 Host 产生标签、资产和任务事件；只有对应区域更新，已载入图片不灰屏重载。
9. 打开标签管理，新增并重命名一个临时分组；把一个临时标签移入该组、重命名后归档，确认
   侧栏和检查器同步更新；只使用可安全清理的测试标签，不修改受保护真实照片内容。
10. 依次完成一项测试标签决定和一项审核决定，确认两个撤销入口同时可用且互不覆盖；分别撤销后
    状态恢复。再次提交同一撤销请求不产生第二次写入，同通道完成新决定后旧撤销入口失效。
11. 在审核总览核对一个已知标签的待审数和来源构成，进入队列再返回；任意勾选多个来源、清空及
    恢复全选后，统计、队列、个人建议和训练配置范围一致，且不改变图库侧栏当前浏览来源。后台
    刷新不关闭来源菜单、不丢键盘焦点，也不把队列摘要改回总览摘要。卡片按 Mac 标签组与顺序
    展示，组标题显示标签数和待审数；折叠偏好、方向键/Home/End 焦点及异步刷新定位均保持。
12. 在视频模式打开一项本机可用视频，验证播放、暂停与拖动定位；Network 中媒体请求返回
    `video/*`、`Accept-Ranges: bytes` 和合法的 206 `Content-Range`，切换其他视频会停止旧播放。
13. 在网格“适应/填充”间切换，确认宽高比变化但当前来源、筛选、已载入数量和滚动位置不变。
14. 打开图库瘦身，照片与视频各核对一条 Mac 分析记录；候选组至少展开两批，选择多个成员并
    跨过一次账号轮询，确认媒体、记录、分组、成员选择和预览不被重置。只做只读查看。
15. 打开“新分析”，核对三种范围、来源全选/清除和四组阈值与 Mac 一致；分别检查当前筛选/
    种子不可用时提交保持禁用。只验证到提交前，不保存阈值、不启动真实分析、不删除分析记录。
16. 打开来源管理，核对每种来源状态只显示 Mac 允许的动作。发起一个低风险文件夹连接并在 Mac
    系统选择器中取消，网页应从“等待 Mac”变为“已取消”且不新增来源；删除等破坏性流程只核对
    到 Mac 二次确认门，不实际确认删除。
17. 在图库瘦身切换到“回收站”，核对照片/视频、来源、文件名筛选和渐进载入；文件夹、Photos、
    授权前失败/中断项只能显示 Host 允许的动作。真实数据只读验收不提交恢复或删除；使用合成
    fixture 验证等待 Mac、取消、完成和失败状态，不实际永久删除媒体。
18. 在合成 fixture 中选择同一候选簇的至少两项，分别核对“移入可恢复回收站”和“立即释放
    空间”的网页警告与 Mac 原生确认门；确认请求体只含冻结 ID，不含路径。模拟部分成功后，
    只有 Host 返回的隐藏资产消失，失败项仍保留，刷新不会恢复已隐藏项。真实数据验收只检查到
    Mac 确认门并取消，不确认任何回收或删除。
19. 对含完全相同分组的合成分析记录打开“一键清理”预览，核对已核验、保留、清理、分组、
    Photos/文件夹来源与跳过数量；请求体只提交 `jobID + mediaKind`，响应不含路径、哈希或 Photos
    标识。执行只提交 `operationID + planID + mode`，模拟方案变化时必须拒绝；模拟完成后显示独立
    核验的完成组数和剩余冗余。真实数据只验收到 Mac 原生确认门并取消。
20. 构造一条运行中且正在暂停的分析记录、一条已完成记录：网页必须显示阶段进度和尝试次数，
    运行态锁定一键清理；用方向键及 Home/End 切换后焦点留在选中记录。完成清理后完整报告中的
    目标、实际读取、当前可用与已清理数字必须来自响应，窄屏不得横向溢出。
21. 构造一条图库瘦身可重试失败任务：从全局任务面板打开时必须定位到同一媒体类型和分析记录；
    检查器显示来源、范围、结果和技术摘要。再从瘦身状态返回任务面板时聚焦同一任务，失败建议
    只使用安全错误码；390px 默认折叠检查器且页面不横向溢出。
22. 打开世界地图，核对照片塔数量、已定位/未定位统计与 Mac 一致；拖拽、缩放后只合并发起一次
    稳定视口查询。点击照片塔后地点名、GPS/标签构成、总数和预览均来自同一聚合桶，打开/关闭
    全屏预览不关闭地图或跳回图库。真实图库验收只读，不触发位置回填或地点解析写入。
23. 打开“图库总览”，核对总量、保守去重、确认冗余、正样本、媒体账本、来源、可用状态、
    正样本标签与年份和 Mac 一致；分别点击来源、“猫”标签和照片/视频卡，确认回到真实筛选而非
    浏览器本地隐藏。连续刷新和跨过一次账号轮询不得重复请求或重置当前图库窗口。
24. 打开“存储”，核对两类用量和内置/外置状态与 Mac 一致；发起导出并在 Mac 目录选择器取消，
    网页必须从等待变为已取消。两类清理只检查到 Mac 原生确认门并取消，不确认真实数据清理；
    Network 响应不得包含应用目录绝对路径或书签，390px 下所有按钮和刷新入口可达。
25. 多选两项后分别从批量栏和窄屏多选检查器确认“准备特征 / 查找相似”可达；准备特征显示逐项
    进度且可取消，完成统计来自 Host。查找相似提交精确种子 ID 并打开同一瘦身记录；真实数据
    验收只核对到提交前，除非用户明确授权启动计算任务。
26. 多选两张合成照片后生成建议，请求顺序必须与网格一致并自动进入建议审核；任务完成后审核
    总览从 Host 刷新。清空选区后从审核页启动抽检，请求必须使用空 `assetIDs` 表示全图库而不是
    当前页；刷新网页不得重复执行，运行态可取消。真实图库只核对到提交前，除非用户明确授权计算。
27. 在审核总览选择一个可生成个人建议的标签，分别核对个人/超级个人的 Host 阈值、Top N 和来源
    范围；提交后卡片显示候选准备、评分和写入阶段，运行态可停止。完成后自动进入同一标签审核队列，
    刷新页面只恢复现有活动而不重复启动。真实图库只核对到提交前，除非用户明确授权计算。
28. 在审核总览对一个 Feature Print 标签验证生成/更新入口和来源预选；运行态只显示 Host 允许的
    暂停、继续、取消动作。由卡片打开关联训练 Run，再从训练详情返回同一标签审核队列；刷新与任务
    动作不得丢失定位。真实图库只核对到提交前，不启动或取消真实计算任务。
29. 构造失败、成功和取消的训练记录：失败详情必须显示安全原因、下一步和技术码；重新配置只回填
    Host 能证明的标签与来源，失效来源明确剔除，缺少来源明细时清空选择。方向键与 Home/End 切换
    Run 后焦点留在选中行，跨一次异步刷新仍保持 Run、焦点和滚动位置；390px 不得横向溢出。
30. 构造三标签个人模型批次并让中间标签失败：后续标签仍完成，活动卡片逐项显示真实状态，汇总
    不把部分成功误报为全成功；重新处理只选中未完成标签。模拟 App 重启后仅进程内个人模型孤儿
    Run 变为中断失败，Feature Print 队列记录保持原状，恢复上下文保留完整批次标签。N/R/V/E、
    方向键与 Home/End 均可无鼠标操作，取消任务不计入进行中角标，390px 不横向溢出。
31. 让终态个人模型批次超过顶部瞬时提示窗口并重建 Host：最近批次历史仍保留逐标签结果和原始
    接受/更新时间；旧运行态明确变为 `hostRestartInterrupted`，终态不被改写。部分完成批次可打开
    关联 Run，并只回填未完成标签。L 和记录栏按钮可隐藏/显示导航栏且刷新后保留偏好；持久文件
    权限为 0600，内容不含资产、路径、书签、向量或模型数据，390px 不横向溢出。
32. 构造含归档标签名的结构化批次 Run、普通单项 Run 和不含新增字段的旧 Run：全部/批次/单项
    与方法筛选组合后的数量和选择必须正确，B 键按三态循环。Run 行与详情显示标签、批次位置、
    样本统计和时间；展开技术详情可见完整标识，但响应与界面不得出现路径、书签或原始文件名。
33. 构造一个已发布模型槽、两个未发布槽、可关联审核标签和失败后台任务：点击已发布槽必须定位
    精确 Run，未发布槽只打开对应方法配置；M、方向键和 Home/End 可遍历模型槽。训练→审核→训练
    的嵌套返回保留各自滚动、选择和触发按钮焦点。J 打开关联任务后，方向键、Home/End、Enter、
    Esc 均可用，弹层位于当前工作区之上，关闭后焦点回到原训练 Run。
34. 打开通用设置，核对本地模型状态、空闲缩略图预热和工具栏显示方式与 Mac 一致；分别用鼠标
    和键盘修改后，Mac 与网页同步显示且同一 operation ID 重放只执行一次。Command-, 可打开，
    Escape 回到齿轮按钮；公网地址、Host 开关、配对设备和账号白名单不得在网页出现可写入口。
    来源与存储面板按方向键遍历并在关闭后回到各自触发按钮，异步刷新不得丢失当前焦点。
35. 在 Mac 与网页分别核对三种建议方法的全局默认门槛；网页修改后 Mac 设置页立即显示同值。
    搜索一个活动标签，分别设置覆盖、恢复继承并采用 Host 参考值，确认有效值和参考样本计数一致；
    输入提交后焦点留在原方法。设置加载时立即 Escape 再重新打开，控件不能永久禁用；390px 下
    搜索、三轨编辑和操作按钮全部可达且不横向溢出。
36. 对文件夹、活动 Photos、不可用 Photos 和待授权来源分别打开右键及 Shift-F10 菜单，动作集合
    必须与来源管理页一致。用方向键选择“在图库中查看”后来源筛选生效且焦点回到该来源；需要
    权限或删除的动作只验收到 Mac 原生确认门并取消。加载来源/存储面板后立即 Escape 再重开，
    内容必须恢复且控件不永久禁用。
37. 在图库筛选一个“已确认”标签，个人模型菜单必须显示该标签和当前范围；分别打开
    质心与 AdamW 配置，方法不得静默变更。有多选时应默认当前选择，无多选时应默认全库；
    Host 不可用时启动按钮必须禁用。方向键、Home/End、Escape 与命令面板均可完成同一路径。
38. 单张和多选检查器必须按 Host 标签组显示。验证组折叠偏好、方向键/Home/End、标签主体左键
    确认、右键清除、X 拒绝和 Delete 清除；右侧三态按钮仍可独立使用。多选混合状态必须来自
    Host 聚合并提交完整选区，异步写入后焦点和检查器滚动上下文不得跳回顶部。
39. 选择一张含至少 6 条、多种来源轨道待审建议的合成照片：检查器默认显示 5 条，展开后显示
    全部并标明来源；分别执行“属于/不属于”，请求必须携带当前资产和建议标签。模拟 Host 清除原
    建议后焦点落到相邻项或区标题；旧详情 JSON 缺少建议字段时只隐藏该区，其他详情仍正常显示。
40. 构造一张普通预览返回 `cloud preview required` 的 Photos 合成资产：网页必须显示云端占位，
    且在点击前不得出现 cloud-preview POST。模拟第一次下载失败后按钮原位变为“重试”；第二次
    成功后自动显示预览并隐藏占位。切到其他资产时旧请求不得覆盖当前检查器，视频和普通 404 不得
    错显 iCloud 入口；测试只使用合成图像，不连接真实 Photos 或 iCloud。
41. 构造无来源且无标签的合成 Host：网页必须显示连接文件夹、连接 Apple Photos 和添加常用标签
    三个首次使用动作。点击安装后只出现 Mac 同一九标签集合，不产生资产写请求；重复同一操作或
    全部已存在时不得重复标签。侧栏、标签管理和命令面板入口一致，成功及幂等空结果后的焦点均
    可追溯，390px 下无横向溢出。
42. 构造两个来源、两个标签组和三项标签：来源拖放及 Option-上/下刷新后保持，且无来源写请求；
    标签同组排序不发 Host 请求，跨组只提交一次既有 move 命令。模拟跨组 409 后必须恢复原组和
    两组顺序；侧栏、单图、多选检查器和选择器顺序一致，搜索期间禁用拖放，焦点回到移动项，
    390px 下无横向溢出。
43. 折叠侧栏标签组后刷新，状态必须保持；搜索命中折叠组时只临时显示结果，清空后恢复折叠。
    分组标题方向键可导航。分别从单图检查器跨组拖动、Option 键组内排序，并从多选检查器组内拖动；
    侧栏与两种检查器必须同步，跨组失败恢复原组和顺序，焦点留在发起操作的检查器且不得误写决定。
44. 依次普通点击两个标签、Command/Ctrl 点击第二项及 Command/Ctrl-Option 点击第一项，验证并集、
    交集和排除的视觉状态与资产查询参数一致。标签右键和 Shift-F10 菜单执行仅筛选、重命名、归档；
    非系统分组菜单执行重命名并取消删除。菜单需支持 Home/End/Escape，操作或确认后焦点回到原项，
    归档项消失后回到所属分组标题；系统分组不得显示删除入口。
45. 对一个可用来源打开右键和来源管理面板，核对“预热缩略图缓存”与“专门用于原比例的缓存”均
    可达。启动合成预热后关闭面板，顶部状态必须持续更新且图库滚动、筛选和选区不变；点击状态返回
    同一进度，再取消并确认不会启动第二项工作。完成态核对总数、成功与失败计数；原比例路径必须
    跳过已有缓存，测试不得读取真实图库或触发 iCloud 下载。
46. 对活动 Apple Photos 来源核对“请求照片写入权限…”，对活动文件夹来源核对“更新回收权限…”；
    右键、Shift-F10 和来源管理面板动作必须一致。合成测试分别模拟授权成功与选择器取消，确认网页
    只提交来源 ID、动作和 operation ID，终态可解释且不重复唤起；自动化不得触发真实 TCC、真实
    文件夹选择器、读取真实图库，响应不得包含路径、书签或 Photos 标识。
47. 选中五种空来源状态：活动 Photos、待授权 Photos、旧 Photos、活动文件夹、待授权文件夹；
    空页面分别显示立即同步、重新授权、连接当前图库、立即重扫、重新授权。仅改变来源状态、保持
    资产响应不变时文案和动作也必须刷新；直接动作进入来源管理及 Mac 确认链路，响应不得包含路径、
    书签或 Photos 标识，自动化不得触发真实图库、TCC 或文件夹选择器。
48. 在 72+2 项图库与 48+1 项审核合成窗口中，从首批末项进入单图并按下一张；必须只追加一次下一
    页，标题、位置、底层选中、检查器和返回焦点同步到新项。快速连续右/左键不得丢失第二个方向，
    返回后网格不跳顶；末页下一张禁用。图库、审核、瘦身、地图返回文案正确，390px 不横向溢出；
    测试只使用合成媒体，不读取真实图库。
49. 在 48 项合成审核队列依次验证普通点击、Command/Ctrl 切换、Shift 区间、追加区间、空白框选和
    Command/Ctrl-A。多选时主预览稳定，P/X 请求必须包含队列顺序的完整冻结选区；U 不发写请求、
    不重排卡片并前进到下一未选项。分页或刷新只清除失效 ID。普通标签批量确认、拒绝、清除与新增
    并应用不得显示额外确认框，成功后仍只产生一项最近操作撤销；390px 不横向溢出。
50. 先提交一项标签决定，再提交一项审核决定；标签与审核撤销按钮必须同时可用，并分别命中对应
    端点，任一撤销不能清除另一通道。相同 undo operation ID 重放不得再次恢复，更早的同通道凭据
    失效。审核工作区内无需退出即可撤销；390px 按钮完整落在首屏，命令面板仍可访问两条撤销命令。
51. 切到视频图库后，每张卡显示播放标识与时长。鼠标停留后出现静音、循环、无控件的悬停播放器；
    离开后媒体元素被移除。悬停前后选择、检查器、已载入窗口和 `scrollTop` 不变；打开单图前先释放
    背景播放器。账号模式不把 Basic 凭据写入 URL 或存储；触控与减少动态效果环境不自动播放。
52. 打开世界地图的“更新照片位置”，核对每个来源的状态、已检查/总数、已定位、无坐标和扫描进度。
    只有点击开始/重试才提交任务，点击取消进入取消中并最终收束；关闭面板不得取消任务，重新打开
    必须恢复 Host 权威状态。相同 operation ID 重放只执行一次；390px 不横向溢出。自动化只使用
    合成来源，不得对受保护真实图库启动位置回填。
53. 打开世界地图的“补全地点”，确认打开面板不触发地点搜索；未识别、待选择、已确认、已忽略与
    未找到状态应与 Host 缓存一致。编辑地点描述后按 Enter，只发送规范化描述且不携带任何照片或
    路径数据；超长描述留在浏览器。选择候选后对应卡原位变为已确认、地图统计刷新且 GPS 照片不被
    覆盖。搜索过程中继续编辑时，完成后须保留当前输入、说明下方结果来自哪个查询、恢复焦点和
    滚动位置；ArrowDown 从输入进入候选，ArrowUp、ArrowDown、Home、End 和 Enter 可完成选择；
    晚于搜索返回的旧缓存快照不得覆盖新候选或已确认地点。相同 operation ID 只执行一次，过期
    候选返回冲突；390px 不横向溢出。
54. 在审核标题栏把“每标签上限”从 500 调到 550，确认 Mac 工作区与四条建议生成路径读取同一值，
    写入后焦点仍在原步进按钮。展开一个标签的“门槛与生成”，分别验证三轨输入、±0.05、采用参考
    值和恢复继承；点击“刷新”只删除低于当前生效门槛的待审项，不启动扫描，并重新读取 Host 总数。
    异步重绘后折叠区、当前来源和原控件焦点保持；同一 operation ID 重放不重复删除，390px 下三行
    编辑器和生成动作均可达且不横向溢出。
55. 打开审核总览，刷新本地模型状态并核对服务版本、provider 与模型 ID 均来自 Host。分别按全部
    来源和来源子集启动标准/个人模型全库任务，确认进度、建议数、跳过数和失败码原位更新；运行中
    依次验证暂停、继续和取消，刷新及 `jobsChanged` 事件不得重置来源、折叠或滚动。旧 Host 必须回退
    App 内抽检而不是伪造全库能力；空来源不得发送请求，390px 下三张卡片与动作均可达。
56. 从图库和审核队列分别打开单图预览，确认红心按钮显示当前 Host 状态；点击加入/取消后全屏保持
    打开、标题和位置不变，返回后网格与检查器一致。审核/瘦身/地图上下文首次进入只补读当前资产
    详情，不触发整页刷新；前后切换拒绝过期详情。红心收藏中取消当前项应平滑前进，最后一项才返回。
57. 在宽屏悬停未收藏缩略图，右上红心出现；鼠标点击和键盘 Enter 各往返一次，确认选择、检查器、
    全屏、已载入数量与滚动均不变。进入多选后红心与左上选择勾不重叠，视频悬停播放器继续播放且
    双击主表面仍打开全屏。390px 下无需悬停即可点击红心；在红心收藏范围取消后相邻主按钮取得焦点。
58. 进入审核队列后在未收藏卡片上分别以鼠标和 Enter 往返红心；Command/Shift 选区、框选、主项目、
    P/X/U 目标、右侧详情、队列滚动和双击单图均保持。外部红心/Photos 同步状态刷新原位显示，旧 Host
    无红心投影时不显示假按钮；390px 下红心常驻且与左上选择勾、底部来源/分数徽章互不遮挡。
59. 在世界地图选择一座照片塔，悬停地点照片后直接加入/取消红心，再以 Enter 往返一次；地点选择、
    统计、照片条横向位置和地图相机不得改变，也不得误开单图。点击照片主体仍进入“返回地图”预览；
    旧 Host 无投影时不显示按钮，390px 下红心常驻且不遮挡底部文件名。
60. 在图库瘦身候选成员分别用普通点击、Command/Shift、Command-A 和空白拖拽框选；在不改变选区、
    锚点、检查器、滚动或单图的前提下，以鼠标和 Enter 往返红心。视频卡左上播放/时长与右上红心不
    重叠；提交回收后冻结成员显示等待/处理中遮罩且红心禁用。进入回收站后对文件夹和 Photos 记录
    往返红心，不得触发恢复、永久删除或 Mac 确认；帮助文案必须说明红心不提供删除保护，390px 可达。
61. 在图库瘦身中选中两项后右键或 Shift-F10 打开成员菜单，确认菜单显示两项回收范围且打开过程不
    改变选区；对未选成员打开菜单仍保持原选区，只有执行回收动作才切为单项。菜单红心不打开单图，
    Escape 恢复成员焦点。调整九档密度并切换“适应/填充”时，主图库控件同步，任务、分组、选区、
    滚动及回收页状态不变；桌面滑杆可操作，390px 的分析、回收标题栏和刷新入口完整可见。
62. 在回收站同时设置媒体、来源和文件名搜索，依次切换“全部 / Photos / 文件夹 / 待处理”，确认
    四项计数来自同一完整查询上下文，结果总数及“载入更多”只属于当前范围。构造第一页前方全是
    Photos、后方才有文件项目的长列表，文件范围首屏仍必须出现文件项目；刷新后范围、其他筛选和
    滚动语义保持。旧 Host 不得对当前页伪造四范围，Photos 操作仍引导到系统“照片”App。
63. 在分析工作台选中任务、候选分组和至少一项成员，记录成员网格滚动位置后点击“隐藏分析记录”；
    记录栏和分组栏必须同时收起，成员区占满可用宽度，任务、分组、选区、锚点、滚动、密度、比例
    和待确认状态均不变。再次点击恢复两栏及原滚动位置；回收站不显示该按钮，390px 工具栏不溢出。
64. 在分析网格多选后按 Delete/Forward Delete，确认取消时选区和滚动原位，确认时请求冻结完整选区。
    再保留旧多选并打开其中间成员的单图预览，按 Delete 后请求必须只含当前可见成员；取消保持预览，
    Host 确认完成后优先进入原分组下一项、末项则回到上一项，位置计数与网格选择同步。原分组不足
    两项或消失时关闭预览，但不得滚动到网格顶部；Mac 原生确认和 Photos 系统规则继续生效。
65. 在同一分析记录中分别切换待处理、已确认和已忽略队列，计数必须来自 Host 完整结果且在分页前
    生效。对待处理分组点击已确认或忽略后，它必须退出当前队列；在目标队列打开后可重新处理回待处理。
    右键或 Shift-F10 打开未选分析记录菜单不得改变当前选择；取消恢复目标焦点，删除未选记录保持当前
    任务，删除当前记录选择 Host 返回的第一条剩余记录。运行中任务不允许删除，390px 不横向溢出。
66. 将已确认分组的当前成员隐藏到零项、已忽略分组隐藏到一项；两条记录仍须出现在各自历史队列，
    保留原始成员数并显示“原 N 项 · 现 M 项可查看”。零成员记录不得请求旧缩略图，详情显示“历史
    处理记录”。重新处理不足两项的历史记录后，它应退出历史队列且不回到待处理；待处理队列仍不得
    展示不足两个当前成员的普通候选分组。旧 Host 缺少新字段时继续兼容当前成员显示。
67. 构造至少 121 条分析记录，初始只返回两条但声明真实总数 121；网页必须显示完整总数与剩余数量，
    点击载入更多后扩展到 102 条，随后从当前窗口末尾按下一条或按 End 可继续扩展并到达第 121 条。
    单纯扩展窗口不得清空当前任务、候选分组、成员选区或滚动。再从任务面板深链到第 102 条，Host
    必须按原顺序扩展前缀到目标，位置与相邻导航不能把目标错误追加成第 3 条。旧 Host 缺少总数字段
    时不得显示虚假的“还有更多”。
68. 从图库、审核、世界地图和图库瘦身任一入口打开图片全屏预览，初始必须为 100%；按钮、滚轮、
    双击和 `+` / `-` 可在 100%～800% 内缩放，放大后左键拖拽不得把整张图片拖出可视区域，`0`
    必须恢复适应大小。切换媒体或重新打开时必须回到 100% 且位移归零；视频必须隐藏缩放控件并保留
    原生播放操作。390px 下返回、标题、缩放、红心和关闭控件不得横向溢出，预览层不得明显干扰底层
    网格的阅读。
69. 在图库瘦身单图预览中必须显示与 Mac 同层级的红色删除入口；取消确认时预览、原多选、锚点和
    滚动不变，确认时请求只能包含当前可见资产，不能误用底层多选。提交期间入口禁用，Host 完成后
    优先进入原分组下一项，末项回退上一项；无剩余项才关闭预览。键盘 Delete 继续执行相同路径，
    390px 下删除入口可达且不得挤出缩放、红心或关闭控件。
70. 来源管理中的“更新全部来源”必须与“刷新状态”严格区分，并提交 `refreshAll + sourceID=nil`。
    Host 重新读取至少一个活跃文件夹、一个活跃 Photos 和一个不可用来源后，只为前两者分别排入
    reconcile / Photos 同步，跳过不可用来源；相同 operation ID 重试不得重复排队。另一个来源请求
    活动时按钮禁用，命令面板入口执行同一动作；390px 下该入口完整可见且不挤出连接来源操作。
71. “缓存所有来源的缩略图”和“缓存所有来源的原比例缩略图”必须提交各自批量动作及
    `sourceID=nil`。Host 按顺序处理全部 active / unavailable 来源，复用已有缓存，只生成可处理缺失项，
    并汇报当前来源位置、生成、复用、不可处理跳过和失败计数；disabled / authorizationRequired 来源
    不得进入队列。单个资产或来源失败后继续下一来源，相同 operation ID 不重复执行；整批取消必须
    使用 `cancelPrewarm + sourceID=nil`，停止当前及剩余来源且保留已生成缓存。390px 下两个入口、进度
    与取消按钮均可达，不得形成横向滚动。
72. 来源管理必须提供与 Mac 同层级的“批量处理权限”：批量访问授权只选择需要授权的文件夹和
    Photos 来源，批量回收权限只选择 active 文件夹，二者均提交 `sourceID=nil` 并按来源顺序在 Mac
    显示系统窗口。取消任意一次必须停止后续来源并保留之前完成的授权；回收权限动作不得立即修改
    照片，删除来源不得进入批量协议。活动请求期间其他来源动作禁用，网页显示当前来源和完成数量；
    390px 展开后全部入口可达且来源列表继续滚动。
73. 审核队列非空时必须提供与 Mac 同源的网格密度和原比例控件；从审核修改后，图库和瘦身控件、
    CSS 网格宽度及本机持久偏好必须同步。横版、竖版和宽银幕审核资产按 Host 投影宽高使用真实卡片
    画幅，并通过 `aspect=original` 优先读取已缓存原比例缩略图；缓存缺失和旧 Host 字段缺失均安全回退。
    切换不得重建队列或改变当前标签、主选、多选、锚点和滚动。390px 下标签、来源、上限、原比例和
    刷新五组操作必须全部落在视口内且可点击。
74. 分别构造至少两页照片和两页视频，并为两种媒体设置不同的来源/图库范围、搜索、排序、筛选、
    单选或多选、锚点、检查器和非零滚动。连续往返至少两轮后，当前媒体的资产顺序、已载入窗口、
    `nextCursor` 和全部交互状态必须逐字段恢复；不得闪回首屏、重用另一媒体选区或因权威增量读取而
    清空已恢复窗口。
75. 构造至少 39 条训练记录，滚动到后段点击 Run 后记录栏不得回顶，选中项和键盘焦点必须原位；
    同一 Run 刷新保留列表及详情阅读位置，切换 Run 只把详情回到顶部。切换“批次 / 全部”后各自恢复
    记录栏位置，关闭并重新进入训练工程仍保持。Host 投影活动个人训练时，新建按钮、N 快捷键和
    已打开设置页的启动按钮都必须禁用；终态后恢复，不能仅依赖 Host 最终冲突来阻止重复训练。
76. 空来源时，侧栏必须直接显示“连接文件夹…”与“连接 Apple Photos…”，两者分别提交既有 Host
    命令并进入 Mac 原生确认链路；来源请求活动时入口禁用，关闭面板后焦点回到可用触发点或来源管理。
    存在活动 Photos 时连接按钮变成“已连接 Apple Photos”，存在待恢复 Photos 时不得出现重复连接。
    图库导航前五项按 Mac 顺序为图库总览、照片世界、全部媒体、红心收藏、无标签；390px 抽屉内入口
    完整可见且不得形成横向滚动。
77. 侧栏焦点位于图库、来源或连接入口时，`↑ / ↓` 必须跨分组移动到相邻可用项，`Home / End`
    必须到达整条侧栏首末项，并跳过隐藏或禁用入口；移动焦点本身不得改变图库范围。来源行的
    `Option-↑ / Option-↓` 排序和标签侧栏自己的方向键语义不得回归。390px 抽屉打开后必须聚焦当前
    `aria-current` 范围，且来源动作仍完整落在视口内。
78. 图库“个人模型”入口必须与 Mac 一样同时提供重建个人模型、训练超级个人模型和生成个人建议；
    抽检 Host 显示“抽 N 张生成建议”，全库 Host 显示“使用个人模型扫描全库”，且两者必须复用既有
    审核任务请求并进入审核总览。视频、离线和活动任务期间入口不得误启动。图库搜索框及命令面板
    必须明确说明可搜索文件名、路径、标签或来源，并继续由 Host 目录查询而非浏览器局部过滤。
79. Mac 全局提示必须在 Web 显示同一安全文案与信息/成功/警告等级；标签批量提示只在 Host 仍可撤销
    时显示“撤销”，来源删除阻挡提示的“前往回收站”必须打开 Web 回收站并精确筛选该来源。关闭和
    动作请求都必须绑定 notice 版本；陈旧请求不能关闭新提示、撤销新操作或导航到旧来源。旧 Host
    缺少能力或动作字段时安全降级；处理期间异步刷新不得覆盖横幅、重建图库窗口或改变滚动位置，
    390px 下消息、上下文动作和关闭按钮必须全部可达且不得形成横向滚动。
80. 在同一高级筛选面板同时勾选“可用 + 文件缺失”和“JPEG + RAW”，请求必须分别发送两个状态及
    JPEG 与三种 RAW UTI，筛选徽标按 1 个标签条件 + 2 个状态 + 2 个格式组显示 5，常驻摘要显示
    用户可读名称而不是 UTI。清除状态或格式只清除对应一组；JPEG 2000 与 RAW 必须可达，切到视频
    时只显示 MP4 / MOV。键盘可逐项勾选，390px 下两组列表、清除和完成按钮不得横向溢出。
81. 高级筛选面板打开时依次快速切换状态、格式、标签状态、标签条件和 ALL / ANY；每项均应在无需
    点击“应用”的情况下更新查询，连续变化只渲染最后一份匹配结果。请求期间旧网格保持可读，状态
    行显示进度；返回后面板不关闭，焦点仍在最后操作的复选框、选择器或清除按钮。“完成”只关闭
    面板，“清除全部”立即恢复无条件查询。切换照片/视频时取消旧媒体的待发查询，并恢复各自筛选
    会话；390px 下状态行、清除和完成按钮不得重叠或形成横向滚动。
82. 分别构造只有搜索、搜索+标签+状态/格式的零结果查询。空状态必须说明实际条件并只显示
    对应的“清除搜索 / 清除标签筛选 / 清除状态和格式 / 清除全部条件”。逐组清除后请求不得夹带
    已清除参数，但必须保留媒体、来源和排序；焦点返回稳定搜索或筛选控件。连续两次查询都为 0 项时，
    条件变化仍必须重绘说明与动作；390px 按钮换行且不得形成横向滚动。
83. 在图库保持来源、搜索、筛选、排序、选区、检查器和非零滚动，依次打开图库总览、照片世界、
    建议审核、训练工程和图库瘦身；浏览器 Back、Forward、显式返回按钮与 Escape 必须进入同一历史
    层级，返回后图库状态逐字段不变。训练与审核双向深链应显示准确返回目标并恢复原 Run、标签、
    选区、滚动与焦点；瘦身 Forward 应恢复任务、分组、成员选区和锚点。390px 下完整返回文案与
    标题、分页、刷新等右侧操作必须同时可见且不得形成横向溢出。
84. 在图库瘦身“分析选项”选择部分活动来源后刷新，请求必须只含所选来源且至少一个；不可用、未知
    或空选择由 Host 拒绝。相似索引初始化必须精确选择一个活动来源，并按当前照片/视频显示
    未初始化、构建进度、就绪计数、需要重建或安全失败状态；构建中不得重复提交，重放 operation ID
    不得重复排队。后台轮询、来源刷新和弹层开关均不得重建当前任务、候选组、成员选区或滚动；
    Escape 恢复触发按钮焦点，390px 下来源列表、状态和两个主动作完整可达且无横向溢出。
85. 焦点位于普通照片卡片时，`Shift-F10` 或 Context Menu 键必须打开该资产菜单并聚焦首个可用动作；
    `↑ / ↓` 循环、Home/End 直达首末动作，Escape 关闭并返回原卡片。打开或取消菜单不得改变主选、
    多选、锚点、已载入分页窗口或滚动位置；执行选择、红心和来源筛选后分别返回仍可见资产或对应
    来源行，单图查看进入既有预览焦点链路。菜单无障碍名称必须包含文件名，隐藏或禁用动作不得进入
    键盘序列。
86. 打开任意可用视频的全屏预览时，工具栏必须提供与 Mac 单图页同源的“使用系统播放器打开”动作；
    请求只提交当前预览的稳定资产 ID，并由既有 Host 权威解析与打开。离线、不可用、导航中或已有
    请求时入口禁用；成功、失败或快速切换后仍保持当前全屏上下文、位置、浏览器播放器、底层选择及
    滚动，不得重新载入资产窗口。图片预览不得显示该入口；390px 下入口收为可辨识图标，文件名进入
    无障碍名称，返回、红心和关闭操作不得被挤出视口。
87. 从任意工作区打开“活动”，标题、说明、刷新、关闭、任务状态和空状态必须与 Mac 活动面板同层级。
    点击刷新或按 `R` 只能读取任务投影，不得重载图库或其他工作区；成功、任务消失、空列表和失败后
    均按稳定任务 ID 恢复任务、动作焦点及非零滚动。5xx 后旧任务继续可读，写操作保持离线禁用，但
    刷新按钮必须允许原位重试并在成功后恢复连接；请求重入必须被拒绝。390px 下刷新与关闭同时可达，
    长任务列表独立滚动且不得形成页面横向溢出。
88. 在全部来源、active Apple Photos、active 文件夹和不可用来源之间切换，工具栏必须分别显示并提交
    `refreshAll + nil`、`syncPhotos + Photos sourceID`、`rescan + folder sourceID`，不可用来源不发送请求。
    该动作必须与“重新读取网页数据”拥有不同按钮、名称和语义；后者只能重读投影，不能启动扫描。
    来源请求进行中两个入口不得重复启动来源任务，Host 终态继续由既有来源管理轮询收束。390px 下
    活动、连接和“更多”保持可见；来源更新、网页重载、设置与退出从同一“更多”菜单可达，不能依赖
    横向滚动或难以区分的相邻图标。
89. 构造一个带来源名称的 `folderReconcile` 或 `photosReconcile` 活跃任务，工具栏必须按 Mac 文案显示
    来源与 `completed/total`、仅 `completed` 或尚无计数三种进度。轮询只能请求 `/v1/jobs`，不得改变
    图库已载入窗口、游标、来源、选区、检查器、选择模式或非零滚动，也不得触发活动面板可见刷新状态。
    点击进度必须打开“活动”并聚焦对应稳定任务；任务终止后工具栏进度隐藏，失败摘要仍可在活动中读取。
    旧 Host 不返回来源字段时安全回退。390px 下进度在“更多”按钮显示活动点，并在菜单首组展示
    完整来源与计数，不得挤出命令、活动、连接或“更多”。
90. 发起单来源与全部来源缩略图预热后，顶部必须像 Mac 一样同时显示进度与直接取消；仅图标工具栏的
    宽屏状态仍保留计数，390px 在“更多”菜单把进度与取消显示为相邻动作。点击取消必须复用活动
    请求的稳定 `sourceID`（全部来源为 `nil`），不能打开来源管理、切换工作区或改变图库/检查器/滚动；请求重入
    禁用，终态隐藏进度并把焦点移到“活动”。完整进度和取消结果仍能从来源管理读取，后续来源动作可
    连续执行。
91. Photos 来源进入 `authorizationRequired / disabled` 时，空状态必须同时提供“重新授权…”与
    “打开照片权限设置…”；后者精确提交 `openPhotosPrivacySettings + sourceID`，不得打开来源管理、
    修改图库状态或暗示权限已经恢复。Host 只能为 Photos 来源在主线程打开系统照片权限页，相同
    operation ID 不重复打开，系统调用失败必须返回失败。离线或活动来源请求期间入口禁用；390px 下
    三条恢复路径完整可达且不得形成横向滚动。
92. 在图库、审核队列和图库瘦身分别构造超过一屏的长列表，从空白处拖拽到滚动区域下边缘时必须持续
    自动滚动，首屏已命中项和后续命中项同时保留；移出边缘或释放后立即停止，不能重载投影或跳回
    顶部。Command/Ctrl 框选继续追加，普通框选继续替换。桌面瘦身使用成员网格滚动，390px 使用成员
    面板滚动，两种布局中的卡片均保持可点击高度、选区和后续红心刷新位置，页面不得横向溢出。
93. 审核队列首批返回下一页游标时，最后一张卡片进入视口或距底部 280px 内必须自动追加；短首屏连续
    补页直到填满视口或游标为空。追加期间主项目、完整选区、锚点和非零滚动保持，不得请求审核总览、
    图库或检查器。相同游标、空追加、失败、离开队列和已有请求均不得形成循环；“载入更多”仍可作为
    显式重试及键盘回退。
94. 在照片世界选择照片塔并完成有限预览后，地点卡必须显示“在图库中查看全部 N 张”。进入后
    `/v1/assets` 每一页都携带 Host 快照给出的精确网格桶与可选视口范围，不能用前缀缩略图在浏览器
    假筛选或无效时降级到全图库；现有分页、排序、搜索、筛选、多选、检查器、红心和预览继续可用。
    顶部地点范围横幅显示同一总数，“返回地图”保留镜头和照片塔，“查看全部照片”清除范围；浏览器
    Back/Forward 双向恢复。390px 下两个动作均可见且页面不得横向溢出。
95. 照片塔有限预览请求失败时，地点卡必须显示 Host 错误而不是空地点文案，并提供原位“重试”；
    失败期间不得显示“在图库中查看全部”。重试必须命中同一 selection query，成功后保留照片塔选择、
    地图镜头和详情位置，并恢复照片条与完整图库入口，不能重载整个世界地图或跳回图库。
96. 取消训练、删除分析记录、候选成员可恢复回收/快速删除及回收站高风险动作不得打开浏览器原生
    confirm。应用内确认必须区分普通、警告和不可逆层级，完整说明原始媒体影响与 Mac 二次确认；取消
    不发送请求并恢复原动作焦点，确认继续使用同一个 Host 权威命令。全屏当前项确认期间不得关闭
    预览或改变底层选区；390px 下标题、说明和两个动作完整可见且不得形成横向滚动。
97. 分别在普通图库、审核队列、训练工程、图库瘦身、照片世界、图库总览和全屏预览中聚焦一个稳定
    控件并按 ⌘K；面板必须显示准确当前上下文，Escape 与再次按 ⌘K 均返回原控件。审核/训练/瘦身的
    照片/视频命令只改变当前工作区权威媒体范围，照片世界不得显示视频切换；图库选择与批量标签命令
    不得在其他工作区误操作底层网格。执行“显示全部”、打开另一工作区或当前“返回”后，浏览器
    Back/Forward 必须恢复原审核标签与模式、训练 Run、瘦身任务/分组、地图照片塔及原焦点；390px
    页脚上下文和键盘提示完整可见且命令面板不得超出视口。
98. 从 1440px 缩窄到完整工具栏真实所需宽度以下时必须自动收纳，并保留桌面侧栏按钮、App 标记、
    标题和检查器按钮；重新扩宽后自动恢复完整工具栏，临界宽度不得反复闪烁。390px 顶部只保留来源
    抽屉、标题、命令、活动、连接状态与“更多”，所有按钮边界必须位于视口内。打开“更多”后，来源扫描进度、缓存取消、撤销、四个工作区、存储、来源更新、
    网页重载、设置、快捷键和退出按当前 Host 状态可达；方向键、Home/End 与 Enter 必须跳过禁用项，
    Escape、点击外部及恢复桌面宽度关闭菜单并回到仍可见的稳定焦点。从菜单打开活动或设置并关闭后焦点返回
    “更多”；动态任务结束后活动点和对应状态项消失，不能重载图库、改变选区、滚动或来源范围。
99. 在单张照片检查器分别运行“标准场景”和“个人标签”。标准结果必须进入既有待审建议并显示自动
    匹配/建议复核状态；个人结果可逐项接受或拒绝，成功后当前标签决定和结果行同步。响应中不得出现
    分数、向量、模型、权重、路径或 catalog 标识。请求尚未返回时切到另一张照片，旧响应不得覆盖新
    检查器；iCloud 原片不可用时显示 Mac 下载提示。全过程保持已载入窗口、选择、排序和非零滚动，
    同一 `operationID` 重试不重复运行模型，390px 下两个入口与决定动作完整可见且页面不横向溢出。
100. 在 1440px 图库打开单图，黑色预览画布必须精确替换中心图库而不是盖住整个 App；右侧检查器
     保持可见可操作，文件名随上一张/下一张同步，并可搜索标签及使用既有标签、红心、原图、AI/模型
     动作。来源和底层网格在单图期间不可误触；Tab 必须在单图工具栏与检查器之间连续循环。跨页导航、
     关闭后的网格焦点、非零滚动和已载入窗口保持。缩到 390px 时自动改为完整模态、`aria-modal=true`
     且不横向溢出；审核、瘦身和地图单图仍保持原有上下文与返回语义。
101. 在“仅图标”和“图标与名称”两种工具栏模式下，主标题栏与图库高频动作必须提供与 Mac
     `persistentHelp` 等价的标题和完整说明。鼠标停留与键盘焦点都能显示，内容需明确动作范围、
     快捷键及是否转交 Mac 确认；显示时不能触发动作、抢焦点或与浏览器原生 tooltip 重叠，收起后
     必须恢复原 `title` 和既有 `aria-describedby`。帮助层在 1440px 与 390px 均保持至少 8px
     视口边距，不遮住锚点；点击、按键、滚动、移出、焦点离开和窗口变化立即收起，且不得改变图库
     来源、筛选、已载入窗口、选择或滚动。
102. 在图库瘦身选中任意分析记录、候选组和成员并保持非零记录/成员滚动后，从“分析选项”打开
     “调整相似阈值…”。编辑器必须读取 Host 当前值而非浏览器缓存；切换“全部候选”、L2 不限、DINOv2
     不限或始终/从不分桶时显示与 Mac 同源的极限模式警告，并禁用对应无效数值输入。应用与恢复默认
     均使用稳定 operation ID 写入同一阈值端点，只影响之后启动的分析，不得启动任务、刷新工作区或
     改变记录、候选组、成员选区、锚点和滚动。Escape 返回可见的“分析选项”工具栏按钮；390px 下
     标题、四组参数、警告、恢复默认、取消与应用全部可达且页面无横向溢出。
103. 在 390px 或粗指针环境进入审核队列和瘦身候选成员，必须显示“选择”；进入后普通点击可连续
     追加或移出项目，“全选”覆盖当前已载入窗口，“完成”收束到最后活动主项。Escape 必须先结束
     选择模式而非退出工作区，选择模式内双击不得误开全屏；桌面宽屏不显示冗余入口，既有 Command/
     Ctrl、Shift、Command-A、框选、收藏、审核决定与回收操作保持。两处活动态工具栏、勾选标记和
     操作条不得横向溢出。
104. 聚焦审核卡或瘦身候选成员后，左右键移动相邻项，上下键按当前渲染列数移动，Home/End 到首尾，
     Page Up/Down 按当前滚动视口页幅移动；密度、原比例和窗口宽度变化后列数必须重新取自实际网格。
     Shift 与上述按键扩展连续区间，无 Shift 收束为单一主项；移动只做 nearest 滚动并保持已载入
     窗口。Space 打开当前主项并返回同一卡片，审核 P/X/U、Command-A 和瘦身 Delete 的工作区级
     快捷键不得因工具栏或卡片焦点而回归。
105. 构造含普通保留、红心保留、全组红心安全跳过、Photos/文件夹待删项及 2/3/4/5+ 分组规模的
     完全相同清理方案。网页确认页的五项指标、保护摘要、去留环图、组规模柱图、来源横条图和三类
     风险提示必须逐项等于 Host 响应；不得从其他总数推算红心拆分。删除请求仍只提交冻结 `planID`
     和模式。旧 Host 缺少新增可选字段时必须可解码并明确显示保护拆分不可用，不能伪造 0；1440px
     和 390px 下全部信息与取消/回收/快速清理动作均可滚动到达且不得横向溢出。
106. 提交完全相同媒体清理后，等待 Mac 确认阶段仍可正常浏览；Mac 确认执行后，无论网页当前位于
     图库、审核、训练、照片世界、回收站还是图库瘦身，必须立即显示不可关闭的全局执行层。依次验证
     复核方案、处理媒体、等待授权、刷新状态和结果核验五阶段标题与说明；处理阶段显示 Host 实际
     `completedAssetCount / totalAssetCount`，其他阶段显示不确定进度。鼠标、Escape、⌘K、⌘,、⌘F
     和工作区快捷键不得穿透。结束后必须关闭执行层、显示已有核验报告，并恢复原工作区、焦点、选择、
     已载入窗口和滚动位置；离开瘦身页面及页面首次载入时仍持续跟踪照片和视频的活动请求。
107. 从网页删除一个仍有回收记录的来源并在 Mac 确认。Host 必须拒绝删除且不在后台继续，网页在
     来源请求首次进入失败终态时立即显示与 Mac 同源的全局警告，明确三类阻塞摘要、原媒体未修改并
     提供“前往回收站”；不得只显示瞬时 toast 或等待下一次目录事件。点击动作必须携带当前 notice
     revision，经 Host 防陈旧校验后打开图库瘦身回收站并精确筛选该来源；旧提示失效时保留最新提示，
     不得打开错误来源。提示刷新和导航不得清空主图库已载入窗口、选择、排序或滚动。
108. 提交便携数据导出、外部存储位置选择或任意缓存清理后，在 Host 仍为 `awaitingMac / running`
     时关闭存储面板。标题栏和紧凑工具栏必须持续显示操作状态并允许浏览其他工作区，后台轮询不得
     因面板关闭停止。终态必须显示结果提示并恢复普通状态；重新打开面板时应立即保留最后快照和
     请求历史，不得闪回空白或加载骨架，再以 Host 最新响应更新。退出登录必须停止旧会话轮询。
109. 在主图库 Command/Ctrl 多选至少两项后，右侧检查器必须继续显示当前主项的缩略图、选区位置与
     Host 详情，而不是只剩批量标签。切换主项时以 `assetID + contentRevision` 更新并拒绝过期响应；
     未载入或失效 ID 不得发起详情请求。点击预览进入既有单图并返回后，完整选区、锚点、已载入
     窗口和滚动保持；详情失败时保留摘要。390px 下主项卡与批量标签均可达且不得横向溢出。
110. 选择一张仅存在于 iCloud 的照片并请求预览。新 Host 必须立即返回 operation ID，网页持续显示
     Host 实际进度且可取消；刷新或重新选中同一照片应恢复活动操作，切换照片则取消旧操作且过期
     轮询不得覆盖新检查器。取消后可用新 operation ID 重试，完成后从普通缓存预览路由显示图片。
     同一 operation ID 重放不得启动第二次下载，不同并发操作必须冲突。旧 Host 仍能使用同步回退；
     390px 下进度、说明、取消与重试均可达且不得横向溢出。
111. 在桌面主图库分别拖动左侧栏和右检查器分隔线到非默认宽度。拖动期间已载入窗口、完整选区、主项、
     检查器详情和非零图库滚动不得重建或跳顶；刷新页面、隐藏再显示对应列后宽度必须恢复。分隔器可
     用方向键、Shift、Home/End 调整并公开正确 ARIA 当前值，双击恢复 220/300px。宽度必须限制在
     Mac 的 180–300px / 240–380px 范围；700px 以下退回既有抽屉/覆盖层且不得横向溢出。
112. 在审核总览拖动本地模型分隔线，并在审核队列拖动右侧检查器分隔线。宽度必须分别限制在 Mac 的
     248–320px 与 240–380px，默认 288/300px；鼠标、方向键、Shift、Home/End、双击复位和刷新偏好
     均生效。调整期间不得重载总览或队列，不得改变待审项目、主项、多选、游标或非零滚动；700px
     以下必须恢复上下布局并隐藏分隔器，触控选择、详情和 P/X/U 决策仍完整可达。
113. 图库瘦身桌面端必须像 Mac 一样只使用一个 196–244px、约 22% 的分析导航栏，按同一纵向滚动轴
     依次显示分析记录、审阅队列和候选分组；不得再拆成两个并排导航列。载入 121 条历史、切换记录、
     切换队列、隐藏再显示导航时，当前记录、分组、成员选区、锚点和两侧非零滚动均保持。700px 以下
     导航与成员区上下排列，但导航内部仍为单一纵向列表，所有内容可滚动到达且页面无横向溢出。
114. 图库瘦身候选详情必须像 Mac 一样在紧凑标题栏后立即显示媒体网格；分析诊断如保留，只能通过
     标题栏“分析概览”按需覆盖显示，默认不得占用独立纵向轨道。打开和关闭浮层不得改变网格几何、
     记录、分组、成员选区、锚点或非零滚动；Escape 关闭并返回触发器。390px 下所有字段可滚动读取，
     浮层边界不得超出视口。
115. 图库瘦身回收站必须显示与 Mac 同源的标题、媒体项目总数、待关注数、处理状态、来源范围横幅、
     全部/Photos/文件夹/待处理范围及带结果数的文件名搜索。条目在桌面使用 380–640px 自适应卡片，
     390px 退为单列；策略和 Host 允许的恢复/修复/说明/删除动作不得丢失。搜索、来源或范围无结果时
     必须提供对应恢复动作并恢复稳定焦点，不能将有数据的筛选结果误报为“回收站为空”；所有查询、
     后台请求和动作刷新保持当前滚动且页面无横向溢出。
116. 来源管理必须像 Mac 侧栏一样使用可选择的来源列表与单一当前来源详情，不得同时铺开所有来源的
     全部动作。详情按更新缓存、权限修复和移除分组，保留 Host 状态机和 Mac 原生确认；方向键可切换
     来源并进入/返回详情。从侧栏上下文打开时必须锚定该来源，“在图库中查看”必须关闭面板、切换
     精确来源并恢复侧栏焦点。390px 上下叠放且无横向溢出，来源与详情均可滚动到达。
117. 来源管理顶部必须与 Mac 来源标题一致：连接新来源可直接触达，全部更新、两种全来源缓存及批量
     权限放入一个“所有来源操作”菜单，不得在桌面或 390px 常驻形成全局按钮墙。菜单用方向键遍历，
     Left/Escape 收起并恢复触发器；命令提交后菜单自动收起，但活动进度、取消入口、图库上下文和
     来源详情必须保持。
118. 图库图片卡片必须显示与 Mac 同源的持续详情：文件名、来源、可选相对位置、尺寸、格式、可选
     拍摄/修改时间、标签统计和状态都来自 Host 摘要；旧 Host 缺少新增字段时只省略对应行。鼠标与
     键盘焦点均可触发，详情层在桌面和 390px 保持至少 8px 边距，收起后恢复原无障碍关系，不得改变
     网格、分页、选区或滚动。视频卡片不得挂载该帮助层，悬停静音播放、原生提示和双击预览必须保持。
119. 在单张检查器输入新标签并按 Return，必须使用一次 Host 原子命令完成创建和当前照片确认，成功后
     清空输入并保留焦点；临时失败后原文与 operation ID 保持，直接重试不得创建重复标签。多选检查器
     必须对冻结的完整选区执行同一命令，并把新标签显示为全部确认。两处均不得打开模态框、重建网格、
     改变主项、选区、锚点、已载入窗口或滚动；Escape 只清除草稿/错误。390px 下输入与加号完整可达且
     页面不得横向溢出。
120. 单张与多选检查器的标签芯片、决定按钮和分组标题必须提供与 Mac `persistentHelp` 同层级的状态化
     说明。鼠标停留和键盘焦点都要显示当前决定、作用范围、左右键、X/Delete、拖动及 Option + 方向键
     语义；搜索期间明确说明不能排序。非系统分组右键、Context Menu 或 Shift-F10 必须打开重命名/删除
     菜单，Escape、关闭标签管理和取消确认均回到原检查器分组。帮助显示不得触发写入或改变选区、主项、
     网格和滚动；桌面与 390px 均保持至少 8px 视口边距。
121. 侧栏来源行、标签分组和标签芯片必须提供与 Mac 同层级的状态化持续帮助。来源需区分可用、离线、
     需授权、停用及 Photos 历史来源，并说明点击、同步/重扫、菜单与排序语义；标签需区分未筛选、并集、
     交集、排除和搜索状态，并说明 Command/Option、右键菜单及跨组排序。鼠标和键盘均可触发，菜单 Escape
     返回原行；桌面与 390px 至少保留 8px 边距，且不得改变来源、筛选、分页、选区或滚动。
122. 侧栏、单张检查器和多选检查器必须像 Mac 一样共享同一标签分组折叠状态；任一表面切换后其他可见
     表面立即同步，刷新后保持。旧侧栏/检查器分离偏好需确定性迁移且支持旧版回退；标签搜索临时展开不
     得覆盖保存状态，审核总览独立折叠不受影响。同步重绘不得改变标签顺序、筛选、选区、主项、分页、
     滚动或键盘焦点，桌面与 390px 均需验证。

### 390px 窄屏

- 来源和模式切换可触控使用；
- 检查器覆盖层可打开、前后导航并关闭；
- 图库、审核和瘦身均有触控多选、全选与完成入口，多选工具栏、命令面板和审核操作不横向溢出；
- 登录、退出、轮询和预览错误状态均可恢复。

## 后续切片

### 训练工程常驻操作帮助验证记录（2026-08-10）

- 对照 Mac `TrainingWorkspaceUI` 的 `persistentHelp`，网页训练工程现在为媒体切换、新建任务、
  方法与记录类型筛选、记录栏显隐、刷新、三种模型槽、批次历史、活动取消/重试、Run 列表、
  关联任务、失败恢复、标签审核以及训练设置中的方法/标签/来源/范围提供同一层级的状态化说明。
  帮助明确动作是否只改变显示、是否会创建或控制后台任务、成功产物是否保留，以及 Host 会再次
  校验的边界，不再只依赖短按钮文字猜测影响范围。
- 动态 Run 和模型槽同时公开真实键盘语义：Run 使用上/下、Home/End 与 `⌘K`，模型槽使用
  左/右、Home/End；鼠标停留与键盘焦点均复用常驻帮助气泡。帮助内容随运行中、已发布、失败、
  取消和不可用状态重建，重绘后不会保留旧状态文案。
- 合成 Chromium 覆盖桌面鼠标悬停和 390px 键盘焦点，验证帮助标题、范围说明、快捷键、边界
  与视口内定位；截图为 `/tmp/imageall-training-persistent-help.png` 和
  `/tmp/imageall-training-persistent-help-390.png`。测试仅使用合成训练记录和来源，未重启现有 App，
  未读取或写入 `/Volumes/HDD2` 受保护真实图库。

123. 在训练工程依次聚焦新建任务、方法/记录筛选、模型槽、Run、活动与失败恢复动作；每个控件
    必须显示与当前状态一致的 Mac 风格常驻说明，并区分“只改变显示”“创建任务”“控制已有任务”
    和“打开其他工作区”。Run 的上/下、Home/End、`⌘K` 与模型槽的左/右、Home/End 必须写入
    可访问快捷键；390px 下帮助气泡不得越出视口，重绘后不得显示上一个 Run 或任务状态。

### 待审核建议常驻操作帮助验证记录（2026-08-10）

- 对照 Mac `PersonalizationReviewUI` 的 `persistentHelp`，网页审核总览现在为返回、来源范围、每标签
  上限、模型状态、标准/个人建议任务、分组折叠、标签卡片、三轨门槛、生成设置、训练记录及任务
  暂停/继续/取消提供状态化常驻说明。说明区分只改变显示、打开确认设置、创建 Host 任务和控制既有
  任务，并明确任务冻结范围、已写入建议保留、原始媒体不变等边界。
- 审核队列补齐标签切换、适应/填充、网格密度、触控选择、全选、续载、刷新、检查器导航、单图预览
  与 P/X/U 决策说明。卡片帮助随主项目、多选、建议来源与可信度重绘，并公开方向键、Home/End、
  Page Up/Down、Space、P/X/U 与 Command/Ctrl-A；刷新和续载明确保持已载入窗口、选择和滚动位置。
- 合成 Chromium 完整筛选/审核/预览流程覆盖桌面鼠标悬停、动态队列卡片和 390px 键盘焦点，验证
  `review` 帮助类型、内容、快捷键及 8px 视口边界；截图为
  `/tmp/imageall-review-persistent-help.png` 与 `/tmp/imageall-review-persistent-help-390.png`。
  测试使用合成来源和媒体，未重启现有 App，未读取或写入 `/Volumes/HDD2` 受保护真实图库。

124. 在审核总览依次聚焦来源、每标签上限、模型刷新/生成、标签卡片、门槛、训练记录及任务控制，
    再进入队列聚焦缩略图、布局、选择、续载、导航、预览和 P/X/U；帮助必须反映当前媒体、来源、
    标签、任务、主项目及多选状态，且明确动作是显示调整、打开确认、创建任务还是控制任务。动态
    重绘后不得保留旧卡片或旧任务文案；桌面鼠标与 390px 键盘均可触发，帮助至少保留 8px 视口
    边距，不得改变已载入窗口、选择、焦点或滚动位置。

下一切片从 Mac 与网页的当前实现重新审计，优先选择仍会阻断连续日常操作的高价值差异，并保持
“Host 权威投影与命令 → 网页状态与交互 → 协议、Facade、浏览器证据”的完整纵向闭环。
每个切片都必须同时更新本矩阵、协议契约测试和已登录浏览器证据。
